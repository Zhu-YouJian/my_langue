# 开发备忘

> 各类待办、跳过项、环境依赖、注意事项均记录于此。
>
> **当前阶段：v0.3.3 → 阶段 1（可用）**（已解决语言核心语法 20 项缺口）
>
> 演进路线与阶段规划见 `CODE_WIKI.md` §10。
>
> **2026-07-25 fix: AUDIT #12 — tenthc bmm FLOPs 预估激活**：编译器部完成代码修复并通过全部验证，文档部同步真理源。**问题**：tenthc 侧 `HirType` 缺 `dim2` 字段，导致 bmm 编译期 FLOPs 预估、3D shape 检查、3D numel/bytes 计算均为 no-op（errors defer to runtime）。该问题由 Problem 21 批次4（2026-07-12）登记为已知限制 #12，因 tenthc 侧 HirType 仅有 dim0/dim1，3D tensor shape 无法在编译期追踪。**修复**：(1) `tenthc/hir/hir.th:39`：`HirType` 新增 `dim2: i64` 字段（`..` 默认零初始化，不破坏现有 1D/2D 路径）；(2) `tenthc/hir/lower.th`：构造函数 `add_tensor_type`/`add_tensor_type_param`/`propagate_tensor_dtype` 加 `dim2` 参数；激活 `get_tensor_dim2`（读 `t.dim2`）；激活 `static_numel`/`fmt_dims` 的 `dc==3` 分支；激活 `emit_bmm_flop_estimate`：完整实现 B*M*K*N*2 FLOPs 计算，分段乘法 + 范围检查防 i64 溢出（每维 < 10^6，累乘 < 10^18）；激活 bmm 分支：3D shape 推断 `(B, M, N)` + 调用 `check_bmm_shape` + `emit_bmm_flop_estimate`；放宽 `parse_tensor_type`：支持 `Tensor[f64, 2, 3, 4]` 解析 1-3 个 dim 字面量。(3) `tenth/tests/native_parity_test.rs`：新增 `test_bmm_flop_estimate_parity`，验证 Rust 母编译器与 tenthc 双侧 bmm FLOPs warning 一致。**字段语义**：dim_count=3 → dim0=B, dim1=M, dim2=K(bmm 输入) 或 N(bmm 结果)。**验证**：build 无新 warning · 自举 `[OK] Full compiler compiled` · cargo test --release 全套全绿 · bmm_test 11/11 · native_parity_test 36/36 · selfhost_frontend 4/4 · three_stage 1/0/2。**影响范围**：仅 tenthc 侧（hir.th + lower.th）+ 测试；未修改 Rust 母编译器源码、bridge.rs、wasm.th；自举三路径未破坏。**改动文件清单**（3 个）：`tenthc/hir/hir.th`、`tenthc/hir/lower.th`、`tenth/tests/native_parity_test.rs`。**禁止事项无违反**：仅编辑现有 .rs/.th 文件 + 1 个测试文件，未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**文档同步**（本条目为文档部执行，纯文档改动）：`AUDIT.md` §六 #12 标记为 ✅ 已修复（保留历史描述，删除线 + 修复说明）、§11.4 新增 `AUDIT-11.4.7` 登记条目、§11.5 元数据更新（总登记条目数 17→18，11.4 ×6→×7，登记日期追加 2026-07-25 / 11.4.7，修复方案追加 11.4.7）；`能力梳理/能力全梳理.md` §2.1 tenthc 编译期反向 shape 验证条目补充 bmm FLOPs 已激活说明（状态保持 ✅）、头部日期 2026-07-17 → 2026-07-25；`基本功核查.md` v2.4 → v2.5，更新日志追加 2026-07-25 AUDIT-11.4.7 同步说明（未在 106 项中新增条目，因 bmm FLOPs 修复属于 AUDIT.md §11.4 登记范围，不在基本功核查 12 大项 106 小项之内）。
>
> **2026-07-25 feat: tenthc 侧接入 yield 关键字（AUDIT #9）**：**问题**：tenthc lexer 已注册 "yield" 关键字（disc=82），但 parser/lower/wasm 三层未接入，导致 tenthc 无法解析含 yield 的源码。Rust 母编译器侧早已完整实现。**修复**：(1) `tenthc/hir/hir.th`：HirExpr disc 列表新增 `37: Yield`（left=inner expr, ty=Unit）；(2) `tenthc/parser/parser.th`：新增 yield (disc=82) 解析，支持 `yield;`（无值，left=0）和 `yield expr`（带值，left=inner）两种形式，参照 Rust 侧 expr.rs:699-712；(3) `tenthc/hir/lower.th`：新增 yield lowering（inner 若存在则 lower 保留副作用，返回 Unit 类型 disc=11），参照 Rust 侧 lower_expr.rs:1102-1112；(4) `tenthc/compile/wasm.th`：新增 yield (disc=37) codegen（inner 若存在则编译并 drop，yield 本身 no-op），参照 await/spawn pass through 模式；(5) `tenth/tests/tenthc_yield_test.rs`（新增）：3 项测试（无值形式/带值形式/多 yield 混合），走 tenthc→WASM-A→wasmi→WASM-B→wasmi 三阶段自举路径。**遗留**：路径 B（bridge.rs）未接 yield（任务约束禁止修改），tenthc/main.th 不含 yield 故自举不受影响；路径 A/C 完整可用。**验证**：build 无新 warning · 自举 [OK] · cargo test --release 全套全绿 · yield_test 6/6 · selfhost 4/4 · three_stage 1/0/2 · native_parity 36/36 · tenthc_yield_test 3/3。
>
> **2026-07-25 feat: 标准库补强 — 统计函数+HashSet+assert 扩展**：**背景**：调研发现三项标准库能力已实现但测试薄弱且文档过时（能力全梳理标记 ❌ 失实）。**统计函数扩展**（std/math/stats.th）：原有 6 函数（mean/median/variance/stddev/variance_sample/stddev_sample）；新增 6 函数：min/max/range/sum/product/percentile（线性插值，与 numpy.percentile 一致）；新增 test_stats.th（25+ 断言，覆盖常规/空 Vec/单元素/越界）。**HashSet 扩展**（std/collections/hashset.th）：原有 8 函数（new/insert/contains/remove/len/is_empty/to_array/clear）；新增 5 函数：from_array/set_union/intersection/difference/is_subset（注：union 是保留字，用 set_union）；新增 hashset_test.rs（12 项行为测试）。**assert/assert_eq 测试**（新增 assert_test.rs，8 项）：native 早已实现（VM+解释器双侧），本次补行为测试；修复解释器路径 to_f64()/to_float() 等 native 无法处理 Value::Shared 包裹值的问题（新增 deref_wrapped() 辅助函数）；native 白名单同步：lower_expr.rs + eval.rs 新增 assert/assert_eq。**prelude 同步**：stats 行追加 min/max/range/sum/product/percentile；hashset 行追加 from_array/set_union/intersection/difference/is_subset。**验证**：build 无新 warning · 自举 [OK] · cargo test --release 全套全绿 · hashset_test 12/12 · assert_test 8/8。**遗留**：VM 模式下直接运行 test_stats.th 在 main() 返回后报 assert 错误（VM 后置清理已知问题，不影响集成测试）。
>
> **2026-07-25 docs: 第 94 项 CUDA 后端文档矛盾修正（AUDIT-11.4.6 登记）**：基本功核查第 94 项披露的文档失实风险消除。**问题**：`compile/gpu/` 模块在 AUDIT.md §六 #4 / §8.3 / CODE_WIKI.md §4.4 中描述为"脚手架已就绪，待 CUDA 环境安装"，易让读者误解为"只差环境配置即可工作"。**实际**（源码核实）：`device.rs:79-82` `CudaDevice::is_available()` 永远返回 `true`（注释自承 "Simulated: always report available for code generation purposes"）；`device.rs:60` `total_memory: 24 * 1024 * 1024 * 1024` 硬编码 24 GB 模拟值；`mod.rs::compile_kernel` 仅遍历 `program.functions` 调 `CudaKernel::from_hir_function` 把每个 HIR 函数转成 CudaKernel 字符串；`cuda_kernel.rs:48-119` `to_cuda_code()` 仅生成 CUDA C 源代码字符串；Grep `nvcc\|cuda_runtime\|cudaRun\|cuLaunch\|exec\|compile_binary\|build_kernel` 在 `compile/gpu/` **0 匹配**——整个 gpu 模块只是 C 代码生成器 + 模拟设备，不编译、不加载、不执行任何 kernel。**修复**（纯文档措辞修正，零代码改动）：(1) `AUDIT.md` §六 #4 措辞改为"仅 CUDA C 源代码生成 + 模拟设备（`device.rs:79-82` `is_available()` 永远返回 true，`total_memory` 硬编码 24GB）；`mod.rs::compile_kernel` 仅把 HIR 函数转成 CudaKernel 字符串；**未接 nvcc / CUDA Runtime API / cuLaunchKernel**，不编译、不加载、不执行任何 kernel"；(2) `AUDIT.md` §8.3 同步修正；(3) `AUDIT.md` §11.4 新增 `AUDIT-11.4.6` 登记条目（论文来源：基本功核查第 94 项；状态：✅ 已修复 2026-07-25 纯文档措辞修正）；(4) `AUDIT.md` §11.5 元数据更新（总登记条目数 16→17，11.4 ×5→×6，登记日期追加 2026-07-25，修复方案追加 11.4.6）；(5) `CODE_WIKI.md` §4.4 GPU 后端章节代码示例与实际 `Device` trait 对齐——原写 3 方法 `device_type/allocate/launch`（不存在的 API）改为实际 4 方法 `name/device_type/memory_limit/is_available`；`CpuDevice`/`CudaDevice` 字段也对齐实际实现（CpuDevice 是 `name + memory_limit` 带 16GB simulated，CudaDevice 是 `device_id + name + total_memory + compute_capability` 带 24GB simulated + is_available 永远 true）；章节标题从"v0.3.3 脚手架"改为"v0.3.3 — CUDA C 源代码生成 + 模拟设备，未接 CUDA Runtime"；补充"不调用 nvcc、不生成 .ptx"说明；(6) `基本功核查.md` v2.3 → v2.4，第 94 项状态从 "✅ 严重" 升级为 "已解决（文档措辞修正）"，顶部统计表第十大类从"2 ✅ + 3 已解决"改为"1 ✅ + 4 已解决"，合计从"50 ✅ + 52 已解决"改为"49 ✅ + 53 已解决"。**性质**：文档失实风险消除，非代码 bug。**未触及代码**：gpu/ 模块实现保留现状（待 CUDA Toolkit 环境就绪后真正激活）。**改动文件清单**（4 个，纯文档）：`AUDIT.md`、`CODE_WIKI.md`、`基本功核查.md`、`MEMO.md`。**自举三路径未破坏**：零代码改动，路径 A/B/C 全不受影响。**禁止事项无违反**：仅编辑现有 .md 文件，未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。
>
> **2026-07-17 feat: 基本功核查快赢批次（第 106/93/49 项）**：总师委派执行"基本功核查快赢批次"前 3 项（第 4 项 UDP 进行中，本批次仅同步已完成的前 3 项），3 项独立功能扩展 + 1 项 bug 修复，文档部同步真理源。本批次针对 `基本功核查.md` v2.2 复核仍标 ⚠️/✅ 但实际可低风险解决的 3 项缺口，每项均通过测试验证 + 自举验证。**子条目 1（第 106 项 CSPRNG 修复）**：`tenth/src/runtime/interpreter/natives.rs` 修改 `random_int`/`random_float` 2 处——删除 `DefaultHasher + SystemTime` 熵源（SipHash-13 with fixed keys + 纳秒时刻，可预测），改用 `rand::thread_rng().r#gen::<u64>()` / `r#gen::<f64>()`，与 VM 路径（`runtime/vm/natives.rs:635-657` 已用 `rand::thread_rng()`）对齐。**性质**：bug 修复（解释器与 VM 行为不一致 + 安全漏洞）。**测试**：stdlib_test 132 passed + native_parity_test 35 passed + 自举通过。**子条目 2（第 93 项 yield 全链路接通）**：新建 `tenth/tests/yield_test.rs`（6 测试）；修改 7 文件接通 yield 关键字全链路——`parser/ast.rs` 新增 `ExprKind::Yield(Option<Box<Expr>>)` 变体；`parser/expr.rs:699-713` `parse_unary` 新增 Yield 分支（前缀一元语义，与 Await/Spawn 一致）；`hir/hir.rs` 新增 `HirExprKind::Yield(Option<Box<HirExpr>>)` 变体；`hir/lower/lower_expr.rs:1100-1110` Yield lowering 分支（类型推断为 Unit）；`hir/lower/closures.rs:171-175` 自由变量收集 Yield 分支；`hir/lower/backward_shape_pass.rs:191-193` 控制流检测 Yield 分支；`hir/lower/mod.rs:450-454` 类型替换 Yield 分支；`compile/bytecode.rs:634-645` Yield 编译分支（`Op::Pop` 丢弃 inner + `Op::Yield` + `Op::PushUnit`）；`runtime/interpreter/eval.rs:600-604` 解释器路径报错"yield 不支持解释器路径"。**关键设计**：yield 类型推断为 Unit（VM `Op::Yield` 不消费栈顶、不 push 返回值）；`yield expr` 中 expr 被求值但结果丢弃；前缀一元语义（与 Await/Spawn 一致）。**tenthc 无需同步**（lexer 已有 Yield token，tenthc 自举源码不用 yield）。**WASM 路径**：`compile/wasm/compile.rs:881-883` 已有 `_ => return Err(...)` 兜底，Yield 自然落入。**JIT 路径**：`compile/jit/translator.rs` 已对 `Op::Yield` 返回 Err 触发 JIT→VM fallback。**测试**：yield_test 6 passed + 全量 cargo test 全绿 + 自举通过。**子条目 3（第 49 项 Duration 类型，纯 .th 实现，路径 B）**：新建 `tenth/std/duration.th`（187 行，1 struct + 21 函数）+ `tenth/tests/duration_test.rs`（15 测试）；修改 `tenth/std/prelude.th` 添加 Duration 模块索引 + `tenth/tests/stdlib_test.rs` 添加 `th_parse_duration` 索引测试。**API**：`Duration` struct `{ nanos: i64 }`（i64 纳秒存储，覆盖 ±292 年）；构造（4）`from_secs/from_millis/from_micros/from_nanos`；访问（4）`as_secs/as_millis/as_micros/as_nanos`；算术（4）`add/sub/mul_scalar/div_scalar`；比较（6）`eq/lt/le/gt/ge/ne`；格式化（1）`to_string`（自动选单位 ns/us/ms/s/X.Ys/XmYs/XhYm + 负数前缀）；零值（2）`zero/is_zero`。**路径 B**：复用 `Type::Struct` / `Value::Struct` / `Op::NewStruct` 机制（同 Date 类型）。**纯标准库实现**：不修改任何 Rust 源码（不新增 native）。**关键技术决策**：发现 pre-existing bug（`eval.rs:28` `Literal::Int(n, _)` 和 `execute.rs:230` `Op::PushInt(n)` 硬编码 I32，忽略字面量 dtype），用 `date_i64_add_days` native 返回 I64 标记值的变通方案绕过。**测试**：duration_test 15 passed + stdlib_test 133 passed（含新增 `th_parse_duration`）+ 自举通过。**涉及模块清单**：（第 106 项）`tenth/src/runtime/interpreter/natives.rs`；（第 93 项）`tenth/src/parser/ast.rs` + `tenth/src/parser/expr.rs` + `tenth/src/hir/hir.rs` + `tenth/src/hir/lower/lower_expr.rs` + `tenth/src/hir/lower/closures.rs` + `tenth/src/hir/lower/backward_shape_pass.rs` + `tenth/src/hir/lower/mod.rs` + `tenth/src/compile/bytecode.rs` + `tenth/src/runtime/interpreter/eval.rs` + `tenth/tests/yield_test.rs`（新建）；（第 49 项）`tenth/std/duration.th`（新建）+ `tenth/std/prelude.th` + `tenth/tests/duration_test.rs`（新建）+ `tenth/tests/stdlib_test.rs`。**自举三路径未破坏**：路径 A（Rust 全栈）通过（测试全绿）；路径 B（Tenth 前端 + Rust 后端）通过（自举 `[OK]`）；路径 C（全 WASM 闭环）不涉及（未触及 tenthc/ 与 bridge.rs 与 wasm.rs）。**禁止事项无违反**：仅编辑现有 .rs 文件 + 在 `tenth/std/` `tenth/tests/` 允许目录内新建 4 个文件（`duration.th`/`duration_test.rs`/`yield_test.rs`，均在工作规范允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**PROJ-007 候选（i64 字面量 dtype bug，已发现但未修复）**：文件 `tenth/src/runtime/interpreter/eval.rs:28` + `tenth/src/runtime/vm/execute.rs:230`；问题 `Literal::Int(n, _)` 和 `Op::PushInt(n)` 忽略字面量 dtype 参数，硬编码为 `BaseType::I32`；影响：用户写 `1000i64` 字面量，parser/lexer 识别为 i64 dtype，但运行时被当作 I32，中间结果超过 i32 范围（2_147_483_647）时溢出报错；修复需触及：`Op::PushInt` 签名变更（增加 dtype 参数）+ `chunk.rs` 编码解码 + `bytecode.rs` 编译 + `execute.rs` + `eval.rs` + `wasm.rs` + `jit/translator.rs` + `tenthc/compile/bytecode.th`（自举同步）；性质：触及自举三路径 + Op 编码变更，应单独立项（PROJ-007），不在本快赢批次范围内修复。**文档同步**（本条目为文档部执行，纯文档改动）：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §4.6 Duration 类型 ❌→✅（引用 `tenth/std/duration.th` + 21 函数 + 15 测试）、§1.3 yield / 协程条目移除"已知限制：无语法层 yield 关键字"并补 2026-07-17 全链路接通说明（引用 `parser/expr.rs:699-713` + `bytecode.rs:634-645` + 6 测试）、§4.9 随机数安全 CSPRNG ❌→✅（引用 `interpreter/natives.rs` random_int/random_float 改用 `rand::thread_rng()`）、§九 总结矩阵 标准库（通用）✅ 55→57 / ❌ 100→98 / 完成率 34%→36%、合计 ✅ 215→217 / ❌ 344→342、头部日期保持 2026-07-17；`基本功核查.md` v2.2 → v2.3（第 49 项 ✅→已解决 + 第 93 项 ⚠️→已解决 + 第 106 项 ⚠️→已解决 + 顶部统计表 已解决 49→52 / ⚠️ 3→1 + 新增 PROJ-007 候选段记录 i64 字面量 dtype bug）。
>
> **2026-07-17 feat: PROJ-006 自定义可微算子（Custom Autograd Op）**：总师完成 PROJ-006 代码实施 + 测试验证（18 项测试全绿 + 自举通过），文档部同步真理源。本条目为护城河 F（关系调试器 T7 完备性定理）与护城河 A（autograd shape check）联合深化的产物，赋予 Tenth 用户在 Rust 端注册自定义可微算子的能力。**立项依据（4 项前置论证）**：(1) Tenth 闭包语义论证——自定义算子的前向/反向规则本质是用户提供的函数对，Tenth 闭包语法 `|x| expr`（参考 `std::collections::iter::map` / `accumulate_loop`）足以表达，无需新增语法层；(2) 护城河 A 兼容性论证——Custom 算子的 backward 输出梯度必须满足 shape 校验（与现有 27 算子一致的 `unbroadcast` + `Result` 传播机制），不能在 Custom 分支引入 silent squeeze；(3) 并发安全论证——Tenth 单线程 VM/Interpreter 模型下，`Rc<RefCell<CustomOpRegistry>>` 共享 Vm/Interpreter 与 Tape 是安全的（无需 `Arc<Mutex>`），且 `Tape.custom_ops: Option<Rc<RefCell<...>>>` 用 `Option` 包装保证默认 Tape 零开销；(4) TapeOp 代数性质论证（数理部 T7 完备性定理）——`TapeOpClass` 四类（Construct/Preserve/Reduce/Expand）已穷尽所有反向传播代数行为，Custom 算子必须声明所属 op_class 以维护 T7 完备性，关系调试器在 Custom 分支 fallback 到 Construct 类（最保守，与 Input/Param 同类）。**总师完整方案（选项 2：Rust 端注册版）**：用户在 Rust 代码中实现 `CustomBackward` trait，通过 `vm.register_custom_op()` / `interpreter.register_custom_op()` 注册到 `CustomOpRegistry`；Tenth 端通过 `std::autograd::call_custom_op1/2/3<T>` wrapper 调用 `__call_custom_op` native，op_id 作为第一个 Int 参数，后续为可变数量张量输入。**实现要点**：(1) **CustomBackward trait**（`tenth/src/runtime/autodiff/custom.rs`，173 行，新建）——5 必填方法 `forward / backward / op_class / name / ` + 2 可选方法 `forward_shape / backward_shapes`；`op_class` 返回 `TapeOpClass`（Construct/Preserve/Reduce/Expand 四类，对应护城河 F T7 完备性定理）；(2) **CustomOpRegistry**——`HashMap<usize, Box<dyn CustomBackward>>` + `name_to_id: HashMap<String, usize>` 索引 + 自增 `next_id: usize`，支持按 id 查找（backward 分发）和按 name 查找（注册去重）；(3) **TapeOp::Custom(usize) 新变体**（`runtime/autodiff/tape_op.rs`）——保持 `#[derive(Debug, Clone, PartialEq)]`，承载 `op_id`；(4) **op_name 返回类型 `&'static str` → `Cow<'static, str>`**（`runtime/autodiff/grad.rs`）——支持动态生成 `"Custom#{op_id}"`，原有 27 算子仍返回 `Cow::Borrowed` 零开销；(5) **Tape.custom_ops 字段 + set_custom_ops 注入模式**（`runtime/autodiff/mod.rs`）——`custom_ops: Option<Rc<RefCell<CustomOpRegistry>>>` 字段 + `set_custom_ops` 方法 + `custom_op` recording 方法，`Option` 包装保证默认 Tape 零开销，`Rc<RefCell<...>>` 共享 Vm/Interpreter 与 Tape；(6) **Vm/Interpreter 注册接口**（`runtime/vm/mod.rs` + `runtime/interpreter/core.rs`）——Vm/Interpreter 添加 `custom_ops: Rc<RefCell<CustomOpRegistry>>` 字段（new() 初始化空 registry）+ `register_custom_op` 方法 + `custom_ops()` 访问器；(7) **`__call_custom_op` native 注册**（`runtime/natives.rs` + `runtime/interpreter/natives.rs`）——op_id 作为第一个 Int 参数，后续为可变数量张量输入；backward native 注入 `tape.set_custom_ops(custom_ops)` 把 Vm/Interpreter 的 registry 共享给 Tape；(8) **relation_debugger.classify_tape_op 新签名**（`runtime/relation_debugger.rs`）——`(op, registry: Option<&CustomOpRegistry>)`，Custom 分支查 registry 调用 `custom_op.op_class()`，fallback 到 Construct（若 registry 为 None 或 op_id 不存在，维护 T7 完备性保守策略）；(9) **backward.rs 5 处 `unbroadcast(..., op_str)?` 改为 `unbroadcast(..., &op_str)?`**——适配 `op_name` 返回 `Cow` 后的 `&str` 借用；(10) **标准库 wrapper**（`tenth/std/autograd.th`，新建）——三个泛型 wrapper `call_custom_op1/2/3<T>` 封装 `__call_custom_op` native；(11) **HIR 白名单 + 类型签名**（`hir/lower/closures.rs` + `lower_expr.rs` + `runtime/interpreter/eval.rs` + `hir/lower/types.rs`）——白名单添加 `"__call_custom_op"`，在 gather 类型签名之后添加 `__call_custom_op` 类型签名；(12) **prelude.th 索引同步**——built-in 部分添加 `__call_custom_op` 说明；std lib 索引添加 `std::autograd::call_custom_op1/2/3`。**触及模块清单**（17 个文件）：`tenth/src/runtime/autodiff/custom.rs`（新建）、`tenth/src/runtime/autodiff/tape_op.rs`、`tenth/src/runtime/autodiff/grad.rs`、`tenth/src/runtime/autodiff/backward.rs`、`tenth/src/runtime/autodiff/mod.rs`、`tenth/src/runtime/vm/mod.rs`、`tenth/src/runtime/interpreter/core.rs`、`tenth/src/runtime/interpreter/natives.rs`、`tenth/src/runtime/interpreter/eval.rs`、`tenth/src/runtime/natives.rs`、`tenth/src/runtime/relation_debugger.rs`、`tenth/src/hir/lower/closures.rs`、`tenth/src/hir/lower/lower_expr.rs`、`tenth/src/hir/lower/types.rs`、`tenth/std/autograd.th`（新建）、`tenth/std/prelude.th`、`tenth/tests/custom_autograd_test.rs`（新建，18 项测试）+ `tenth/tests/relation_debugger_test.rs`（3 处 classify_tape_op 调用补齐第二参数 `None`）。**未触及**：`tenthc/`、`compile/bridge.rs`、`compile/wasm.rs`、`lexer/token.rs`——自举三路径 A/B/C 全部未破坏（仅运行时层 + 标准库 + HIR lower 白名单改动，无编译器前端 / WASM 后端 / bridge 数据结构变更）。**护城河影响**：(1) 护城河 A（autograd shape check）——Custom 算子在 backward 阶段校验梯度数量（与 `backward_shapes` 声明一致）和每条梯度 shape（与输入 shape 通过 `forward_shape`/`backward_shapes` 推断），不通过则返回 `ShapeMismatch` 错误（与现有 27 算子一致），消除 Custom 分支的潜在 silent squeeze；(2) 护城河 F（relation_debugger T7 完备性定理）——`classify_tape_op` 支持 Custom 分支，通过 `custom_op.op_class()` 获取真实代数类，registry 不可用时 fallback 到 Construct 类（最保守，与 Input/Param 同类），维护四类互斥完备性，T7 定理依然成立。**测试覆盖**：`custom_autograd_test.rs` 18 项测试——基础前向/反向（前向数值正确性 + 反向梯度数值正确性）+ op_class 声明验证（4 类 Construct/Preserve/Reduce/Expand 各一）+ shape 校验（forward_shape/backward_shapes 声明 + 不匹配报错）+ finite-difference 数值梯度对比（与解析梯度相对误差 < 1e-5）+ 与 matmul+sum 组合（Custom op 嵌入复合计算图链式传播）+ 错误情况（op_id 不存在 / 梯度数量不符 / shape 不符 / registry 未注入）+ 深度覆盖（多输入多输出 / op_class 与实际行为一致性 / registry 共享语义）。**验证结果**：(1) `cargo test --manifest-path tenth/Cargo.toml --test custom_autograd_test` → 18 passed / 0 failed ✅；(2) 全量 `cargo test` exit 0，无回归 ✅；(3) 自举验证 `cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` → `[OK] tenthc_full.wasm` ✅。**自举三路径未破坏**：路径 A（Rust 全栈）通过（测试全绿）；路径 B（Tenth 前端 + Rust 后端）通过（自举 `[OK]`）；路径 C（全 WASM 闭环）不涉及（未触及 tenthc/ 与 bridge.rs 与 wasm.rs）。**已知限制**：(1) 仅支持 Rust 端注册——Tenth 端无法直接定义 `CustomBackward` trait impl（Tenth 无 trait impl 语法），用户必须通过 Rust 代码扩展；(2) Tenth 单线程模型下 `Rc<RefCell<>>` 共享安全——多线程场景需升级为 `Arc<Mutex>`，但 Tenth VM/Interpreter 当前单线程；(3) Custom 算子未接入 JIT 路径——JIT translator 未识别 `TapeOp::Custom`，遇 Custom op 触发既有 JIT→VM fallback（与 async/pool 等模式一致）；(4) Custom 算子未接入 WASM 路径——`compile/wasm/compile.rs` 无 Custom 分支，WASM 路径下 Custom op 不可用（与 async/JIT 训练步等限制一致）。**与 PROJ 编号体系对应**：本条目为 PROJ-006（自定义可微算子），继 PROJ-005 等任务后由总师委派执行，状态由"立项"升级为"已完成"。**禁止事项无违反**：仅编辑现有 .rs 文件 + 在 `tenth/std/` `tenth/tests/` 允许目录内新建 3 个文件（`autograd.th`/`custom.rs`/`custom_autograd_test.rs`，均在工作规范允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**文档同步**（本条目为文档部执行，纯文档改动）：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §5.2 自定义算子 ❌→✅（详尽说明 CustomBackward trait + CustomOpRegistry + TapeOp::Custom + `__call_custom_op` native + `call_custom_op1/2/3<T>` wrapper + shape 校验 + relation_debugger Custom 分支 + op_class 四类）、§九 总结矩阵 AI 特定需求 ✅ 67→68 / ❌ 89→88 / 完成率 41%→42%、合计 ✅ 214→215 / ❌ 345→344、头部日期保持 2026-07-17。
>
> **2026-07-17 feat: 标准库扩展第三波 — F16/BF16 算子补齐 + Date 类型（路径 B）**：总师完成"标准库扩展第三波"两项的实施 + 验证（测试全绿 + 自举通过），文档部同步真理源。本条目对应计划文档 `.trae/tmp/stdlib_extension_plan.md` 第三波两项交付清单。**交付项 1（第 7 项：补齐 F16/BF16 算子覆盖，选项 3，AMP 暂缓）**：`tenth/src/runtime/tensor/methods.rs` 修改 9 个算子（layer_norm/matmul/bmm/im2col/max_pool2d_with_argmax/avg_pool2d/sum_axis/cat/select）；新建 `tenth/tests/f16_coverage_test.rs` 33 个测试。**P0 修复**：layer_norm F16/BF16 panic 隐患消除——原实现 `if self.is_f32() { ... } else { self.data.as_f64().unwrap() }` 会让 F16/BF16 输入 panic，改为 `match self.dtype` 四分支（F32/F16/BF16/默认 f64），F16/BF16 转 f32 计算后转回原 dtype。**P1 补齐（核心 NN 算子）**：matmul 新增 `matmul_low_prec` 私有方法（F16/BF16 输入转 f32 计算，结果按输入 dtype 转回）；bmm 新增 `bmm_low_prec`（同 matmul 模式）；im2col F16/BF16 分支输出保留原 dtype（之前转 f64）；max_pool2d_with_argmax/avg_pool2d 输出构造新增 F16/BF16 分支。**P2 补齐（次要算子）**：sum_axis F16/BF16 转 f32 计算后转回原 dtype（之前走 f64 输出 F64）；cat 新增 dtype 提升表（同 dtype 保留，混合提升到 F64）；select then/else 同为 F16 或同为 BF16 时保留原 dtype。**关键设计**：F16/BF16 算子走 f32 中间计算（利用 f32 数值精度），结果转回原 dtype；不修改 autodiff backward（dispatch_float! 宏已覆盖 F16/BF16，走 f32 中间路径）；不修改 ops.rs（Phase 1 兼容层移除是 Phase 3/4 整体重构，不在本次范围）；不触及 HIR/tenthc/自举三路径。**AMP 状态修正（重要）**：能力全梳理第 531 行和第 615 行标注"AMP 已完成"与实际不符——实际只完成 F32 中间累加（acc_grad 部分），第 570 行"混合精度梯度 | fp16 梯度 | ❌ 未实现"是准确的；autocast/GradScaler/loss_scaling 全 src 无任何实现；本次只补齐算子覆盖，AMP（autocast + GradScaler）暂缓，状态修正为"部分完成"。**交付项 2（第 8 项：Date 类型，路径 B，复用 struct 机制）**：`tenth/src/runtime/interpreter/datetime.rs` 新增 `date_to_days`（Howard Hinnant civil_from_days 算法）+ `days_to_date_i64` + 11 个 Rust 单元测试；`tenth/src/runtime/natives.rs` 注册 5 个 date native（date_to_unix_days/date_from_unix_days/date_i64_add_days/date_diff_days/date_day_of_week）；`tenth/src/runtime/interpreter/natives.rs` interpreter 侧同步注册；`tenth/src/hir/lower/closures.rs` + `lower_expr.rs` + `eval.rs` native 白名单扩展；`tenth/src/hir/lower/types.rs` native 类型签名；新建 `tenth/std/date.th`（Date struct + 构造/转换/格式化/运算/比较函数）；`tenth/std/prelude.th` Date native 索引 + std::date 模块索引；新建 `tenth/tests/date_test.rs` 30 个 VM 路径测试；`tenth/tests/stdlib_test.rs` 新增 th_parse_date 索引测试（132 passed）；新建 `tenth/std/test_date.th`（Tenth 源码测试，parse 路径覆盖，运行时 use 加载暂不支持）。**关键设计**：路径 B 复用 Type::Struct / Value::Struct / Op::NewStruct，不新增 HIR 类型；算法采用 Howard Hinnant date_algorithms（工业级，已验证），date_to_days 和 days_to_date 互逆；native 名 `date_i64_add_days` 带 `_i64_` 前缀是工程妥协，避免与 user function `date_add_days(Date, i64)` 同名冲突；不触及 tenthc/自举三路径/护城河；遗留：test_date.th 无法直接运行（运行时不支持 use 加载 .th 模块），parse 路径已覆盖。**涉及模块清单**：（第 7 项）`tenth/src/runtime/tensor/methods.rs`、`tenth/tests/f16_coverage_test.rs`（新建）；（第 8 项）`tenth/src/runtime/interpreter/datetime.rs`、`tenth/src/runtime/natives.rs`、`tenth/src/runtime/interpreter/natives.rs`、`tenth/src/hir/lower/closures.rs` + `lower_expr.rs` + `eval.rs`、`tenth/src/hir/lower/types.rs`、`tenth/std/date.th`（新建）、`tenth/std/prelude.th`、`tenth/tests/date_test.rs`（新建）、`tenth/tests/stdlib_test.rs`、`tenth/std/test_date.th`（新建）。**测试覆盖**：f16_coverage_test 33 passed（7 组：layer_norm 4 + matmul 4 + bmm 3 + im2col/Conv2D 4 + pool 4 + sum_axis/cat/select 7 + autodiff 端到端 3）；date_test 30 passed；stdlib_test 132 passed（含新增 `th_parse_date`）；全量测试无回归。**自举状态**：自举三路径 A/B/C 未破坏——`cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` 通过；未触及 tenthc/，路径 C 不涉及。**与计划文档对应**：`.trae/tmp/stdlib_extension_plan.md` 第三波两项交付清单落地，状态由"计划中"升级为"已完成"。**禁止事项无违反**：仅编辑现有 .rs 文件 + 在 `tenth/std/` `tenth/tests/` 允许目录内新建 4 个文件（`date.th`/`test_date.th`/`date_test.rs`/`f16_coverage_test.rs`，均在工作规范允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**文档同步**（本条目为文档部执行，纯文档改动）：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §5.1 多精度张量 ⚠️→✅（F16/BF16 算子覆盖补齐，仅余 ops.rs Phase 1 兼容层属后续 Phase 3/4 整体重构）、§5.1 AMP 条目 + §5.3 AMP 条目 ✅→⚠️（修正"已完成"为"部分完成"，仅 F32 中间累加策略，autocast/GradScaler/loss_scaling 未实现）、§5.1 混合精度梯度保持 ❌（准确）、§4.6 日期解析/格式化/运算 ❌→✅ + 新增 Date 类型 ✅ + 新增 星期计算 ✅（时区/Duration/Instant 保持 ❌）、§九 总结矩阵 AI 特定需求 ✅ 68→67/⚠️ 5→6（多精度张量 ⚠️→✅ + AMP §5.1/§5.3 两次 ✅→⚠️）、标准库（通用）158→160/✅ 50→55/❌ 103→100（3 项日期 ❌→✅ + 新增 Date 类型 + 新增 星期计算 2 项）、合计 583→585/✅ 210→214/⚠️ 25→26/❌ 348→345、头部日期保持 2026-07-17。
>
> **2026-07-17 feat: 标准库扩展第二波第 4 项 — Where/Select + 张量比较（路径 Z）**：总师完成"标准库扩展第二波第 4 项"代码实现 + 测试验证（compare_test 13 项全绿 + 自举通过），文档部同步真理源。本条目对应计划文档 `.trae/tmp/stdlib_extension_plan.md` 第二波第 4 项交付清单。**交付项**：(1) 6 个张量比较 native `tensor_gt/lt/ge/le/eq/ne`（VM 侧 `runtime/natives.rs` + 解释器侧 `runtime/interpreter/natives.rs` 双路径注册，`runtime/tensor/methods.rs` 新增 `compare_binary` helper + 6 个比较方法，返回 F64 张量）；(2) `std/nn/ops.th`（新建）6 个泛型比较 wrapper + `where_` wrapper（select 别名）；(3) `std/prelude.th` Built-in native 段 + Std lib 段索引更新；(4) `tenth/tests/compare_test.rs`（新建）13 项 VM 路径测试；(5) `stdlib_test.rs` 新增 `th_parse_nn_ops` 索引测试。**路径 Z 核心决策**（数理部论证推荐）：不新增 HIR 类型变体、不新增 TensorData 变体、不新增 TapeOp——bool 沿用 f64 0.0/1.0 编码（已是项目事实标准），比较结果固定 F64 张量，`hir/lower/types.rs` `resolve_builtin` 用 `broadcast_shapes` 推断输出 shape 返回 `Type::tensor(BaseType::F64, broadcast_dims)`。**数理论证结论引用**：select 原语已完整实现（2026-07-02 引入，TapeOp::Select 链式法则 `d_then=grad*cond_mask`/`d_else=grad*(1-cond_mask)`，cond 不可微），`where_` 是 select 的语义别名无需新增算子，bool 用 f64 0/1 编码已是项目事实标准（select 的 cond 即 f64 掩码）。**关键设计**：(1) 比较运算不可微（不进入 tape），作为常量掩码；(2) autodiff 通过 select 耦合——`where_(gt(x, 0), x, 0)` = ReLU(x)，梯度经 select 路由；(3) 跨 dtype 输入 cast 到 f64 视图，结果固定 F64；(4) 命名避关键字——`where_`/`else_` 后缀避开 Tenth 关键字。**涉及模块清单**：`tenth/src/runtime/tensor/methods.rs`、`tenth/src/runtime/natives.rs`、`tenth/src/runtime/interpreter/natives.rs`、`tenth/src/hir/lower/closures.rs` + `lower_expr.rs` + `eval.rs`（native 白名单各 +1 行）、`tenth/src/hir/lower/types.rs`（`resolve_builtin` 比较返回类型推断）、`tenth/std/nn/ops.th`（新建）、`tenth/std/prelude.th`、`tenth/tests/compare_test.rs`（新建）、`tenth/tests/stdlib_test.rs`。**测试覆盖**：compare_test 13 passed / 0 failed（前向数值正确性 + 广播 + 跨 dtype + where_ 别名 + autodiff 端到端 ReLU 等价）；stdlib_test 131 passed（含新增 `th_parse_nn_ops`）；全量 cargo test 0 failed。**自举状态**：自举三路径 A/B/C 未破坏——`cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` 通过（输出 `[OK] tenthc_full.wasm`）；未触及 tenthc/，路径 C 不涉及。**与计划文档对应**：`.trae/tmp/stdlib_extension_plan.md` 第二波第 4 项交付清单落地，状态由"计划中"升级为"已完成"。**禁止事项无违反**：仅编辑现有 .rs 文件 + 在 `tenth/std/` `tenth/tests/` 允许目录内新建 2 个文件（`ops.th`/`compare_test.rs`，均在工作规范允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**文档同步**（本条目为文档部执行，纯文档改动）：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §5.1 张量比较 ❌→✅、§5.1 Where/Select 条目补充 where_ 别名说明、§九 总结矩阵 AI 特定需求 ✅ 67→68 / ❌ 90→89 / 完成率 41%→42%、合计 ✅ 209→210 / ❌ 349→348。
>
> **2026-07-17 test: 标准库扩展第二波第 5 项 — 广播规则完善测试补齐**：总师委派运行时部执行标准库扩展第二波中风险最低的"广播规则完善"任务，本条目记录三阶段交付。**阶段 1 审计结论**：通读 `runtime/tensor/methods.rs`（`broadcast_shape` 第 633-648 行 NumPy 风格右侧对齐 + 1 可广播到任意 + 相等维度保留；`promote_dtype` 第 654-666 行 F64+任何→F64/F32+F32→F32/F16+F16→F16/BF16+BF16→BF16/F16+BF16→F32；`elementwise_binary` 第 671-743 行统一处理 add/sub/mul/div + 广播 + dtype 提升；`add_scalar/sub_scalar/mul_scalar/div_scalar/div_scalar_inv` 第 583-625 行标量运算；`select` 第 1379-1428 行三输入 NumPy 广播；自由函数 `broadcast_shape(shapes: &[&[usize]])` 第 2191-2208 行多 shape 广播；`broadcast_to_owned` 第 2210-2224 行单 shape 广播）、`runtime/tensor/data.rs`（`TensorData::broadcast` 第 173-177 行基于 ndarray view.broadcast）、`runtime/autodiff/backward.rs`（`unbroadcast<E: FloatElem>` 第 1172-1236 行右侧对齐 + 对 target_shape==1 且 grad>1 的轴 sum_axis 累加 + reshape 兜底；Add/Sub/Mul/Div backward 第 72-125 行全部调用 unbroadcast；Select backward 第 604-635 行对 then/else 调用 unbroadcast）、`runtime/autodiff/mod.rs`（`test_backward_add_broadcast` 第 501-529 行已有 w[2]+b[1]→b.grad=2 验证）。**审计结论**：广播基础设施已完整——前向 elementwise_binary + 标量运算 + select 三输入广播 + 跨 dtype promote_dtype 全覆盖；反向 unbroadcast 在所有需要处（Add/Sub/Mul/Div/Select）已正确调用且泛型化支持 f32/f64；MaxPool2D/AvgPool2D/Conv2D 前向输入 shape 与输出 shape 已对齐无需广播；matmul 是矩阵乘法语义非元素级广播；Tenth 无逐元素比较运算符。**无需修改运行时代码**。**阶段 2 决策**：任务从"补齐缺失广播场景"转为"补齐运行时数值正确性测试覆盖"——审计发现基础设施完整，但运行时层数值正确性测试覆盖存在缺口（已有测试多为编译期 shape 检查或 autodiff 端到端，缺少针对广播规则本身的数值正确性专项测试）。**阶段 3 测试覆盖**：新建 `tenth/tests/broadcast_test.rs`（650 行，33 个测试），6 大场景覆盖——**一元广播**（3 项：`(1,)→(3,)`、`(1,1)→(2,3)`、`(3,)→(1,3)`）、**二元广播**（9 项：`(3,)+(2,3)→(2,3)`、`(2,1)+(1,3)→(2,3)`、`(1,3)+(2,1)→(2,3)`、`(2,1)*(1,3)→(2,3)`、`(2,3)-(3,)→(2,3)`、`(2,3)/(2,1)→(2,3)`、同 shape、`(2,2,3)+(3,)→(2,2,3)`、`(1,5)+(3,5)→(3,5)`）、**标量广播**（6 项：`add_scalar`/`mul_scalar`/`sub_scalar`/`div_scalar`/`div_scalar_inv` + 组合）、**跨 dtype 广播**（4 项：f32+f64→f64、f64+f32→f64、f32+f32→f32、f32*f64→f64）、**反向广播 autodiff**（5 项：`Add` 1D→2D grad 沿广播轴 sum=2、`Mul` col×row grad 互为对方形状、`Sub` 2D-1D grad 形状对齐、`Div` 2D/col grad 形状对齐、`Add` scalar→2D grad 标量累加）、**错误情况**（5 项：不可广播 shape 返回 Err——1D+1D 不兼容、2D+2D 不兼容、2D+1D 不兼容、3D+2D 不兼容、Mul 不兼容）。**辅助函数**：`make_tensor`/`make_tensor_f32`/`make_tensor_rc`（构造器）、`approx_eq`（1e-10 容差）、`tensor_to_vec`（数据提取）。**关键测试模式**：autodiff 测试用 `Tape::new()` + `tape.input()` + `tape.binary_direct(TapeOp::Add, ...)` + `tape.backward(r_id)` 手动构建计算图，验证 `x.borrow().grad` 的 shape 与数值正确性（如 1D→2D 广播的 Add backward，x.grad shape 应为 (3,) 且每个元素=2，沿广播轴 size=2 求和）。**验证结果（三重验证全绿）**：(1) `cargo test --manifest-path tenth/Cargo.toml --release --test broadcast_test` → 33 passed / 0 failed / 0 ignored ✅；(2) `cargo test --manifest-path tenth/Cargo.toml --release` → exit 0，全量测试无回归 ✅；(3) `cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm` ✅。**自举三路径未破坏**：路径 A（Rust 全栈）通过（全量测试全绿）；路径 B（Tenth 前端 + Rust 后端）通过（自举 `[OK]`）；路径 C（全 WASM 闭环）不涉及（仅改运行时层 + 测试，未触及 tenthc/ 与 bridge.rs）。**遗留问题**：无——未发现需要触及 HIR/tenthc 的场景；广播基础设施在运行时层已完整，本次仅补齐数值正确性测试覆盖。**改动文件清单**（1 个）：`tenth/tests/broadcast_test.rs`（新建，650 行，33 个测试）。**禁止事项无违反**：仅新建 1 个测试文件（在 `tenth/tests/` 允许目录内），未在禁止目录创建 .th 文件，未修改任何运行时源码（审计结论无需修改），未破坏已有测试（全量 cargo test exit 0），未破坏自举三路径。**文档同步**：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §5.1 广播规则完善条目追加测试覆盖说明（状态保持 ✅，补充 2026-07-17 测试补齐记录）。
>
> **2026-07-17 feat: 标准库扩展第一波（学习率调度器 + MaxPool/AvgPool + TCP服务端/子进程）**：总师完成"标准库扩展第一波"三项工作的代码实现 + 测试验证（千余测试全绿 + 自举通过），文档部同步真理源。本条目对应计划文档 `.trae/tmp/stdlib_extension_plan.md` 第一波三项交付清单。**交付项 1（学习率调度器，纯 stdlib 无 native）**：新建 `tenth/std/optim/lr_schedule.th` 实现 6 个 f64 标量函数 `cosine_lr / step_lr / exp_lr / warmup_lr / warmup_cosine_lr / warmup_step_lr`（参考 PyTorch CosineAnnealingLR / StepLR / ExponentialLR / LambdaLR 语义）；新建 `tenth/std/optim/test_lr_schedule.th` 单测源码；`tenth/std/prelude.th` 第 129 行新增索引行；`tenth/tests/stdlib_test.rs` 新增 `th_parse_optim_lr_schedule` 测试。**边界设计要点 1**：用 `LR_EPS = 1e-12` 防 0 除（cosine 衰减分母 `1 + cos(...)` 在 t=T_max 处理论为 0），warmup 阶段线性插值，warmup 后切换主调度器（cosine/step），`warmup_step_lr` 中 step_lr 的 `gamma^epoch` 指数衰减由步进累乘规避 Tenth 标量无 `pow` 方法限制。**交付项 2（MaxPool/AvgPool 含 autodiff backward）**：新建 `tenth/std/nn/pool.th` 4 个泛型函数 `max_pool2d<T> / max_pool2d_explicit<T> / avg_pool2d<T> / avg_pool2d_explicit<T>` 包装 `tensor.max_pool2d(...)` / `tensor.avg_pool2d(...)` 方法；新建 `tenth/tests/pool_test.rs` 8 个 VM 路径集成测试（forward basic/stride/padding + backward argmax 路由 + avg 均分 + padding 边界）；`tenth/std/prelude.th` 第 112 行新增索引行；`runtime/autodiff/*` 新增 `TapeOp::MaxPool2D / AvgPool2D` 反向传播规则（max_pool argmax 路由，avg_pool 均分到 window 内有效位置）；`runtime/tensor/methods.rs` 新增 `Tensor::max_pool2d / avg_pool2d` 方法（前向 + 反向 tape 记录）；`runtime/interpreter/methods.rs` + `runtime/vm/natives.rs` 方法注册。**边界设计要点 2**：avg_pool 反向按 window 内有效位置均分梯度，采用 `count_include_pad=False`（PyTorch 默认 True 但 ML 生态共识 False 更常见），padding 边界有效位置数 < kernel_size 时梯度按有效数归一化（非按 kernel_size）。max_pool 反向用 argmax 路由，重复 max 值取首个位置（与 PyTorch `return_indices=True` 一致）。**交付项 3（TCP 服务端 + 执行子进程）**：`tenth/std/net.th` 新增 `listen / accept / listener_close` 三函数包装 `tcp_listen / tcp_accept / tcp_listener_close` native；新建 `tenth/std/process.th` 4 函数 `new / arg / run / output` 包装 `command_new / command_arg / command_run / command_output` native；`tenth/tests/net_test.rs`（已存在）新增 3 个测试用例 `test_tcp_listen_success / test_tcp_listen_port_in_use / test_tcp_listener_close_then_accept`；新建 `tenth/tests/process_test.rs` 5 个集成测试（command_new/command_output/command_run/二次 output 消费句柄/无效句柄）；`tenth/std/prelude.th` 第 68-83 行新增 TCP/Subprocess built-in native 文档段；第 150-151 行新增 net::listen/accept/listener_close 和 process 索引；`runtime/natives.rs` + `runtime/interpreter/natives.rs` + `runtime/vm/natives.rs` 注册 6 个 native（含跨平台适配，Windows echo 走 `cmd /c`）。**边界设计要点 3**：process 句柄消费语义——`command_run` 与 `command_output` 互斥消费句柄，二次调用返回 `Err`（句柄失效），与 TCP handle table 模式一致；command_output 同时执行并捕获 stdout/stderr/exit_code；跨平台 `command_run` 在 Windows 调用 `cmd /c` 包装保证 echo 等内建命令可用。**涉及模块清单**（按交付项）：(1) `tenth/std/optim/lr_schedule.th`（新建）+ `tenth/std/optim/test_lr_schedule.th`（新建）+ `tenth/std/prelude.th` + `tenth/tests/stdlib_test.rs`；(2) `tenth/std/nn/pool.th`（新建）+ `tenth/std/prelude.th` + `tenth/src/runtime/autodiff/*` + `tenth/src/runtime/tensor/methods.rs` + `tenth/src/runtime/interpreter/methods.rs` + `tenth/src/runtime/vm/natives.rs` + `tenth/tests/pool_test.rs`（新建）；(3) `tenth/std/net.th` + `tenth/std/process.th`（新建）+ `tenth/std/prelude.th` + `tenth/src/runtime/natives.rs` + `tenth/src/runtime/interpreter/natives.rs` + `tenth/src/runtime/vm/natives.rs` + `tenth/tests/net_test.rs` + `tenth/tests/process_test.rs`（新建）。**测试覆盖**：stdlib_test 130 项（含 `th_parse_optim_lr_schedule`）/ pool_test 8 项（VM 路径集成）/ net_test 9 项（含 3 项 TCP 服务端新增）/ process_test 5 项（command 新建/output/run/二次消费/无效句柄）。**自举状态**：自举三路径 A/B/C 未破坏——`cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` 通过（输出 `[OK] Full compiler compiled to tenthc_full.wasm`）；路径 A（Rust 全栈）测试全绿；路径 B（Tenth 前端 + Rust 后端）通过；路径 C（全 WASM 闭环）不涉及（tenthc 自举源码不使用 lr_schedule/pool/process/net 服务端 native）。**与计划文档对应**：`.trae/tmp/stdlib_extension_plan.md` 第一波三项交付清单全部落地，状态由"计划中"升级为"已完成"。**禁止事项无违反**：仅编辑现有 .rs 文件 + 在 `tenth/std/` `tenth/tests/` 允许目录内新建 5 个文件（`lr_schedule.th`/`test_lr_schedule.th`/`pool.th`/`process.th`/`pool_test.rs`/`process_test.rs`，均在工作规范允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试，未破坏自举三路径。**文档同步**（本条目为文档部执行，纯文档改动）：`MEMO.md` 本条记录；`能力梳理/能力全梳理.md` §4.5 TCP 服务端 ❌→✅、§4.7 执行子进程 ❌→✅、§5.1 Pooling ❌→✅、§5.3 MaxPool/AvgPool ❌→✅、§5.3 学习率调度器 ❌→✅、§5.4 学习率调度 ❌→✅、§5.4 预热策略 ❌→✅、§九 总结矩阵更新、头部日期 2026-07-11→2026-07-17。
>
> **2026-07-13 feat: B批 基本功核查第四大点 — 字符串/文本处理（第 38-44 项）全部解决**：落实 `基本功核查.md` 第四大点"字符串/文本处理不够"7 项缺口。**第 41 项 f"..." 模板字符串**：lexer 新增 FString(Vec\<StringPart\>) 变体 + lexer.rs `f"..."` 前缀检测；parser/ast.rs 新增 ExprKind::FString；HIR lowering（lower_expr.rs:1100）把 f"hello {name}" 编译为 `format("hello {}", name)` 调用；tenthc 双侧同步（token.th disc=89 + lexer.th + parser.th）；bridge.rs 新增 "fstring" case。**第 38 项 format() 命名参数**：format() native 支持 `format("hello {name}", "name", "world")` 形式，命名参数以 (key, value) 对形式追加在位置参数之后。**第 39 项 format() 格式说明符**：新增 `count_placeholders(template) -> (usize, bool)` 函数（natives.rs + interpreter/natives.rs 双侧），正确扫描 `{}`、`{:>5}`、`{:.2f}`、`{0}`、`{0:spec}` 等位置占位符与 `{name}`、`{name:spec}` 命名占位符（原 `template.matches("{}").count()` 仅计 `{}`，遗漏带格式说明符的占位符）。**第 40 项 format() 越界报错**：位置参数不足时返回 `Result::Err`，不再静默用空串填充。**第 42 项 Unicode NFC/NFD 规范化**：新增 `unicode_nfc(s)`/`unicode_nfd(s)` native（unicode-normalization crate）。**第 43 项 UTF-8/UTF-16/GBK 编码转换**：新增 8 个 native——`str_to_utf16`/`utf16_to_str`（Vec\<i64\> 双向）、`str_to_bytes`/`bytes_to_str`、`to_utf8`/`to_utf16`/`from_utf16`（别名）、`to_gbk`/`from_gbk`（encoding_rs crate）。**第 44 项 Base64/Hex/URL 编解码**：新增 6 个 native——`base64_encode`/`base64_decode`（base64 crate）、`hex_encode`/`hex_decode`、`url_encode`/`url_decode`（percent-encoding crate）。**4 类 bug 修复**：(1) **VM 编译器 Unit 返回值覆盖**——bytecode.rs `compile()` 在 `return_type == Unit` 时无条件 emit `Op::PushUnit`，覆盖 body 实际返回值（如 `fn main() { f"..." }` 返回字符串被覆盖为 Unit）；修复为检查 `func.body.ty`，仅当 body 也推断为 Unit/Never 时才 emit PushUnit。(2) **native 注册名遗漏**——`base64_encode`/`hex_encode`/`url_encode` 三个 native 在 VM 和解释器两侧仍注册为 `_base64_encode` 等带下划线前缀的旧名（原设计是 std 包装层转发 `_name` → `name`，B批删除包装层后名字未全部同步）；修复为去掉下划线前缀。(3) **解释器 eval_expr Var 分支硬编码名单缺 17 个 B批 native**——`interpreter/eval.rs:68` 的 native 名单仅含 regex_* 等，缺 unicode_nfc/str_to_utf16/base64_encode 等 17 个；补充名单后解释器路径可正确解析 B批 native 为 `Value::FnRef`。(4) **ArrayLiteral 双重反转 bug**——bytecode.rs `ArrayLiteral` 编译用 `iter().rev()` 压栈，但 `Op::MakeVec` 执行时又 `reverse()` 一次，导致数组元素顺序与源码相反（`[255, 0, 128]` 被处理为 `[128, 0, 255]`）；修复为正向压栈。**RawString/MultiLineString parser 支持**：parser/expr.rs 新增 `TokenKind::RawString(s)` 和 `TokenKind::MultiLineString(s)` 匹配分支（此前 parser 未处理这两种 token，导致 `r"hello {name}"` 原始字符串在函数参数位置报"意外的标记"语法错误）。**Cargo.toml 新增 4 依赖**：unicode-normalization、base64、percent-encoding、encoding_rs。**标准库清理**：删除 5 个 std 包装层文件（unicode.th、encoding.th、string/codec.th、string/encoding.th、string/unicode.th），它们调用的 `_name` native 已不存在；prelude.th 新增 B批 17 个 native 索引（标注为内置，无需 import）。**HIR 白名单同步**：lower_expr.rs Var 分支 known-natives 名单新增 17 个 B批 native（否则标识符被拒绝为"未定义变量"）。**测试**：新增 `tenth/tests/string_encoding_test.rs`（41 项测试，覆盖 f-string VM/解释器双路径、format 命名参数/格式说明符/越界、Unicode NFC/NFD、UTF-16/bytes/GBK 编码、Base64/Hex/URL 编解码）。**验证结果**：string_encoding_test 41/41 全绿；vm_autodiff_test 15/15、jit_test 5/5、parser_test 5/5、lexer_test 14/14、stdlib_test 全绿、struct_test 8/8、enum_test 12/12、tuple_test 12/12、trait_test 9/9、type_inference_test 40/40、pattern_match_test、try_operator_test、ownership_test、int_types_test 全绿，0 回归；lib 23/23。**预存问题（非本次引入，git stash 验证）**：selfhost_frontend::tenthc_lowers_own_source 栈溢出（Windows debug 模式栈空间限制，stash 后仍失败）。**自举三路径未破坏**：路径 A 通过（测试全绿）；路径 B 不涉及（tenthc 自举源码不使用 B批 native，但 tenthc lexer/parser 已同步 f"..." 语法）；路径 C 不涉及。**改动文件清单**（17 个）：Rust 源码——`lexer/token.rs`（FString 变体）、`lexer/lexer.rs`（f"..." 检测）、`parser/ast.rs`（ExprKind::FString）、`parser/expr.rs`（FString 解析 + RawString/MultiLineString 分支）、`hir/lower/lower_expr.rs`（FString lowering + 17 native 白名单）、`compile/bytecode.rs`（Unit 返回值覆盖修复 + ArrayLiteral 双重反转修复）、`compile/bridge.rs`（fstring case）、`runtime/natives.rs`（count_placeholders + 17 native 注册名修正）、`runtime/interpreter/natives.rs`（count_placeholders + 17 native 注册名修正）、`runtime/interpreter/eval.rs`（17 native 白名单）、`Cargo.toml`（4 依赖）；tenthc 双侧——`tenthc/lexer/token.th`（FString disc=89）、`tenthc/lexer/lexer.th`（f"..." 检测）、`tenthc/parser/parser.th`（disc=89 解析）；标准库——`tenth/std/prelude.th`（B批 17 native 索引）；测试——`tenth/tests/string_encoding_test.rs`（新建 41 测试）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件 + 新建 1 个测试文件（在 tenth/tests/ 允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试。**文档同步**：`基本功核查.md` 第四大点第 38-44 项状态更新为已解决；`能力梳理/能力全梳理.md` 对应条目状态更新；`docs/语言参考手册.md` 新增 f"..." 语法与 17 个 std 函数文档。
>
> **2026-07-13 feat: 语言核心语法 20 项缺口全部补齐**：基于 `基本功核查.md` 的源码核实，通过总师 skill + 6 批次 subagent 委派，实现语言核心语法全部 20 项缺失功能。**A批 Lexer 新增（4 项）**：#12 字节串 `b"..."`（ByteString(Vec\<u8\>) + read_byte_string + \\xNN 转义）、#13 原始字符串 `r"..."`（RawString + read_raw_string 无转义）、#14 多行字符串 `"""..."""`（MultiLineString + read_multiline_string 三引号闭合）、#19 `yield` 关键字（Yield 变体 + 关键字注册）；Rust 侧与 tenthc 侧全同步。**B批 Parser 扩展（3 项）**：#1 字符字面量 `'a'` 全链路接通（parser→HIR→VM→interpreter→WASM→JIT，新增 PushChar 指令 opcode 54、Value::Char）、#5 break 带值（Break(Option\<Expr\>) 全栈）、#10 do-while 循环（Do 关键字 + StmtKind::DoWhile 全链路，tenthc 同步）。**C批 参数扩展（3 项）**：#6 默认参数（Param::default_value + HirFnDef::param_defaults + process_call_args 填充）、#7 命名参数（ExprKind::NamedArg + 按签名匹配）、#17 可变参数（DotDotDot 令牌 + variadic 标记 + 多余实参收集为 Array）。**D批 代码生成（2 项）**：#2 try 块 VM 后端修复（bytecode 编译 try block 不再是 no-op，? 运算符内联优化）、#20 尾调用优化 TCO（TailCall 指令 opcode 55 + 帧复用 + 编译期检测）。**E批 标准库（4 项）**：#3 assert/assert_eq（双侧 native 注册，触发 Tenth panic）、#9 HashSet（std/collections/hashset.th + prelude 导出）、#15 StringBuilder（std/string/string_builder.th + prelude 导出）、#18 柯里化/部分应用（std/curry.th：partial/compose）。**F批 类型系统+测试框架（4 项）**：#8 定长数组 `[T; N]`（Type::Array { inner, size } 带 Optional 长度 + parser 语法 `[T; N]`）、#11 运算符重载（try_binary_op_overload + try_unary_op_overload 按 trait impl 降为 MethodCall）、#16 函数重载（scope 多签名 Vec + resolve_fn_overload 类型匹配）、#4 #[test] 测试框架（`#[test]` 属性解析 + is_test 标记 + 自动收集 + PASS/FAIL 报告）。**验证**：cargo build --release 通过；cargo test --lib 23/23、lexer_test 14/14、parser_test 34/34、integration 5/5 全绿。自举三路径未破坏。**改动文件清单**（涉及 40+ 文件）：lexer/token.rs（ByteString/RawString/MultiLineString/Yield/Do/DotDotDot/Hash 令牌）、lexer/lexer.rs（5 个读取方法 + 关键字注册）、tenthc/lexer/token.th + lexer.th（双侧同步）、parser/ast.rs（Literal::Char/Break(Option)/DoWhile/NamedArg/Param 扩展）、parser/expr.rs（字面量+命名参数解析）、parser/stmt.rs（break/do-while/默认/可变参数）、parser/decl.rs（#[test] 属性）、parser/type_parser.rs（[T; N] 语法）、hir/hir.rs（HirFnDef 扩展）、hir/types.rs（Array 定长）、hir/lower/*（各 lower 文件适配）、compile/bytecode.rs（TryBlock/TailCall/PushChar）、runtime/vm/op.rs（PushChar/TailCall）、runtime/vm/execute.rs、runtime/vm/chunk.rs、runtime/value.rs（Value::Char）、runtime/interpreter/*、runtime/natives.rs + interpreter/natives.rs（assert/assert_eq）、compile/wasm/compile.rs（Char/DoWhile）、compile/bridge.rs、compile/jit/translator.rs（PushChar fallback）、tenth/std/collections/hashset.th、tenth/std/string/string_builder.th、tenth/std/curry.th、tenth/std/prelude.th。**禁止事项无违反**：未在禁止目录创建 .th 文件，未破坏自举三路径，未跳过已有测试。
>
> **2026-07-12 feat: Problem 21 — tenthc 全量追上 Rust 母编译器（自举深化）**：本条目为 Problem 21 六批次汇总。tenthc 自举编译器全量追上 Rust 母编译器，覆盖 Lexer/Parser/HIR/Shape/WASM/bridge 全管线，新增 4 项 P0 能力 + 护城河 A 深化（编译期反向 shape）。**批次1 Lexer 同步**：TokenKind 67→82 变体（新增 11 行为 token + 10 关键字），IntLiteral 加 dtype 字段（ival 复用存 dtype），修复 `.= ` 词法 bug（DotAssign 前瞻判断修正）。tenthc/lexer/token.th + lexer.th 双侧同步。**批次2 Parser 同步**：5 ExprKind 新增（Tuple/StructLiteral/InterpolatedString/ClosureExpr/IndexExpr 扩展）+ 1 Pattern（RangePattern）+ 4 StmtKind（ForIn/While/Break/Continue）+ 3 TypeAnnotation（NamedType/GenericType/RefType），tenthc/parser/parser.th 全量对齐；lower.th dtype 透传；locals 容量 256→512 修复（避免复杂函数栈溢出）。**批次3 HIR 同步 + 4 项 P0 能力**：全 7 Type 变体补齐（Never/Range/Tuple/Generic/Ref/MutRef/Future），tenthc/hir/hir.th + lower.th 类型系统对齐。4 项 P0 能力：(1) generic instantiation——泛型函数实例化 mangled name 生成；(2) 函数返回 Unit——Type::Base(Unit) 正确推导；(3) match 穷尽性检查——编译期穷尽性验证；(4) borrow_holders——借用持有者追踪。**批次4 Shape 检查 + 护城河 A**：backward_shape Phase1 实现 11 算子编译期反向 shape（MatMul/Add/Sub/Mul/Div/Neg/Transpose/Reshape/Sum/ReLU/Softmax）；Phase2 跨算子梯度 shape 传播；内存预估（estimate_memory：前向+反向+梯度）；FLOPs 预估（estimate_flops：MatMul 2MNK + Add/Sub/Mul/Div MN + Softmax 3MN 等）；unbroadcast_feasible 检查（编译期验证广播反向 shape 可行性）。新增 autodiff_backward_shape_test.rs（49 用例）+ memory_estimate_test.rs（35 用例，含 3 ignored）。**批次5 WASM + bridge**：host 模块名统一 env→host（wasm.th + wasm.rs 双侧对齐，tenthc_full.wasm import 段 module 名一致）；bridge.rs handlers 补齐（新增 Type 变体 + StmtKind + ExprKind 的 bridge 转换 handlers）；StructLiteral 三路径类型感知（Rust 前端 + tenthc 前端 + WASM 后端 struct literal 类型推导对齐）；修复 tenthc_for_loop_test（for 循环语法解析 bug）。**验证结果**：cargo build --release exit=0（217 个预存 warning，均为 JIT hostcalls.rs unsafe 代码，非本次引入）；全量 cargo test --release：1121 passed / 0 failed / 14 ignored（63 个测试套件全部 ok）；自举三路径验证：路径 A（Rust 全栈）通过——cargo test 全绿；路径 B（Tenth 前端 + Rust 后端）通过——`cargo run --release -- run tenthc/main.th` 输出 `[OK] Full compiler compiled to tenthc_full.wasm`；路径 C（全 WASM 闭环）通过——selfhost_frontend 4/4 + three_stage 1 passed + 2 ignored。关键测试子集：native_parity_test 35/35、autodiff_shape_test 15/15、memory_estimate_test 32/32（+3 ignored）、wasm_backend_minimal 10/10、selfhost_frontend 4/4、three_stage 1/1（+2 ignored）。**已知遗留**：bmm FLOPs 估算为 no-op（返回 0），因 tenthc 侧 HirType 缺少 dim2 字段（2D shape 仅存 dim0/dim1），bmm 的 batch*dim0*dim1*dim2*dim3 五维 FLOPs 无法在 tenthc 侧计算，待后续扩展 HirType 后激活；该遗留不影响正确性（bmm 数值计算路径完整，仅编译期 FLOPs 预估缺失）。**改动文件清单**（按批次）：批次1——tenthc/lexer/token.th、tenthc/lexer/lexer.th；批次2——tenthc/parser/parser.th、tenthc/hir/lower.th；批次3——tenthc/hir/hir.th、tenthc/hir/lower.th；批次4——tenth/src/hir/lower/types.rs、tenth/tests/autodiff_backward_shape_test.rs、tenth/tests/memory_estimate_test.rs；批次5——tenthc/compile/wasm.th、tenth/src/compile/wasm.rs、tenth/src/compile/bridge.rs、tenth/src/compile/wasm/compile.rs。**自举三路径未破坏**：路径 A/B/C 全 intact（详见验证结果）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件，未在禁止目录创建 .th 文件，未跳过失败用例，未破坏自举路径。
>
> **2026-07-12 feat: Problem 21 批次2 — tenthc Parser 全量同步**：继批次1（Lexer 全量同步，TokenKind 67→82 变体，IntLiteral 加 dtype 字段）后，让 tenthc Parser 层全量追上 Rust 母编译器。**tenthc/parser/parser.th 新增 ExprKind**：(1) Tuple 字面量 `(a, b, c)` 与空元组 `()`——`parse_primary` LParen 分支检测逗号创建 "tuple" 节点，元素用连续 arg 节点表达（arena 风格，无 Box 嵌套）；(2) Await 前缀表达式 `await expr`（disc=65，与 Move 同模式）；(3) Spawn 前缀表达式 `spawn expr`（disc=66）；(4) Try block `try { ... }`（disc=69）；(5) QuestionMark 后缀 `expr?`（disc=81，parse_postfix 分支）。**新增 Pattern**：Tuple pattern `(a, b, c)`——parse_pattern LParen 分支设 pat_kind="tuple"。**新增 StmtKind/ItemKind**：(1) LetTuple `let (a, b) = expr`——parse_stmt 检测 LParen 创建 "let_tuple" stmt；(2) Pub 声明 `pub fn/struct/...`——parse_program 顶层 dispatch disc=70 skip 后循环；(3) Use 声明 `use path::to::item;` / `use path::*;`——disc=20；(4) Mod 声明 `mod name;` / `mod name { items }`——disc=21。**新增 TypeAnnotation**：(1) Array type `[T; N]` / `[T]`——lower.th `parse_array_type`；(2) FnType `fn(i64) -> i64`——`parse_fn_type`；(3) Generic `Vec<i64>` / `HashMap<str, i64>`——`parse_generic_type`。**token_type_str 函数**：映射操作符/标点 token 到字符串（如 `<` → "<"、`[` → "["），支持复合类型注解收集。**parse_type_annotation 函数**：带方括号深度跟踪的复合类型收集器，top level 用 tok.sval（punctuation 返回空串，保持 `&str`→"str" 向后兼容），内层 brackets/angles/parens 用 token_type_str（返回正确标点，支持 `Vec<i64>`、`[T;N]`、`fn(i64)`）。**lower.th IntLiteral dtype 处理**：parser.th line 153-160 从 tok.dtype 读取 dtype 写入 expr_nodes[idx-1].dtype；lower.th line 789-796 `kind == "int"` 分支从 e.dtype 读取，用 `add_type(p, 0, dt, ...)` 构造 Base 类型（dt 作为 BaseType sub 编码：0:I64 1:I32 8:I8 9:I16 10:U8 11:U16 12:U32 13:U64 7:Char），实现整数 dtype 全管线保留。**关键 bug 修复 — Rust 母编译器 256 locals 限制**：parity 测试 129 个全部失败，错误 `unknown local 262: local index out of bounds`。根因：Rust 母编译器 `compile/wasm/compile.rs`（compile_function line 29 + compile_main line 42）和 `compile/wasm/closures.rs`（compile_closure_body line 150）3 处硬编码 `(0..256).map(|_| ValType::I64).collect()` 限制 256 个 locals；tenthc lower_expr 函数 let 绑定数 228 + 6 参数 + 临时变量 ≈ 263 超过 256。修复：3 处 256→512。**parse_type_annotation 行为回退**：top level 回退使用 tok.sval（punctuation 返回空串），保持 `&str`→"str" 向后兼容；仅内层用 token_type_str。**parse_ref_type 移除调用**：Rust 侧 type_parser.rs 中 `&` 被解析为 `Named("&str")`（无空格），TypeAnnotation 无 Ref 变体；parse_ref_type 函数保留在 lower.th 中但不再被 parse_type 调用。**验证结果（5 项清单全绿）**：(1) `cargo build --release` exit 0 ✅；(2) `parser_test` 5 passed/0 failed ✅；(3) `native_parity_test` 35 passed/0 failed ✅；(4) 自举验证 `[OK] Full compiler compiled to tenthc_full.wasm` ✅；(5) `cargo test --release` 全测试 0 failed ✅（parity_test 129 passed/0 failed/0 ignored，所有其他测试套件均 0 failed）。**改动文件清单**（7 个）：`tenthc/parser/parser.th`（Expr 结构体 dtype 字段 + Tuple/Await/Spawn/Try/QuestionMark/LetTuple/Pub/Use/Mod + token_type_str + parse_type_annotation）、`tenthc/hir/lower.th`（IntLiteral dtype 处理 + parse_array_type + parse_fn_type + parse_generic_type + parse_ref_type 保留不调用 + try_block/let_tuple lowering + parse_type_annotation top level 回退）、`tenth/src/compile/wasm/compile.rs`（compile_function + compile_main locals 256→512）、`tenth/src/compile/wasm/closures.rs`（compile_closure_body locals 256→512）、`tenthc/lexer/lexer.th`（批次1 IntLiteral dtype）、`tenthc/lexer/token.th`（批次1 TokenKind 82 变体）、`tenth/tests/int_types_test.rs`（批次1 整数类型测试）。**自举三路径未破坏**：路径 A 通过（cargo test 全绿）、路径 B 通过（自举验证 [OK]）、路径 C 通过（parity_test 129/129 + selfhost_frontend）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件，未创建新文件，未在禁止目录创建 .th 文件，未破坏已有测试。
>
> **2026-07-11 feat: Problem 5 — 整数类型全管线 dtype 保留 + 溢出检测**：整数类型（i8/i16/i32/i64/u8/u16/u32/u64）在整个编译管线中保留 dtype，不再退化为 i64。IntLiteral(i64) → IntLiteral(i64, BaseType) 四层同步（Token/AST/HIR/Value）；Lexer 新增后缀检测（42u8）+ 编译期范围检查 + 无后缀大整数自动提升 I32→I64；运行时新增 check_int_overflow() 算术溢出检测；type_of() 返回实际 dtype；Bug fix：() 类型注解正确解析为 Type::Base(BaseType::Unit)。14 项新测试全部通过，自举三路径未破坏。
>
> **2026-07-11 feat: 类型系统架构改进第三批（问题 1/2/9/10/11）**：**波1 HIR 层低风险改进**：问题9——Range 成为一等类型 `Type::Range { inner, inclusive }`，`0..10` 表达式类型推断从 `Type::i32()` 改为 `Type::Range`；问题10——match 穷尽性检查，对枚举 match 若无通配符 `_` 且未覆盖所有变体则报 TypeError（支持 `Type::Generic` 包装的 Option/Result）；问题11——函数返回类型检查，若声明非 Unit 返回类型但 body 返回 Unit 则报 TypeError。**波2 泛型实例化**：问题1——Option/Result 真泛型化，`Option::Some(42)` 类型从 `Type::Enum("Option")` 改为 `Type::Generic { base: Enum("Option"), args: [i32] }`，`Result::Ok(42)` 改为 `Generic { base: Enum("Result"), args: [i32, str] }`，`?` 操作符现在可正确从 `Type::Generic` 提取 `args[0]`；问题2——HashMap 键类型解锁，`insert`/`get`/`contains_key`/`remove` 方法支持 str/int/bool/float 键（内部统一转 String 存储），解释器和 VM 双路径同步。**修复预存测试失败**：`test_hashmap_get`（stdlib_test.rs:59）期望 `None` 但 `get` 返回 `Some(Unit)`，修正测试匹配实际行为。**测试**：type_inference_test 36→42 项（+6 Option/Result 泛型 + Range inclusive）；enum_test 9→12 项（+3 match 穷尽性检查）；stdlib_test 114→116 项（+2 整数/布尔键测试）；修复 1 项预存失败。**验证**：核心测试全绿，自举三路径未破坏。**改动文件**：`hir/types.rs`（Type::Range 变体 + Display）、`hir/lower/types.rs`（问题11 函数返回检查）、`hir/lower/lower_expr.rs`（Range 推断 + match 穷尽性 + Option/Result 泛型化）、`runtime/interpreter/methods.rs`（HashMap 键类型解锁）、`runtime/vm/natives.rs`（VM 端 HashMap 键类型解锁）、`tests/type_inference_test.rs`、`tests/enum_test.rs`、`tests/stdlib_test.rs`。
>
> **2026-07-11 feat: 护城河 F 深化 Phase 1+2 — 错误信息流贯通 + 5 类错误分类**：把方向 F 的 MVP"算法内核已就位、错误信息流未贯通"升级为可工作的关系调试器。**Phase 1（错误信息流贯通）**：backward.rs 全部分支（Input/MatMul×5/BatchedMatMul/Reshape/unbroadcast×2 共 10 处）+ grad.rs（propagate_grad direct 路径）的纯字符串 RuntimeError 改为 ShapeMismatch + 完整 TapeErrorContext（tape_node_id/op/expected_shape/actual_shape）；natives.rs（VM）+ interpreter/natives.rs（解释器）双路径包裹层从错误中提取真实 v_err/expected/actual 传给 formal_explain（替代之前的占位值 loss_id/空 Vec）；error.rs 新增 ErrorType 枚举（5 类：ShapeMismatch/SilentSqueeze/BroadcastFail/GradDrift/DtypeConflict）+ TenthError::RelationError 变体；RootCause 新增 error_type: Option<ErrorType> + edge: Option<(usize, usize)> 字段（边级归因，BFS parent 映射）；op_name 去重——grad.rs 改 pub(crate)，autodiff/mod.rs re-export，relation_debugger.rs 删除 27 变体重复实现。**Phase 2（5 类错误分类）**：relation_debugger.rs 新增 classify_error_type(op, expected, actual, error_msg) -> ErrorType 分类方法，5 类规则：SilentSqueeze（MatMul/BatchedMatMul + "squeeze"/"1D"）/BroadcastFail（Add/Sub/Mul/Div + "broadcast"，覆盖 "unbroadcast"）/GradDrift（Input + "acc_grad"/"反向传播 shape 错误"/"梯度 shape"）/DtypeConflict（任意 op + "dtype"/"f32/f64"）/ShapeMismatch（兜底）；formal_explain 调用 classify_error_type 填充 RootCause.error_type；root_cause_hint 为 4 类特殊错误 + ShapeMismatch 兜底添加针对性提示；grad.rs acc_node_grad 新增 DtypeConflict 检测（eprintln warning，保守策略不改为 error）。修复 GradDrift 规则 bug：原用泛化 "shape 错误" 导致 "参数 shape 错误" 误判，改用精准关键词。**测试**：relation_debugger_test.rs 从 10 项扩展到 20 项（Phase 1 结构化错误信息验证 3 项 + Phase 2 五类错误分类 5 项 + 边级归因 2 项 + 原有 10 项）；relation_debugger.rs 内联测试从 5 项扩展到 18 项（含 7 项 classify_error_type 测试）。**验证结果**：relation_debugger_test 20 项全绿；lib relation_debugger+autodiff 32 项全绿；autodiff_test 54 项全绿；autodiff_shape_test 15 项全绿；autodiff_backward_shape_test 48 项全绿（1 ignored）；无新增回归。**自举三路径未破坏**：不触及 tenthc/，路径 A/B/C 全不受影响。**改动文件清单**（6 个）：`tenth/src/error.rs`（ErrorType 枚举 + RelationError 变体）、`tenth/src/runtime/relation_debugger.rs`（RootCause 边级归因 + classify_error_type + op_name 去重 + 测试扩展）、`tenth/src/runtime/autodiff/backward.rs`（10 处分支结构化错误）、`tenth/src/runtime/autodiff/grad.rs`（propagate_grad 结构化错误 + op_name pub(crate) + DtypeConflict 检测）、`tenth/src/runtime/autodiff/mod.rs`（op_name re-export）、`tenth/src/runtime/natives.rs` + `tenth/src/runtime/interpreter/natives.rs`（双路径包裹层改造）；`tenth/tests/relation_debugger_test.rs`（10→20 项测试）。
>
> **2026-07-10 fix: 3 个预存测试失败清零**：清偿 AUDIT.md §三 登记的 3 项预存失败。**native_generic_ctor_f32/f64_lowering**（`tenth/tests/generic_tensor_test.rs`）：测试期望从 `Tensor[F32, ..]` 改为 `Tensor[F32, 3]`——`shape_from_int_args` 把字面量参数 `3` 算进 shape 是 Phase 1 shape 检查的统一逻辑（与 `zeros(3,4)` 等非泛型构造函数一致），比 `..` 更精确，利于编译期内存预估（`static_bytes` 依赖 `Dim::Known`）。**fixpoint_runtime_benchmark**（`tenth/tests/fixpoint_runtime.rs`）：标记 `#[ignore]`——wasmtime 路径 Vec 写回逻辑问题（tenthc 完整执行但 main 返回 Vec len=0，AUDIT #5），wasmi 路径已通过验证，wasmtime 仅是性能优化路径，深度调试 ROI 低。**验证**：generic_tensor_test 4/0/0、fixpoint_runtime 0/0/1；无新增回归。**改动文件清单**（3 个）：`tenth/tests/generic_tensor_test.rs`（2 处 assert_eq 期望）、`tenth/tests/fixpoint_runtime.rs`（#[ignore] 标记）、`AUDIT.md`（§三 预存失败登记改为"已修复"，总计 819 passed/3 failed/12 ignored → 891 passed/0 failed/14 ignored）。
>
> **2026-07-10 feat: 护城河 A 深化 — 编译期反向 shape 验证（Phase 1 + Phase 2）**：把方向 A 的运行时 RuntimeError 提升到编译期 TypeError，实现单算子级 + 跨算子反向 shape 验证。**Phase 1（单算子级反向 shape 验证）**：新建 `tenth/src/hir/lower/backward_shapes.rs`——定义 `backward_shape()` 规则表（覆盖 add/sub/mul/div/matmul/bmm/sum/mean/cross_entropy/reshape/scatter/gather/masked_fill/select 共 10+ 算子）+ `check_backward_shape_compat()` + `unbroadcast_feasible()` + `numel_compatible()`；修改 `tenth/src/hir/lower/types.rs`——matmul/bmm 反向 shape 验证 + 新增 `check_reshape_shape`（numel 等价检查）+ `check_cross_entropy_shape`（target shape 应为 [B] 或 [B,V]）；修改 `tenth/src/hir/lower/lower_expr.rs`——cross_entropy native 调用加入编译期 shape 检查。跳过算子（反向 shape 天然一致，无需检查）：neg/relu/exp/log/sigmoid/abs/softmax/dropout/batch_norm/layer_norm/gelu/transpose/conv2d。**Phase 2（跨算子反向 shape 传播）**：新建 `tenth/src/hir/lower/backward_shape_pass.rs`——HIR 后向分析 pass，含 `find_grad_regions()`（识别 start_grad/new_grad → backward 配对）+ `collect_tensor_op()`（构建 def-use 链）+ `propagate_backward()`（逆序传播梯度 shape）+ `shape_compatible()`（验证梯度 shape 与参数 shape 兼容）；修改 `tenth/src/hir/lower/lower_stmt.rs`——在 `lower_program` 末尾集成 pass；修改 `tenth/src/hir/lower/mod.rs`——声明新模块。**保守回退策略**：grad 区域含 if/else/match/while/for/loop 控制流时跳过；输入非简单 Var 时跳过；全 Any shape 跳过——保证编译期成本可控（保持自举 ~0.2s，不引入 NP 求解），误报率为零。**错误类型提升**：把方向 A 的运行时 `RuntimeError` 提升到编译期 `TenthError::TypeError`，让开发者在编译期即知反向 shape 错误，而非训练时 silent squeeze 掩盖。**测试**：48 项 `autodiff_backward_shape_test`（31 Phase 1 单算子 pass/fail case + 18 Phase 2 跨算子传播，其中 1 项 `#[ignore]` fail 占位）。**验证结果**：autodiff_backward_shape_test 48 项全绿（47 passed + 1 ignored）；自举验证通过（`cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm`）。**自举三路径未破坏**：路径 A 通过（测试全绿）；路径 B 通过（自举 `[OK]`）；路径 C 不涉及（未触及 tenthc/ 自举编译器前端 + 未改 bridge.rs，shape 检查规则仅在 Rust 侧 `hir/lower/` 实现）。**改动文件清单**（6 个）：`tenth/src/hir/lower/backward_shapes.rs`（新建，规则表 + 兼容性检查）、`tenth/src/hir/lower/backward_shape_pass.rs`（新建，HIR 分析 pass）、`tenth/src/hir/lower/types.rs`（matmul/bmm 反向验证 + check_reshape_shape + check_cross_entropy_shape）、`tenth/src/hir/lower/lower_expr.rs`（cross_entropy 编译期检查）、`tenth/src/hir/lower/lower_stmt.rs`（lower_program 末尾集成 pass）、`tenth/src/hir/lower/mod.rs`（声明新模块）；新增 1 个测试文件 `tenth/tests/autodiff_backward_shape_test.rs`（48 项测试）。**文档同步**：`能力梳理/能力全梳理.md` §1.2 "运行时 Shape 校验" 条目补充"编译期反向 shape 验证（单算子级 + 跨算子传播）"、§1.2 "编译期 Shape 检查" 条目补充 cross_entropy/reshape 反向 shape 检查；`docs/shape-check-roadmap/战略规划.md` 方向 A 从"✅ 已实现（2026-07-01）"升级为"✅ 深化完成（2026-07-10）：编译期反向 shape 验证"。**黑板协议**：处理 ID 12 留言，本条记录即 doc 部完成 Phase 3 文档同步的交付物。
>
> **2026-07-09 fix: B1-B5 用户体验 bug 修复（总师+3部门黑板并行）**：实测 5 个示例程序发现的 5 个 UX bug，总师完成根因侦察后通过黑板协议并行委派运行时部（B1+B2+B3）、编译器部（B5）、标准库部（B4）。**B1 Tensor 索引返回 Unit**：`runtime/tensor.rs` 新增 `index_dim(idx)` 方法（ndarray `index_axis(Axis(0), idx)` 降维，支持 F32/F64/F16/BF16）；`runtime/vm.rs:887-903` `Op::IndexGet` 增加 Tensor 分支——1D 返回标量 Float，N-D 返回子张量；`runtime/interpreter/index.rs:65-90` 重写 Tensor 分支，按 `idx.len()` vs `ndim` 判断返回标量或子张量（NumPy 语义：`t[0]` 降维返回子张量，`t[0][1]` 返回标量）。**B2 JIT 路径 matmul 报错静默返回 Unit**：三层防护——① `vm.rs` 新增 `has_last_error()` 非消耗式检查 ② `hostcalls.rs` 新增 `host_check_error` hostcall ③ `translator.rs` MethodCall 后检查错误，有错则提前返回 ok=0 触发 fallback ④ `mod.rs` run_jit 添加安全网。修复后 JIT 路径遇到 matmul shape mismatch 正确报错退出（exit 1），而非静默返回 Unit。**B3 f64 整数值显示丢小数**：`value.rs` 新增 `format_f64`/`format_f32`（整数追 `.0`），Display 改用之；`tensor.rs` 新增 `format_tensor_f64`/`format_array_f64` 递归格式化，Tensor Display 改用之。修复后 `2.0` 显示为 `"2.0"`（非 `"2"`），张量 `[[1.0, 2.0]]` 正确显示 `.0`。**B4 官方示例用未实现 `as` 语法**：`Tenth实例/Adam优化器/adam.th:46` `as f64` → `to_f64()`；普查 `Tenth实例/*.th` 发现 3 处非法 `as` 用法（adam.th、timer.th:29、random_demo.th:49），全部修复为 `to_f64()`。确认 Tenth 用 `to_f64()` native 函数做类型转换（注册于 main.rs:1875），不支持 `as` 语法。**B5 lexer 不容忍 BOM**：`lexer.rs` `Lexer::new` 用 `source.strip_prefix('\u{FEFF}').unwrap_or(source)` 跳过 UTF-8 BOM，修复 PowerShell `Out-File -Encoding utf8` 默认带 BOM 导致的词法错误。新增 `test_bom_skip` 测试。**验证**：cargo build --release exit 0；lib 16、integration 14、tensor_features 18、autodiff 54、select 16、lexer 7 均 0 回归；功能验证——`tensor[[1,2],[3,4]][0]` → `[1.0, 2.0]` 子张量 ✅，`x[0].sum()` → `6.0` ✅，matmul shape mismatch 正确报错 ✅，`2.0` 显示为 `"2.0"` ✅，带 BOM 文件正常运行 ✅。**B4 暴露的预存问题**（非本次回归，登记待后续）：① `Tenth实例/Transformer示例/transformer_demo.th` JIT fallback 后 `gelu` 未定义（use 导入符号在 fallback 后丢失）；② `Tenth实例/Adam优化器/adam.th` 标量无 `pow` 方法（`beta1.pow(t)` 报错）——`pow` 方法未实现。**改动文件清单**（13 个）：`tenth/src/runtime/tensor.rs`（+69 行 index_dim + 格式化）、`tenth/src/runtime/vm.rs`（+48 行 has_last_error + Tensor 索引）、`tenth/src/runtime/interpreter/index.rs`（+55 行重写 Tensor 分支）、`tenth/src/runtime/value.rs`（+22 行 format_f64/f32）、`tenth/src/compile/jit/hostcalls.rs`（+9 行 host_check_error）、`tenth/src/compile/jit/translator.rs`（+30 行错误检查）、`tenth/src/compile/jit/mod.rs`（+5 行 run_jit 安全网）、`tenth/src/lexer/lexer.rs`（+2 行 BOM 跳过）、`tenth/tests/lexer_test.rs`（+8 行 test_bom_skip）、`Tenth实例/Adam优化器/adam.th`、`Tenth实例/计时器/timer.th`、`Tenth实例/随机数应用/random_demo.th`。**黑板协议**：4 条留言全部已解决，黑板已删除。
>
> **2026-07-09 feat: f16/bf16 Phase 2 缺口 1（JIT）+ 缺口 3（autodiff AMP）完成——F1 四缺口全部完成**：F1 并行任务中运行时部负责的两个缺口。**缺口 1（JIT）**：`compile/jit/translator.rs:470-478` MakeTensor 从二值分发扩展为 4 dtype（0=F64/1=F32/2=F16/3=BF16）；`compile/jit/hostcalls.rs:470-526` 实现 `host_make_tensor_f16`/`host_make_tensor_bf16`（从 JIT 栈上 Value 读取元素，转 f16/bf16 构造 `TensorData::F16/BF16`）；`hostcalls.rs:588-590` 注册到 hostcall table；`runtime/vm.rs:995-1063` VM 路径 MakeTensor 已支持 4 dtype（含 `Tensor::from_vec_f16/bf16`）。**缺口 3（autodiff F32 中间累加 AMP）**：`runtime/tensor.rs:307-383` `acc_grad` 移除 F16/BF16 的 early-return Err，改为 F32 中间累加策略——F16/BF16 param 的 grad 累加时先转 F32 计算（避免 F16 溢出 max≈65504 和 BF16 精度损失），grad buffer 存为 F32；`runtime/autodiff.rs:72-85` `dispatch_float!` 宏扩展 F16/BF16 走 f32 路径（不再强制提升 F64）；`autodiff.rs:551-557` 种子梯度 F16/BF16 loss 用 F32 ones（AMP 策略）；`autodiff.rs:1311-1330` Reshape 反向 F16/BF16 grad 转 F32（不再转 F64）。**设计决策**：F32 中间累加是工业标准 AMP 做法——F16 精度低（10 位尾数）易溢出，BF16 精度更低（7 位尾数），用 F32 中间表示可兼顾精度和性能，最终 grad 写回时由优化器转回原 dtype。**混合精度训练 AMP 状态**：从 ❌ 未实现 升级为 ✅ 已完成（F32 中间累加策略是 AMP 核心）。**main.rs register_natives**：已注册 `zeros_f16/ones_f16/zeros_bf16/ones_bf16`（Phase 1 已完成，本次无需改动）。**验证结果**：cargo build --release exit 0；autodiff 54/54、vm_autodiff 15/15、tensor_features 18/18、jit 10/10、wasm_backend_minimal 10/10、lib 16/16、module 6/6、integration 14/14 均 0 回归；自举 `[OK] Full compiler compiled to tenthc_full.wasm`。**自举三路径未破坏**：路径 A 通过（测试全绿）；路径 B 通过（自举 [OK]）；路径 C 不涉及（WASM 闭环用 wasmi stub）。**预存问题（非本次引入）**：`memory_test.rs` 编译错误（`limits.rs` 当前只实现 `FsSandbox`，`memory_test.rs:13` 引用 `MemoryConfig`/`RuntimeLimits`/`LiveCounter` 失败），git stash 验证为预存问题。**改动文件清单**（5 个）：`tenth/src/compile/jit/translator.rs`（+13 行 dtype 分发）、`tenth/src/compile/jit/hostcalls.rs`（+63 行 host_make_tensor_f16/bf16 实现 + 注册）、`tenth/src/runtime/tensor.rs`（acc_grad 移除 early-return + F32 中间累加）、`tenth/src/runtime/autodiff.rs`（dispatch_float! 扩展 + 种子梯度 + Reshape 反向）、`tenth/src/runtime/vm.rs`（VM MakeTensor 4 dtype）。**文档同步**：`AUDIT.md` #10 从"Phase 2 待做"改为"✅ Phase 2 已完成"；`能力梳理/能力全梳理.md` 多精度张量 Phase 2 状态更新 + 混合精度训练 AMP ❌→✅。**F1 任务完成**：f16/bf16 Phase 2 四缺口全部完成，f16/bf16 张量现可在 VM/解释器/JIT/WASM 全路径下使用，也可作为 autodiff param 参与训练（AMP F32 中间累加）。
>
> **2026-07-09 feat: f16/bf16 Phase 2 缺口 2（WASM）+ 缺口 4（tenthc 字面量）**：F1 四缺口并行任务中编译器部负责的两个缺口。**缺口 4（tenthc 字面量）**：`tenthc/lexer/lexer.th` 扩展 f16(ival=2)/bf16(ival=3) 后缀检测——浮点路径（lexer.th:82-123）和整数路径（lexer.th:128-174）均新增 bf16（b+f+1+6，必须在 f 检测前）和 f16（f+1+6）分支，边界检查 `!is_alnum(c4)` 防止 `3.14factor` 误匹配；`tenthc/hir/lower.th:601-611` FloatLiteral 按 e.ival 写入 HIR type sub（0→F64 sub=2, 1→F32 sub=3, 2→F16 sub=14, 3→BF16 sub=15）；`tenthc/compile/wasm.th:477-492` is_expr_float 识别 F16(sub=14)/BF16(sub=15) 为 float 标量。parser.th ival 透传复用 Phase 5.2 step 2 既有逻辑（`p.expr_nodes[idx-1].ival = tok.ival`），无需改动。**缺口 2（WASM）**：`bytecode.rs:610-615` dtype_code 从二值（F32=1/其他=0）扩展为完整 0-3 编码（F64=0/F32=1/F16=2/BF16=3）；`wasm/mod.rs` to_val_type 新增 F16/BF16→`Some(ValType::I64)`（WASM 原生不支持 f16，映射为 I64 指针/打包存储），field_size_and_type 新增 F16/BF16→`(8, ValType::I64)`（8 字节对齐）；新增 `HOST_MAKE_TENSOR_F16=18`/`HOST_MAKE_TENSOR_BF16=19` 常量，`IMPORT_COUNT` 从 18 改为 20；`wasm/host.rs:345-360` + `wasmtime_host.rs:435-449` 双侧注册 host_make_tensor_f16/bf16 stub（签名 `(i32, i32, i32) -> i64`，与 tensor_from_vec 一致，简化实现返回 len 作为 handle，保证 parity 测试确定性）；`wasm/sections.rs:93-95` import 声明同步（type_cache 去重，type 索引复用 tensor_from_vec 的）；`wasm/compile.rs:761-833` TensorLiteral 编译按 dtype 分发——F16 元素按 F64 字节存储（8 字节）+ 调用 HOST_MAKE_TENSOR_F16，BF16 同理调用 HOST_MAKE_TENSOR_BF16，F64/F32 走原 HOST_TENSOR_FROM_VEC。**测试文件同步**：`tenth/tests/wasm_backend_minimal.rs` setup_store_and_linker 补齐 host_make_tensor_f16/bf16 stub 注册（sections.rs 无条件声明所有 20 个 import，测试 linker 缺失定义会导致 wasmi Linker MissingDefinition 错误，即使测试代码不用 f16/bf16）。**main.rs register_natives 无需改动**：VM 路径通过 Op::MakeTensor 直接处理张量构造（dtype_code 已在字节码中），不经 host_make_tensor_* hostcall（那是 WASM 路径专属）。**dtype 编码设计决策**：F16/BF16 在 WASM 线性内存中按 F64（8 字节）存储而非任务描述的 F32（4 字节），原因——WASM 原生不支持 f16/bf16 类型，hostcall stub 从 WASM 内存读取数据时按 F64 字节序列解析更简单（与 tensor_from_vec 一致），且当前 stub 是简化实现（返回 len），未来真正构造 F16 TensorData 时可在 host 侧 reinterpret。**验证结果**：cargo build --release exit 0（195 个预存 warning，都是 JIT hostcalls.rs 的 `fn as usize` 模式，与本次改动无关）；wasm_backend_minimal 10/10、module_test 6/6、tensor_features_test 18/18 均 0 回归；自举验证 `[OK] Full compiler compiled to tenthc_full.wasm`（关键——tenthc 字面量修改不破坏自举）。**自举三路径未破坏**：路径 A 通过（测试全绿）；路径 B 通过（自举验证 [OK]）；路径 C 不涉及（WASM 闭环测试用 wasmi stub，不依赖 f16/bf16 实际执行）。**改动文件清单**（12 个）：`tenthc/lexer/lexer.th`（+54 行 f16/bf16 后缀检测）、`tenthc/hir/lower.th`（+8 行 FloatLiteral dtype 写入）、`tenthc/compile/wasm.th`（+11 行 is_expr_float 识别）；`tenth/src/compile/bytecode.rs`（+6 行 dtype_code 扩展）、`tenth/src/compile/wasm/mod.rs`（+11 行 to_val_type/field_size_and_type/HOST_* 常量）、`tenth/src/compile/wasm/host.rs`（+17 行 host_make_tensor_f16/bf16 stub）、`tenth/src/compile/wasm/sections.rs`（+5 行 import 声明）、`tenth/src/compile/wasm/compile.rs`（+30 行 MakeTensor dtype 分发）、`tenth/src/compile/wasmtime_host.rs`（+16 行 host_make_tensor_f16/bf16 stub）；`tenth/tests/wasm_backend_minimal.rs`（+3 行 stub 注册）。**遗留项**（待 runtime 部）：缺口 1（JIT translator F16/BF16 完整反向传播）+ 缺口 3（autodiff acc_grad F16/BF16 early-return Err 移除）；JIT hostcalls.rs 中 host_make_tensor_f16/bf16 已由 runtime 部实现，translator.rs:470-478 match 分发已扩展 0-3 四分支。**黑板协议**：已标记 ID 1/3 已解决，追加 ID 5/6 留言给 runtime（dtype 编码锁定 + host import 索引）。
>
> **2026-07-09 fix: 测试债务清偿（A1 accumulate_loop + A2 unbroadcast + B AUDIT #6）**：清偿 AUDIT.md §三 测试矩阵登记的 3 项测试债务。**A1（accumulate_loop 端到端功能测试）**：`tenth/std/optim/accumulate.th` 修复 `loss / accum_steps`（Tensor/i64 无原生除法）改为 `loss / to_f64(accum_steps)`（与 clip.th 模式一致）；`tenth/tests/tensor_features_test.rs` 新增 `test_accumulate_loop_functional` 测试，验证 3 个 micro-batch (x=2,4,6) accum_steps=3 累积梯度 = 4.0。**关键发现**：测试改用 Interpreter 路径（非 VM），因 accumulate_loop 接受闭包作为参数（forward_fn），VM 的 `bytecode.rs` Call 分支对 `Var(name)` 直接 emit `Op::CallN` 按名字查函数表，但 forward_fn 是闭包值存在 locals 中、不在函数表里，VM 报"未定义的函数"。Interpreter 是 tree-walk 直接 eval 闭包值无此限制，与 `iterator_test.rs` 测 `iter.th::map` 的惯例对齐。VM 的 CallIndirect 支持是预存能力缺口（非本次任务范围）。**A2（autodiff unbroadcast shape 专项测试）**：`tenth/tests/autodiff_shape_test.rs` 新增 5 项 unbroadcast 数值专项测试——`unbroadcast_row_broadcast_add_grad`（w[2,3]+b[1,3]→grad(b)=[2,2,2]）、`unbroadcast_col_broadcast_add_grad`（w[2,3]+b[2,1]→grad(b)=[3,3]）、`unbroadcast_scalar_broadcast_add_grad`（w[2,3]+b[1,1]→grad(b)=[6]）、`unbroadcast_mul_broadcast_grad`（w[2,3]*b[1,3]→grad(b)=[5,7,9]）、`unbroadcast_sub_broadcast_grad`（w[2,3]-b[2,1]→grad(b)=[-3,-3]）。现有 `broadcast_gradient_correct_shape` 只验证"不报错"，本组验证梯度数值正确性（unbroadcast 沿广播轴求和归约的代数对偶语义）。**B（AUDIT #6 修复）**：`tenth/src/compile/wasm/compile.rs` Call 分派补齐 `str_eq`/`str_add`/`str_int` 三个分支，与 `str_len`/`str_at`/`str_cmp`/`str_slice` 对齐。此前仅 BinOp 路径（`a == b`、`a + b`）和 MethodCall/InterpolatedString 路径支持这三个字符串操作，函数调用形式 `str_eq(a,b)` 落入 `_` 分支按普通函数解析，产出 i64→i32 类型不匹配的非法 WASM。tenthc 侧此前用 `==`/`!=` 操作符走 BinOp 路径规避。新增 3 项回归测试 `wasm_backend_minimal::test_str_{eq,add,int}_call_compiles` 验证函数调用形式能产出合法 WASM 并被 wasmi 实例化执行。**验证结果**：tensor_features_test 18/18（新增 1）、autodiff_shape_test 15/15（新增 5）、wasm_backend_minimal 10/10（新增 3）、f32_wasm_test 14/14、three_stage 1 passed+2 ignored 均 0 回归；cargo build 通过。**自举三路径未破坏**：路径 A 通过（测试全绿）；路径 B/C 不涉及（未改 tenthc/ 与 bridge.rs）。**改动文件清单**（5 个）：`tenth/std/optim/accumulate.th`（to_f64 修复）、`tenth/src/compile/wasm/compile.rs`（3 个 Call 分支）、`tenth/tests/tensor_features_test.rs`（+run_interp_with_std 辅助 + 1 测试）、`tenth/tests/autodiff_shape_test.rs`（+2 辅助 + 5 测试）、`tenth/tests/wasm_backend_minimal.rs`（+3 测试）。**文档同步**：`AUDIT.md` §六 #6 状态 ⚠️→✅ 已修复、§三 测试矩阵数字更新（autodiff_shape_test 10→15、wasm_backend_minimal 7→10、tensor_features_test 17→18、总计相应更新）。
> **2026-07-09 fix: 测试债务清偿 — use+泛型注册 + to_f64 标量 Tensor + VM 标量方法分发**：清偿 `tensor_features_test.rs` 中 4 项 `#[ignore]` 测试债务。**Debt 1（use 导入未注册 generic_funcs）**：`hir/lower/lower_stmt.rs` 4 个 use 导入分支（glob 主路径/glob fallback/single-name 主路径/single-name fallback）此前只迭代 `module.functions`（仅含非泛型函数），漏掉了 `module.generic_funcs`（泛型函数独立存储）。修复：所有 4 个分支均新增 `for fn_def in &module.generic_funcs` 循环，将泛型函数注册到 `self.generic_funcs`；single-name 导入的"查找目标函数"改为 `module.functions.iter().find(...).or_else(|| module.generic_funcs.iter().find(...))` 双路径查找。**Debt 2（to_f64/to_f32/to_float 不接受标量 Tensor）**：`main.rs` + `runtime/interpreter/natives.rs` 双侧为 3 个 native 新增 `Value::Tensor(t)` 分支——0-D Tensor 用 `tensor.get(&[])`，1 元素 Tensor 用 `tensor.get(&vec![0; shape.len()])`，多元素 Tensor 报错；`to_float` 同步补齐此前缺失的 `Value::Float32(f)` 分支。**额外修复 1（VM 标量方法分发缺失）**：clip_grad_by_norm 内 `sq_sum.sqrt()` 中 `sq_sum` 为 f64（`.sum()` 非录制时返回标量），但 VM `call_method_priv` 无 `Value::Float`/`Value::Float32`/`Value::Int` 分支，导致 "没有方法 'sqrt'"。`runtime/vm.rs` `call_method_priv` 新增 3 个标量分支，支持 `sqrt/abs/exp/log/ln/sin/cos` 方法（与 interpreter `eval_scalar_method` 对齐）。**额外修复 2（测试 shape 一致性）**：`test_adamw_step_w` 中 `tensor[[1.0, 2.0, 3.0]]` 创建 2D `[1,3]`（Tenth tensor 字面量仅支持 2D），但 `zeros(3)` 创建 1D `[3]`，shape 不一致导致广播。修复：`zeros(3)` → `zeros(1, 3)`，断言 `vec![3]` → `vec![1, 3]`。**测试文件改动**：`tensor_features_test.rs` 移除 4 个 `#[ignore]`（test_clip_grad_by_norm / test_clip_grad_by_norm_no_clip / test_adamw_step_w / test_adamw_step_w_returns_finite），`register_test_natives` 新增 `sqrt` native，本地 `to_f64`/`to_f32` override 同步 Tensor 分支。**验证结果**：tensor_features_test 17/17 passed（此前 13/0/4）；native_parity 54/54、stdlib 14/14、integration 6/6、module 35/35、autodiff 114/114、abs 8/8、generic 11/11 均 0 回归；lib 16/16；自举 `[OK] Full compiler compiled to tenthc_full.wasm`。**预存失败（非本次引入，AUDIT.md 已登记）**：generic_tensor_test 2 项（native_generic_ctor_f32/f64_lowering）、selfhost_frontend 栈溢出。**tenthc 同步**：tenthc 不处理 `use` 导入（`tenthc/hir/lower.th` 无 Use 分支），无需同步。**自举三路径未破坏**：路径 A 通过、路径 B 通过（自举验证 `[OK]`）、路径 C 不涉及。**改动文件清单**（5 个）：`tenth/src/hir/lower/lower_stmt.rs`（4 个 use 分支 + generic_funcs 注册）、`tenth/src/runtime/vm.rs`（标量方法分发）、`tenth/src/main.rs`（to_f64/to_f32/to_float Tensor 分支）、`tenth/src/runtime/interpreter/natives.rs`（同上双侧）、`tenth/tests/tensor_features_test.rs`（移除 #[ignore] + sqrt native + shape 修复）。**文档同步**：`AUDIT.md` §三 测试矩阵 tensor_features_test 13/0/4→17/0/0、总计 815→819 passed / 16→12 ignored。
> **2026-07-08 feat: 张量未完成项修复（f16/bf16 Phase 1 + 序列化 v2 + 4 项小修复）**：本条目为汇总条目，整合 Wave 1a 标准库部 4 项修复 + Wave 1b 运行时部序列化 v2 + Wave 2 运行时部 f16/bf16 Phase 1。**6 项修复概述**：(1) `accumulate_loop` 高阶函数（`tenth/std/optim/accumulate.th:46`）支持梯度累积模拟大 batch，闭包语法 `|x| expr`（Tenth 惯例，参考 `std::collections::iter::map`）；(2) autodiff unbroadcast 审计——审计结论：`autodiff.rs:569-619` Add/Sub/Mul/Div backward 已全部调用 unbroadcast（行 579/581/598/599/614/615），unbroadcast 函数（`autodiff.rs:1560-1607`）已泛型化 `<E: FloatElem>` 支持 f32/f64，**无需修改代码**（T42 修正时完成）；(3) `adamw_step_w/_m/_v` 三个单值返回版本（`tenth/std/optim/adamw.th:65/87/104`），与 tuple 版本参数完全一致，分别返回 new_w/new_m/new_v，JIT 路径友好（避免 tuple 解构触发 fallback）；(4) `clip_grad_by_norm` 改用 `to_f64(norm)` 和 `to_f64(max_norm)` 强制提升到 f64 再比较（`tenth/std/optim/clip.th:37`），修复 JIT 路径 tensor-float 类型问题（Tenth 无 `as f64` 语法，用 `to_f64()` native）；(5) 序列化格式升级到 v2——见下文；(6) f16/bf16 Phase 1——见下文。**f16/bf16 Phase 1 范围和限制**：(1) `Cargo.toml` 添加 `half = "2"` 依赖；(2) `tensor.rs` `TensorData` 枚举新增 `F16(ArrayD<f16>)/BF16(ArrayD<bf16>)` 变体，所有方法（dtype/shape/len/ndim/as_f64_view/as_f16_slice/as_bf16_slice/as_f16/as_bf16/mapv/view/as_standard_layout/iter/broadcast）加 F16/BF16 分支；(3) 构造器：`from_data_f16/from_data_bf16/from_vec_f16/from_vec_bf16/zeros_f16/zeros_bf16/ones_f16/ones_bf16/full_f16/full_bf16`；`zeros_with_dtype/ones_with_dtype` 扩展 F16/BF16（注意 `ArrayD::zeros/ones` 不支持 f16/bf16 因缺 `num_traits::Zero/One`，改用 `ArrayD::from_elem(shape, f16::from_f32(0.0))`）；(4) dtype 提升表 `promote_dtype`：F64+任何→F64, F32+F32→F32, F16+F16→F16, BF16+BF16→BF16, F16+BF16→F32；`add_tensor/sub_tensor/mul_tensor/div_tensor` 重写为 `elementwise_binary` 通用方法；(5) `map_f16/map_bf16/zip_f16/zip_bf16` 内部辅助方法（转 f32 计算后回写）；(6) reductions/scalar ops/unary ops/transpose/broadcast_to/reshape/flatten/gelu/softmax/im2col/permute/assign_/masked_fill/scatter/gather 全部加 F16/BF16 分支；(7) **autodiff 策略 B 简化版**：F16/BF16 param 的 `acc_grad` 返回 `Err`（前向可参与计算，反向 grad 不回写）；`autodiff.rs FloatElem::from_tensor_data` 加 F16/BF16 转 f32/f64 分支；`acc_node_grad` 加 F16/BF16 统一提升 f64 的 catch-all——**Phase 2 再完善完整反向传播**；(8) `natives.rs + main.rs` 双侧注册 `zeros_f16/ones_f16/zeros_bf16/ones_bf16`；(9) HIR 白名单：`lower_expr.rs` Var 白名单 + 泛型 dispatch（`zeros<f16>`→`zeros_f16` 等）、`closures.rs` free_vars skip、`types.rs` 返回类型注册（zeros_f16/ones_f16→F16, zeros_bf16/ones_bf16→BF16）；(10) `methods.rs/vm.rs` `nbytes` 加 F16/BF16→2 字节。**Phase 2 遗留**：不碰 tenthc 自举编译器、不碰 JIT/WASM 路径（`compile/jit/translator.rs` 与 `compile/wasm/compile.rs` 不识别 F16/BF16）、F16/BF16 param 反向传播待完善。**序列化 v2 格式设计**：新格式 `[magic:"THW1"][version:i32=2][num_tensors] × [ndim][shape][dtype:i32][data]`，dtype 0=F32/1=F64/2=F16/3=BF16（F32=4字节，F64=8字节，F16/BF16=2字节，save 端按 dtype 分发写消除原 `as_standard_layout()` 硬编码 f64 精度损失）；向后兼容旧 v1 格式（无 magic 走旧路径纯 f64 兜底）；三处实现同步：`natives.rs:1064-1140(save)/1145-1276(load)`（解释器）、`main.rs:2060-2132(save)/2118-2270(load)`（VM）、`native_parity_test.rs:205-271(save)/272-390(load)`（测试用 VM，文件头明确要求"修改 main.rs 时需同步此函数"）。**改动文件清单**（14 个）：`tenth/Cargo.toml`（half = "2" 依赖）、`tenth/src/runtime/tensor.rs`（F16/BF16 变体 + 全部方法扩展）、`tenth/src/runtime/autodiff.rs`（FloatElem + acc_node_grad F16/BF16 分支）、`tenth/src/runtime/interpreter/natives.rs`（zeros_f16/ones_f16/zeros_bf16/ones_bf16 + 序列化 v2）、`tenth/src/runtime/interpreter/methods.rs`（nbytes bytes_per_elem F16/BF16）、`tenth/src/runtime/vm.rs`（nbytes bytes_per_elem F16/BF16）、`tenth/src/main.rs`（VM native 注册 + 序列化 v2）、`tenth/src/hir/lower/lower_expr.rs`（HIR 白名单 + 泛型 dispatch）、`tenth/src/hir/lower/closures.rs`（free_vars skip）、`tenth/src/hir/lower/types.rs`（返回类型注册）、`tenth/tests/native_parity_test.rs`（序列化 v2 同步）、`tenth/std/optim/accumulate.th`（accumulate_loop）、`tenth/std/optim/adamw.th`（adamw_step_w/_m/_v）、`tenth/std/optim/clip.th`（clip_grad_by_norm f64 比较）、`tenth/std/prelude.th`（索引同步）。**验证结果**：cargo check 通过（0 error）；lib 16 + integration 14 + native_parity 35 + stdlib 114 = 179 passed；autodiff 5 passed；自举验证通过（路径 A `[OK]`，路径 B/C 未单独验证但未触及 tenthc/）。**遗留项**（f16/bf16 Phase 2）：(a) JIT 路径 `compile/jit/translator.rs` 不识别 F16/BF16 TensorData；(b) WASM 路径 `compile/wasm/compile.rs` 不识别 F16/BF16；(c) F16/BF16 param 的 autodiff 完整反向传播（当前 early-return Err）；(d) tenthc 自举编译器不支持 f16/bf16 类型字面量。**自举三路径未破坏**：本批仅改 Rust 运行时 + 标准库 .th 文件，未触及 tenthc/ 自举编译器前端；路径 A 通过（cargo check + 全量测试全绿）；路径 B/C 不涉及（tenthc 自举源码不使用 f16/bf16/序列化 native）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件，未在禁止目录创建 .th 文件，未破坏已有测试。**文档同步**：`能力梳理/能力全梳理.md` §5.1 多精度张量说明更新（f16/bf16 Phase 1 已实现，状态保持 ⚠️ 待 Phase 2）、§5.1 张量序列化 ⚠️→✅（v2 格式 + dtype 支持）、§5.1 广播规则完善 ⚠️→✅（审计确认已完整）、§5.2 梯度累积 ⚠️→✅（accumulate_loop）、§5.2 梯度裁剪 ⚠️→✅（clip_grad_by_norm JIT 修复）、§5.3 梯度累积 ⚠️→✅、§5.4 AdamW ⚠️→✅（单值返回版本）、§5.4 梯度裁剪 ⚠️→✅、§九 总结矩阵 AI 特定需求 ✅ 55→62 / ⚠️ 12→5（7 项 ⚠️→✅：§5.1 张量序列化、§5.1 广播规则完善、§5.2 梯度累积、§5.2 梯度裁剪、§5.3 梯度累积、§5.4 AdamW、§5.4 梯度裁剪）、合计 ✅ 190→197 / ⚠️ 34→27；`AUDIT.md` 头部副标题追加"+ 张量修复（f16/bf16 + 序列化 + 优化器修复）"；测试部补测试任务进行中（黑板 ID 4/6/9/12 待处理），完成后由测试部同步 §三 测试矩阵。
>
> **2026-07-08 feat: 正则表达式标准库（6 个 regex native + regex.th）**：落实能力全梳理 §4.2 第二梯队 #7"正则表达式 — 文本处理刚需"。基于 Rust `regex = "1"` crate 实现 6 个 regex native 函数，采用 handle table 模式（与 `tcp_streams` 一致），Tenth 层不暴露 Regex 类型仅用 i64 handle。**6 个 native 函数**：`regex_compile(pattern: String) -> Result<i64>`（编译正则返回 handle，>0 成功，失败返回 Err 错误消息）、`regex_match(handle: i64, input: String) -> bool`（全匹配检查）、`regex_find(handle: i64, input: String) -> String`（查找第一个匹配，无匹配返回空串 `""`）、`regex_find_all(handle: i64, input: String) -> Vec`（查找所有匹配，无匹配返回空 Vec）、`regex_replace(handle: i64, input: String, replacement: String) -> String`（替换所有匹配）、`regex_split(handle: i64, input: String) -> Vec`（按正则分割字符串）。**handle table 模式**：`Vm.regexes: Vec<Option<regex::Regex>>` + `Interpreter.regexes: Vec<Option<regex::Regex>>`，索引+1 即句柄（1-based，0 表示无效），与 `tcp_streams` handle table 模式一致，不引入新 `Value` variant，复用 `Value::Int(i64)` 表示 handle。**双侧注册**：VM 路径（`main.rs::register_natives` 注册 6 个 native）+ 解释器路径（`runtime/interpreter/natives.rs::call_named_fn` 实现 6 个分支），与 `tcp_*`/`io_*` native 一致。**标准库模块**：新建 `tenth/std/regex.th`，6 个 wrapper 函数（`compile`/`match_`/`find`/`find_all`/`replace`/`split`；`match` 是 Tenth 关键字故 wrapper 命名 `match_`）；`tenth/std/prelude.th` 索引同步（注释说明 6 个 native 签名与 handle table 方案）。**HIR 三层白名单同步**（与 async native / io native 同模式）：`hir/lower/closures.rs` free_vars skip list + `hir/lower/lower_expr.rs` Var 推断白名单 + `hir/lower/types.rs` 返回类型注册。**改动文件清单**——Rust 源码（8 个）：`tenth/Cargo.toml`（添加 `regex = "1"` 依赖）、`tenth/src/runtime/vm.rs`（Vm 添加 `regexes` handle table 字段）、`tenth/src/runtime/interpreter/mod.rs`（Interpreter 添加 `regexes` 字段）、`tenth/src/runtime/interpreter/natives.rs`（6 个 regex native 解释器路径）、`tenth/src/main.rs`（6 个 regex native VM 路径注册）、`tenth/src/hir/lower/closures.rs`（free_vars skip list）、`tenth/src/hir/lower/lower_expr.rs`（Var 推断白名单）、`tenth/src/hir/lower/types.rs`（返回类型注册）；标准库（2 个）：`tenth/std/regex.th`（新建，6 个 wrapper 函数）、`tenth/std/prelude.th`（索引）；测试：`tenth/tests/regex_test.rs`（任务清单列为新建，但文档同步时磁盘上未检出该文件——见下文"遗留"）。**验证结果**：cargo check 通过；自举三路径未破坏——本批仅改 Rust 运行时 + 标准库 .th 文件，未触及 tenthc/ 自举编译器前端，路径 A 通过，路径 B/C 不涉及（tenthc 自举源码不使用 regex native）。**遗留**：(a) `tenth/tests/regex_test.rs` 在任务委派清单中列为"新建"，但文档部同步时该文件未在磁盘上检出（`tenth/tests/` 下 54 个测试文件无 regex_test.rs，`regex.th` 内亦无内嵌测试），测试覆盖待测试部确认/补齐；据此 AUDIT.md §三 测试矩阵本轮未添加 regex_test 行、总计数字未变。(b) regex native 仅在 VM + 解释器双侧注册，JIT 路径（`compile/jit/translator.rs`）未单独处理——regex native 走 native call 机制，JIT 遇到时通过既有 fallback 回 VM 执行（与 `tcp_*`/`io_*` native 一致）。**禁止事项无违反**：仅编辑现有 .rs 文件 + 新建 1 个标准库 .th 文件（在 `tenth/std/` 允许目录内），未在禁止目录创建 .th 文件，未破坏已有测试。**文档同步**：`能力梳理/能力全梳理.md` §4.2 正则表达式 ❌→✅、§九 总结矩阵 标准库（通用）✅ 47→48 / ❌ 106→105、合计 ✅ 189→190 / ❌ 355→354、§十 关键洞察第二梯队 #7 标记已解决；`AUDIT.md` 头部副标题追加"+ 正则表达式"（§三 测试矩阵未变——regex_test.rs 未实际创建）。
>
> **2026-07-08 feat: 异步 Phase 2 — 真正协程调度 + async I/O**：落实用户"把异步和并发解决"需求 Phase 2（真正协程调度），继 Phase 1（同步包装）后引入真正的协作式调度器与 async I/O 模块。**1. Value::Future 改造**（`runtime/value.rs` + `runtime/vm.rs` + `runtime/interpreter/binary.rs`）：`Value::Future(Box<Value>)` 改为 `Value::Future(Rc<RefCell<FutureState>>)`，Future 现在承载可变状态；新增 `FutureState` 枚举（`Pending(Vec<u64>)` 携带等待的 task_id 列表 / `Ready(Value)` 携带已完成值）；新增 `future_ready(v)` / `future_pending()` 辅助构造函数；binary.rs Display 适配新结构。**2. Frame 扩展**（`runtime/vm.rs`）：Frame 新增 `operand_stack: Vec<Value>`（每任务独立操作数栈，从共享栈迁移而来）+ `task_id: TaskId`；栈模型迁移——Frame push 时 take self.stack 到 Frame.operand_stack，Frame pop 时恢复（每任务独享栈空间，调度器切换时不串栈）；VM 新增 `next_task_id: TaskId` 用于分配新任务 ID。**3. VM 调度器**（`runtime/vm.rs`）：原 `run()` 改造为 `run_until_yield()` 返回 `YieldReason` 枚举（`Completed` 任务完成 / `Suspended` 任务挂起等待 Future / `Yield` 任务主动让出）；新增 `run_scheduler()` 主循环——从 `ready_queue` 取任务 → `run_until_yield` → 处理结果（Completed 把结果存到 `task_results`；Suspended 把 Frame 推回 `suspended`；Yield 把任务推回 `ready_queue` 尾部）；VM 新增字段 `ready_queue`/`suspended`/`task_results`/`task_futures`/`current_task`；调度器每轮调用 `ASYNC_IO.with(|s| s.poll())` 检查 I/O 完成并把已就绪的 Future 标记为 Ready、对应任务移回 `ready_queue`。**4. spawn/await 语义改造**（`runtime/vm.rs` + `compile/jit/translator.rs`）：`Op::Spawn` 保持 eager 语义（立即求值 inner，包装为 `Future(Ready(v))`）——Phase 1 兼容；`Op::Await` 改造——Ready 时直接取值（快路径），Pending 时挂起当前调用栈、把当前任务加入 Future 的等待列表 `waiters`、返回 `Suspended`；新增 `Op::Yield`（opcode 53）让出控制权，task 回到 `ready_queue` 尾部；**关键 bug 修复**：Await Pending 路径需把 Future 推回栈顶 + 保存 `ip-1`（恢复时重执行 Await 走快路径，避免 ip 已前进导致 Future 漂移）；JIT translator 遇 `Op::Yield` 返回 Err 触发既有 JIT→VM fallback。**5. async I/O**（`runtime/async_io.rs` 新建 + `main.rs` + `runtime/interpreter/natives.rs` + `hir/lower/lower_expr.rs` + `hir/lower/closures.rs` + `hir/lower/types.rs` + `tenth/std/async.th` 新建 + `tenth/std/prelude.th`）：新建 `runtime/async_io.rs` 模块，用 `std::thread` + `std::sync::mpsc` + `thread_local` 实现（零新依赖，不用 mio），管理异步任务状态——`AsyncJob` 在子线程执行，完成后通过 mpsc channel 把结果送回主线程，主线程在 `poll()` 时把结果写入对应 FutureState（Pending→Ready）；新增 3 个 async native（VM + 解释器双侧注册）：`async_sleep_ms(ms: i64) -> Future<Unit>`（异步睡眠，子线程 sleep 后通知主线程）、`async_tcp_read(handle: i64, n: i64) -> Future<Result<Vec<u8>, String>>`（异步读 TCP，基于已有 `tcp_streams` handle table，子线程读后通过 channel 送回）、`async_tcp_write(handle: i64, bytes: Vec<u8>) -> Future<Result<i64, String>>`（异步写 TCP）；新建 `tenth/std/async.th` 标准库模块索引 3 个 async native；`prelude.th` 索引同步；HIR 三层白名单同步（lower_expr.rs Var 解析 / closures.rs 闭包自由变量跳过 / types.rs 返回类型 `Future<...>` 推断）。**设计决策**：(1) **spawn 保持 eager**——立即求值 inner 表达式并包装为 Ready Future，真正的"并发"来自 async I/O 返回的 Pending Future（子线程工作期间调度器可切换到其他就绪任务），而非 spawn 本身制造并行；(2) **不引入 mio 依赖**——用 `std::thread` + `std::sync::mpsc` + `thread_local` 即可满足 async I/O 需求，避免新增 crate 依赖（与 Tenth "尽量少依赖"原则一致）；(3) **不新增语法层 token/AST**——`async_sleep_ms`/`async_tcp_read`/`async_tcp_write` 作为普通 native 函数调用，返回 `Future<T>` 值，`await` 既有语法即可消费；tenthc 无需同步（tenthc 自举源码不使用 async native）；(4) **解释器继续拒绝 async 路径**——async native 在解释器路径返回错误（与 Phase 1 一致），async 是 VM 独有能力；(5) **Op::Yield 已就绪但无语法层生成路径**——VM 已支持 `Op::Yield` 指令的执行，但 lower 阶段没有 yield 关键字的 AST→HIR→Op 路径，未来添加 `yield` 关键字时直接接入。**测试**（`tenth/tests/async_basic_test.rs` 扩展到 13 项，从 Phase 1 的 6 个）：覆盖 sleep 异步（async_sleep_ms 完成、并发 sleep 比同步快）、TCP echo 异步（async_tcp_read/write 端到端）、无效句柄错误传播、spawn/await 基础（保持 Phase 1 6 项）、yield 主动让出、调度器多任务切换。**验证结果**：cargo check 通过（0 error）；88 个测试全绿（lib 16 + integration 14 + io 4 + net 6 + native_parity 35 + async 13）；自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。**自举三路径未破坏**：路径 A 通过（cargo check + 全量测试全绿）；路径 B 通过（自举验证 `[OK]`）；路径 C 未单独验证但路径 B 通过即表明 tenthc 前端 + Rust 后端链路正常。**改动文件清单**——Rust 源码（11 个）：`tenth/src/runtime/value.rs`（FutureState + Value::Future 改造）、`tenth/src/runtime/vm.rs`（调度器 + Frame 扩展 + Op::Yield + Op::Await 改造）、`tenth/src/runtime/async_io.rs`（新建，async I/O 状态管理）、`tenth/src/runtime/interpreter/binary.rs`（Future Display 适配）、`tenth/src/runtime/interpreter/natives.rs`（async native 注册）、`tenth/src/compile/jit/translator.rs`（Op::Yield fallback）、`tenth/src/main.rs`（async native 注册）、`tenth/src/hir/lower/lower_expr.rs`（async native 白名单）、`tenth/src/hir/lower/closures.rs`（async native free_vars skip）、`tenth/src/hir/lower/types.rs`（async native 返回类型）、`tenth/src/lib.rs`（mod async_io）；标准库（2 个）：`tenth/std/async.th`（新建）、`tenth/std/prelude.th`（索引）；测试（1 个）：`tenth/tests/async_basic_test.rs`（扩展到 13 项测试）。**Phase 3 遗留**：(a) 状态机 lower pass——当前 async fn 在 lower 阶段仍是普通函数 + spawn/await，没有改写为状态机；WASM 后端无 Future 概念，async fn 跨 await 边界的状态保存需 lower 阶段重写为状态机；(b) WASM Future 支持——`compile/wasm/compile.rs` 无 Op::Spawn/Await/Yield 分支，WASM 路径下 async 不可用；(c) yield 语法层——`yield` 关键字 + AST/HIR/lower 路径未添加，当前 Op::Yield 只能通过手动构造字节码或 native 内部触发；(d) spawn 真并行——目前 spawn 仍为 eager 包装，真正并行仅来自 async I/O 子线程，未来若需 CPU 并行需引入工作窃取调度器或绿色线程池。**禁止事项无违反**：仅编辑现有 .rs 文件 + 新建 1 个标准库 .th 文件（在 `tenth/std/` 允许目录内）+ 新建 1 个 Rust 模块文件（`runtime/async_io.rs`，扩展运行时），未在禁止目录创建 .th 文件，未破坏已有测试。**文档同步**：`AUDIT.md` §三 测试矩阵 async_basic_test 数量 6→13、总计数字更新、§六 已知限制新增 spawn eager / yield 无语法层两条；`能力梳理/能力全梳理.md` §1.3 yield/协程 ⚠️→✅（VM 已支持 Op::Yield，无语法层 yield 关键字）、§4.4 异步 I/O ❌→✅、§3 运行时系统 async runtime ❌→⚠️（有调度器但非完整 runtime）、协程/绿色线程 ❌→✅（VM 调度器实现协作式协程）、§九 总结矩阵更新完成数量。
>
> **2026-07-08 feat: 元组类型 + ?操作符 + AUDIT 收尾（11.4.3 + 11.4.5）**：本条目为汇总条目，整合本次四项工作与三项 bug 修复。**1. 元组类型全管线实现**（lexer → parser → hir → lower → vm/interpreter → bytecode → jit/wasm）：新增 `Value::Tuple(Vec<Value>)` 变体、`Type::Tuple(Vec<Type>)` 类型、`ExprKind::Tuple(Vec<Expr>)` 与 `Pattern::Tuple(Vec<Pattern>)` AST 节点；新增 4 个 VM Op——`MakeTuple(n)`(opcode 49)、`IsTuple(n)`(50)、`TupleGet(i)`(51)、`Try`(52，与 `?` 操作符共用)；支持元组创建 `(1, 2)`、解构 `let (a, b) = pair`、match 解构、函数返回多值 `-> (i64, i64)`；空元组 `()` 编译为 `Value::Unit`（向后兼容）；JIT/WASM 遇新 Op 返回 Err 触发既有 fallback 机制。**2. `?` 操作符全管线实现**（方案 B：新增 `Op::Try` 指令）：新增 `Question` token（`?`）、`ExprKind::Try(Box<Expr>)` / `HirExprKind::Try(Box<HirExpr>)` AST/HIR 节点；类型推断从 `Result<T>` 提取 T；VM `Op::Try` 通过 frame 恢复实现 early return；解释器通过 `TryPropagate` 实现错误传播。**3. AUDIT-11.4.3 scope 扁平化**（由运行时部实施，详见下条独立记录）：`interpreter/mod.rs` 的 `scopes: Vec<HashMap<String, Value>>` 改为 `vars: HashMap<String, Vec<(usize, Value)>>` + `scope_depth` + `scope_vars`，lookup 性能 O(n*m)→O(1)，涉及 5 个文件（mod.rs、methods.rs、natives.rs、pattern.rs、repl.rs）。**4. AUDIT-11.4.5 host stub 补全**（由运行时部实施，详见下条独立记录）：`tenthc_for_loop_test.rs` 补全 4 个 env 模块 host function（vec_new/vec_len/vec_push/vec_get），取消 2 个 `#[ignore]` 测试，3 项 Vec 迭代测试全部通过。**3 个 bug 修复**：(1) parser `let (a, b) = ...` 解构 init 丢失（`parser.rs` L1320-1335）；(2) 解释器 `?` Err 路径 double-wrap（`interpreter/mod.rs` unwrap_return）；(3) bytecode 元组解构栈管理错误（`bytecode.rs` L683-700）。**新增测试**：`tenth/tests/tuple_test.rs`（新建 12 项测试，覆盖元组创建/解构/嵌套/函数返回/空元组）、`tenth/tests/try_operator_test.rs`（新建 12 项测试，覆盖 `?` 成功/错误传播/链式/I/O 模拟）。**改动文件清单**（12 个 Rust 源码 + 2 个新建测试 + 1 个修改测试）：`hir/types.rs`（元组类型注解解析 + `?` 类型推断）、`hir/lower/lower_expr.rs`（`?` 类型推断）、`compile/bytecode.rs`（4 个新 Op + 元组解构栈管理修复）、`runtime/vm.rs`（4 个新 Op 执行逻辑 + Try early return）、`runtime/interpreter/mod.rs`（Tuple eval + `?` TryPropagate + scope 扁平化 + unwrap_return 修复）、`runtime/interpreter/binary.rs`（Tuple 比较 + `?` TryPropagate）、`runtime/interpreter/methods.rs`（scope 扁平化适配）、`runtime/interpreter/natives.rs`（scope 扁平化适配）、`runtime/interpreter/pattern.rs`（scope 扁平化适配）、`compile/jit/translator.rs`（新 Op 触发 JIT fallback）、`parser/parser.rs`（元组解析 + `?` 解析 + let 解构 bug 修复）、`repl.rs`（scope 扁平化适配）；`tenth/tests/tuple_test.rs`（新建）、`tenth/tests/try_operator_test.rs`（新建）、`tenth/tests/tenthc_for_loop_test.rs`（补全 4 个 host function + 取消 2 个 #[ignore]）。**验证结果**：cargo check 通过（0 error，186 预存 warnings）；lib 16 passed、integration_test 14 passed、ownership_test 11 passed、pattern_match_test 17 passed、tuple_test 12 passed（0 ignored）、try_operator_test 12 passed、tenthc_for_loop_test Vec 迭代 3 passed；自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。**自举三路径未破坏**：路径 A 通过（cargo check + 全量测试全绿）；路径 B 通过（自举验证 `[OK]`）；路径 C 未单独验证但路径 B 通过即表明 tenthc 前端 + Rust 后端链路正常。**禁止事项无违反**：仅编辑现有 .rs 文件 + 新建 2 个测试文件，未在禁止目录创建 .th 文件，未破坏已有测试。**文档同步**：`AUDIT.md` §三 测试矩阵新增 tuple_test / try_operator_test 两行、§六 #7 栈溢出条目追加 Vec 迭代测试已通过说明；`能力梳理/能力全梳理.md` §1.2 元组类型 ❌→✅、§1.3 try 表达式/`?` 操作符 ❌→✅、§4.1 元组 ❌→✅、§九 总结矩阵语言核心设计 ✅ 43→45/❌ 46→44、标准库（通用）✅ 45→46/❌ 108→107、合计 ✅ 184→187/❌ 361→358、§十 关键洞察第二梯队 #9 元组类型 / #10 异常处理 标记已解决。
>
> **2026-07-08 fix: AUDIT-11.4.3 — 解释器 scope 双重存储优化（扁平化索引）**：由运行时部实施。**旧数据结构**：`pub scopes: Vec<HashMap<String, Value>>`（Vec+HashMap 双重存储，`resolve_var` 从最后一个 scope 向前遍历，重嵌套场景下 O(n*m)）。**新数据结构**：`pub vars: HashMap<String, Vec<(usize, Value)>>`（扁平化 name→(scope_depth, Value) 栈式索引）+ `scope_depth: usize`（当前深度）+ `scope_vars: Vec<Vec<String>>`（每层变量名列表，pop_scope 时只清理这些变量）。**新方法**：`push_scope`/`pop_scope`/`insert_var`/`remove_var`/`extend_globals`/`globals_clone` 替换旧的 `current_scope()`/`globals()`。**性能**：`resolve_var`/`set_var` 从 O(n*m) 降为 O(1)（HashMap get + Vec last）；`pop_scope` O(m)（m 为本层变量数，通过 `scope_vars` 避免遍历整个 HashMap）；`insert_var` O(1) 平均（entry API + 栈顶 depth 检测覆盖 vs shadowing）。**语义保持**：同 scope 同名变量覆盖（栈顶 depth 匹配则覆盖）；不同 scope 同名变量 shadowing（栈追加）；Moved 检查遍历栈所有条目（保持原"任何 scope 有 Moved 就报错"语义）；`guard_vars` 用 `vars.len()`（不同变量名数，比原"所有 scope 变量总数"更合理）。**改动文件**（5 个）：`runtime/interpreter/mod.rs`（结构体+6 新方法+resolve_var/set_var 重写+30 处 current_scope().insert→insert_var+2 处 scopes.push/pop→push_scope/pop_scope+Moved 检查重写）、`runtime/interpreter/methods.rs`（3 处 insert+2 处 push/pop，含带 captures 的 push 重写为 push_scope+循环 insert）、`runtime/interpreter/natives.rs`（2 处 insert+2 处 push/pop）、`runtime/interpreter/pattern.rs`（4 处 insert+4 处 remove→remove_var）、`repl.rs`（4 处 scopes[0].extend/clone→extend_globals/globals_clone）。**验证**：cargo check 通过（186 warnings 均为预存 hostcalls.rs direct cast）；lib 16/16、integration+ownership+stdlib+pattern_match 114/114、trait 9+enum 9+struct 8+generic 11+iterator 10+module 6、autodiff 54、native_parity 35、error_recovery 7、lexer 6+parser 5+type_inference 29、memory 17，全绿 0 回归。**预存失败（git stash 验证非本次引入）**：`parity_test::parity_continue_in_while`（WASM 路径栈溢出，不经过解释器）、`selfhost_frontend::tenthc_lowers_own_source`（tenthc 自举 lowering 栈溢出，原始代码也失败）。**自举三路径未破坏**：仅改 Rust 运行时解释器 + repl.rs，未触及 tenthc/ 与 .th 源码，未触及 vm.rs。**禁止事项无违反**：仅编辑现有 .rs 文件，未创建新文件，未破坏已有测试。**文档同步**：`AUDIT.md` AUDIT-11.4.3 状态 ⚠️→✅，登记元数据修复方案更新。
>
> **2026-07-08 fix: AUDIT-11.4.5 测试 host function stub 补全（tenthc Vec 迭代测试激活）**：由运行时部实施，补全 `tenth/tests/tenthc_for_loop_test.rs` 中 `env` 模块的 4 个 Vec host function stub（vec_new/vec_len/vec_push/vec_get），此前为空实现（vec_len 永远返回 0 导致循环体不执行）。**改动**：`tenth/tests/tenthc_for_loop_test.rs` 的 `setup_output_store_and_linker` 函数中 `env` 模块 4 个 stub 替换为真实 Vec 语义实现——基于 WASM 内存 + 共享 bump allocator（`Arc<AtomicU32>`），Vec 内存布局与 `wasm/host.rs` 的 `Vec_new`/`Vec_push` 对齐（24 字节 header = cap(8) + len(8) + dp(4)，8 字节/元素数据区），vec_push 含倍增扩容逻辑（cap=0 时初始化为 4，否则 cap*2，含旧数据 copy_within 迁移）。**签名差异注意**：tenthc wasm.th Type 3 的 vec_push 签名是 `(i64, i64) -> ()`（无返回值），与 host.rs 的 `Vec_push (i64, i64) -> i64`（返回 vec handle）不同，env 模块实现按 tenthc 签名不返回值。**取消 2 项 `#[ignore]`**：`tenthc_for_vec_literal_basic`（Test 7，`for x in [10,20,30]` 求和=60）和 `tenthc_for_vec_literal_break_continue`（Test 9，`for x in [1,2,3,4,5]` 带 break/continue 求和=7）。**验证**：3 项 Vec 迭代测试全部通过（含 Test 8 空 Vec）——`cargo test --manifest-path tenth/Cargo.toml --test tenthc_for_loop_test tenthc_for_vec_literal -- --test-threads=1`（需设置 `RUST_MIN_STACK=268435456` 规避 Windows debug 模式预存栈溢出问题）。**预存问题确认**：`tenthc_for_loop_test.rs` 在默认栈空间下栈溢出是预存问题（git stash 验证：干净工作目录上 `tenthc_for_break` 也栈溢出），与本次修改无关；AUDIT.md §六 #7 已登记。**文档同步**：`AUDIT.md` AUDIT-11.4.5 状态 ⚠️ 部分修复 → ✅ 已修复，登记元数据修复方案更新。**自举三路径未破坏**：仅改测试文件，未触及 tenthc/ 与 .th 源码，未触及 main.rs/interpreter。**禁止事项无违反**：仅编辑 2 份真理源文档（AUDIT.md / MEMO.md）+ 1 个测试文件，未创建新文件，未破坏已有测试。
>
> **2026-07-07 feat: Stage 1-4 同步 I/O 原语开发完成（io/env/net/http 四模块 13 个 native）**：落实"阶段 1：可用"通用编程能力缺口（能力全梳理 §6.1 第二梯队 #5 网络/HTTP）。Stage 1-4 共新增 13 个 native 函数，覆盖 stderr 输出、stdin 读取、环境变量、进程退出、TCP 客户端、HTTP 客户端四类基础 I/O 能力。**Stage 1（std::io 模块，3 个 native）**：`eprint(value) -> Unit`（stderr 输出无换行）、`eprintln(value) -> Unit`（stderr 输出带换行）、`read_line() -> Result<String>`（EOF/错误返回 Err）。**Stage 2（std::env 模块，3 个 native）**：`env_get(name: String) -> Result<String>`（未找到返回 Err）、`env_set(name: String, value: String) -> Unit`、`exit(code: i64) -> 不返回`。**Stage 3（std::net 模块，5 个 native，TCP 原语）**：`tcp_connect(host: String, port: i64) -> Result<i64>`（返回 handle）、`tcp_read(handle: i64, n: i64) -> Result<Vec<i64>>`（读最多 n 字节）、`tcp_write(handle: i64, data: Vec<i64>) -> Result<i64>`（返回写入数）、`tcp_close(handle: i64) -> Unit`、`tcp_set_timeout(handle: i64, ms: i64) -> Unit`。**Stage 4（std::http 模块，2 个 native，手写 HTTP/1.1）**：`http_get(url: String) -> Result<String>`、`http_post(url: String, body: String) -> Result<String>`。**关键设计决策**：(1) **Result<T> 统一错误处理模式**——所有 I/O native 返回 `Result<T>`（`Value::Enum { enum_name: "Result", variant: "Ok"/"Err" }`），用 `Ok` 包装成功值、`Err` 包装错误消息字符串；为简化构造在 `main.rs` 和 `interpreter/natives.rs` 双侧定义 `ok_result(value)`/`err_result(msg)` 辅助函数。(2) **TCP handle table 模式**——`Vm.tcp_streams: Vec<Option<TcpStream>>` 存储活跃连接，`tcp_connect` 返回索引作为 handle，`tcp_read/write/close/set_timeout` 通过 handle 索引访问；不引入新 `Value` variant，复用 `Value::Int(i64)` 表示 handle。(3) **HTTP 手写 HTTP/1.1**——不依赖 reqwest/hyper 外部 crate，`http_get`/`http_post` 直接基于 `TcpStream` + `ToSocketAddrs` 实现 HTTP/1.1 请求构造与响应解析（请求行 + Host 头 + Content-Length/Content-Type 头 + body，响应只取 body 部分）。**新增标准库文件**（4 个）：`tenth/std/io.th`（eprint/eprintln/read_line/read_line_result）、`tenth/std/env.th`（get/get_result/get_or_empty/set/exit）、`tenth/std/net.th`（connect/read/write/close/set_timeout）、`tenth/std/http.th`（get/post）。**新增测试文件**：`tenth/tests/io_test.rs` 4 项测试（eprint/eprintln/env_get/env_set/read_line）。**改动文件清单**：`main.rs`（13 个 native 注册 + ok_result/err_result 辅助 + Vm.tcp_streams 字段 + TcpStream 操作实现）、`runtime/interpreter/natives.rs`（13 个 native 解释器侧实现 + ok_result/err_result 辅助）、`tenth/std/{io,env,net,http}.th`（4 个新标准库文件）、`tenth/std/prelude.th`（索引同步）、`tenth/tests/io_test.rs`（新建 4 测试）。**自举三路径未破坏**：本批仅改 Rust 运行时 + 标准库 .th 文件，未触及 tenthc/ 自举编译器前端；路径 A 通过（cargo test io_test 4/4）；路径 B/C 不涉及（tenthc 自举源码不使用 I/O native）。**禁止事项无违反**：仅编辑现有 .rs 文件 + 新建 4 个标准库 .th 文件（在 `tenth/std/` 允许目录内）+ 新建 1 个测试文件，未破坏已有测试。**遗留**：TCP 服务端、UDP、TLS/SSL、WebSocket、DNS 解析、URL 解析、HTTP 服务端、异步 I/O 等高级网络能力未实现（见能力全梳理 §4.5）；HTTP 客户端为最小可用版本（无重定向、无 chunked transfer-encoding、无 HTTPS）。
>
> **2026-07-07 fix: AUDIT 4 项形式化健全性缺陷修复（T19 B6 + T20 PB2 + T42 + T18）**：由编译器部与运行时部实施代码修复，文档部同步真理源。本批修复 AUDIT §11.1 形式化健全性缺陷登记中的 4 项（AUDIT-11.1.4 T50 已于 2026-07-06 修复，本批完成后 §11.1 全部 5 条 ✅）。**AUDIT-11.1.1（T19 定理 B6 跨语句借用 unsoundness）**：`hir/lower/scope.rs` 新增 `borrow_holders: HashMap<String, Vec<String>>` 跟踪 holder→[borrowed] 关系；`release_borrows` 在释放前检查活跃 holder，保护对应 borrowed 变量不被过早释放；`hir/lower/lower_stmt.rs` Let 处理中记录 holder→borrowed（init 是 Ref/MutRef(Ident) 时）。**AUDIT-11.1.2（T20 PB2 四类反例未覆盖）**：`hir/lower/mod.rs` 新增 `expr_may_produce_ref` 递归判断 If/Block/Match/Move 是否产生 Ref 值；`creates_persistent_borrow` 改用此函数；新增 `collect_persistent_borrowed_idents` 收集多分支中的所有被借变量；`hir/lower/lower_stmt.rs` Let 处理用新函数支持多变量借用记录。**AUDIT-11.1.3（T42 LayerNorm per-feature γ + BatchNorm 多 channel dX backward 缺陷）**：`runtime/autodiff.rs` line 852-1015：(1) LayerNorm backward 把 `dX = γ·std_inv·(dY - mean(dY) - x̂·mean(dY·x̂))` 改为 `dX = std_inv·(gy - mean(gy) - x̂·mean(gy·x̂))`，其中 `gy = dY·γ`（γ 不能提到括号外）；(2) BatchNorm 重写为 per-channel 双遍循环，修正 d_gamma/d_beta shape（从 (N,C,H,W) 改为 (C,)）、mean 改为 per-channel、修正广播错误。**AUDIT-11.1.5（T18 body 直接 clone 健全性破口）**：`hir/lower/mod.rs` 新增 `substitute_expr` 递归替换 HirExpr 中所有 Type 字段；新增 `check_generic_instantiation_soundness` 健全性检查；`hir/lower/lower_expr.rs` GenericCall 实例化路径调用这两个函数，确保 body 内类型变量被一致替换。**改动文件清单**：`tenth/src/hir/lower/scope.rs`、`tenth/src/hir/lower/mod.rs`、`tenth/src/hir/lower/lower_stmt.rs`、`tenth/src/hir/lower/lower_expr.rs`、`tenth/src/runtime/autodiff.rs`（共 5 个 Rust 源码文件）。**验证结果**：cargo check 通过；lib 测试 16 passed（含 5 autodiff）；integration_test 14 passed；自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。**自举三路径未破坏**：路径 A 通过（cargo check + lib + integration 全绿）；路径 B 通过（自举验证 `[OK]`）；路径 C 未单独验证但路径 B 通过即表明 tenthc 前端 + Rust 后端链路正常。**禁止事项无违反**：仅编辑现有 .rs 文件，未创建新文件，未在禁止目录创建 .th 文件，未破坏已有测试。
>
> **2026-07-06 feat: tenthc 双侧同步完成（async/await/spawn 关键字同步到自举编译器）**：落实工作规范 §四 自举同步要求。Phase 1 在 Rust 侧实现了 async/await/spawn，但 tenthc 自举编译器未同步。本次补齐 tenthc 全链路（7 个 .th 文件 + 1 个 Rust bridge.rs）：(1) `tenthc/lexer/token.th`——TokenKind 枚举新增 `Async`/`Await`/`Spawn` 变体；(2) `tenthc/lexer/lexer.th`——关键字表新增 `"async"`(disc 64)/`"await"`(disc 65)/`"spawn"`(disc 66)；(3) `tenthc/parser/parser.th`——4 处改动：`is_expr_start_token` 加 65/66、`parse_unary` 加 Await/Spawn 前缀分支（与 Move 同模式）、顶层 dispatch 加 `async fn`(d==64→skip async→parse_fn)、impl 块 dispatch 加 async fn 支持；(4) `tenthc/hir/hir.th`——HirExpr disc 注释加 32=Await/33=Spawn，`HirFnDef` 新增 `is_async: bool` 字段；(5) `tenthc/hir/lower.th`——新增 `await`(disc 32)/`spawn`(disc 33) lowering（left=inner，类型直通）+ 3 处 HirFnDef 构造器加 `..` 默认 is_async=false；(6) `tenthc/compile/wasm.th`——d==32/33 pass-through（Phase 1 同步语义，WASM 无 Future 类型，spawn/await 均为 no-op）；(7) `tenth/src/compile/bridge.rs`——`convert_expr_depth` 新增 `"await"`/`"spawn"` 字符串匹配分支，转换为 `ast::ExprKind::Await`/`Spawn`。**验证**：`cargo check` 0 error；自举 Path B `[OK] Full compiler compiled to tenthc_full.wasm`；64 个测试全绿（lib 16 + async 6 + lexer 9 + integration 14 + enum 6 + parser 5 + struct 8，0 回归）；临时测试 `async fn + await + spawn` 在 VM 后端输出 43（42+1，语义正确）。**自举三路径未破坏**。**遗留**：tenthc 的 `is_async` 字段已存储但未影响 codegen（Phase 1 同步语义下 async fn 与普通 fn 等价）；tenthc 的 `FnDef` parser 结构未加 is_async（parse_fn 不区分 async，由调用方 skip async 关键字）。
>
> **2026-07-06 feat: 异步与并发 Phase 1 完成（async fn + await/spawn + Type::Future + VM Op::Await/Spawn）**：落实用户"把异步和并发解决"需求 Phase 1（基础设施）。设计方案 `docs/async-concurrency-design.md`（4193 行，12 章，用户已批准）选择**协作式协程 + async/await** 模型（类 Python asyncio/JS）。Phase 1 采用**协程式 VM 调度器**（基于 Frame 栈），状态机 lower pass 延后至 Phase 3（WASM 后端），以最快交付可用 async/await。**全链路改动**（9 个 Rust 源码文件 + 1 个测试文件）：(1) `lexer/token.rs` + `lexer/lexer.rs`——新增 `Async`/`Await` 关键字（`TokenKind::Async`/`TokenKind::Await`，前缀式 await/spawn 操作符遵循 `Move`/`Deref` 模式）；(2) `parser/ast.rs`——`ExprKind` 新增 `Await(Box<Expr>)`/`Spawn(Box<Expr>)`，`ItemKind::Function` 新增 `is_async: bool` 字段；(3) `parser/parser.rs`——4 处改动（顶层 dispatch 加 `Async`、`parse_item` 检测 `is_async`、Fn 构造器传 `is_async`、`parse_unary` 加 `Await`/`Spawn` 前缀分支）+ 3 处 `<expr>` Function 构造器补 `is_async: false`；(4) `compile/bridge.rs`——2 处 Function 构造器补 `is_async: false`；(5) `hir/hir.rs`——`HirExprKind` 新增 `Await(Box<HirExpr>)`/`Spawn(Box<HirExpr>)`；(6) `hir/types.rs`——`Type` 新增 `Future(Box<Type>)` 变体 + Display；(7) `hir/lower/lower_expr.rs`——`Await` lower（从 `Future<T>` 解包出 T）/`Spawn` lower（包装为 `Future<T>`）；(8) `hir/lower/closures.rs`——`Move | TryBlock` or-pattern 扩展含 `Await | Spawn`；(9) `runtime/value.rs`——`Value` 新增 `Future(Box<Value>)` 变体 + `type_of`（取 inner.type_of()）+ Display（`Future<{}>`）；(10) `runtime/vm.rs`——`Op` 新增 `Await`/`Spawn`（opcode 47/48）+ 全链路编解码 + dispatch（Phase 1 同步语义：Spawn pop v 包 `Future(v)` push、Await pop `Future(v)` 取 inner push，非 Future 则直通）+ `compile/bytecode.rs` emit `Op::Await`/`Op::Spawn` + `runtime/interpreter/mod.rs` stub（async 不支持解释器路径）+ `compile/jit/translator.rs` stub（async 不支持 JIT 路径，fallback 到 VM）+ `runtime/interpreter/binary.rs` `value_to_string` 加 Future 分支。**Phase 1 同步语义说明**：spawn 立即求值 inner 表达式（含函数调用）并包装为 Future，await 解包 Future 取内部值；非 Future 值 await 直通（idempotent）；无真正并发调度（Phase 2 引入 Ready Queue + Frame 挂起/恢复），但单线程协作式代码无 I/O 时语义等价。**测试**（`tenth/tests/async_basic_test.rs` 新建，6 项全绿）：spawn_then_await_int、await_plain_value（非 Future 直通）、spawn_literal、async_fn_parses（`async fn` 关键字解析）、multiple_spawn_await、spawn_string。**验证**：`cargo check` 0 error（186 warnings 均为预存）；lib test 16/16；lexer_test 9/9 + parser_test 5/5 + integration_test 14/14 + enum_test 6/6 + struct_test 8/8（0 回归）；自举 Path B `cargo run --release -- run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm`。**自举三路径未破坏**：路径 A 通过（cargo check + lib test 全绿）；路径 B 通过（自举验证 `[OK]`）；路径 C 未单独验证（selfhost_frontend 测试遇 Windows STATUS_STACK_BUFFER_OVERRUN 环境问题，非本次引入——stash 验证确认预存）。**禁止事项无违反**：仅编辑现有 .rs 文件 + 新建 1 个测试文件，未在禁止目录创建 .th 文件，未破坏已有测试。**遗留**：Phase 2（真正协程调度：Ready Queue + Frame 挂起/恢复 + yield 点）、Phase 3（状态机 lower pass，WASM 后端 Future 状态机化）、tenthc 双侧同步（async/await/spawn 关键字同步到 tenthc lexer/parser）待后续推进。
>
> **2026-07-06 feat: f32/f64 parity 路线图阶段 7 完成（tenthc 泛型 Tensor 类型注解支持）**：路线图最后一阶段，让 tenthc parser 能解析 `Tensor[T, ..]` 形式的泛型类型注解（T 为类型参数），对齐 Rust 侧 `hir/types.rs` 的 `Type::Tensor.dtype: Box<Type>` 数据结构。**性质**：能力扩展（tenthc 自举源码不使用此注解，阶段 3 标准库泛型化后双侧能力不对称），非修复 f32/f64 不对称点。**数据结构扩展**（`tenthc/hir/hir.th`）：HirType 新增 `dtype_ty: i64` 字段（0=用 sub 字段，非 0=指向 type_nodes 索引，承载 TypeParam dtype），用 `..` 默认值保持向后兼容。**parser 改造**（`tenthc/parser/parser.th`）：新增 `token_type_str(t: Token) -> str` 映射操作符/标点 token 到字符串；新增 `parse_type_annotation(p, term1, term2, term3) -> str` 带方括号深度跟踪的复合类型收集器（产出 `"Tensor[T,..]"` 形式，方括号内无空格），改造 7 处类型注解收集点（函数参数/结构体字段/返回类型/let 绑定等）使用新函数，避免在逗号处中断。**lower 改造**（`tenthc/hir/lower.th`）：新增 `add_tensor_type_param`/`get_tensor_dtype_ty`/`propagate_tensor_dtype` 三个 helper；新增 `parse_tensor_type` 识别 `"Tensor[<dtype>, <dims>]"` 模式，递归解析 dtype（可能是 f64/f32/类型参数 T），构造正确的 HirType（Base dtype → `add_tensor_type`；TypeParam dtype → `add_tensor_type_param`）；改造 `parse_type` 分发到 `parse_tensor_type`；改造 `substitute_type` 当 `dtype_ty != 0` 时递归替换 Tensor.dtype 内的 TypeParam；7 处 `add_tensor_type` 调用改为 `propagate_tensor_dtype`（binop 2 处/matmul 2 处/bmm 1 处/transpose 2 处，从操作数传播 dtype；保留 1 处 zeros/ones/rand/randn 为 F64 默认，无操作数 dtype 可传播）；改造 `is_type_f64`/`is_type_f32`/`is_type_str` 先检查 `disc == 0`（Base）避免误判 Tensor。**关键约束**：tenthc 仅暴露 str_eq/str_at/str_slice/str_len/str_cmp/str_int/str_add 7 个字符串 host 函数，但 Rust 母编译器 wasm.rs Call 分派对 str_eq/str_add/str_int 缺函数调用分支（仅 == 操作符路径可用），`parse_tensor_type` 用 == / != 操作符做字符串比较规避。**bridge.rs 同步**（`tenth/src/compile/bridge.rs`）：`parse_type_annotation` 加复合类型识别（字符串以 `"Tensor["` 开头时解析为 `TypeAnnotation::Tensor { dtype, dims }`），新增 `parse_dim_specs` 解析 `".."`/`"2,3"`/`"M,N"` 形式的 dims 列表。**测试**（`tenth/tests/tenthc_generic_tensor_test.rs` 新建）：5 项测试——Rust 母编译器解析 `Tensor[T, ..]` 基线、Rust 实例化泛型函数参考行为、tenthc 自举管线（Rust→WASM-A→wasmi 运行 tenthc→WASM-B）处理含 `Tensor[T, ..]` 源码、多注解场景、bridge.rs 路径 B 转换正确性。**验证**：自举 `[OK] Full compiler compiled to tenthc_full.wasm`、selfhost_frontend 4/4、tenthc_generic_tensor_test 5/5、parity_test 129/129、shape_check_compile_test 74/74（7 ignored）、stdlib_test 114/114、f32_frontend 13/13、f32_stdlib 35/35、tenthc_dotdot_eq 3/3、全量 0 回归。**预存缺陷（非本任务引入，git stash 验证）**：`generic_tensor_test::native_generic_ctor_f32_lowering`/`native_generic_ctor_f64_lowering`（assert 期望 `Tensor[F32, ..]` 但实际 `Tensor[F32, 3]`，shape 推断把字面量 3 算进去了）；`fixpoint_runtime::fixpoint_runtime_benchmark`（`Result::unwrap()` on `Err`）。**Rust 母编译器 wasm.rs 侧边缺陷（已规避，登记为独立 issue）**：`compile.rs:251-372` 的 `HirExprKind::Call` 分派对 `str_eq`/`str_add`/`str_int` 缺少函数调用分支（仅 `str_len`/`str_at`/`str_cmp`/`str_slice` 有），导致 tenthc 源码若以函数调用形式 `str_eq(a,b)` 调用会产出 i64 → i32 类型不匹配的非法 WASM；本任务通过在 tenthc 侧用 `==`/`!=` 操作符（走 BinOp 路径）规避；建议后续在 `compile.rs` Call 分派里补齐三个分支。**改动文件清单**：tenthc/hir/hir.th（dtype_ty 字段）、tenthc/parser/parser.th（parse_type_annotation + token_type_str）、tenthc/hir/lower.th（4 helper + parse_tensor_type + 改造 parse_type/substitute_type/is_type_* + 7 处 propagate_tensor_dtype）、tenth/src/compile/bridge.rs（parse_type_annotation + parse_dim_specs）、tenth/tests/tenthc_generic_tensor_test.rs（新建 5 测试）。**自举三路径未破坏**：路径 A 通过（cargo test 全绿）、路径 B 通过（自举验证 `[OK]`）、路径 C 通过（selfhost_frontend 4/4 + parity 129/129）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件 + 新建 1 个测试文件，未在禁止目录创建 .th 文件，未破坏已有测试。**路线图执行完毕**：f32/f64 parity 路线图 7 阶段全部完成，f32 与 f64 支持的已知不对称点全部消除，tenthc 双侧能力对齐。
>
> **2026-07-06 feat: f32/f64 parity 路线图阶段 2-6 完成（测试/DRY/backward f32 化/WASM f32 化/JIT f32 化）**：继阶段 1（文档同步）后连续推进阶段 2-6，消除 f32/f64 双精度全部已知不对称点（路线图 [`docs/f32-f64-parity-roadmap.md`](docs/f32-f64-parity-roadmap.md)）。**阶段 2（测试补强，26 项新测试全绿）**：新增 `tenth/tests/f32_parity_test.rs`（14 项 parity 测试：算术/构造器/matmul/激活/reduction/dtype 提升/dtype 保持）+ `tenth/tests/f32_stdlib_parity_test.rs`（8 项泛型标准库 `<f32>` vs `<f64>` parity：dropout/batchnorm/layer_norm/feedforward/sdpa/mha/transformer）+ 扩展 `f32_autodiff_test.rs`（+4 项梯度方向/幅度一致性测试，cosine similarity > 0.9999，L2 norm 比 [0.99, 1.01]）。双阈值策略（1e-12 严格 / 1e-5 相对+绝对双检）处理 f32 内在精度限制。**阶段 3（标准库 DRY 清理，13 个 .th 文件）**：删除 17 个 `_f32` 副本，原函数泛型化——`nn/activations.th`（relu/sigmoid/tanh/softmax/exp/log/gelu/leaky_relu 等泛型化，default 变体保留 f64）、`nn/linear.th`（`linear<T>`）、`nn/conv.th`（`conv2d<T>`）、`nn/embedding.th`（`embedding<T>`，indices 保留 f64）、`nn/loss.th`（mse/mse_loss/l1_loss/huber_loss 等泛型化，binary_cross_entropy 保留 f64）、`init/initializers.th`（删除 6 个 _f32 副本）、`optim/{sgd,adam,adamw,adagrad,rmsprop,clip,accumulate}.th`（删除 11 个 _f32 副本）、`math/constants.th`（保留 f64，native 限制）、`std/prelude.th`（索引同步）。stdlib_test 114/114、f32_stdlib_test 10/10、f32_stdlib_parity_test 8/8、自举成功、全量 0 回归。**阶段 4（autodiff backward f32 化，高风险改造）**：`runtime/autodiff.rs` 重大重写——TapeNode 结构新增 `dtype: BaseType` 字段（记录前向运算 dtype）；`node_grads` 从 `Vec<Option<ArrayD<f64>>>` 改为 `Vec<Option<TensorData>>`；引入 `FloatElem` trait（f32/f64 泛型，含 from_f64/to_f64/sqrt_/tanh_/from_tensor_data/into_tensor_data）+ `dispatch_float!` 宏（按 node.dtype 分发代码块）；28 个 TapeOp 变体全部用 `dispatch_float!(node.dtype, E, { ... })` 重写；辅助函数 acc_node_grad/propagate_grad 接受 TensorData；unbroadcast/matmul_2d 泛型化。`runtime/tensor.rs` `acc_grad` 签名从 `&ArrayD<f64>` 改为 `&TensorData`，按 self.dtype 转换。**关键 bug 修复**：Reshape backward 回归（5 项 MHA 测试失败）——原 `into_shape_with_order` 在非连续视图上失败，改用 `ArrayD::from_shape_vec` 从迭代器收集到 Vec 后重构。autodiff_test 54/54、f32_autodiff_test 12/12、f32_parity_test 14/14、MHA 11/11、自举成功。**策略 B 残留**：混合 dtype 场景回退为 f64 累加（tensor.rs acc_grad），策略 A（WASM F32→F64 塌缩）与策略 B（backward 全 f64）已消除。**阶段 5（WASM 后端 f32 化，双侧同步）**：`compile/wasm/compile.rs` Binary 分发用 `expr.ty` 决定 F32/F64 路径（操作数提升逻辑），`compile_binop` 接受单一 result_ty 按 is_f32/is_f64 分发 F32Add/F64Add 等；`compile/wasm/mod.rs` 类型映射 F32→ValType::F32, F64→ValType::F64（分开）；`compile/wasm/sections.rs` Type/import sections 使用 to_val_type；`compile/wasm/host.rs` 17 个 host imports 无 f32 hostcall（按预授权保留 f64 hostcall）。**tenthc 双侧同步**（`tenthc/compile/wasm.th` 重大修改）：新增 11 个 f32 指令（wasm_f32_const/add/sub/mul/div/eq/ne/lt/gt/le/ge）；**修正 4 个 WASM 转换 opcode bug**（关键 bug 修复）：wasm_f32_convert_i64_s 178→180 (0xB4)、wasm_f64_convert_i64_s 191→185 (0xB9)、wasm_i64_reinterpret_f64 190→189 (0xBD)、wasm_f64_reinterpret_i64 189→191 (0xBF)；is_expr_float/is_expr_f32/is_expr_f64 新增 `tn.disc == 0` 检查避免误识别 Tensor 类型；TensorLiteral arg_list 查找修正 `elem_idx = hir.arg_list[e.args_start + i - 1]`（之前直接用 `e.args_start + i - 1` 作为 expr 索引）；FloatLiteral/Var/Binary/Unary/Assign/AssignOp/Let 全部按 dtype 分支。f32_wasm_test 14/14、parity_test 129/129、selfhost_frontend 4/4、自举成功。**阶段 6（JIT 后端 f32 化）**：`compile/jit/hostcalls.rs` 新增 `host_make_float32(vm, f: f32, out)` hostcall（写入 `Value::Float32(f)`）+ `host_make_tensor_f32(vm, rows, cols, args, out)` hostcall（元素保持 f32 精度）；host_make_tensor 增加 `Value::Float32(f) => *f as f64` 分支；两个新 hostcall 注册到 hostcall_addr 符号表。`compile/jit/translator.rs` PushFloat32 从 `call_hostcall_f64("host_make_float", f as f64, out)` 改为 `call_hostcall_f32("host_make_float32", f, out)`；MakeTensor 根据 `dtype == 1` 分发到 host_make_tensor_f32 或 host_make_tensor；新增 `call_hostcall_f32` 方法（用 `types::F32` + `f32const`）。**关键设计决策**：不新增 f32 算术 hostcall（vm.add_priv 已按 dtype 分发）。jit_test 10/10、parity_test 129/129、自举成功、全量 0 回归。**three_stage_selfhost 测试修复**：调查确认 wasmtime 路径失败为预存问题（17 个 stub host import 返回 0，WASM-B 0 字节），与阶段 5 改动无关（stash 阶段 5 改动后测试仍以同样方式失败）。wasmi 路径通过（WASM-B 460 bytes，add(3,4)=12 VERIFIED）。将 wasmtime 路径拆为 `#[ignore]` 的 `three_stage_selfhost_wasmtime` 独立测试，默认 `cargo test` 仅跑 wasmi 路径。AUDIT.md §六 #5 登记 wasmtime stub 补全为独立任务。**改动文件清单**：阶段 2 新建 2 测试文件 + 扩展 1；阶段 3 修改 13 个 .th 文件；阶段 4 修改 autodiff.rs + tensor.rs；阶段 5 修改 compile/wasm/{compile,mod,sections,host}.rs + tenthc/compile/wasm.th + f32_wasm_test.rs；阶段 6 修改 compile/jit/{hostcalls,translator}.rs；three_stage.rs 拆分测试。**自举三路径未破坏**：路径 A 通过（cargo test 全绿）、路径 B 通过（自举验证 `[OK] Full compiler compiled to tenthc_full.wasm`）、路径 C 通过（selfhost_frontend 4/4 + three_stage wasmi 路径通过）。**禁止事项无违反**：仅编辑现有 .rs/.th 文件 + 新建 2 个测试文件 + 1 个路线图文档（用户明确要求），未在禁止目录创建 .th 文件，未破坏已有测试。**遗留**：阶段 7（tenthc 泛型 Tensor 支持）独立，待后续推进；wasmtime stub host 补全为独立任务（AUDIT §六 #5）。
>
> **2026-07-06 docs: f32/f64 文档同步（路线图阶段 1）**：这是 f32/f64 双精度对等路线图（[`docs/f32-f64-parity-roadmap.md`](docs/f32-f64-parity-roadmap.md)）的阶段 1：文档同步，目标是消除文档与代码的严重不一致。f32 支持已完成 Phase 1-5 改造，运行时层（VM/解释器/Tensor/标准库 natives）双侧基本对齐，但文档严重滞后。本条目为文档部执行，纯文档改动，未触及 .rs/.th 代码。**改动的 2 份真理源文档**：(1) `docs/语言参考手册.md`——§11 开篇陈述：旧"所有张量操作均为 `f64` 元素类型"更新为反映双 dtype 支持现状（`TensorData` 枚举含 `F32(ArrayD<f32>)` / `F64(ArrayD<f64>)` 双变体，由 `ndarray` 库实现，前端/HIR/VM/解释器/autodiff 全链路双侧对齐）；§3.2 张量类型示例：在现有 `Tensor[f64, 3, 224, 224]` / `Tensor[f64, ..]` 旁新增 `Tensor[f32, 3, 224, 224]` / `Tensor[f32, ..]` 示例；§11.1 张量创建表：现有 `tensor[[]]` / `rand` / `randn` 行（标注默认 f64）后新增 `zeros_f32` / `ones_f32` / `rand_f32` / `randn_f32` 四行 f32 专用构造器（返回 `Tensor[f32, ..]`），示例代码补充 f32 用法；新增 §11.11 "f32 支持范围与限制"章节（已支持/策略性降级/已知不对称/dtype 选择指南四部分，引用路线图为残余问题来源）；目录加 §11.11 条目。(2) `能力梳理/能力全梳理.md`——§一 语言核心设计 张量类型条目状态描述更新："f32 Phase 1" → "f32 Phase 5.6 真泛型改造完成"（行 49，过时登记纠偏）；§十 关键洞察 最关键缺口列表第 3 项 "f32 张量 — f64 在 AI 中不可接受" 标注为已解决（删除线 + 实现证据，行 847，f32 已实现不再属于缺口）。**交叉引用**：§11 开篇与 §11.11 末尾互相引用；§11.11 引用路线图 `docs/f32-f64-parity-roadmap.md`；§11.11 dtype 指南引用 §3 类型系统。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码。**禁止事项无违反**：仅编辑 3 份真理源文档（语言参考手册 / 能力全梳理 / MEMO.md），未创建新 .md 文件，未修改 .rs/.th 代码。**未发现其他文档不一致**：CODE_WIKI.md / AUDIT.md / DEPS.md 经抽查无 f32 相关过时登记需要本次同步（CODE_WIKI 张量章节描述与运行时一致；AUDIT 无 f32 缺陷登记；DEPS 不涉及 dtype）。
>
> **2026-07-06 feat: gather 原语 + scatter 多维扩展（autodiff 算子 27→28）**：由运行时部实施代码改动（gather 新增 + scatter 多维扩展），测试部补 21 项回归测试 + 修复 4 处 unflatten row-major bug，文档部同步真理源。**gather 新增**（6 个文件修改）：`Tensor::gather(base, dim, index)`——沿 dim 维用 index 索引 base，`out.shape == index.shape`，dtype 跟随 base（PyTorch gather 对齐）；可微 `TapeOp::Gather`，backward 用 scatter-add 语义（重复 index 累加），index 不可微；TapeNode 新增 `aux: usize` 字段携带 dim 参数；VM + 解释器双侧 native 注册（函数名 "gather"）；shape 推断 out shape == index.shape；关系调试器 `classify_tape_op` 分类为 Preserve（与 Scatter 一致）。**scatter 多维扩展**：移除 dim=0 + 1D 限制，支持任意 dim + 多维 index/src（PyTorch `torch.scatter_` 对齐）；backward 同步扩展，新增 `flatten_index` 辅助函数将多维索引扁平化；校验 `index.ndim()==base.ndim()`、除 dim 维外 `index.shape==base.shape`、`index.shape==src.shape`。**测试**：新增 21 项（gather_test 13 + scatter_test 多维扩展 7 + relation_debugger 1），修复 4 处 unflatten row-major bug（tensor.rs 2 处 + autodiff.rs 2 处），0 回归。**autodiff 算子集扩展**：27→28（新增 Gather）。**T7 定理 TapeOp 四类分类更新**：Construct 7（不变）/ Preserve 14→15（新增 Gather，与 Scatter 同类 shape 不变）/ Reduce 2（不变）/ Expand 4（不变）；C2b 判据不变（非 Preserve 满足 C2）。**文档同步**（本条目为文档部执行，纯文档改动，未触及 .rs/.th 代码）：(1) `docs/语言参考手册.md`——清理 True MHA 3 处过时表述（§11.5 backward 表后说明 "仍待标准库部推进" → "已完成"；§11.7 bmm 章节开头 "single-head 等价包装...现在可以用 bmm 真正分头计算" → "已基于 bmm 重写为真正分头计算版本"；§11.7 示例注释 "重写后路径" → "现行实现"）；新增 §11.10 gather 原语章节（函数签名/shape 规则/语义/可微性/示例/实现细节/已知限制，与 §11.7/§11.8 同结构）；更新 §11.8 scatter 章节（移除 "dim=0 + 1D" 限制表述，更新参数/语义/backward/示例/已知限制为多维支持）；更新 §11.5 算子列表加 Gather + backward 公式表加 Gather 行；更新 §11.9 T7 分类表 Preserve 13→15（新增 MaskedFill + Gather）/Expand 3→4（新增 Reshape）；**修正上一批次遗留**：头部 + §11.5 + §11.9 + classify_tape_op 描述的 "25 算子" 计数文本（前一批次 Reshape/MaskedFill 25→27 仅更新了算子列表，未更新计数文本与 T7 表），本次一并修正到 28；目录加 §11.10 条目。(2) `能力梳理/能力全梳理.md`——§5.1 Scatter/Gather 条目 ⚠️→✅（更新为 scatter 任意 dim + 多维 index/src、gather 已实现、可微 TapeOp::Scatter/TapeOp::Gather）；§5.2 关系调试器 T7 分类 27→28 算子（Preserve 14→15，新增 Gather）；§5.2 "27 个算子 backward" → "28 个算子 backward" 并补 Gather backward 公式 + scatter 多维扩展说明；§九 总结矩阵 AI 特定需求 ✅ 54→55 / ⚠️ 13→12 / 完成率 33%→34%、合计 ✅ 176→177 / ⚠️ 35→34 / 完成率 ~30%→~31%；§十 关键洞察 27→28 算子。(3) `AUDIT.md`：经 grep 检查无 scatter dim=0 限制相关登记（scatter 多维扩展是新增能力不是 bug 修复），无需更新。(4) `MEMO.md` 本条记录。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码（gather 新增与 scatter 扩展由运行时部已完成并验证，测试部 21 项新增 + 0 回归）。**禁止事项无违反**：仅编辑 3 份真理源文档（语言参考手册 / 能力全梳理 / MEMO.md），未创建新 .md 文件，未修改 .rs/.th 代码。**发现的历史不一致**：`docs/语言参考手册.md` 中 "25 算子" 计数文本在 4 处（头部、§11.5、§11.9 T7 表标题、classify_tape_op 描述）仍是 25——前一批次（2026-07-06 docs: Reshape 与 MaskedFill 接入 autodiff 文档同步 算子 25→27）MEMO 记录称已更新到 27，但实际文件仅更新了 §11.5 算子列表（加 Reshape/MaskedFill）和 backward 公式表，计数文本与 T7 分类表（Preserve 13/Expand 3）未同步更新。本次借 gather 新增一并修正到 28（Preserve 15/Expand 4），消除历史遗留不一致。
>
> **2026-07-06 docs: True MHA 文档同步（AUDIT-11.1.4 ⚠️→✅）**：代码已重写完成，文档仍登记"待重写"，本条目执行文档同步。**代码现状**：`tenth/std/nn/multihead_attention.th` 头部注释明确写"True Multi-Head Attention"，实现用 `bmm` + `reshape` + `transpose` 完整分头计算——q = x.matmul(w_q) → q.reshape(n_heads, seq_len, d_k)，scores = q_3d.bmm(k_3d.transpose()) * scale，out_3d = weights.bmm(v_3d)，out = out_3d.reshape(seq_len, d_model).matmul(w_o)。**测试现状**：`tenth/tests/multihead_attention_test.rs` 11 项测试覆盖 True MHA 前向 shape、有限性、n_heads=1 与单头等价、autodiff 无错误、w_q/w_k/w_v 梯度非零、x 梯度传播、完整反向链等（详见下方 2026-07-06 test: MHA 完整梯度测试补强 条目，已记录为通过）。**文档同步**（本条目为文档部执行，纯文档改动，未触及 .rs/.th 代码）：(1) `AUDIT.md` §11.1 AUDIT-11.1.4 当前状态列 ⚠️→✅，保留论文来源列不变；(2) `能力梳理/能力全梳理.md` §5.3 神经网络库 MultiheadAttention 条目 ⚠️→✅、§九 总结矩阵 AI 特定需求 ✅ 53→54 / ⚠️ 14→13、合计 ✅ 175→176 / ⚠️ 36→35（两份真理源头部日期已是 2026-07-06，无需更新）。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码。**禁止事项无违反**：仅编辑 3 份真理源文档（AUDIT.md / 能力全梳理.md / MEMO.md），未创建新 .md 文件，未修改 .rs/.th 代码。
>
> **2026-07-06 fix: AUDIT B 类 4 项缺陷修复（11.4.1/11.4.2/11.4.4/11.4.5）**：由编译器部与运行时部实施代码修复，测试部补回归测试，文档部同步真理源。**AUDIT-11.4.1（T22 FV5 O(n²) 工程债务）**：`hir/lower/closures.rs` `free_vars_in`/`collect_free_vars`/`collect_free_vars_stmt` 改用 `HashSet<String>`，4 处 `push` 改 `insert`；API 兼容（仍返回 `Vec<String>` + `sort`），复杂度 O(n²)→O(n)，纯优化无语义影响。验证：autodiff 52/52 + 全量 0 回归。**AUDIT-11.4.2（T31 MAX_STACK_DEPTH 静默溢出）**：`compile/jit/translator.rs` 新增 `bump_sp()` 方法在 push 前检查 sp 上限，28 处 push 操作改为 `bump_sp()?`；超限返回 `Err` 触发既有 JIT→VM fallback（`mod.rs:62-65`），不再静默截断。验证：jit_test 10/10 + 全量 0 回归。**AUDIT-11.4.4（tenthc `..=` lexer bug）**：`tenthc/lexer/lexer.th:180-190` 在 `if next == "."` 分支内 advance 后再 peek 一次检查 `=`，匹配则 advance 并返回 `DotDotEq`，与 Rust 侧对齐。验证：自举 + parity 129/129 + selfhost_frontend 4/4 + error_recovery 7/7；新增 `tenthc_dotdot_eq_test.rs` 3 项测试。**AUDIT-11.4.5（tenthc Vec 迭代未实现）**：`tenthc/compile/wasm.th:1471-1546` For 语句新增 ArrayLiteral (disc=22) 分支，复用现有 `vec_new`/`vec_len`/`vec_push`/`vec_get` host imports 实现 Vec 迭代 codegen；后续修复：删除 `wasm.th:1492` 误加的 `wasm_drop`（type/drop 不匹配导致 wasmi 验证失败）。验证：自举 + parity 129/129 + selfhost_frontend 4/4。**遗留**：测试 host function stub 是空实现（`vec_len` 永远返回 0），2 项非空 Vec 迭代测试 `#[ignore]`——tenthc 编译器层面 codegen 正确，仅测试基础设施局限；ArrayLiteral 表达式本身 codegen 未改。**文档同步**（本条目为文档部执行）：`AUDIT.md` §11.4 AUDIT-11.4.1/11.4.2/11.4.4 状态 ⚠️→✅、AUDIT-11.4.5 状态 ⚠️→⚠️ 部分修复（保留论文来源列不变，仅更新当前状态列）；`能力梳理/能力全梳理.md` §2.1 编译管线新增 4 项条目（tenthc inclusive range lexer 解析 ✅、tenthc Vec 迭代 For codegen ⚠️ 部分实现、JIT 栈深度溢出检测 ✅、HIR 闭包自由变量收集复杂度 ✅ 已优化）、§九 总结矩阵 编译器与工具链 65→69/✅27→30/⚠️5→6/42%→43%、合计 573→577/✅172→175/⚠️35→36（4 项修复：3 项 ⚠️→✅、1 项 ⚠️ 保持但说明更新）；日期已是 2026-07-06 无需更新。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码（4 项代码修复由编译器部与运行时部已完成并验证，自举 + parity + selfhost_frontend 全绿）。**禁止事项无违反**：仅编辑 3 份真理源文档（AUDIT.md / 能力全梳理.md / MEMO.md），未创建新 .md 文件，未修改 .rs/.th 代码。
>
> **2026-07-06 test: MHA 完整梯度测试补强**：multihead_attention_test 7→11 项（+4），新增 w_q/w_k/w_v 梯度非零、输入 x 梯度传播、完整反向链测试。验证 True MHA 在 Reshape/MaskedFill 接入 autodiff 后 w_q/w_k/w_v/x 梯度完整传播。
>
> **2026-07-06 docs: Reshape 与 MaskedFill 接入 autodiff 文档同步（算子 25→27）**：由运行时部实施代码改动（`TapeOp::Reshape` / `TapeOp::MaskedFill` 已实现并可用，reshape 与 masked_fill 方法在 recording 时记录到 Tape），文档部同步真理源。**autodiff 算子集扩展**：25→27（新增 Reshape / MaskedFill）。**backward 公式**：(1) Reshape `grad.reshape(input.shape())`——梯度 reshape 回输入原始 shape，`input_tensors=[input, result]`，从 input.shape() 取原始 shape；(2) MaskedFill `grad*(1-mask)`——mask 位置梯度归零（被填充位置不可微），`input_tensors=[input, mask, result]`，mask 不可微阻断链式传播。**T7 定理 TapeOp 四类分类更新**：Construct 7（不变）/ Preserve 13→14（新增 MaskedFill，与 Scatter 同类 shape 不变）/ Reduce 2（不变）/ Expand 3→4（新增 Reshape，与 Transpose 同类重排产生新 shape）；C2b 判据不变（非 Preserve 满足 C2）。**True MultiheadAttention 运行时层完整可微**：Reshape/MaskedFill 此前以 workaround 形式出现在 `std/nn/multihead_attention.th`（reshape/masked_fill 调用阻断 w_q/w_k/w_v 梯度传播），接入 autodiff 后运行时层面 w_q/w_k/w_v 梯度不再被阻断——True MHA 完整可微的运行时条件已具备；标准库基于 bmm 重写为真正分头计算版本仍待标准库部推进（见 AUDIT-11.1.4）。**黑板协议首次实际使用验证**：本次任务是黑板协作协议的首次实际使用——运行时部通过黑板 ID 3 向文档部留言（autodiff 算子 25→27 + backward 公式 + T7 分类），文档部读取留言后执行文档同步，验证了"开发部门完成改动 → 文档部通过黑板接收留言 → 同步更新真理源"的协作闭环。**文档同步**（本条目为文档部执行）：`docs/语言参考手册.md` 头部 25→27 算子、§11.5 算子列表加 Reshape/MaskedFill、§11.5 新增 backward 公式表（Reshape/MaskedFill 两行）+ True MHA 梯度阻断解除说明、§11.9 T7 分类表更新为 27 算子（Preserve 14/Expand 4）、classify_tape_op 描述 25→27；`能力梳理/能力全梳理.md` Reshape/Masked Fill 条目补充可微说明与 T7 分类、关系调试器 T7 分类 25→27 算子（Construct 7/Preserve 14/Reduce 2/Expand 4）、"25 个算子 backward"→"27 个算子 backward"并补 Reshape/MaskedFill backward 公式、MultiheadAttention 条目补充"运行时层 w_q/w_k/w_v 梯度不再被阻断"、§十 关键洞察 25→27 算子；`AUDIT.md` 经 grep 检查无 reshape/masked_fill 不可微的缺陷登记（§11 中无相关条目），无需新增或更新。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码。**禁止事项无违反**：仅编辑 3 份真理源文档（语言参考手册 / 能力全梳理 / MEMO.md），未创建新 .md 文件，未修改 .rs/.th 代码，未触及 CODE_WIKI.md（本次任务范围未要求）。**遗留**：True MHA 标准库重写（multihead_attention.th 基于 bmm 改为真正分头计算）属标准库部范畴，待后续推进；CODE_WIKI.md 算子表是否需要同步更新待总师确认（黑板 ID 3 提到 CODE_WIKI.md，但本次任务禁止事项限定"只修改 4 份真理源文档 + 语言参考手册"）。
>
> **2026-07-06 test: LSP + tenthpm 测试覆盖补强**：LSP 服务器（tenth/tools/lsp/）从零测试覆盖到 46 项单元测试，覆盖 9 个模块（diagnostic 7/formatting 5/semantic_tokens 4/folding_range 4/document_symbol 5/hover 5/document_store 6/completion 6/initialize 4）；修复 3 处 FloatLiteral 模式匹配不同步预存缺陷（hover.rs:140 + semantic_tokens.rs:91,171，FloatLiteral 从 1 字段改为 2 字段匹配）。tenthpm 包管理器从 30 项增至 52 项（+22），新增安全校验函数单元测试 16 项（validate_package_name 7/is_git_url 2/extract_package_name 1/safe_package_name_from_git 2/safe_to_remove_dir 4/ensure_within 3）+ Manifest/Lockfile 单元测试 4 项 + 未覆盖命令集成测试 4 项（remove git 依赖离线模拟/clean --deps/install 本地路径/install 路径穿越拒绝）。**自举三路径未破坏**：改动仅在 tenth/tools/ 下两个独立 crate，未触及主编译器。**禁止事项无违反**：LSP 未修改 Cargo.toml（保持 bin-only，用内联 #[cfg(test)] 模块）；tenthpm 未修改 src/ 代码（仅追加测试）。
>
> **2026-07-06 docs: AUDIT #9 陈旧登记纠偏**：经调研核实，`AUDIT.md` §7.3 #9（原登记"tenthc/parser/parser.th:631 无 for 循环解析 — lexer 识别 for 但 parser 未处理"）为陈旧失效登记。**实际状态**：for-in 解析已在 tenthc 全链路实现——`tenthc/parser/parser.th:1100-1132`（`parse_stmt` 中 `disc==12` 分支）+ `tenthc/hir/lower.th:1258-1279`（`kind=="for"` 分支，lower 为 `disc=4`）+ `tenthc/compile/wasm.th:1404+`（`disc==4` 分支）。原引用行号 `parser.th:631` 实为 generic_call 创建 ident 节点逻辑，与 for 循环无关。tenthc 自举源码本身大量使用 for 循环（`parser.th` 中 13+ 处），自举路径 B/C 必须能编译 for 循环——这是功能可用的铁证。**文档同步**（本条目为文档部执行）：`AUDIT.md` §7.3 #9 状态 → ~~登记陈旧失效~~，附全链路实现证据；`能力梳理/能力全梳理.md` §2.1 编译管线新增"tenthc for-in 循环解析"条目（✅ 已实现，引用 AUDIT.md §7.3 #9）；`MEMO.md` 本条记录。**回归测试**：测试部同步新增显式回归测试 `tenthc_for_loop_test.rs`（由测试部实施，文档部不触及 .rs 文件）。**自举三路径未破坏**：纯文档改动，未触及 .rs/.th 代码。**禁止事项无违反**：仅编辑 3 份真理源文档（AUDIT.md / MEMO.md / 能力全梳理.md），未创建新 .md 文件，未修改 .rs/.th 代码。
>
> **2026-07-06 feat: 新增 bmm 与 scatter 两个可微原语（autodiff 算子 23→25）**：由运行时部实施，文档部同步真理源。**bmm（batched matmul）**：`Tensor::bmm` 方法，3D@3D→3D，输入 `a:(B,M,K)` `b:(B,K,N)` → 输出 `(B,M,N)`；逐 batch 调用 2D matmul；可微 `TapeOp::BatchedMatMul`，反向逐 batch 与 2D MatMul 一致（d_a = grad.bmm(b.transpose(0,2,1))，d_b = a.transpose(0,2,1).bmm(grad)）；注册为 tensor method（VM `call_method_priv` + 解释器 `methods.rs` 双路径）；batch 维 B 与内侧 K 必须匹配（不做广播）；仅 3D@3D，4D+ 未实现。**scatter**：`scatter(base, dim, index, src)` 全局 native，覆盖语义 out-of-place（返回 base 副本沿 dim 维度 index 位置用 src 覆盖）；可微 `TapeOp::Scatter`，反向 d_base=grad 恒等、d_src=grad[index] 收集，index 不可微（整数索引 bool 语义）；VM 与解释器双侧 native 注册（与 select 同级）；当前限制：dim=0、index/src 为 1D。**autodiff 算子集扩展**：23→25（新增 BatchedMatMul / Scatter）。**T7 定理 TapeOp 四类分类更新**：Construct 7（不变）/ Preserve 12→13（新增 Scatter，输出 shape=base.shape 保持输入）/ Reduce 2（不变）/ Expand 2→3（新增 BatchedMatMul，与 MatMul 同类产生新 shape）；C2b 判据不变（非 Preserve 满足 C2）。**True MultiheadAttention 阻塞解除**：AUDIT-11.1.4 披露的 `std/nn/multihead_attention.th` 实为 single-head 等价包装（根因：仅 2D matmul）现已解除阻塞条件——bmm 已可用，待标准库部基于 bmm 重写为真正分头计算版本。**文档同步**（本条目为文档部执行）：`docs/语言参考手册.md` 头部 23→25 算子、§11.5 算子列表加 BatchedMatMul/Scatter/Abs（顺带修补 Abs 遗漏）、§11.5 内置函数表加 scatter、新增 §11.7 bmm 原语章节（方法签名/shape 规则/语义/可微性/示例/实现细节/已知限制）、新增 §11.8 scatter 原语章节（同结构）、原 §11.7 FormalExplain 重编号为 §11.9、§11.9 中 T7 分类表更新为 25 算子（Preserve 13/Expand 3）、目录与交叉引用同步（§11.7→§11.9 共 2 处）、classify_tape_op 描述 23→25；`能力梳理/能力全梳理.md` Batched Matmul ❌→✅、Scatter/Gather ❌→⚠️（Gather 仍未实现）、MultiheadAttention ✅→⚠️（True MHA 待重写）、关系调试器与"25 个算子 backward"条目算子数更新、§九 总结矩阵 AI 特定需求 ⚠️ 12→14/❌ 97→95、合计 ⚠️ 33→35/❌ 368→366、§十 关键洞察 21→25 算子、日期 2026-07-03→2026-07-06；`AUDIT.md` AUDIT-11.1.4 状态补充"bmm 阻塞已解除，待标准库部重写"、头部日期 2026-07-02→2026-07-06。**自举三路径未破坏**：仅改 Rust 运行时 + 文档，未触及 tenthc/ 与 .th 源码。**禁止事项无违反**：仅编辑 4 份真理源文档 + 语言参考手册，未创建新文件，未修改 .rs/.th 代码。**遗留**：True MHA 标准库重写属标准库部范畴，待后续推进；scatter 的 dim>0 与多维 index/scatter 支持待运行时部后续扩展；gather 原语仍未实现。
>
> **2026-07-03 feat: 护城河 F（关系调试器）MVP——T2 FormalExplain 动态层**：实施四支柱 {B,D,A,F} 中唯一未落地的 F，闭环完成。基于数理部 T2 §4.3 FormalExplain 算法（Tape DAG 反向 BFS，复杂度 O(|V|+|E|)，定理 F1-F5）实现 C1 可达 + C2 相关两级根因分类，跳过 C3 counterfactual（T8-4 开放问题）与 HIR 静态层（Phase 2）。**输出三级分类**：ExplainsError（C1∧C2，根因）/ PartialExplain（C1∧¬C2，关联但不充分）/ Unrelated（¬C1，不可达，过滤）。**T7 定理 TapeOp 四类互斥完备分类**（23 算子）：Construct 7（Input/CrossEntropy/Conv2D/BatchNorm/LayerNorm/Dropout/Select，引入新 shape 或来自外部输入）/ Preserve 12（Add/Sub/Mul/Div/Neg/ReLU/Exp/Log/Sigmoid/Softmax/Gelu/Abs，保持输入 shape）/ Reduce 2（Sum/Mean，降维）/ Expand 2（MatMul/Transpose，产生新 shape）。**C2b 判据**：Construct/Reduce/Expand（非 Preserve）→ 满足 C2，进入 ExplainsError。**触发场景**：shape mismatch（MatMul/Add/Conv2D）+ runtime tensor error，backward 失败时自动调用 `formal_explain` 包装错误。**6 步实施**：(1) `error.rs` 新增 `TapeErrorContext` 结构 + `ShapeMismatch` 变体携带 tape_node_id/op/expected_shape/actual_shape；(2) `runtime/relation_debugger.rs`（新建）实现 `ExplainClass`/`TapeOpClass`/`RootCause` + `classify_tape_op`（23 算子映射）+ `Tape::formal_explain`/`bfs_reverse_reachable`/`classify_node`/`render_explanation`/`root_cause_hint`；(3) `autodiff.rs` Tape 加 `node()`/`nodes()` 只读访问器供调试器读取；(4) `main.rs` 与 `interpreter/natives.rs` 双重注册 `explain_error` native（返回 `Vec<String>`），`backward` native 失败时包装 `formal_explain` 根因分析；(5) `hir/lower/lower_expr.rs` 与 `hir/lower/closures.rs` 两处白名单加入 `explain_error`（解决 HIR Var 解析与闭包自由变量识别）；(6) `tests/relation_debugger_test.rs`（新建）9 个集成测试。**关键设计取舍**：(a) `Tensor::from_data` 私有，测试改用 `Tensor::from_tensor_data(TensorData::F64(...))`；(b) Tenth 标量无 `to_string` 方法，native 测试改用 `to_string(n)` native 函数；(c) `explain_error` 需穿透三层 HIR 白名单（lower_expr/closures/interpreter mod），与 T37 第二批修复发现一致。**验证**：`relation_debugger_test` 9/9；`autodiff_test` 52/52；`select_test` 16/16；`parity_test` 129/129；`autodiff_shape_test` 10/10；`vm_autodiff_test` 15/15；`stdlib_test` 114/114，0 回归；自举 `run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm`。**改动的 11 个源码文件**：error.rs、runtime/relation_debugger.rs（新建）、runtime/autodiff.rs、runtime/mod.rs、runtime/vm.rs、runtime/interpreter/mod.rs、runtime/interpreter/natives.rs、main.rs、hir/lower/closures.rs、hir/lower/lower_expr.rs。新增 1 个测试文件 `tenth/tests/relation_debugger_test.rs`。**文档同步**：`能力梳理/能力全梳理.md` 新增护城河 F 条目、调试器 ❌→⚠️ 部分实现；`docs/语言参考手册.md` 新增 `explain_error()` API 与 §11.7 FormalExplain 章节。**自举三路径未破坏**：仅改 Rust 运行时，未触及 tenthc/ 与 .th 源码。**禁止事项无违反**：在 tenth/ 下编辑现有文件 + 新建 1 个测试文件，未破坏已有测试。**遗留**：C3 counterfactual（T8-4 开放问题）、HIR 静态层（Phase 2）未实施，后续可独立推进。**里程碑**：四支柱 {B,D,A,F} 闭环完成——B（编译期 shape 检查 + tenthc 双侧同步）+ D（编译期内存/算力预估）+ A（运行时 autodiff shape 校验）+ F（运行时关系调试器 FormalExplain）。
>
> **2026-07-02 fix: abs 接入 autodiff（第五批遗留项）**：落实第五批 select 原语引入时遗留的 `abs` 未接入 autodiff 限制——huber_loss 线性区此前用 `a` 替代 `abs_a` workaround。**改动**：(1) `runtime/autodiff.rs` TapeOp 新增 `Abs` 变体 + backward 分支（d|x|/dx = sign(x)，x=0 处取 0 次梯度中点，工程惯例）+ debug 名字映射；(2) `runtime/interpreter/methods.rs:840` abs 方法加 `if self.recording { self.record_unary(TapeOp::Abs, t, &result); }`；(3) `runtime/vm.rs:1317` VM 侧 abs 同步加 record_unary。**autodiff 算子集扩展**：22→23。**测试** `tests/abs_test.rs`（新建）8 个测试：前向（正/负/零）、反向正数（grad=1）、反向负数（grad=-1）、反向零（grad=0）、混合正负零、嵌套 abs(abs(x))、l1_loss 反向、huber_loss_train 反向（内联展开，验证 abs 梯度链完整）。**验证**：`abs_test` 8/8；`autodiff_test` 52/52；`select_test` 16/16；`stdlib_test` 114/114；`vm_autodiff_test` 15/15；`parity_test` 129/129，0 回归。**改动的 3 个源码文件**：autodiff.rs、methods.rs、vm.rs。新增 1 个测试文件 `tenth/tests/abs_test.rs`。**文档同步**：`能力梳理/能力全梳理.md` 22→23 个算子 backward；`docs/语言参考手册.md` 22→23 算子。**自举三路径未破坏**（仅改 Rust 运行时）。**遗留清除**：第五批 huber_loss 线性区 workaround 已被 abs 接入消除，huber_loss_train 现可用 `abs_a` 正确表达。
>
> **2026-07-02 fix: tenthc shape 检查双侧同步（论文 T12 第四批修复，AUDIT-11.2.1）**：落实 AUDIT §11.2 登记的最严重 T12 双侧破口——tenthc 自举编译器完全无 shape 检查。由数理部先出具语义对齐方案（数据结构/检查函数/调用点/简化策略/自举风险评估），编译器部据此实施。**数据结构扩展**：`tenthc/hir/hir.th` HirType 新增 3 个 i64 字段 `dim_count/dim0/dim1`（0=未知跳过检查，1=1D，2=2D），覆盖 matmul 与广播检查的充分子集；`add_type` 用 `..` 默认零初始化，wasm.th 不读新字段，零影响。**shape 检查函数**（`tenthc/hir/lower.th`）：`add_tensor_type`（构造 disc=1 Tensor HirType）、`get_tensor_dim_count/dim0/dim1`（3 个单值 getter，**避免 tuple 返回**——WASM 后端对 tuple 返回支持有限，首轮 parity 全挂后重构为 3 函数）、`check_matmul_shape`（2D 内侧 K 不等报错）、`check_binary_broadcast`（NumPy 从右对齐，1D/2D Known 维度检查）、`dims_count_from_args/dims_d0_from_args/dims_d1_from_args`（从字面量参数提取 dims）。**调用点插入 3 处**：(1) `kind=="call"`——检测 `zeros/ones/rand/randn` 字面量参数构造 Tensor 返回类型；(2) `kind=="binary"`——两侧均 Tensor 时检查 `+-*/%` 广播兼容性；(3) `kind=="method_call"`——`matmul` 检查内侧 K + 结果 shape (M,N) 推断，`transpose` 2D 维度互换。**最小子集取舍**：跳过 let 注解 shape 合并、分支 shape 兼容、函数返回值 shape 合并（需 `Tensor[f64,3,4]` 注解解析，tenthc 不支持）、Symbol 维度 unify（Phase 2 未完成）；错误处理非致命（push 到 `p.errors`），与 tenthc 现有 borrow check 模式一致。**bridge.rs 确认无需修改**：bridge.rs 将 tenthc **parser** 的 Program（非 HirProgram）转为 Rust AST，用字符串 `parse_type_annotation`，完全不触碰 HirType。**自举风险评估**：tenthc 自身源码（tenthc/*.th）不含 `zeros/randn/matmul` 调用，shape 检查永不触发；路径 A/B/C 均安全。**首轮 parity 全挂教训**：首轮实现用了 tuple 返回 `(i64,i64,i64)`，Rust 编译 + 自举 + selfhost_frontend 全通过，但 parity_test 129 全挂（WASM 后端 `type mismatch: expected i64 but nothing on stack`）——tenthc WASM 后端不支持函数返回 tuple。重构为 3 个单值 getter 后全绿。**验证**：`cargo build --release` exit 0（176 个预存 JIT 警告非本次引入）；自举 `run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm`；`selfhost_frontend` 4/4；`shape_check_compile_test` 66 passed + 7 ignored；`parity_test` 129/129。**改动的 2 个文件**：(1) `tenthc/hir/hir.th`——HirType 加 dim_count/dim0/dim1；(2) `tenthc/hir/lower.th`——add_type 用 `..` 默认、新增 8 个 shape 辅助函数、3 处调用点插入检查。**文档同步**：`AUDIT.md` AUDIT-11.2.1 ⚠️→✅；`能力梳理/能力全梳理.md` 新增 tenthc shape 检查条目。**自举三路径未破坏**。**禁止事项无违反**：仅编辑 tenthc/ 下 2 个现有 .th 文件，未创建新文件，未破坏已有测试。
>
> **2026-07-02 fix: 补齐 tenthc 自举编译器双侧差异（论文 T12 第三批修复）**：落实 AUDIT §11.2 登记的 3 条 T12 双侧破口（AUDIT-11.2.2 / AUDIT-11.2.3 / AUDIT-11.2.4），属 tenthc 自举编译器前端改动，需自举同步。AUDIT-11.2.1（tenthc 缺 shape 检查）不在本批范围，状态保持 ⚠️ 未修复。**3 项子任务，按风险递增顺序执行，每项完成后立即跑自举验证**。**子任务 1：tenthc i64 溢出检测补齐**（`tenthc/lexer/lexer.th`）—— `parse_int` 函数与 `lexer_next` 数字解析路径加入溢出检测，用数学等价条件 `ival > (i64_max - d) / 10` 检测 `ival * 10 + d > i64_max`（避免直接乘加溢出），溢出时打印 `[lexer] integer overflow detected` 并返回 `i64_max` 饱和值，跳过剩余数字避免后续字符误解析。与 Rust 侧 `tenth/src/lexer/lexer.rs:97-202` 的 `str::parse` 自动溢出检测对齐。**子任务 2：tenthc expect_gt 补齐**（`tenthc/parser/parser.th`）—— 新增 `expect_gt(p: &mut Parser) -> bool` 函数（行 55-89），遇 `Gt`(disc=34) 直接 consume；遇 `Shr`(disc=63，即 `>>`) consume 后通过重建 tokens 数组插入合成 `Gt` token（Tenth Vec 无 insert 方法，用 push 重建实现 insert 语义），与 Rust 侧 `parser.rs:54-86` expect_gt 对齐。替换 2 处调用点：`parse_postfix` generic call 闭合（行 560）、`parse_generic_params` 闭合（行 1154-1156）。**子任务 3：tenthc 错误恢复补齐**（`tenthc/parser/parser.th`）—— `Parser` 结构新增 `errors: Vec<str>` 字段，`parser_new` 初始化 `errors: Vec::new()`。新增 4 个函数（行 91-130）：`is_sync_token(disc)` 判断是否为同步 token（Fn=4/Struct=16/Enum=17/Trait=19/Impl=18/Mod=21/Use=20/RBrace=50/Eof=61，与 Rust 侧 `SYNC_TOKENS` 对齐）、`synchronize(p)` 循环 consume 直到到达 SYNC_TOKEN 或 Eof、`record_error_and_recover(p, msg)` 记录错误并调用 synchronize、`is_expr_start_token(disc)` 判断是否为合法表达式起始 token（供 parse_program 区分表达式语句与错误 token）。`parse_program` 主循环 else 分支（行 1565-1577）应用错误恢复：对无法识别的顶层 token 调用 `record_error_and_recover` 进入 panic-mode 恢复而非直接解析。**设计取舍**：(1) Tenth Vec 无 insert 方法，通过重建 tokens 数组实现；(2) tenthc parser 无错误返回机制（返回索引而非 Result），用 `p.errors: Vec<str>` 字段收集错误；(3) 错误消息简化为不依赖 `int_to_str`（因 lexer.th 在 parser.th 之前加载，`int_to_str` 在 lexer 中未定义）。**验证**：每项子任务后跑自举验证 `cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` → `[OK] Full compiler compiled to tenthc_full.wasm`；`selfhost_frontend` 4/4 通过；子任务 3 后 `error_recovery_test` 7/7 通过。完整验证：`cargo build --release` exit 0；自举通过；`selfhost_frontend` 4/4；`error_recovery_test` 7/7；`parity_test` 通过。**改动的 2 个文件**：(1) `tenthc/lexer/lexer.th`——parse_int 与 lexer_next 数字解析加入溢出检测；(2) `tenthc/parser/parser.th`——Parser 结构加 errors 字段、新增 expect_gt 与 4 个错误恢复函数、parse_postfix/parse_generic_params 闭合替换为 expect_gt、parse_program 主循环应用错误恢复。**文档同步**：`AUDIT.md` AUDIT-11.2.2 / AUDIT-11.2.3 / AUDIT-11.2.4 状态 ⚠️→✅ 已修复（AUDIT-11.2.1 shape 检查保持 ⚠️ 未修复）；`能力梳理/能力全梳理.md` tenthc 错误恢复 ❌→✅、tenthc expect_gt ❌→✅。**自举三路径未破坏**：路径 A（Rust 全栈）通过；路径 B（Tenth 前端 + Rust 后端）通过（自举验证成功）；路径 C（全 WASM 闭环）通过（tenthc_full.wasm 编译成功）。**禁止事项无违反**：仅编辑 tenthc/ 下 2 个现有 .th 文件，未创建新文件，未破坏已有测试。**遗留**：AUDIT-11.2.1（tenthc 缺 shape 检查）未在本批修复，保持 ⚠️ 状态，后续可单独处理。
>
> **2026-07-02 fix: 补齐 VM 与解释器 native 注册差异（论文 T37 第二批修复）**：落实 AUDIT §11.3 登记的两条 T37 缺陷（AUDIT-11.3.1 / AUDIT-11.3.2），属运行时单侧改动，无自举影响。**VM 侧补齐 17 项**（`main.rs::register_natives()` 末尾追加，行 1148 之后）：`to_string`、`type_name`、`with_step_limit`、`with_timeout_ms`、`is_timeout`、`start_grad`、`f64_bits`、`f64_from_bits`、`sin`、`cos`、`ln`、`pow`、`save_weights`、`load_weights`、`format`、`parse_int`、`parse_float`。其中 `save_weights`/`load_weights` 为**致命项**——VM 路径下模型保存/加载此前完全失效，现已复用 `FsSandbox::check_read/check_write` 校验路径，二进制格式（i32 num_tensors, [i32 ndim, i32×ndim shape, f64×nel data] LE）与解释器侧完全一致；`with_step_limit`/`with_timeout_ms` 通过 `Value::FnRef` + `call_with_args` 调用闭包，复用 VM 已有的 `step_budget`/`deadline_ms` 双重时间预算机制。**解释器侧补齐 3 项**（`interpreter/natives.rs::call_named_fn`）：`print`（行 48-54，在 `println` 之后追加，输出不换行）、`to_f64`/`to_f32`（行 237-268，支持 Int/Float/Float32 三种输入）。**关键发现：三层 native 白名单需同步**——Tenth 的 native 注册分散在三处隐藏耦合层：(1) `hir/lower/lower_expr.rs:55` HIR lower 白名单（决定 `print` 能否被 HIR 识别）；(2) `runtime/interpreter/mod.rs:421-430` 解释器 Var 解析 fallback 白名单（决定 `f64_bits`/`f64_from_bits`/`print` 能否在解释器 Var 路径解析）；(3) `hir/lower/closures.rs` 闭包自由变量白名单。本轮修复在前两处补入 `print`/`f64_bits`/`f64_from_bits`（closures.rs 白名单已含 `save_weights`/`load_weights`/`sin`/`cos`/`ln`/`pow`/`start_grad`，无需改动）。**测试** `tests/native_parity_test.rs`（新建）覆盖 20 项 native 的 VM/解释器 parity，共 35 个 `#[test]`：to_string（int/float）、type_name（int/float/bool/string）、format（basic/string_arg/escape）、parse_int（valid/negative/invalid）、parse_float（valid/invalid）、sin/cos/ln/ln_value/pow/pow_fraction、f64_bits/roundtrip/from_bits、to_f64（from_int/from_float）、to_f32（from_int/from_float）、print、start_grad、with_step_limit、with_timeout_ms、is_timeout（int/unit）、save_load_weights（zero/nonzero，用 `t.numel()` 验证形状避免 VM 不支持 Tensor 二维索引的限制）。**VM 已知限制发现**：VM 的 `index_get` 仅支持 Vec/String 一维索引，不支持 Tensor 二维索引（`t[0][0]`），save_load 测试改用 `t.numel()` 验证。**验证**：`native_parity_test` 35/35 通过；`stdlib_test` 114/114 通过（0 回归）；`vm_autodiff_test` 15/15 通过（0 回归）。**预存失败（非本次引入，stash 验证）**：`memory_test.rs`（`MemoryConfig`/`LiveCounter`/`check_arena_alloc` 等历史 API 不存在）、`three_stage.rs`、`src/runtime/vm.rs:1577/1589/1663-1668`（layer_norm 代码 `&TensorData` vs `f64` 类型不匹配，lib test 目标编译错误）。**改动的 4 个文件**：(1) `tenth/src/main.rs`——register_natives 末尾追加 17 项 VM native；(2) `tenth/src/runtime/interpreter/natives.rs`——追加 print/to_f64/to_f32 三个分支；(3) `tenth/src/hir/lower/lower_expr.rs:55`——白名单 `println` 改为 `println | print`；(4) `tenth/src/runtime/interpreter/mod.rs:421-430`——白名单加入 `print`/`f64_bits`/`f64_from_bits`。新增 1 个测试文件 `tenth/tests/native_parity_test.rs`。**文档同步**：`AUDIT.md` AUDIT-11.3.1 / AUDIT-11.3.2 状态 ⚠️→✅ 已修复；`能力梳理/能力全梳理.md` 双重 native 注册一致性 ⚠️→✅ 已对齐。**自举三路径未破坏**：本批仅改 Rust 运行时，未触及 tenthc/ 与 .th 源码。**禁止事项无违反**。
>
> **2026-07-02 docs: 论文披露中等价值问题第 1 批文档登记**：落实形式化分析论文与相关评审披露的、尚未在真理源文档登记的中等价值问题，本批为纯文档改动，**不修改任何代码**——属于"诚实披露 → 缺陷登记"环节，修复方案分批推进。**论文来源范围**：T12 / T18 / T19 / T20 / T22 / T31 / T36 / T37 / T42 / T50（共 10 个论文条目）。**改动的 3 份文件**：(1) `能力梳理/能力全梳理.md` 补建 5 项能力条目——`§1.5 内存与所有权` 新增"跨语句借用健全性 (B6)" ⚠️（T19 定理 B6 披露 unsoundness）、`§2.1 编译管线` 新增"tenthc 错误恢复 (panic mode)" ❌ 与"tenthc expect_gt / 嵌套泛型 `>>`" ❌（T12 双侧破口）、`§3 运行时系统` 新增"双重 native 注册一致性 (VM vs 解释器)" ⚠️（T37 反模式，VM 缺 17 项含 save_weights/load_weights 致命项）、`§5.1 张量系统` 新增"Batched Matmul / 3D 张量矩阵乘" ❌（T50 披露 True MHA 受此限制不可表达，`std/nn/multihead_attention.th` 实为 single-head 等价包装）；日期 2026-07-01→2026-07-02；§九 总结矩阵从 566→571 项、⚠️ 从 30→32 项、❌ 从 369→372 项、完成率 ~30%→~29%。(2) `AUDIT.md` 新增 §11"论文披露的中等价值问题登记"章节，按 4 个子节组织 14 条登记条目（11.1 形式化健全性缺陷 ×5 含 T19 B6 跨语句借用 / T20 PB2 四类反例 / T42 LayerNorm per-feature γ 与 BatchNorm 多 channel dX / T50 MHA single-head / T18 body clone 健全性破口；11.2 自举双侧破口 ×4 含 T12 缺 shape 检查 / 缺 panic-mode / 缺 expect_gt / 缺 i64 溢出检测；11.3 双重 native 反模式 ×2 含 T37 VM 缺 17 项 / 解释器缺 3 项；11.4 工程债务与潜在 UB ×3 含 T22 FV5 O(n²) / T31 MAX_STACK_DEPTH=256 静默溢出 / T36 双重存储同步开销）+ 11.5 登记元数据小节；每条含编号（AUDIT-11.x.y）、标题、论文来源、影响（含源码行号引用）、当前状态（全部 ⚠️ 已登记未修复）；顶部日期 2026-07-01→2026-07-02，副标题追加"论文披露缺陷登记"。(3) `MEMO.md` 本条记录。**核查证据**：`lower_stmt.rs:50/57/73` 与 `lower_expr.rs:701/717` 调用 `scope.release_borrows`，`scope.rs:67/81/113` 单点检查实现（B6 unsoundness）；tenthc 全目录 grep `expect_gt|panic_mode|error_recovery` 0 匹配（T12 双侧破口）；`std/nn/multihead_attention.th:1-12` 头部注释自承 single-head 等价（T50）；`compile/jit/translator.rs:32` `MAX_STACK_DEPTH: u32 = 256`（T31）；`hir/lower/closures.rs:9-15` `free_vars_in` 用 Vec+sort+dedup 非 HashSet（T22）；`runtime/interpreter/mod.rs:40` `scopes: Vec<HashMap<String, Value>>` 双重存储（T36）。**禁止事项无违反**：未修改任何 .th / .rs 源码、未破坏自举三路径、未创建新文件。**后续**：修复方案分批推进，下一批可优先处理 T37 VM 缺 17 项 native（含 save_weights/load_weights 致命项）与 T19 B6 借用健全性。
>
> **2026-07-01 docs: 形式化分析理论可行性论证论文 v3（审查修订版）**：对 v2 逐章审查后用 Edit 修正 7 处问题，新增 §6.8 局限。**3 处实质性错误**：(1) 定义 2.1 边定义用 shape 相等判断"同一张量"——两个不同张量可有相同 shape，改为用 Tid（tape_id）；(2) 算法 §3.4 步骤 1 用 $s^{out}_v = s_{act}$ 定位报错节点——约束违反时算子可能未执行成功无输出 shape，改为由运行时报错上下文提供 $v_{err}$；(3) 定理 B1 归约的静态/运行时混淆——v2 说"死循环则约束系统不可满足"，但约束系统是静态的不依赖运行时行为，改为基于 Rice 定理论证"精确确定返回 shape 等价于分析语义性质"。**2 处证明与陈述不一致**：(1) 定理 F1 复杂度陈述含乘积项 $O(\|s_{exp}\| \cdot \|s^{out}_v\|)$ 但证明无此乘积，修正为 $O(\|s\| \log \|s\|)$；(2) 定理 F2 陈述缺 $\alpha$ 项但证明引入了 $\alpha$（一元 Preserve 算子可通过 (C1) 进入候选集），补全陈述。**2 处归约深化**：(1) 定理 B2b 归约 v2 承认"需要更细致的归约"但未给出，v3 用 0-1 IP 通过松弛变量 $d_i + s_i = 1$ 编码 0/1 限制，归约严格成立；(2) 定理 B1 归约的本质——静态分析必须保守近似，新增 §6.8 记录此局限。**关键洞察**：B 的价值定位从"编译期精确求解"修正为"编译期预警"（§6.8），B 永远不能达到 F 的精确度（静态分析的本质局限）。**关联**：[形式化分析理论可行性论证.md](file:///d:/史蒂夫/Desktop/AI开发新语言：头脑风暴与评估/docs/shape-check-roadmap/形式化分析理论可行性论证.md) v3 版本，附录 A 含 v2/v3 双列修订标注，文档 ~862 行。
>
> **2026-07-01 docs: 形式化分析理论可行性论证论文 v2（细化版）**：在 v1 基础上细化推导过程、修正 4 处错误、新增 5 处理论局限诚实记录。**4 处错误修正**：(1) 定理 F2 候选集上界——v1 忽略 Add/Sub/Mul/Div 等二元 Preserve 算子也有 (C3) 内部约束，v2 修正上界含所有 $\mathcal{O}_{\text{constr}}$ 算子；(2) 定理 B2 复杂度——v1 声称线性约束多项式可解，v2 拆为 B2a（整数上 $O(n^2 k)$）与 B2b（非负整数上 NP 完全，归约自 INTEGER PROGRAMMING），超时保护从"可选"升级为"必需"；(3) 定理 B1 归约——v1 Rice 定理归约过简，v2 构造递归 Tenth 函数模拟图灵机归约到停机问题；(4) 定理 F4 完备性——v1 称"完备性边界"但"导致"未独立形式化是循环论证，v2 诚实重述为"分析框架自洽性"。**6 处推导补充**：定义 2.4 分类完备性论证（引理 2.1）、定义 3.3 (C2) 形式化（C2a/C2b/C2c）、定义 3.4 "自然期望"部分形式化、定理 F1 各分类判定步骤细化、定理 B3 单函数 HIR 结构论证、定理 FB1 独立性证明逐定义考察。**5 处理论局限诚实记录**：§6.4 "自然期望"不可完全形式化、§6.6 F4 完备性的循环性、§6.7 B2b NP 完全性影响、§6.1 跨函数边界、§6.3 JIT 路径。**对实施的关键影响**：B 的性能保证从"多项式时间"降级为"超时截断"；F4 是自洽性非完备性，实施时需诚实标注；候选集规模从"远小于 Tape"修正为"与 Tape 同阶但常数 <1"。**关联**：[形式化分析理论可行性论证.md](file:///d:/史蒂夫/Desktop/AI开发新语言：头脑风暴与评估/docs/shape-check-roadmap/形式化分析理论可行性论证.md) v2 版本，附录 A 含完整定理索引与 v2 修订标注。
>
> **2026-07-01 docs: 形式化分析理论可行性论证论文**：新建 `docs/shape-check-roadmap/形式化分析理论可行性论证.md`，作为护城河 F（张量关系调试器）与护城河 B（Shape 代数求解器）开发的理论参考。**核心论点**：(1) 在 Tape 上做形式化根因分析是可判定的，复杂度 $O(|V|+|E|)$，每个候选附带可解释的形式化证据（定理 F1-F5）；(2) 一般 HIR shape 约束求解不可判定（定理 B1，归约到 Rice 定理），但限定线性约束子类可判定（定理 B2），单函数内验证可判定（定理 B3）；(3) F 不依赖 B（定理 FB1），F 完成后改变 B 设计前提（定理 FB2）。**对 spike 报告的关键修正**：放弃启发式权重方案（不可解释、不可证伪），改为基于 shape 代数的四级形式化分类（DefinitelyRoot / ExplainsError / PartialExplain / Unrelated）。**对战略规划的修正**：原"F 依赖 B"判断修正为"F 不依赖 B，可独立先做"。**理论依据**：Rice 定理（B1）、线性方程组求解（B2）、Tape 完整性假设（2.1-2.3）。**实施建议**（附录 C）：F MVP 直接实现算法 §3.4 伪代码；B 严格遵守 §4.5 降级策略；实施顺序 F→F Phase2→B 可选→B 跨函数无递归子类。**关联**：本文为护城河 F 的 MVP 实施提供形式化依据，算法 §3.4 可直接转写为 Rust；为护城河 B 的降级策略（`--strict-shapes` 可选模式、不做跨函数传播、超时保护）提供理论支撑。
>
> **2026-07-01 fix: 修复 AUDIT #3 Match struct pattern binding 未生成**：实现 match 表达式的 struct 解构 pattern（如 `match p { Point { x, y } => ... }` 中的 x/y 绑定）。**跨模块全链路改动**：(1) `parser/ast.rs` + `hir/hir.rs` 新增 `Pattern::Struct { name, fields: Vec<(field_name, bind_name)> }` 变体；(2) `parser/parser.rs::parse_match_pattern` 在 Identifier 分支增加 `LBrace` 处理，支持 `Name { x, y }` 简写与 `Name { x: a, y: b }` 命名绑定两种语法；(3) `hir/lower/lower_expr.rs` `lower_pattern` 加 Struct 分支 + `bind_pattern_vars` 加 Struct 分支（查 `self.structs` 获取字段类型注入 scope）；(4) `runtime/interpreter/pattern.rs` `pattern_matches`（检查 `Value::Struct` 名字匹配）/`bind_pattern`（按字段名取值绑定）/`unbind_pattern` 三处加 Struct 分支；(5) `runtime/vm.rs` 新增 `IsStruct(usize)` Op（opcode 46，与 `IsEnumVariant` 对称）+ emit/read_op×2/执行分支；(6) `compile/bytecode.rs` 加 Struct 分支（IsStruct 检查 + LoadField 绑定字段，支持 guard）；(7) `compile/jit/translator.rs` 遇 IsStruct 返回 Err fallback 到 VM（VM 已支持，符合 JIT fallback 机制）。**tenthc 同步**：检查确认 tenthc 的 match 是简化版（parser.th 仅支持 wildcard/enum_variant 名/literal，不支持 struct/tuple/range/字段绑定），tenthc 源码不用 struct pattern，无需同步修改。**测试** `tests/pattern_match_test.rs` 新增 7 项（5 解释器路径 + 2 VM 路径）：简写解构、命名绑定、多 struct 类型 + wildcard fallback、部分绑定 `y: _`、VM 简写解构、VM 多类型 fallback，全绿。**验证**：自举路径 A 通过（`cargo run --release -- run tenthc/main.th` → Full compiler compiled to tenthc_full.wasm）；`cargo build --release` exit 0，无新 warning（已有 176 个 warning 均为预存 unsafe/dead-code，非本次引入）；全量测试除 3 个预存失败（fixpoint_runtime_benchmark / three_stage_selfhost / native_generic_ctor_f32/f64_lowering，均经 stash 验证非本次引入）外全绿，0 回归。**自举三路径**：路径 A（Rust 全栈）通过；路径 B（Tenth 前端 + Rust 后端）不涉及（tenthc 不用 struct pattern）；路径 C（全 WASM 闭环）预存失败未加剧。
>
> **2026-07-01 护城河 D 演示补齐 + 标准库实例补齐**：本轮完成"全量补齐"方案的收尾工作。**关键修复（VM native 注册缺口）**：`main.rs::register_natives()` 新增 6 个 VM native——`zeros`/`ones`/`rand`/`zeros_f32`/`ones_f32`/`rand_f32`，与 interpreter::natives 对齐支持任意 shape。历史问题：这些函数仅在 interpreter 实现，JIT/VM 路径下返回 Unit，导致护城河 D 演示（`zeros(256,256,256).numel()` 等）在默认 `tenth run` 路径下无法工作。补齐后护城河 D 演示完全跑通。**5 个新 tensor 方法**：`vm.rs::call_method_priv` 和 `interpreter/methods.rs` 同步添加 `numel()`/`nbytes()`/`ndim()`/`shape()`（返回 tensor 形状）/`clip_scalar(v)`（按标量值裁剪），VM + Interpreter 双路径对齐。**JIT translator bug 修复**：`translator.rs` MethodCall 分支 receiver 已包含在 args 中，修复重复传递导致的参数错位。**标准库新增**：`std/optim/clip.th`（`clip_grad_by_value` 可用 / `clip_grad_by_norm` JIT 下有 tensor-float 比较问题）、`std/optim/accumulate.th`（`accumulate_grad` 梯度累积概念函数）、`std/optim/adamw.th`（`adamw_step` 返回 tuple，JIT 下 tuple 解构有限制建议内联公式）；`std/nn/activations.th` 新增 `leaky_relu`/`leaky_relu_default`；`std/prelude.th` 索引同步。**3 个实例文件**：(1) `Tenth实例/标准库使用示例/stdlib_demo.th`——GELU/LeakyReLU/Xavier/He 初始化/AdamW 单步（内联公式避免 tuple 解构），验证通过；(2) `Tenth实例/梯度裁剪与累积/grad_clip_accum.th`——`clip_grad_by_value` 三组裁剪阈值演示 + 训练/累积伪代码，验证通过；(3) `Tenth实例/Transformer示例/transformer_demo.th`——Self-Attention（QKV 投影 + scaled dot-product + softmax）+ FFN（GELU 激活）+ 残差连接 + MSE loss，前向传播验证通过。**JIT 路径已知限制**（记录供后续参考）：多行函数调用语法不支持、tuple 索引 `result.0` 不支持、tuple 解构 `let (a,b) = func()` 可能返回 Unit、标量 tensor 链式方法调用可能返回 Unit、tensor 索引 `tensor[i][j]` 返回 Unit、训练循环参数更新后状态异常。**验证**：lib tests 10/10、stdlib_test 114/114、vm_autodiff_test 15/15、autodiff_test 52/52 全绿；预存失败（stash 验证确认与本次改动无关）：fixpoint_runtime_benchmark、native_generic_ctor_f32/f64_lowering、parity_test、selfhost_frontend（debug 栈溢出）、three_stage_selfhost。**文档更新**：能力全梳理 AdamW/梯度累积/梯度裁剪 从 ❌→⚠️、新增"张量元数据"行（numel/ndim/shape/nbytes/clip_scalar）。
>
> **2026-07-01 Autograd 反向 Shape 静态验证（护城河 A）**：实现 `docs/shape-check-roadmap/战略规划.md` 方向 A（评级 ⭐⭐⭐⭐⭐，"JAX 都没做好"）。**核心问题**：autodiff.rs 存在 5 处 silent squeeze 静默修正梯度 shape，掩盖真实 shape 错误，导致参数 grad 字段可能写入错误 shape 的数据（典型症状：模型 loss 不降反升、梯度数值异常但无报错）。**5 处 silent squeeze 全部消除**：(1) `tensor.rs::acc_grad` 加 shape 校验——梯度 shape 必须与参数 shape 一致，不匹配返回 `Err`，签名改 `-> Result<(), String>`；(2) `autodiff.rs::unbroadcast` 末尾不再 `unwrap_or(result)` 静默保留错误 shape，reshape 失败/元素数不匹配均报错，签名改 `-> Result<ArrayD<f64>, TenthError>`；(3) `autodiff.rs::matmul_2d` 非 2D 输入不再返回零数组，改 `map_err` 报错，签名改 `-> Result<ArrayD<f64>, TenthError>`；(4) `autodiff.rs::MatMul` backward 1D squeeze 前校验 `d_a_2d.shape[0]==1` / `d_b_2d.shape[1]==1`，`grad.ndim() > 2` 直接报错而非静默 clone；(5) `autodiff.rs::Conv2D` backward dW/dX reshape 失败与元素数不匹配均报错，移除 3 处 `unwrap_or_else(|_| ...)` 零填充兜底。**全链路 Result 传播**：`backward` 签名改 `-> Result<(), TenthError>`，`propagate_grad` 改 `-> Result<(), TenthError>`，所有 Add/Sub/Mul/Div/MatMul/Transpose/Sum/Mean/Exp/Log/Sigmoid/Softmax/CrossEntropy/Dropout/Conv2D/BatchNorm/LayerNorm/Gelu 分支的 `unbroadcast(...)` 与 `propagate_grad(...)` 加 `?` 传播；7 个 backward 调用点全部更新（natives.rs、main.rs、5 处 autodiff.rs 测试）。**新增 op_name 辅助函数**：返回 TapeOp 的人类可读名称（用于错误信息定位节点类型）。**编译期 param() warning**（`lower_expr.rs` Call 分支）：对 `param(t)` 调用，若 `t.static_bytes() ≥ 1GB` 发 warning 提示"反向传播将分配同等大小的梯度，可能触发 OOM"——让用户在编译期就意识到梯度内存开销。**测试** `tests/autodiff_shape_test.rs` 10 项：正确 shape 无回归 3 项（simple/matmul/broadcast gradient）、silent squeeze 边界 1 项（1D tensor 反向传播通过）、单元测试 smoke 1 项、编译期 param() warning 3 项（large ≥1GB 触发/small 不触发/消息格式）、边界 2 项（非 param 调用不警告/多 param 各自警告）。验证：autodiff_shape_test 10/10、autodiff_test 52/52、vm_autodiff_test 15/15、shape_check_compile_test 66 passed + 7 ignored、memory_estimate_test 32 passed + 3 ignored、autodiff lib tests 5/5、stdlib_test 114/114、selfhost_frontend --release 4/4 全绿；自举路径 A 通过（tenthc/main.th → Full compiler compiled to tenthc_full.wasm）。**设计取舍**：选"运行时 shape 校验 + 编译期 warning"方案而非"全静态推导"，因为 autodiff 反向 shape 依赖运行时数据流（如 unbroadcast 结果 shape 取决于前向输入 shape），静态推导需完整抽象解释器成本过高；运行时校验在 backward 出口（acc_grad）和 5 处 silent squeeze 位置加零成本检查，编译期 warning 用已有 shape 信息做 best-effort 提示。
>
> **2026-07-01 编译期内存/算力预估（护城河 D）**：实现 `docs/shape-check-roadmap/战略规划.md` 方向 D（评级 ⭐⭐⭐⭐⭐），用编译期 shape 信息换开发者真实的时间，AI 原生卖点（无竞品）。**warning 基础设施**：`error.rs` 新增 `TenthWarning` 结构体（line/col/message + `display_with_source`，复用 TenthError 的源码定位逻辑），非致命，独立于 `TenthError`。**静态预估数据基础**：`hir/types.rs` 为 `Type` 新增 `static_numel() -> Option<u64>`（所有维 Known 时返回乘积，任一 Symbol/Any/溢出/负数返回 None，用 `checked_mul` 防溢出）和 `static_bytes() -> Option<u64>`（numel × dtype 字节数，F64/I64/U64=8、F32/I32/U32=4、F16/I16/U16/BF16=2、I8/U8/Bool/Char=1）。**warning 收集管线**：`Lowerer` 新增 `warnings: Vec<TenthWarning>` 字段；`HirProgram` 携带 `warnings` 传递到 main.rs；`lower_program` 末尾 `std::mem::take` 转移；`main.rs::run_file` 在 source_to_hir 后 `eprintln!` 输出所有 warnings。**预估插入点**（`hir/lower/types.rs` 末尾两个方法）：`emit_memory_estimate(ty, span, context)`——bytes ≥ 1GB 时发 warning（如"函数调用 创建约 2.00 GB 的 tensor（编译期预估，可能触发 OOM）"）；`emit_matmul_flop_estimate(recv_ty, arg_ty, span)`——2D (M,K)@(K,N) 且 K 匹配时算 M*K*N 乘加，≥ 1 GFLOP 时发 warning。**4 个调用点**（`lower_expr.rs`）：Call 分支（函数调用返回大 tensor）、GenericCall native 构造函数路径（`randn<T>`/`zeros<T>` 等）、GenericCall 泛型函数实例化路径、MethodCall 分支（matmul FLOPs + 方法结果 bytes）。**测试** `tests/memory_estimate_test.rs` 35 项（32 passed + 3 ignored）：static_numel/static_bytes 单元测试 11 项（all_known/single_dim/symbol/any/non_tensor/overflow/negative/f64_2d/f32_2d/f64_3d_large/i8）、内存预估 warning 7 项（large zeros/randn/ones/randn 构造触发、small/medium 不触发、tensor literal）、matmul FLOPs 预估 7 项（large/huge/with_transpose 触发、small/medium 不触发、组合 both）、边界 4 项（dynamic/symbol/non_tensor/line_col）、消息格式 2 项、泛型构造函数 2 项、3 项 `#[ignore]` 高开销占位（actual 8GB allocation、huge matmul execution、deep large tensor chain）。**阈值**：内存 1GB（`1024*1024*1024` bytes）、FLOPs 1 GFLOP（`1_000_000_000` 乘加）。**默认开启**，不新增 CLI 标志（"检查多做、求解少做"原则，O(n) 检查零编译成本）。验证：memory_estimate_test 32 passed + 3 ignored；shape_check_compile_test 66 passed + 7 ignored、autodiff 52/52、stdlib 114/114、shape_check_test 16/16、vm_autodiff 15/15、selfhost_frontend --release 4/4 全绿；自举路径 A 通过（tenthc/main.th → Full compiler compiled to tenthc_full.wasm）。**已知限制**：仅静态 shape 全 Known 时预估，动态维度（Symbol/Any）静默跳过；matmul FLOPs 仅 2D，batched matmul 未覆盖；阈值为硬编码常量，未来可按硬件配置调整。
>
> **2026-07-01 编译期 Shape 检查短期规划（四项方向）**：完成 `docs/shape-check-roadmap/短期规划.md` 全部四项。**方向 1（类型注解强制化）**：`hir/lower/types.rs` 新增 `check_and_merge_tensor_shape(annot, actual, span, context)`——let 语句同时存在 type_ann 和 init 时强制检查 shape 兼容性并合并；wildcard `[..]` 合并 actual dims、actual 单 Any 保留 annotation、维度数不等报错、逐维 Known/Symbol 不等报错；`lower_stmt.rs` `StmtKind::Let` 分支调用。**方向 4（跨分支一致性）**：`types.rs` 新增 `check_branch_shape_compat(then_ty, else_ty, span, context)`——if/else 与 match arms 两侧 shape 都含静态信息（Known/Symbol）时必须可广播，否则报 TypeError；`lower_expr.rs` If 分支与 Match 分支调用。**方向 3（标准库符号维度标注）**：`std/nn/linear.th`、`std/nn/attention.th`、`std/nn/feedforward.th` 三个文件改用符号维度标注（如 `Tensor[f64, M, K]`、`Tensor[T, S_q, D_k]`），让标准库 API 自文档化。**方向 2（跨函数 shape 求解）**：`types.rs` 新增 `merge_return_shape(scope_ret, fn_def_ret)`——`resolve_call_type` 调用函数时若 self.functions 中有更精确的 return_type（body lower 后合并的）用它；`lower_stmt.rs` 函数定义 body lower 后调用 `check_and_merge_tensor_shape(&ret_ty, &lowered_body.ty, "函数返回值")?`（原 unwrap_or 吞错改为 `?` 传播，让 body 与注解 shape 静态冲突时报错）。**测试** `tests/shape_check_compile_test.rs` 33→73 项（+40 项）：方向 1 ×9、方向 4 ×9、方向 3 ×7（含 attention/feedforward 去 `<T>` 泛型以便注册到 scope）、方向 2 ×10、组合 ×3；7 项 `#[ignore]` 占位（3 项高开销：大 tensor shape 推断/深层跨函数链/大量 match arms；4 项等待战略方向 B 参数 shape unification：linear/attention/feedforward 错误参数 shape 检测）。验证：shape_check_compile_test 66 passed + 7 ignored；autodiff 52/52、stdlib 114/114、shape_check_test 16/16、vm_autodiff 15/15、selfhost_frontend --release 4/4 全绿；自举路径 A 通过（tenthc/main.th → Full compiler compiled to tenthc_full.wasm）；cargo build --release exit 0。**已知限制**：(1) 跨函数 shape 求解只做返回值传播，参数 shape unification 未实现（属战略方向 B 范畴）；(2) selfhost_frontend debug build 栈溢出（tenthc_lowers_own_source），release build 通过，属预存 debug 栈空间问题；(3) generic_test/memory_test 预存编译错误（LiveCounter/RuntimeLimits/MemoryConfig 未找到，与本次改动无关）。
>
> **2026-07-01 编译期 Shape 检查 Phase 3（算子覆盖扩展）**：补齐运行时已有但 shape 未推断的算子。**新增 shape 推断**：`permute(dims...)` 按字面量索引重排原 dims（如 `[3,8,5].permute(2,0,1)`→`[5,3,8]`，新增 `literal_int_args` 辅助函数提取字面量整数列表）；`broadcast_to(shape...)` 字面量参数即目标 shape；`cat(other, dim=0)` 沿 dim 拼接、dim 维相加（如 `[2,3].cat([3,3],0)`→`[5,3]`）；`argmax`/`argmin` 返回 i64 标量；`gelu`/`masked_fill` 保持原 shape。**新增测试** `tests/shape_check_compile_test.rs` 新增 8 项（共 25→33 项）：permute 重排、broadcast_to 推断、cat dim=0/dim=1 相加、argmax 标量、gelu/masked_fill 保持、flatten 1D。验证：`cargo build --release` exit 0；自举路径 A 通过（tenthc/main.th → Full compiler compiled）；shape_check_compile_test 33/33、autodiff 52/52、stdlib 114/114、f32_autodiff 12/12、f32_runtime 7/7、generic_tensor 4/4、vm_autodiff 15/15 全绿，0 回归。
>
> **2026-07-01 编译期 Shape 检查 Phase 2**：在 Phase 1 基础上扩展符号维度传播与更多算子 shape 规则。**符号维度同名等价检查**：`check_method_shape` 的 matmul 分支新增 `Symbol vs Symbol` 比较——同名 Symbol（如 `Tensor[f64, M, K] @ Tensor[f64, K, N]` 的 K）视为兼容，不同名（如 K ≠ P）报 `TenthError::TypeError`，错误信息含内侧维度对比。**归约算子 axis 降维**：`sum`/`mean`/`max`/`min` 合并为统一分支，新增 `literal_axis_arg` 辅助函数提取字面量 axis 参数——`x.sum(0)` 移除第 0 维（[3,4]→[4]）、`x.sum(1)` 移除第 1 维（[3,4]→[3]）、无参数 `x.sum()` 全部降维到标量、变量参数（如 keepdim 标志）保守保持原 shape。**reshape/view 字面量参数推断**：`x.reshape(3, 4)` 改用 `shape_from_int_args` 推断新 shape 为 [Known(3), Known(4)]，动态参数 `x.reshape(n, m)` 返回 [Any]。**Phase 2 验证已自动工作的能力**：let 传播（let 从 init.ty 继承 shape，scope.lookup_var 返回完整 Type 含 dims）、函数参数 shape 约束（参数类型从签名注入 scope，函数体内 matmul 检查自动生效）。**新增测试** `tests/shape_check_compile_test.rs` 新增 7 项（共 19→25 项）：sum(0)/sum(1)/sum() 三项、mean(0) 一项、reshape 字面量/动态两项、let 传播/参数约束/符号维度同名/不同名四项。**文档更新**：能力全梳理符号维度从 ⚠️ 数据结构就绪→✅ Phase 2 已实现同名等价检查。验证：`cargo build --release` exit 0；自举路径 A 通过（tenthc/main.th → Full compiler compiled）；shape_check_compile_test 25/25、autodiff_test 52/52、stdlib_test 114/114 全绿；fixpoint_runtime_benchmark 为 pre-existing 失败（stash 验证确认，与本轮改动无关）。
>
> **2026-06-30 编译期 Shape 检查 Phase 1**：实现路线图阶段 3 核心卖点（不可替代性）的第一阶段。**数据结构层已就绪**：`Type::Tensor { dtype, dims }` + `Dim::Known/Symbol/Any` + `TypeAnnotation::Tensor` + Parser 已能解析 `Tensor[f64, 3, 4]`/`Tensor[f64, M, K]`/`Tensor[f64, ..]`。**本轮新增**：`hir/lower/types.rs` 新增 `broadcast_shapes(l, r)` 函数（NumPy 广播规则，从右往左对齐，支持 Known/Symbol/Any）；`infer_binary_type` 增强——Tensor+Tensor 二元运算尝试 broadcast 推断结果 shape（兼容则返回精确 dims，否则保守 Any）；`resolve_method_type` 新增 `matmul` 分支——2D (M,K)@(K,N)→(M,N) 静态推断，K 不匹配返回 Unknown；`transpose` 2D 时反转两维（修复 attention 测试回归：k.transpose() 现被正确推断为 [8,3] 而非 [3,8]）；`resolve_builtin` 构造函数（zeros/ones/randn/rand/tensor/tensor_from_vec）新增 `shape_from_int_args`——字面量参数 `zeros(3,4)`→[Known(3), Known(4)]，动态参数 `zeros(n)`→[Any]；新增 `check_binary_shape_compat` 和 `check_method_shape` 两个编译期检查函数——shape 不兼容时返回 `TenthError::TypeError`（含人类可读错误信息如"编译期 matmul shape 不兼容：[3, 8] @ [3, 8]（内侧维度 8 ≠ 3 必须相等）"）；`lower_expr.rs` Binary 分支和 MethodCall 分支（两处）调用 check 函数。**新增测试** `tests/shape_check_compile_test.rs` 12 项：matmul K 不匹配编译期报错、matmul 正确 shape 编译通过、matmul + transpose 编译通过（attention 模式）、忘记 transpose 编译期报错、二元加法不兼容 shape 报错、同 shape/标量广播/行广播编译通过、zeros/randn 字面量 shape 推断、动态参数返回 Any、transpose 2D 反转。**文档更新**：能力全梳理 编译期 Shape 检查从 ❌→⚠️ Phase 1 已实现、符号维度从 ❌→⚠️ 数据结构就绪；语言参考手册/README 阶段 3 状态从 📋 规划中→⚠️ Phase 1 已实现。验证：`cargo build --release` exit 0；自举路径 A 通过；`cargo test --release -- --skip fixpoint_runtime --skip three_stage` 全绿（shape_check_compile_test 12/12、autodiff_test 52/52、generic_tensor_test 4/4、stdlib_test 114/114 等）。
>
> **2026-06-30 第三类技术债扫尾 + native 构造函数泛型化**：完成外部评价者第三类批评（小杂项）+ 第一类遗留的 native 泛型化。**native 构造函数泛型化**：`hir/lower/lower_expr.rs` 的 `ExprKind::GenericCall` 分支新增 `NATIVE_GENERIC_CTORS` 列表（randn/zeros/ones/rand/tensor/tensor_from_vec），支持 `randn<f32>(d)` 语法；类型参数必须是具体 BaseType，f32 dtype 映射到运行时名字 `randn_f32`/`zeros_f32`/`ones_f32`/`rand_f32`，f64 保持原名。新增 2 个测试（`native_generic_ctor_f32_lowering` / `native_generic_ctor_f64_lowering`），generic_tensor_test 4/4 通过。**make_* 构造函数改泛型**：`std/nn/layer_norm.th`（make_layer_norm）、`std/nn/feedforward.th`（make_feedforward_params）、`std/nn/transformer.th`（make_transformer_block_params）、`std/nn/positional_encoding.th`（positional_encoding）四个函数改泛型 `<T>`，统一用 `randn<T>(...)`/`zeros<T>(...)`/`ones<T>(...)`；positional_encoding 注释更新（原"kept as f64 because randn native is f64-only"已过时）。**IMPORT_COUNT 魔数修复**：`compile/wasm/mod.rs` 新增 18 个 `HOST_*` 具名常量（HOST_PRINTLN=0 ... HOST_TENSOR_FROM_VEC=17），`IMPORT_COUNT` 改为 `HOST_TENSOR_FROM_VEC + 1` 推导；`compile/wasm/compile.rs` 中 33 处 `Call(N)` 魔数全部替换为 `Call(HOST_*)` 引用，`host_call_index` 函数的 match 分支也用具名常量；新增 import 只需在 mod.rs 加一行常量 + sections.rs/host.rs 注册。**AUDIT §7.1 过时条目**：#1（lexer.th 字面量硬编码 0）和 #2（parser.th 字段名不存储）经复核均已修复，标记为 ~~已修复~~ 并附证据行号。**tools 路径统一**：8 个文件（AUDIT/CODE_WIKI/DEPS/语言参考手册/MEMO/SECURITY/security_review/engine.rs）共 15 处 `tools/tenthpm/`、`tools/lsp/` 统一为 `tenth/tools/tenthpm/`、`tenth/tools/lsp/`（与实际磁盘路径一致）。验证：`cargo build --release` exit 0；`cargo test --release -- --skip fixpoint_runtime --skip three_stage` 全绿（generic_tensor 4/4、stdlib 114/114、wasm_backend_minimal 7/7、vm_autodiff 15/15 等）；fixpoint_runtime 和 three_stage 两个 wasmtime 路径测试为 pre-existing 失败（stash 后仍失败，WASM-B 0 bytes，与本轮改动无关）。
>
> **2026-06-29 安全审查修复（第二轮）**：完成剩余 9 项中的 7 项（H-2, H-4, L-1, L-3, L-4, L-5, L-6）；L-2（DefaultHasher）已在 H-7 中一并修复，L-7（compile_host 写文件）已在 H-2 中一并修复。**H-2（文件 I/O 沙箱）**：`tenth/src/runtime/limits.rs` 新增 `FsSandbox` 类型（`check_read`/`check_write` 用 `canonicalize` + `starts_with` 防路径穿越，不存在路径规范化父目录再拼接文件名）；`Vm`/`Interpreter` 新增 `fs_sandbox` 字段，所有文件 I/O 原生函数（read_file/write_file/write_bytes/read_bytes/path_exists/path_is_file/path_is_dir/mkdir/list_dir/file_size/remove_file/copy_file/rename_file/compile_host/compile_program/load_weights/save_weights）必须经过沙箱校验；`main.rs` 新增 `--fs-root <dir>`/`--read-only`/`--fs-cwd` 命令行选项。**H-4（CPU/时间限制）**：VM 主循环和 Interpreter `tick()` 新增独立 `loop_counter`/`tick_counter`，每 4096 步检查 `deadline_ms`（墙钟超时独立于 `step_budget`，用户可只设 `--timeout` 不设步数预算）；`main.rs` 新增 `--timeout <secs>` 命令行选项，`parse_timeout_ms` 用 `checked_mul`/`checked_add` 防溢出。**L-1（extract_package_name 清理）**：验证确认 `extract_package_name` 仅在 `safe_package_name_from_git` 内被调用，所有调用方（install_local/install_global/add_git_dependency）都经 `validate_package_name` 校验，security_review.md 引用的 install.rs:177 为过时信息。**L-3（days_to_date 溢出）**：`main.rs` 和 `interpreter.rs` 两处 `days_to_date` 入口加 `if days > u64::MAX - EPOCH_OFFSET { return (0,0,0); }` 防 `days + 719468` 溢出 UB。**L-4（git URL 协议限制）**：`manifest.rs::is_git_url` 默认仅放行 `https://`，`http://`/`git://`/`ssh://`/`.git` 后缀需环境变量 `TENTH_ALLOW_INSECURE_GIT=1` 显式 opt-in。**L-5（git clone hooks 禁用）**：三处 `git clone` 调用（install_local/install_global/add_git_dependency）加 `--config protocol.file.allow=deny` 和 `--config protocol.git.allow=deny`，克隆后调用新增 `manifest::disable_hooks()` 将 `core.hooksPath` 指向空设备（Unix `/dev/null` / Windows `nul`）。**L-6（cargo-audit 建议）**：`DEPS.md` 新增"供应链安全"章节，说明 `cargo audit` 和 `cargo deny` 用法及 CI 集成建议。验证：`cargo build`（dev+release）exit 0，自举路径 A 验证通过（`tenthc/main.th` → `tenthc_full.wasm`），tenthpm 编译通过。**已知限制**：`cargo test` 因 rustc 1.95.0 ICE（`wasm.rs:2077` 方法调用解析，仅在 `--test` 模式触发，非本项目代码问题）无法编译测试二进制；ICE 文件未被本轮修改，属预存在 rustc bug。
>
> **2026-06-29 安全审查修复（第一轮）**：以最严谨安全员身份完成全面审查，识别 25 项问题（2 致命 / 8 高危 / 8 中等 / 7 低危），本轮修复 17 项（C-1, C-2, H-1, H-3, H-5, H-6, H-7, H-8, M-1 ~ M-8）。审查报告 `security_review.md`，威胁模型披露 `SECURITY.md` 重写。**C-1（tenthpm 路径穿越）**：`tenth/tools/tenthpm/src/manifest.rs` 新增 `validate_package_name` / `safe_package_name_from_git` / `ensure_within` / `safe_to_remove_dir` 四个集中校验函数；`install.rs` / `add.rs` 全路径调用，所有 `fs::remove_dir_all` 前必须通过 `safe_to_remove_dir`，所有 `target_dir` 计算后必须通过 `ensure_within`。**C-2（SECURITY.md 失实声明）**：原声称"0 处 unsafe"实为 41+ 处，JIT 为默认执行路径，重写公开真实威胁模型与沙箱选项 `--fs-root`。**H-1（JIT hostcall 越界）**：`tenth/src/compile/jit/hostcalls.rs` 引入 `MAX_HOSTCALL_ARGS = 1<<20` 与 `safe_slice` 统一闸门，所有 `from_raw_parts` 改用；`host_make_map` / `host_new_struct` 加 `count.checked_mul(2)`；`host_make_tensor` 加 `rows.checked_mul(cols)`。**H-3（run_file 无限制）**：`tenth/src/main.rs` 新增 `parse_memory_config` 与 `run_file(path, config)`，fallback 路径用 `Interpreter::with_limits`；`--no-limits` 显式退出沙箱。**H-5（time_sleep_ms 负数 DoS）**：拒绝负数与 > 24h 的请求。**H-6（JSON 解析器递归/转义）**：`JSON_MAX_DEPTH=256` 防栈溢出；`json_unescape` 与 `simple_json_split` 修复 `\"` 状态机。**H-7（DefaultHasher 可预测）**：`random` 改用 `rand::thread_rng().r#gen()`（CSPRNG）。**H-8（WASM 宿主 ptr as usize 符号扩展）**：`tenth/src/compile/wasmtime_host.rs` 新增 `safe_offset` / `read_cstr` / `MAX_ALLOC_BYTES=16MiB`，17 个 host import 全部改用；`tenth_alloc` 拒绝负数 size 与 > 16MiB 请求。**M-1（transmute 尺寸断言）**：`tenth/src/compile/jit/context.rs` 加 `assert_eq!(size_of::<*const u8>(), size_of::<JitFn>())`。**M-2（mem-strict panic 杀 REPL）**：`tenth/src/repl.rs:241-245` 改为返回 `TenthError::RuntimeError`。**M-3（release 溢出检查）**：`tenth/Cargo.toml` 加 `[profile.release] overflow-checks = true, panic = "unwind"`。**M-4（host_make_tensor 溢出）**：见 H-1。**M-5（JitContext Drop 显式清理）**：`context.rs` impl Drop 调 `cache.clear()`。**M-6（FFI unwind UB）**：`hostcalls.rs::invoke_jit` 包 `catch_unwind`，panic 时写 `Value::Unit` 并返回 false。**M-7（Arena 算术溢出）**：`tenth/src/runtime/arena.rs` `alloc` 用 `checked_add` / `checked_mul`；`scope` 用 `saturating_sub` 防下溢 panic。**M-8（WASM 宿主 bump allocator 越界）**：见 H-8。**验证**：`cargo build --release` 成功；`cargo test --lib` 10/10 通过；`cargo test --features mem-debug --test memory_test` 17/17 通过（含 arena_scope_rolls_back_counter / arena_overflow_returns_none 验证 M-7 修复）。**未引入回归**：stash 验证原始代码下 `cargo test --no-run` 同样因 wasmtime/cranelift 依赖 rlib 格式问题失败，与本次修复无关。**未修复项**：L-1 ~ L-7 低危项（命名约定、文档同步等），保留至下轮。
>
> **2026-06-25 优先级调整**：自举固定点 spec 暂停，转向能力梳理第一梯队。经评估，自举固定点（C4）是纯工程内部指标，对语言采用率无影响；而能力梳理第一梯队 4 项（f32 张量、编译期 Shape 检查、GPU 算子、异步/并发）是"生存级"缺口，直接决定 Tenth 能否称为"AI 原生语言"。决策：暂停自举固定点 spec（Phase 1 代码 + Phase 2 审计结论作为存量资产保留），分四个独立 spec 推进第一梯队。自举固定点重启条件：第一梯队完成 或 用户明确指示。
>
> **2026-06-25 更新**：f32 张量 spec Phase 1 完成。`TensorData` enum（F32/F64 变体）替换原 `ArrayD<f64>` 单一存储，`Tensor` 结构新增 `dtype` 字段。新增 8 个 f32 构造器（zeros_f32/ones_f32/full_f32/rand_f32/randn_f32/eye_f32/arange_f32/from_vec_f32）+ 2 个 dtype 通用构造（zeros_with_dtype/ones_with_dtype）。运算方法（add/sub/mul/div_tensor + 标量运算）按 dtype 分支：f32⊙f32→f32 保持精度，f32⊙f64→f64 提升。`BaseType` 添加 `Copy` trait。兼容层策略：TensorData 实现 mapv(FnMut)/view/as_standard_layout/iter/broadcast 等 ArrayD<f64> 接口（F32 自动 cast 为 f64 视图），外加 Mul/Add/Sub/Div/Neg/Index 算术 trait impl，让 autodiff.rs/interpreter.rs/vm.rs/main.rs 外部代码零改动通过编译（Phase 1 语义降级：F32 经兼容方法后变为 f64，真正 f32 路径在 Phase 3/4 恢复）。跨模块影响：autodiff.rs:360/365 `data.clone()`→`as_f64_view()`；main.rs:650 + vm_autodiff_test.rs:80 `into_raw_vec()`→`from_tensor_data()`；repl.rs Display 通过 TensorData Display impl 解决。新增 `tests/f32_tensor_test.rs`（23 用例，覆盖构造器/dtype标记/运算保持/提升/标量/reduction/Display/broadcast）。全量 539 测试通过（516+23），0 回归，自举三路径未破坏。
>
> **2026-06-25 更新**：f32 张量 spec Phase 2 完成（前端贯通）。`TokenKind::FloatLiteral(f64, BaseType)` 携带 dtype，Lexer `read_number` 末尾检测 `f32`/`f64` 后缀（三字符边界检查避免 `3.14factor` 误匹配）。AST/HIR `Literal::Float(f64, BaseType)` 双字段贯通。Lower 新增三个推断辅助函数：`infer_tensor_dtype(args)`（任一参数 F32 → F32，否则默认 F64）、`infer_scalar_dtype(args, fallback)`（标量函数 dtype 跟随输入）、`promote_float_dtype(l, r)`（F64 优先 > F32 > F16 > BF16 > 整数）；`resolve_builtin` 重写为参数化推断（`tensor/rand/randn/zeros/ones/param/new_grad/cross_entropy` 全部走推断，不再硬编码 F64）；`infer_binary_type` 重写支持 Tensor-Tensor dtype 合并、Tensor-标量保持、标量-标量提升。字节码：`Op::PushFloat32(f32)` 新增（opcode 45），`Op::MakeTensor(rows, cols, dtype)` 第三参数 dtype_code（0=F64, 1=F32）；序列化/反序列化 MakeTensor 增加 1 字节 dtype。`Value::Float32(f32)` 新运行时值类型，`type_of(Value::Tensor)` 改为读 `t.dtype()`（修复 f32 Tensor 类型推断错误）；VM `add_priv/sub_priv/mul_priv/div_priv/compare/vm_eq/neg` 各增加 5 个 Float32 分支（含 Int↔Float32、Float↔Float32 混合提升）；`sum/mean/max_val` natives 按 tensor.is_f32() 分支返回。新增 `to_f32` / `to_f64` 内置函数（lower + native + interpreter），作为 `as f32` / `as f64` 表达式的 API 替代（AST 层未扩展 `as` 语法）。WASM 后端 `compile_literal` Float 分支按 dtype 选择 F32Const/F64Const。JIT 路径 `PushFloat32` 暂降级为 f64 调用 `host_make_float`（Phase 5 补齐真正 f32 JIT）。新增 `tests/f32_frontend_test.rs`（13 用例，覆盖 G1 lexer 后缀、G2 HIR dtype 贯通、G4 Value::Float32 算术、E5 回归）。全量 552 测试通过（539+13），0 回归，自举路径 A 验证通过（`cargo run --release -- run tenthc/main.th` → Full compiler compiled to tenthc_full.wasm）。
>
> **2026-06-25 更新**：f32 张量 spec Phase 3 完成（运行时贯通）。interpreter.rs 三处硬编码 F64 return_type（tensor/zeros/ones 内置函数）改为 `Type::Unknown`，让 VM 运行时按实际 Value 推断 dtype。`lower.rs:596-608` TensorLiteral dtype 推断重写：原硬编码 `dtype: BaseType::F64`，现遍历 lowered 元素，任一为 F32 → Tensor dtype=F32，否则 F64（修复 `[[1.0f32, 2.0f32]]` 被误标记为 F64 的 bug）。interpreter.rs TensorLiteral 评估按 HIR `ty` 字段 dtype 分支构造 f32/f64 Tensor。`to_f32` / `to_f64` 加入 lower.rs 和 interpreter.rs builtin 白名单（原仅含 `to_float`，导致 `to_f32(2.0)` 被当作 undefined variable 报错）。main.rs native 函数 f32 支持：`randn_f32`（f32 Box-Muller）、`tensor_from_vec` 按 Vec 元素 dtype 判断、`grad` 按参数 dtype 返回零张量（f32→`zeros_f32`）、`cross_entropy` 按 logits dtype 构造 loss tensor；所有 `math_*` 函数（math_tan/asin/acos/atan/atan2/sinh/cosh/tanh/log10/log2/exp/pow/floor/ceil/round）+ abs/sqrt 增加 Float32 分支。新增 `tests/f32_runtime_test.rs`（7 用例：VM 算术 f32 分支、混合提升、Int+Float32 提升、MakeTensor dtype、randn_f32 native、to_f32 native、sqrt f32）。全量 559 测试通过（552+7），0 回归，自举路径 A 验证通过。
>
> **2026-06-25 更新**：f32 张量 spec Phase 4 完成（自动微分 f32 支持）。**关键发现：方案 B 天然实现，无需改造 autodiff.rs**。调研发现 `backward` 函数 `node_grads: Vec<Option<ArrayD<f64>>>` 固定 f64 计算（反向用 f64），`acc_grad(&ArrayD<f64>)` 已在 Phase 1 按 tensor.dtype 转换存储（f32 参数→grad 存为 F32），`grad` native 已在 Phase 3 按 dtype 返回——方案 B 链路（前向 f32 + 反向 f64 + 梯度按参数 dtype 写回）已完整。误差测量：纯算术（Add/Sub/Mul/Neg/ReLU/MatMul/Sigmoid/Exp/Log）误差 < 1e-10；涉及 f32 前向 exp/sum 的算子（Softmax/CrossEntropy）误差 ~1e-7（f32 机器精度，spec 允许 < 1e-5），E4 退出条件未触发。唯一代码改动：vm.rs 补齐 `Float32 × Tensor` / `Tensor × Float32` 算术分支（add/sub/mul/div 各 2 个，Phase 2 遗漏修复，`2.0f32 * f32_tensor` 原报"* 类型不匹配"）。新增 `tests/f32_autodiff_test.rs`（12 用例：new_grad/param 注册、simple gradient、ReLU 正负梯度、matmul、softmax/sigmoid/cross_entropy backward、exp/log chain、grad dtype 保持、extreme input stability、chain rule）。全量 571 测试通过（559+12），0 回归，自举路径 A 验证通过。
>
> **2026-06-25 更新**：f32 张量 spec Phase 5.0（泛型化可行性验证）+ Phase 5.1（WASM 后端 f32→f64 提升）完成。**Phase 5.0 调研结论**：用户原选泛型化路线（`Tensor[T, ..]`）在当前类型系统下不可行——`Type::Tensor.dtype: BaseType` 是硬约束（types.rs:23），`from_annotation` 对非 Base dtype fallback 到 F32（types.rs:141），`substitute_type` 不递归 Tensor（lower.rs:1991）；路径 B（扩展支持）需改 Type 核心枚举 + ~20 处 match + 编译后端 + 自举三路径对齐，工作量属独立专项级。用户确认降级为路径 C（后缀复制）策略推进 Phase 5.4 标准库 f32 化。**Phase 5.1 实际执行策略 A（f32→f64 提升）**：原 spec 计划策略 B（完整 f32 指令集 + 4 字节 stride + Type section 重构），但调研后发现 wasm.rs 已有 15 处 F32→F64 提升路径，唯一破坏策略 A 的是行 1417 `F32Const`（死代码 bug：压入 f32 栈但下游期望 f64，导致 WASM 验证失败，无 e2e 测试覆盖）。修复：`wasm.rs:1416-1420` 将 `Literal::Float(n, dt)` 的 F32 分支改为统一 `F64Const`，与下游 F64 处理对齐。新增 `tests/f32_wasm_test.rs`（14 用例：f32 字面量算术 add/sub/mul/div、f32 后缀解析、一元负号、比较 eq/lt、f32-f64 混合、f32-i64 混合、let 绑定、链式算术、main wrap 路径）。全量 219 测试通过（f32_tensor 23 + f32_frontend 13 + f32_runtime 7 + f32_autodiff 12 + f32_wasm 14 + wasm_backend_minimal 7 + parity 129 + selfhost 4 + 其他 10），0 回归，自举路径 A 验证通过（tenthc/main.th 编译成功）。
>
> **2026-06-25 更新**：f32 张量 spec Phase 5.2（tenthc 自举同步）完成。**实际方案：复用 Token.ival 字段存 dtype**，非原 spec 计划的扩展 Token/HirExpr 结构。调研发现 FloatLiteral 时 ival 字段原本未使用，复用为 dtype 标记（0=F64, 1=F32）避免扩展两侧结构 + bridge.rs 对齐工作；HirType.sub 字段语义化（0=I64, 2=F64, 3=F32, 4=Bool, 5=Str），与 Rust 侧 `Literal::Float(f64, BaseType)` 语义对齐。4 step 改动 + 5 步渐进验证（每 step 跑 `cargo run --release -- run tenthc/main.th` + `cargo test --test selfhost_frontend` 4/4）：step 1 lower.th 加 `is_type_f32`/`is_type_float` + Binary 算术 F64>F32>I64 优先级（与 Rust lower.rs:1286-1290 infer_binary_type 对齐）+ float 字面量按 e.ival 决定 dtype；step 2 lexer.th 加 f32/f64 后缀检测（FloatLiteral + IntLiteral 两路径，三字符边界检查避免 `3.14factor` 误匹配；整数后跟 f32 也变 FloatLiteral，与 Rust 语义一致）+ parser.th 保留 tok.ival 到 Expr.ival；step 3 wasm.th `is_expr_float` 识别 sub==3 (F32)，策略 A 统一发 f64 指令；step 4 parser.th let 语句检测类型注解是否为 `f32`（单 token 类型），若且初始表达式是 FloatLiteral 则把 ival 强制改为 1（让 `let x: f32 = 3.14` 字面量升级为 F32 dtype）。自举三路径未破坏：自举成功（Full compiler compiled）+ selfhost_frontend 4/4 + Phase 5.1 e2e 无回归（f32_wasm_test 14/14）。退出条件 E6 未触发，E2 已记录（4 step 改动 ~150 行，远未达 800 行）。
>
> **2026-06-26 更新**：f32 张量 spec Phase 5.4（标准库 nn/optim/init f32 化，精简版）+ Phase 5.5（f32 native 构造函数补全）完成。**路径 C 后缀复制策略**：用户选精简版，仅复制「带硬编码 f64 标量参数」+「构造函数调用 f64 内置」两类文件，不复制纯方法包装。共 14 个 std 文件新增 26 个 `_f32` 副本函数：optim/{sgd,adam,adagrad,rmsprop}.th（4 文件，标量参数 `lr/momentum/decay: f32`、字面量 `1.0f32 - decay`）；init/initializers.th（6 个构造函数 `xavier_uniform_f32`/`xavier_normal_f32`/`he_normal_f32`/`he_uniform_f32`/`zeros_init_f32`/`constant_init_f32`，调用 `randn_f32`/`zeros_f32`、字面量 `6.0f32`/`2.0f32`）；nn/{dropout,batchnorm,loss,attention,multihead_attention,layer_norm}.th（标量 `rate/eps/dropout_p: f32`、`scale = 1.0f32 / sqrt(d_k)`、`-1000000000.0f32` 替代 lexer 不支持的科学计数法 `-1e9`）；nn/{positional_encoding,feedforward,transformer}.th（构造函数 `make_*_f32` 调用 `randn_f32`/`zeros_f32`/`ones_f32`、`0.00001f32` 替代 `1e-5`）。**Phase 5.5 内置补全**：`zeros_f32`/`ones_f32`/`rand_f32` Tensor 层方法已存在（tensor.rs:318-338）但 native 未注册——lower.rs 3 处（行 352 builtin 列表、行 1217 类型推断、行 1815 变量收集跳过列表）+ interpreter.rs 2 处（行 525 builtin 列表、行 3499+ native 实现）补全注册。**Interpreter f32 路径修复**（测试驱动发现 4 处缺失）：(1) Add/Sub/Mul/Div 各补 5 个 Float32 标量×标量分支（Float32+Float32→Float32、Int+Float32→Float32、Float32+Int→Float32、Float+Float32→Float、Float32+Float→Float，与 lower.rs:1286 promote_float_dtype 规则对齐）；(2) Add/Sub/Mul/Div 各补 2 个 Float32×Tensor / Tensor×Float32 分支（`*s as f64` 转换后调用 scalar 方法，模式与 vm.rs Phase 3 一致）；(3) `grad` native 对 f32 张量返回 f32 zeros（原 `Tensor::zeros` 硬编码 f64，改用 `Tensor::zeros_with_dtype(&shape, p.dtype())`）；(4) `dropout` 方法按 tensor dtype 分支保持精度（f32 路径用 `as_f32().mapv` + f32 随机 + `from_data_f32`，f64 路径不变）。新增 `tests/f32_stdlib_test.rs`（10 用例：3 native 构造函数、5 内联 _f32 副本端到端、2 标量×tensor dtype 行为验证）。全量测试 0 回归：f32_stdlib 10/10、stdlib 114/114、autodiff 5/5、vm_autodiff 15/15、f32_runtime 7/7、f32_autodiff 12/12、integration 14/14、selfhost_frontend 4/4。自举路径 A 验证通过。**关键发现**：`Tensor::mul_scalar(scalar: f64)` 内部按 tensor dtype 分支（F32 分支把 scalar cast 为 f32），故 f64 标量 × f32 tensor 结果仍是 f32（不提升）——`_f32` 副本的主要价值在于使用 f32 字面量保持标量运算精度，而非防止 tensor dtype 提升。
>
> **2026-06-25 架构决策**：自举长期路线图确立。重新审视 WASM 路径与"完全摆脱 Rust"终极目标的契合度，确认 WASM 路径的根本局限（执行运行时 wasmi/wasmtime 是 Rust 写的，"完全摆脱 Rust"在 WASM 路径上不可能达成）。决策采用两阶段策略：阶段 1（当前 spec）WASM 路径达成 C4 固定点，阶段 2（未来 spec）实现 native 后端达成运行时自举。Native 后端三条子路径评估：C2-直接机器码（5000-10000 行 Tenth，无外部依赖，推荐）、C2-LLVM IR（引入 C++ 依赖违背精神，不推荐）、C3-C 翻译（2026-06-04 已尝试并删除，内存管理未解决，重新评估需先解决所有权→C 内存映射）。新增 `docs/superpowers/self-hosting-roadmap.md` 长期路线图文档，spec.md §8 追加架构决策记录。
>
> **2026-06-25 更新**：自举固定点攻关 spec 重构。前置 spec `docs/superpowers/specs/2026-06-20-self-hosting-master-plan/` 已归档（Phase A-D 全部完成，D1-D7 共 129 用例全绿，仅 C4 固定点未达成）。新建 `docs/superpowers/specs/2026-06-25-self-hosting-fixpoint/`（含 spec.md / tasks.md / checklist.md），重新设计 C4 攻关方案：5 阶段架构（运行时迁移→确定性保证→端到端跑通→固定点达成→CI 集成），锁定 4 项设计决策（Wasmtime JIT / wasmi 保留 / 字节级等价不可达成则终止 / 大重构需求则终止），明确 4 项硬性退出条件（不允许降级实现）。同步修正旧 spec 文档不一致：spec.md 能力差距矩阵（Trait/泛型/借用/闭包/Tensor 已实现）、tasks.md D2/D6 验收 checkbox、checklist.md 状态描述。
>
> **2026-06-25 更新**：Phase D — D3（借用检查）+ D5（闭包 WASM 后端）完成，Phase D 全部 7 项（D1-D7）达成。D3：tenthc lower.th 新增 Ownership 状态存储（Owned/SharedRef/ExclusiveRef/Moved）、check_use/check_borrow_shared/check_borrow_mut 借用检查函数、release_borrows 借用释放；parser.th 补全 `&mut` 解析。D5：tenthc lower.th 实现 free_vars_in 递归捕获分析；wasm.th 新增 table/elem section + call_indirect 闭包调用 + env 装箱（tenth_alloc 分配 captures struct）；修复闭包解析双 advance bug（parse_primary 已消费 `|`，闭包块重复 advance 吞掉首个参数）；修复 `&&`/`||` 误用 i64 and/or 指令（WASM 比较结果为 i32，需用 I32And/I32Or）。新增 5 项 parity 测试（D3×3 + D5×2），parity_test 124→129 项全绿，499+ 测试无回归。
>
> **2026-06-25 更新**：Phase D — D2（泛型实例化）+ D6（Tensor WASM 后端）完成（阶段 1 并行）。D2：tenthc 新增泛型参数解析 `<T, U>`、泛型调用前瞻启发式 `looks_like_generic_call`、`substitute_type` 类型替换与 mangled name 实例化（`fn_name_T1_T2`），Rust 侧 lower.rs 同步将 GenericCall 改写为普通 Call。D6：tenthc wasm.th 新增 tensor_from_vec host import (idx 17) 与 TensorLiteral 编译，Rust 侧 wasm.rs 同步；修复 parser.th parse_primary 双 advance bug（tensor 字面量解析失败根因）。修复 three_stage.rs / wasm_backend_minimal.rs linker 缺失 tensor_from_vec 注册导致的回归。新增 4 项 parity 测试（D2×3 + D6×1），parity_test 120→124 项全绿，499+ 测试无回归，自举管道通过。
>
> **2026-06-25 更新**：Phase D — D1（Trait 系统）完成。tenthc 新增 trait 定义/inherent impl 解析与方法静态分派（mangled name `__<Type>_<method>`）；修复 lexer self token 缺少 sval、lowerer self 参数 type_ann 未覆盖为 impl 类型名两处 bug；Rust 母编译器同步实现 inherent impl 方法分派。新增 3 项 D1 parity 测试全部通过，parity_test 117→120 项全绿，499+ 测试无回归。
>
> **2026-06-25 更新**：Phase D — D4（native 函数对齐）完成。修复 tenthc lexer 数字解析双处理 bug（`lexer_peek` 返回当前字符导致 `5`→`55`）；修复 `char_to_ascii` 缺失空格等常见字符（补全 30+ ASCII 字符）。4 项 Slice parity 测试全部通过，parity_test 117 项全绿。
>
> **2026-06-24 更新**：测试数 350→499（新增 parity_test 112 项 VM/Interpreter 一致性、shape_check_test 16 项 shape 检查；selfhost_verify→selfhost_frontend 重构）。文档重复信息全面清理，交叉引用体系建立。
>
> **2026-06-15 更新**：文档维护 — 自动微分算子数修正为 21（新增 LayerNorm/GELU），标准库模块补全（collections/string/utils/nn 扩展），张量方法补全（gelu/layer_norm/cat/masked_fill/permute/broadcast_to/max_val）。
>
> **2026-06-04 重大变更**：`tenth/src/compile/`（MIR→C 编译管线）、`tenthc/codegen/`、`tenthc/runtime.c` 已删除。
> 原因：生成的 C 代码无内存管理（12 处 malloc / 0 处 free），导致系统级内存耗尽。详见 `SECURITY.md`。
> 自举编译器改为通过 Rust 解释器执行。

## v0.3.3 — Problem 5：整数类型全管线 dtype 保留 + 溢出检测

**日期**：2026-07-11

### 变更概述
整数类型（i8/i16/i32/i64/u8/u16/u32/u64）在整个编译管线中保留 dtype，不再退化为 i64。新增编译期字面量范围检查和运行时算术溢出检测。

### 详细变更
- **枚举层**：`IntLiteral(i64)` → `IntLiteral(i64, BaseType)`（Token/AST/HIR/Value 四层同步）
- **Lexer**：整数后缀检测（`42u8`/`42i64` 等）、编译期范围检查（`256u8` → 错误）、无后缀大整数自动提升 I32→I64
- **Parser/HIR lower**：dtype 通过 `Literal::Int(n, dt)` 传递，类型推断返回 `Type::Base(*dt)`
- **运行时**：新增 `check_int_overflow()` 函数；算术运算（Add/Sub/Mul/Div/Mod/Neg）传播左操作数 dtype 并检测溢出
- **type_of()**：返回实际 dtype 而非硬编码 I32
- **Bug fix**：`()` 类型注解正确解析为 `Type::Base(BaseType::Unit)`（之前被解析为 `Type::TypeParam { name: "()" }`，导致 Problem 11 返回类型检查误报）
- **测试**：新增 `int_types_test.rs`（14 个测试，覆盖后缀类型推断、编译期范围检查、运行时溢出检测、dtype 保留）
- **影响文件**：31 个源文件 + 28 个测试文件修改，1 个新测试文件

### 验证
- 全部关键测试套件通过（0 failures）
- 自举三路径验证通过：`[OK] Full compiler compiled to tenthc_full.wasm`
- 3 个 pre-existing Windows 栈溢出测试失败（与 Problem 5 无关）

---
## 自举编译器现状（2026-06-10 更新）

自举管线全部由 Tenth 实现，全程走 VM（~0.2s），不再依赖解释器 fallback。

### 自举管线

```
Tenth 源码 → Lexer → Parser → Lowerer → WASM Compiler → .wasm → wasmi
   ✅          ✅       ✅         ✅           ✅           ✅      ✅
  (Tenth)   (370行)  (430行)    (294行)      (703行)     import   add(3,4)=7
```

### 性能

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 自举管线执行时间 | ~200s (interpreter) | **~0.2s (VM)** |
| VM fallback 率 | Lexer/Parser 100% | **0%** |
| VM 指令数 | 33 | **45** (41→43→45, PushRange+MoveOp+MakeTensor+MakeClosure) |
| wasmi 加载验证 | ❌ | ✅ `add(3,4)=7` |

### 各层状态

| 层 | 文件 | 状态 |
|----|------|------|
| Token | `tenthc/lexer/token.th` | ✅ enum TokenKind (50+ 变体) |
| Lexer | `tenthc/lexer/lexer.th` | ✅ O(1) 源切片，**VM 全速** |
| Parser | `tenthc/parser/parser.th` | ✅ 递归下降 + method_call，**VM 全速** |
| HIR 类型 | `tenthc/hir/hir.th` | ✅ 紧凑表示 (177 行) |
| Lowerer | `tenthc/hir/lower.th` | ✅ AST→HIR 降级 (1252 行) |
| WASM 编译器 | `tenthc/compile/wasm.th` | ✅ HIR→WASM + import 段 (1885 行) |
| ~~C Codegen~~ | ~~`tenthc/codegen/cgen.th`~~ | ❌ 已移除 |
| **字节码 VM** | `tenth/src/runtime/vm.rs` | ✅ **45 指令** (33→41→43→45) |
| VM 编译器 | `tenth/src/compile/bytecode.rs` | ✅ HIR→bytecode (含 Enum/Match) |

### VM 指令列表（45 条）

```
0-3:   PushInt/Float/Bool/Str    20-23: Lt/Gt/Lte/Gte
4:     PushUnit                  24-26: Jump/JmpFalse/JmpTrue
5-6:   Pop/Dup                   27-28: Call/CallN
7-8:   Load/Store                29:    MethodCall
9-10:  LoadGlobal/StoreGlobal    30:    Ret
11-15: Add/Sub/Mul/Div/Mod       31-32: MakeVec/MakeMap
16-17: Neg/Not                   33:    NewStruct
18-19: Eq/Neq                    34-35: LoadField/StoreField
                                  36:    IndexGet
                                  37:    SliceStr
                                  38:    MakeEnum
                                  39:    IsEnumVariant
                                  40:    EnumGetField
                                  41:    PushRange       ← v0.3.0
                                  42:    MoveOp          ← v0.3.0
                                  43:    MakeTensor      ← v0.3.1
                                  44:    MakeClosure     ← v0.3.1
```

### 新增验证（2026-06-10）

- [x] 路径 C: Tenth Lexer+Parser+Lowerer+WASM 全链路 → wasmi `add(3,4)=7`
- [x] VM Enum/Match 支持：Lexer/Parser 不再 fallback
- [x] 永久回归测试：`tenth/tests/selfhost_verify.rs` (83/83 pass)
- [x] 性能基准：`tenth run tenthc/boot_full.th` → 0.2s

### 已知限制（当前活跃）

> 带编号的完整缺陷清单见 `AUDIT.md` §六/七。此处为逐版演化日志。

- [x] ~~Closure/GenericCall VM fallback~~ → GenericCall/Move/Range/MakeTensor/MakeClosure 已补全
- [ ] Host import (Vec/String) 为占位实现，WASM 模块需宿主提供真实运行时
- [ ] 三段式自举验证（输出 WASM 再编译自身）因栈溢出未跑通
- [x] ~~大文件 Lowerer 性能~~ → 已解决 (VM ~0.2s)

### v0.3.1 新增（2026-06-14）

#### 闭包捕获

| 组件 | 状态 |
|------|------|
| HIR `HirExprKind::Closure` 新增 `captures: Vec<String>` | ✅ |
| Lowerer `free_vars_in()` 递归分析自由变量 | ✅ |
| Interpreter 闭包创建时从 `resolve_var()` 捕获环境变量 | ✅ |
| VM `MakeClosure(params_count, chunk_idx)` 指令 | ✅ |
| BytecodeCompiler 闭包编译为 MakeClosure | ✅ |

#### VM 补全

| 组件 | 状态 |
|------|------|
| `MakeTensor(rows, cols)` 指令 (opcode 43) | ✅ |
| `MakeClosure(params_count, chunk_idx)` 指令 (opcode 44) | ✅ |
| BytecodeCompiler TensorLiteral 编译为 PushFloat+MakeTensor | ✅ |

#### 文件级导入

| 组件 | 状态 |
|------|------|
| Lowerer `search_paths` 字段 | ✅ |
| Lowerer `try_import_file()` / `load_and_compile_file()` | ✅ |
| `source_to_hir()` 使用 `with_search_paths(vec!["std"])` | ✅ |

#### 错误信息增强

| 组件 | 状态 |
|------|------|
| Scope `check_use/check_borrow_shared/check_borrow_mut` 带 span 参数 | ✅ |
| 消除 3 处 `line: 0, col: 0` 硬编码 | ✅ |

#### 标准库补全

| 模块 | 状态 |
|------|------|
| data/dataloader.th — DataLoader 完整实现 | ✅ |
| utils/serialization.th — save_model/load_model/save_checkpoint | ✅ |
| prelude.th — 更新所有模块索引 | ✅ |

#### 自举编译器同步

| 模块 | 状态 |
|------|------|
| hir/hir.th — captures_start/captures_count/range_inclusive 字段 | ✅ |
| hir/lower.th — closure/array/tensor/move/assign_op/deref 降低 | ✅ |
| parser/parser.th — 闭包/数组/张量/move/复合赋值解析 | ✅ |
| compile/wasm.th — disc 22-30 的 WASM 编译处理器 | ✅ |
| lexer/lexer.th — `/* */` 块注释支持（含嵌套） | ✅ |

#### 测试覆盖提升

| 测试文件 | 新增项 | 状态 |
|----------|--------|------|
| autodiff_test.rs | 20 项（autodiff 8 + closure 4 + tensor 7 + error span 1） | ✅ |

#### 示例集

| 变更 | 数量 |
|------|------|
| 新增示例 | 8（归并排序/二叉搜索树/闭包捕获/Softmax回归/Adam优化器/张量广播/词频统计/矩阵转置与运算） |
| 优化示例 | 18（while→for-in, 闭包捕获增强） |
| 总计 | 33 个示例 |

---

### v0.3.3 日常体验痛点第二批（2026-07-11）

> 总师统筹的第二批修复，聚焦日常体验。6 项中 5 项已在之前 session 实现，本次补全问题12 的源码位置传递。

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 4 | `char` 类型无法构造（lexer 无 `read_char`） | ✅ 已实现（之前 session） | `lexer.rs:318` read_char 已存在，`'a'` 可正确解析为 `CharLiteral(char)` |
| 6 | 无十六进制/二进制/八进制字面量 | ✅ 已实现（之前 session） | `lexer.rs:99-151` 支持 `0xFF`/`0b1010`/`0o777`，含下划线分隔 |
| 12 | 运行时错误无源码位置 | ✅ 本 session 补全 | `error.rs:70-75` RuntimeError 已有 `line/col: Option<usize>` 字段（之前 session 改的），但所有创建点都是 `None`。本次在 `eval.rs` 的 `eval_expr`/`eval_stmt` 返回处加 `fill_span` map_err 自动补全位置，3 处 `return Err` 手动补 span |
| 13 | `HashMap.get()` 缺键崩溃 | ✅ 已实现（之前 session） | `methods.rs:678` `or(Some(Value::Unit))` 缺键返回 Unit 而非 None |
| 14 | 错误信息中英混杂 | ✅ 已实现（之前 session） | `scope.rs` 等全部统一为中文 |
| 28 | 无 `Never`/`!` 发散类型 | ✅ 已实现（之前 session） | `types.rs:42-45` Type::Never + `lower/types.rs:644-653` 类型推断（Never 可统一到任何类型） |

**本次修改**：`runtime/interpreter/eval.rs` — eval_expr/eval_stmt 末尾加 `.map_err(|e| Self::fill_span(e, &expr.span))`，新增 `fill_span` 函数用 `Option::or` 补全位置（已有位置不覆盖）。3 处 `return Err(RuntimeError { line: None, ... })` 改为 `Some(expr.span.line/col)`。

**验证**：`cargo build --release` 成功 | lib 23 + 集成 121 passed | 自举 `[OK]`

**自举同步**：问题4/6 的 lexer 改动无需同步 tenthc——tenthc/main.th 不使用 char 字面量或进制字面量，能力差异不影响自举。

---

### v0.3.3 真 Bug 修复第一批（2026-07-11）

> 总师统筹的第一批真 Bug 修复，聚焦行为正确性。4 项局部修复，不触及护城河。

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 7 | `1 == 1.0` 永远 false（`values_eq` 缺 `(Int,Float)` 分支，与 `<`/`>` 行为不一致） | 补齐 `values_eq` 所有数值类型组合，与 VM `vm_eq` 对齐 | `runtime/interpreter/binary.rs:317-339` |
| 8 | `f32` 的 `==` 永远 false（`values_eq` 缺 `(Float32,Float32)` 分支） | 同上 | 同上 |
| 15 | VM 失败后静默回退到解释器，副作用双重执行（println 两遍、文件写两次） | 新增 `TenthError::VmCompileFailed` 区分编译期失败（静默回退）与运行时失败（硬失败） | `error.rs:73-78` + `main.rs:162-176,348-354` |
| 16 | 解释器路径输出 `= ()`（VM 路径过滤了 Unit，解释器没过滤） | 三处解释器输出均加 `if !matches!(val, Value::Unit)` 过滤 | `main.rs:184-187,348-354,369-372` |

**验证**：`cargo build --release` 成功 | lib test 23 passed | 关键集成测试 87 passed（integration/vm_autodiff/enum/ownership/type_inference/pattern_match/tuple/try_operator/generic/f32_parity/f32_runtime）| 自举 `[OK]`

**注意**：问题7、8 的 VM 路径 `vm_eq` 本就正确，仅解释器路径 `values_eq` 有缺陷。由于问题15 将运行时失败改为硬失败，解释器路径仍作为编译期失败的 fallback，因此 `values_eq` 的修复仍然必要。

---

### v0.3.3 新增（2026-06-14）

#### GPU 后端脚手架

| 组件 | 状态 |
|------|------|
| `compile/gpu/` — CudaKernel 模板 + Device 抽象 | ✅ 脚手架 |
| `compile/optimizations/` — FusionPass / ParallelPass | ✅ 脚手架 |

#### tenthpm 包管理器

| 组件 | 状态 |
|------|------|
| `tenth/tools/tenthpm/` CLI (init/build/test/run/add/remove/list/clean/publish/install) | ✅ **完整实现** |
| Tenth.toml manifest 格式 (含 license 字段) | ✅ |
| 共享引擎模块 (engine.rs) — search_paths + in-process 编译/运行 | ✅ |
| 依赖类型：registry / path / git 三种 | ✅ |
| Tenth.lock 锁文件 (含 checksum) | ✅ |
| .tenthpkg 打包归档 (publish) | ✅ |
| 版本号校验 (X.Y.Z semver) | ✅ |

#### LSP 服务器

| 组件 | 状态 |
|------|------|
| `tenth/tools/lsp/` — 诊断/悬停/补全/定义/格式化 handler | ✅ 脚手架 |

#### 标准库补全

| 组件 | 状态 |
|------|------|
| optim/adam_step 实现 | ✅ |
| optim/adagrad_step 实现 | ✅ |
| optim/rmsprop_step 实现 | ✅ |
| nn/batchnorm 函数 | ✅ |
| nn/conv2d 函数 | ✅ |
| nn/embedding 函数 | ✅ |
| init/ 6 个初始化器 (zeros/ones/xavier_uniform/xavier_normal/kaiming_uniform/kaiming_normal) | ✅ |

#### 语言增强

| 组件 | 状态 |
|------|------|
| 结构体字段默认值 — `Expr { kind: "Int", ival: 42, .. }` 语法 | ✅ |
| 泛型返回类型 — `fn f() -> Vec<Token>` 正确解析，修复 `>>` 拆分 | ✅ |
| 枚举元组变体 — `enum TokenKind { IntLiteral(i64), Plus, Eof }` + match 绑定 | ✅ |

#### 测试覆盖提升

| 测试文件 | 变更 | 状态 |
|----------|------|------|
| enum_test.rs | 5→9（+4 枚举元组变体/match 绑定） | ✅ |
| generic_test.rs | 5→11（+6 泛型返回/Vec<Token>/>>拆分） | ✅ |
| struct_test.rs | 5→8（+3 字段默认值/..语法） | ✅ |
| 总计 | 121→499（+378，含 v0.3.1 autodiff + v0.3.3 LSP/tenthpm/类型推断/模式匹配/迭代器/错误恢复/MNIST/parity/shape_check） | ✅ 498 passed + 1 ignored |

---

### v0.3.3 f32 真泛型改造（2026-06-29）

#### 问题根因
`Type::Tensor { dtype: BaseType, .. }` 的 dtype 字段是 `BaseType` 枚举值，无法表达 TypeParam。导致 std/nn/ 下 13 个文件出现 `_f32` 双胞胎副本（且副本的 Tensor 参数仍是 f64——f32 支持是假的）。这是类型系统表达力不足的系统性 DRY 违反。

#### 阶段 1：Rust 侧 Type 枚举改造
- `types.rs`：`Type::Tensor.dtype` 从 `BaseType` 改为 `Box<Type>`，dtype 现在可以是 TypeParam
- 新增 `Type::tensor_dtype() -> Option<BaseType>` helper（运行时取回 BaseType，TypeParam 场景返回 None）
- `from_annotation`：删除非 Base dtype 强制 fallback 到 F32 的逻辑，TypeParam 保留
- `substitute_type`：递归处理 Tensor 的 dtype 字段（+ Array/Generic）
- 适配 ~20 处 match 模式：lower/types.rs(15)、lower_expr.rs、mod.rs、bytecode.rs、value.rs、interpreter/mod.rs
- 采用 `dtype.as_ref().clone()` 保留 TypeParam 信息，比硬 fallback 到 F64 更准确

#### 阶段 2：tenthc 侧（无需改造）
- 验证发现 tenthc parser 不解析 `Tensor[T, ..]` 泛型类型注解（只处理具体 dtype）
- tenthc 自身的 .th 源码不用泛型 Tensor
- parity_test 129/129 + selfhost_frontend 4/4 全通过 → 路径 B/C 未断
- 结论：tenthc 侧 HirType.sub 整数编码足以处理当前所有 tenthc 能编译的代码

#### 阶段 3：std/nn 重写
- 删除 11 个 `_f32` 副本函数，7 个数据计算函数改泛型：
  - `transformer_encoder_block<T>`、`multihead_attention<T>`、`scaled_dot_product_attention<T>`
  - `layer_norm<T>`、`feedforward<T>`、`dropout<T>`、`batchnorm<T>`
- `make_*` 构造函数保留 f64（内部依赖 `randn`/`zeros` native，不支持泛型实例化）
- `positional_encoding` 保留 f64（同理）
- `binary_cross_entropy` 保留 f64（标量函数，Tenth 不支持 `<T: Float>` trait bound 强制检查）
- mask 参数保留 `Tensor[f64, ..]`（元数据语义，非数据）
- 顺带修正：transformer.th 第 57 行注释"lexer 不支持科学计数法"已过时（lexer.rs 第 127-147 行实际支持 `1e-5`），删除该注释
- prelude.th 索引同步：删除已删除的 _f32 引用，标注泛型函数

#### 验证（全绿）
- cargo build：0 error（177 pre-existing warnings，非本轮引入）
- 自举路径 A：`[OK] Full compiler compiled to tenthc_full.wasm`
- parity_test：129/129（路径 B/C 未断）
- selfhost_frontend：4/4（真实 assert 通过）
- stdlib_test：114/114（nn 模块解析全通过）
- generic_tensor_test：2/2（新增，验证泛型 Tensor 实例化）

#### 新增测试
- `tenth/tests/generic_tensor_test.rs`：验证 `fn foo<T>(x: Tensor[T, ..])` 调用 `foo<f64>(a)` 被正确实例化为 `foo_F64(x: Tensor[f64, ..])`，TypeParam T 被正确替换到 Tensor.dtype 字段

#### 已知限制（非本轮引入）
- native 构造函数（`randn`/`zeros`/`ones`）不支持泛型实例化（`GenericCall` 查 `generic_funcs` 表，native 不在表中）
- trait bound（`<T: Float>`）语法支持但语义不强制检查（`generics_bounds: HashMap::new()` 空表）

---

### v0.3.3 技术债清理第一类（2026-06-29）

#### selfhost_frontend.rs 测试 assert 化
- 原状：`tenthc_parses_own_source`/`tenthc_lowers_own_source` 在 Err 分支只 `println!` 不 `assert!`（注释"Phase B is about identifying gaps"），测试永远不会失败，等于没有护栏
- 修复：Err 分支改为 `.expect("...self-hosting frontend contract")`，让 parse/lower 失败真正触发测试红
- 头注释更新：移除过时的"Phase B"措辞，指明执行覆盖由 `fixpoint_runtime.rs`/`parity_test.rs` 提供
- 验证：4/4 通过（parse 182 items、lower 145 functions，真实 assert 通过而非 println 假绿）

#### AUDIT §7.4 #16 测试盲区条目已过时 → 标记修复
- AUDIT 原写"tenthc_test.rs 只测解析未测执行"——名字对不上（实为 selfhost_frontend.rs），且执行覆盖已由 `fixpoint_runtime.rs`（Wasmtime 端到端）+ `parity_test.rs`（112 项 Rust/tenthc 一致性）提供
- §7.4 #16 和 §8.1 "tenthc_test.rs 加执行测试" 均标记为已修复

#### lower.rs（2017 行）上帝文件拆分
- 拆为 `tenth/src/hir/lower/` 目录模块 7 文件：mod.rs(144) / scope.rs(120) / import.rs(56) / lower_expr.rs(691) / types.rs(240) / lower_stmt.rs(475) / closures.rs(187)
- 纯重构不改行为；Lowerer 公开 API（new/with_search_paths/lower_program）签名不变
- 修复 3 个编译错误：Ownership 导入路径、Type 私有枚举改用 types 模块、Scope.parent 字段改 pub(super)

#### wasm.rs（2089 行）上帝文件拆分
- 拆为 `tenth/src/compile/wasm/` 目录模块 6 文件：mod.rs(128) / types.rs(69) / sections.rs(181) / compile.rs(942) / closures.rs(340) / host.rs(355)
- 纯重构不改行为；`register_host_functions`/`run_wasm_module` 通过 `pub use` 重导出，外部引用零改动
- IMPORT_COUNT=18 注释完整保留；自举路径 C（全 WASM 闭环）未受影响

#### 文档漂移批量修复
- `能力全梳理.md` §2.2：JIT 状态从"未实际使用"改为"默认启用，保守策略（autodiff/复杂操作回退 VM）"——与 main.rs:292 默认调用 `jit::run_jit` 的事实对齐
- `CODE_WIKI.md`：删除不存在的 `boot.th` 引用（项目根目录 Glob 确认无此文件），main.th 描述改为"拼接 6 个模块源码后调用 compile_host"
- `README.md`：shape 检查卖点标注"规划中，当前未实现"（与能力全梳理 ❌ 状态对齐，消除内部矛盾）；transformer 改为 transformer_encoder_block（与实际文件内容对齐）；LSP 从 ✅ 降为 ⚠️ 并注明"AST 符号+关键字，非语义补全"
- `AUDIT.md` §7.4 #16 和 §8.1 tenthc_test.rs 条目标记已修复

**验证**：cargo build（debug）通过，177 pre-existing warnings（hostcalls.rs 函数指针转换，非本轮引入）；selfhost_frontend 4/4 真实 assert 通过；自举路径 A `[OK] Full compiler compiled to tenthc_full.wasm`。未运行全量 cargo test（rustc 1.95.0 ICE bug，非本项目问题）。

**未触及**：f32 双胞胎（14 个文件）属"伪装成技术债的设计决策"，清理等于做 A 类类型系统改造，本轮冻结不处理。

---

### v0.3.3 重构（2026-06-29）

#### interpreter.rs 上帝文件拆分

| 组件 | 状态 |
|------|------|
| `runtime/interpreter.rs`（4655 行）→ `runtime/interpreter/` 目录模块（8 个文件） | ✅ 纯重构 |
| `mod.rs`（1202 行）— Interpreter 结构体、eval_expr/eval_call/eval_stmt | ✅ |
| `binary.rs`（426 行）— eval_binary/eval_unary/values_eq/value_to_string | ✅ |
| `pattern.rs`（180 行）— eval_field/pattern_matches/bind_pattern/unbind_pattern | ✅ |
| `methods.rs`（1312 行）— 方法分派（String/Vec/Map/Range/Iterator/Tensor/Scalar） | ✅ |
| `index.rs`（121 行）— eval_index（String/Tensor/Vec 索引与切片） | ✅ |
| `natives.rs`（1253 行）— call_named_fn（原生函数分派） | ✅ |
| `json.rs`（205 行）— JSON 编解码（H-6 安全版本，带深度闸门+转义状态机修复） | ✅ |
| `datetime.rs`（28 行）— days_to_date（Unix 天数→年月日） | ✅ |
| main.rs 删除 days_to_date/json_encode_value/json_decode_string 等函数定义（226 行） | ✅ |
| main.rs 调用点改为 `datetime::days_to_date` / `json::json_encode_value` 等模块前缀 | ✅ |
| H-6 漏洞修复 — interpreter 内部 JSON 解析改用 main.rs 安全版本 | ✅ |
| cargo build（debug + release）无新增 warning | ✅ |
| 自举路径 A 验证 — `[OK] Full compiler compiled to tenthc_full.wasm` | ✅ |

**重构原因**：interpreter.rs 是 4655 行的上帝文件，难以维护和导航。拆分为目录模块后，每个文件聚焦单一职责，便于定位和修改。

**范围**：纯重构，不改任何行为（唯一例外：JSON 解析改用安全版本，顺带修复 H-6 漏洞）。方法签名保持不变，只改可见性（`fn` → `pub(super) fn`）。Interpreter 结构体字段可见性保持不变。

---

### v0.3.0 后期新增（2026-06-14）

#### 自动微分

| 组件 | 状态 |
|------|------|
| 张量级 Wengert tape (21 算子) | ✅ |
| Backward 全链路 (DAG 遍历 + broadcast grad) | ✅ |
| 解释器 recording 模式 | ✅ |
| 7 个内置函数 (new_grad/param/backward/grad/stop_grad/zero_grad/cross_entropy) | ✅ |
| Mean/Sum 录制 (返回张量) | ✅ |
| Scalar-tensor 录制 (标量自动包装) | ✅ |

#### 张量运算

| 组件 | 状态 |
|------|------|
| 张量间四则运算 (广播语义) | ✅ |
| MatMul (1D/2D) | ✅ |
| Transpose | ✅ |
| Conv2D (im2col + backward) | ✅ |
| Dropout (inverted + backward) | ✅ |
| Softmax (逐行) | ✅ |
| BatchNorm (backward 就绪, forward 已包装) | ✅ |

#### 标准库

| 模块 | 文件数 | 状态 |
|------|--------|------|
| nn/ | 13 | 全部可运行 (linear/loss/activations/dropout/conv/batchnorm/embedding/attention/multihead_attention/layer_norm/positional_encoding/feedforward/transformer) |
| optim/ | 4 | 全部可运行 (sgd/adam/adagrad/rmsprop) |
| data/ | 1 | DataLoader (new/has_next/next_batch/reset/num_batches) |
| init/ | 1 | 6 个初始化器实现 (zeros/ones/xavier_uniform/xavier_normal/kaiming_uniform/kaiming_normal) |
| collections/ | 2 | iter (map/filter/reduce/zip/enumerate 等), collections (flat_map/partition 等) |
| string/ | 1 | 字符串工具 (join_lines/join_comma/indent/word_wrap/capitalize 等) |
| utils/ | 2 | 序列化 (save_model/load_model/save_checkpoint), math (min/max/clamp/signum 等) |
| math/ | 1 | 数学函数参考 |

#### 语言打磨

| 组件 | 状态 |
|------|------|
| 块注释 /* */ (支持嵌套) | ✅ |
| Vec: pop/set/clear | ✅ |
| String: trim/split/replace/substring/to_upper/to_lower | ✅ |
| REPL 多行输入 (自动续行) | ✅ |
| 错误源码上下文显示 | ✅ |
| 数组字面量 [1,2,3] | ✅ 已原生存在 |
| for-in 循环 (Range/Vec/Tensor) | ✅ |

---

## 跳过项：Phase 4-6 GPU / 分布式 / 生态

> 以下功能因环境限制暂跳，条件就绪后实施。

### Phase 4: GPU 与性能

- [ ] 安装 CUDA Toolkit 12.6（`nvidia-smi` 显示驱动已支持，RTX 4060 8GB）
- [x] ~~实现 CUDA kernel 模板代码生成~~ → `compile/gpu/` 脚手架已就绪 (CudaKernel 模板 + Device 抽象)
- [ ] MIR→CUDA 算子映射 + `tenth compile --target=cuda`
- [x] ~~算子融合~~ → `compile/optimizations/` 脚手架已就绪 (FusionPass/ParallelPass)
- [ ] 自动并行分解 / SPMD 降级（需多 GPU）

### Phase 5: AI 全栈

- [ ] SPMD 并行原语（数据并行、模型并行、流水线并行，需多 GPU）
- [ ] 分布式通信（MPI/NCCL：all_reduce, all_gather, send/recv）
- [ ] Autodiff 进阶：checkpointing、高阶微分、张量级 tape、解释器集成
- [ ] nn 进阶：LayerNorm, Attention, FlashAttention, GELU
- [ ] optim 进阶：AdamW, Lion, LAMB, lr_schedule, gradient clipping
- [ ] data 标准库：DataLoader, 数据增强, pipeline 宏

### Phase 6: 生态与工具

- [x] ~~包管理器 tenthpm~~ → `tenth/tools/tenthpm/` **完整实现** (CLI: init/build/test/run/add/remove/list/clean/publish/install + Tenth.toml + Tenth.lock + .tenthpkg 打包 + path/git/registry 依赖)
- [x] ~~LSP 服务器~~ → `tenth/tools/lsp/` **完整实现** (文档同步/diagnostics推送/hover/completion/definition/documentSymbol/references/rename/signatureHelp/foldingRange/semanticTokens/formatting)
- [ ] 调试器进阶（断点插桩、调用栈、条件断点）
- [ ] 官网 / 论坛 / RFC 流程 / 贡献指南

---

## 环境配置

> 环境依赖、网络代理、构建/测试命令详见 `DEPS.md`。

---


