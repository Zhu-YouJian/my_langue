[package]
name = "tenth-prof"
version = "0.1.0"
edition = "2024"
description = "Tenth profiler (CLI, VM-based): function-level timing & call counts, hot spot report"

[[bin]]
name = "tenth-prof"
path = "src/main.rs"

[dependencies]
tenth = { path = "../.." }
