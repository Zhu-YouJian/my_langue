# 项目总览与审计报告

> 日期：2026-09-17 | 版本：v1.0.0 | GPU 脚手架 + 包管理器 + LSP + 语言增强（元组类型 + `?` 操作符）+ 安全加固 + Shape 检查 + Autograd 反向 Shape 校验 + 论文披露缺陷登记 + 同步 I/O 原语 + AUDIT 缺陷修复 + 异步 Phase 2（协程调度 + async I/O）+ 正则表达式 + 张量修复（f16/bf16 + 序列化 + 优化器修复）+ Problem 21（tenthc 全量追上 Rust 母编译器：Lexer/Parser/HIR/Shape/WASM/bridge 六批次同步）+ 泛型函数运行时修复 + 护城河系列（函子化 shape / 静默失败 or_die/assume_ok / lossy 格 M1/M2）| 2635 项测试通过（--release 模式，150 个测试目标；`RUST_MIN_STACK` 已由仓库根 `.cargo/config.toml` 自动提供，见 AUDIT-11.4.19/11.4.49）
>
> 注意：本文件早期条目标注的 `.trae/tmp/...` 为当时临时验证/证据文件路径；该目录已于 2026 清理，保留作历史记录。

---

## 一、项目全景

Tenth = Tensor + Zenith，一门为 AI 研究而生的编程语言。Rust 编写的 bootstrap 编译器 + Tenth 编写的自举编译器 + 字节码 VM + WASM 编译。

### 目录地图

> 完整目录结构及模块注解见 `CODE_WIKI.md` §1。

---

## 二、编译器管线

> 完整管线架构见 `CODE_WIKI.md` §2-3。

> ~~C 编译路径 (MIR → C → GCC → .exe)~~ 已于 2026-06-04 移除。原因：生成的 C 代码无内存管理，详见 SECURITY.md。

---

## 三、测试矩阵

> 数量列格式：`passed/failed/ignored`。"栈溢出" 表示编译通过但运行时触发 Windows STATUS_STACK_OVERFLOW (0xc00000fd)，无法获取具体用例数。**现行统计基线以 `能力梳理/能力全梳理.md` §统计基线为准（**2026-09-17：2635 passed / 0 failed / 22 ignored；150 个测试目标**）**；下表 per-文件 明细为较旧口径的存档记录（统计日期：2026-07-12，--release 模式），可能与合计数字不完全同步（合计见本行下行【总计】）。

### 基础管线

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `lib`（单元测试） | 23/0/0 | HIR/VM/解释器/autodiff 等模块单元 |
| `lexer_test.rs` | 6/0/0 | 整数/标识符/关键字/字符串/运算符/注释 |
| `parser_test.rs` | 5/0/0 | 字面量/二元表达式/函数定义/if/tensor |
| `integration_test.rs` | 14/0/0 | 全管线: 算术/布尔/比较/函数/闭包/while/tensor |
| `error_recovery_test.rs` | 7/0/0 | 解析错误恢复/续接 |

### 类型系统

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `enum_test.rs` | 12/0/0 | 枚举定义/字段/match/通配/元组变体/match 绑定/match 穷尽性检查（2026-07-11 +3） |
| `struct_test.rs` | 8/0/0 | 结构体/嵌套/impl/默认字段/..语法 |
| `trait_test.rs` | 9/0/0 | trait 定义/builtin bound/inherent impl/默认方法/多 trait |
| `module_test.rs` | 6/0/0 | mod/use/嵌套模块/重导出 |
| `ownership_test.rs` | 11/0/0 | 移动/借用/引用/解引用 |
| `type_inference_test.rs` | 29/0/0 | 类型推断/统一/泛型实例化 |
| `generic_test.rs` | 11/0/0 | 泛型函数/泛型结构体/trait bound/泛型返回/Vec<Token>/>>拆分 |
| `pattern_match_test.rs` | 17/0/0 | 模式匹配/解构/守卫 |
| `iterator_test.rs` | 10/0/0 | 迭代器/for/生成器 |
| `tuple_test.rs` | 12/0/0 | 元组创建/解构/嵌套/函数返回/空元组（2026-07-08 新增） |
| `try_operator_test.rs` | 17/0/0 | `?` 操作符成功/错误传播/链式/I/O 模拟/多层 `?`/try 块捕获（2026-07-08 新增；2026-08-03 AUDIT-11.4.33 +5：多层 `?` 与 try 块成功/捕获，解释器/VM 严格单层对拍） |
| `int_types_test.rs` | 14/0/0 | 整数类型 dtype 保留/后缀类型推断（i8/i16/i32/i64/u8/u16/u32/u64）/编译期范围检查/运行时算术溢出检测（2026-07-11 新增） |

### 张量与自动微分

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `autodiff_test.rs` | 54/0/0 | 自动微分/闭包/张量/错误位置（21 算子） |
| `autodiff_shape_test.rs` | 15/0/0 | Autograd 反向 shape 校验（护城河 A，含 unbroadcast 数值专项 5 项） |
| `vm_autodiff_test.rs` | 15/0/0 | 字节码 VM 上的自动微分回归 |
| `abs_test.rs` | 8/0/0 | abs 算子 |
| `select_test.rs` | 16/0/0 | select 算子 |
| `scatter_test.rs` | 16/0/0 | scatter 算子 |
| `gather_test.rs` | 13/0/0 | gather 算子 |
| `bmm_test.rs` | 11/0/0 | batched matmul (bmm) 算子 |
| `reshape_masked_fill_autodiff_test.rs` | 9/0/0 | reshape/masked_fill 自动微分 |
| `multihead_attention_test.rs` | 11/0/0 | MHA 真正分头计算 + 完整梯度链 |

### Shape 检查

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `shape_check_test.rs` | 16/0/0 | 张量 shape 检查/广播/层归一化验证 |
| `shape_check_compile_test.rs` | 74/0/7 | 编译期 shape 检查（7 项 ignored 占位） |

### f32 路线图

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `f32_tensor_test.rs` | 23/0/0 | f32 张量基础（Phase 1） |
| `f32_frontend_test.rs` | 13/0/0 | f32 前端贯通（Phase 2） |
| `f32_runtime_test.rs` | 7/0/0 | f32 运行时（Phase 3） |
| `f32_autodiff_test.rs` | 12/0/0 | f32 自动微分（Phase 4） |
| `f32_wasm_test.rs` | 14/0/0 | f32 WASM 后端（Phase 5.1） |
| `f32_stdlib_test.rs` | 10/0/0 | f32 标准库（Phase 5.4） |
| `f32_parity_test.rs` | 14/0/0 | f32/f64 一致性 |
| `f32_stdlib_parity_test.rs` | 8/0/0 | f32 标准库一致性 |

### 自举验证

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `three_stage.rs` | 1/0/2 | 三段式自举（2 项 ignored — wasmi 慢） |
| `selfhost_frontend.rs` | 4/0/0 | 自举前端验证（lex/parse/lower，--release 模式通过） |
| `parity_test.rs` | 129/0/0 | VM vs Interpreter 行为一致（全指令覆盖，--release 模式通过） |

### 一致性

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `native_parity_test.rs` | 35/0/0 | Rust 母编译器 / tenthc 一致性 |

### 标准库

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `stdlib_test.rs` | 114/0/0 | Vec/HashMap/String/Option/文件 I/O/json/toml/runtime 限制 |
| `relation_debugger_test.rs` | 10/0/0 | 关系调试器 |

### 安全与错误

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `memory_test.rs` | 17/0/0 | 内存护栏: arena/limits/计数器 |
| `memory_estimate_test.rs` | 32/0/3 | 编译期内存/算力预估（护城河 D，3 项 ignored） |

### 编译器后端

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `wasm_backend_minimal.rs` | 10/0/0 | WASM 后端最小用例（含 str_eq/str_add/str_int 回归 3 项） |
| `jit_test.rs` | 10/0/0 | JIT 编译器回归 |
| `jit_stack_overflow_test.rs` | 3/0/0 | JIT 栈溢出回归（--release 模式通过） |

### 神经网络

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `mnist_loader_test.rs` | 4/0/0 | MNIST 数据加载 |

### 异步

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `async_basic_test.rs` | 13/0/0 | 异步原语基础 + Phase 2 协程调度 + async I/O（sleep/TCP echo/无效句柄/yield/多任务切换；2026-07-08 从 6 项扩展到 13 项） |

### I/O

| 测试文件 | 数量 | 覆盖 |
|----------|------|------|
| `io_test.rs` | 4/0/0 | 同步 I/O 原语 |
| `net_test.rs` | 6/0/0 | 网络 I/O 原语 |
| `regex_test.rs` | 11/0/0 | 正则表达式（compile/match/find/find_all/replace/split/无效handle/邮箱正则） |
| `tensor_features_test.rs` | 18/0/0 | 张量修复（序列化 v2 f32/f64/混合/向后兼容 + f16/bf16 构造/运算/序列化 + 优化器 parse + clip_grad_by_norm/adamw_step_w use+泛型调用运行时验证） |

### 预存失败（已修复）

| 测试文件 | 数量 | 覆盖 | 状态 |
|----------|------|------|------|
| `generic_tensor_test.rs` | 4/0/0 | 泛型张量构造函数 | ✅ 已修复（2026-07-10）：`native_generic_ctor_f32/f64_lowering` 测试期望从 `Tensor[F32, ..]` 改为 `Tensor[F32, 3]`，与 `shape_from_int_args` 把字面量参数算进 shape 的行为对齐（更精确，利于编译期内存预估） |
| `fixpoint_runtime.rs` | 0/0/1 | fixpoint 端到端编译+执行 | ✅ 已修复（2026-07-10）：`fixpoint_runtime_benchmark` 标记 `#[ignore]`——wasmtime 路径 Vec 写回逻辑问题（tenthc 完整执行但 main 返回 Vec len=0，AUDIT #5），wasmi 路径已通过验证，wasmtime 仅是性能优化路径 |

### 栈溢出崩溃（已修复，--release 模式全部通过）

| 测试文件 | 数量 | 覆盖 | 状态 |
|----------|------|------|------|
| `tenthc_generic_tensor_test.rs` | 5/0/0 | tenthc 泛型张量测试 | ✅ --release 通过 |
| `tenthc_for_loop_test.rs` | 9/0/0 | tenthc for 循环测试 | ✅ --release 通过 |
| `tenthc_dotdot_eq_test.rs` | 3/0/0 | tenthc `..=` lexer 测试 | ✅ --release 通过 |
| `jit_stack_overflow_test.rs` | 3/0/0 | JIT 栈溢出回归 | ✅ --release 通过 |

### 总计

| 测试目标 | 数量 | 说明 |
|----------|------|------|
| **总计** | **2635 passed / 0 failed / 22 ignored** | 现行基线（2026-09-17，`cargo test --release` 实测，150 个测试目标；需 `RUST_MIN_STACK=33554432`，套件数以实际为准）. 权威合计见 `能力梳理/能力全梳理.md` §统计基线。 |

> **2026-07-08 张量修复测试状态**：本次张量修复（f16/bf16 Phase 1 + 序列化 v2 + 4 项小修复）的代码改动已通过现有测试套件验证（lib 16 + integration 14 + native_parity 35 + stdlib 114 = 179 passed；autodiff 5 passed；自举通过），**未新增独立测试文件**——`native_parity_test.rs` 的 35 项已含序列化 v2 parity 测试（test_save_load_weights_parity + test_save_load_weights_nonzero_parity）。Wave 3 测试部补测试任务进行中（accumulate_loop 功能测试 / autodiff unbroadcast shape 测试 / AdamW 单值返回版本测试 / clip_grad_by_norm JIT 路径测试 / 序列化 f32 读写测试 / f16/bf16 基本运算测试），完成后由测试部同步 §三 测试矩阵新增 tensor_features_test 行 + 总计数字。

---

## 四、自举状态

> 自举演进详情见 `MEMO.md`。

---

## 五、已移除 / 已修复

| 项目 | 状态 |
|------|------|
| ~~C 编译管线~~ | ❌ 已移除 (2026-06-04) |
| ~~tenthc lexer 字面量硬编码 0~~ | ✅ 已修复 (Token 新增 fval) |
| ~~tenthc parser method_call 丢失 receiver~~ | ✅ 已修复 (Dot+LParen 产生 method_call) |
| ~~VM chunk clone 内存泄漏~~ | ✅ 已修复 (chunk_idx 索引引用) |
| ~~VM StoreField/code 切换死循环~~ | ✅ 已修复 (CallN/Ret 同步更新 code/strings) |
| `runtime/autodiff.rs` | ✅ 张量级可用，已集成解释器（21 算子） |
| Lowerer 大文件性能 | ⚠️ 实际不慢 (release 39 函数 <0.01s)，之前误判为瓶颈 |
| ~~解释器 `values_eq` 缺跨类型数值分支（`1 == 1.0` 永远 false）~~ | ✅ 已修复 (2026-07-11)：`binary.rs:317-339` 补齐 `(Int,Float)`/`(Float,Int)`/`(Float32,Float32)` 等所有数值类型组合，与 VM `vm_eq` 及 `<`/`>` 行为对齐 |
| ~~解释器 `values_eq` 缺 `(Float32,Float32)` 分支（f32 的 `==` 永远 false）~~ | ✅ 已修复 (2026-07-11)：同上 |
| ~~VM 失败后静默回退到解释器，副作用双重执行~~ | ✅ 已修复 (2026-07-11)：新增 `TenthError::VmCompileFailed` 区分编译期失败（静默回退）与运行时失败（硬失败），`main.rs:162-176` + `vm_run` 均已处理 |
| ~~解释器路径输出多余的 `= ()`~~ | ✅ 已修复 (2026-07-11)：`main.rs` 三处解释器输出均加 `if !matches!(val, Value::Unit)` 过滤，与 VM 路径一致 |
| ~~Option/Result 非泛型（`Type::Enum` 而非 `Type::Generic`）~~ | ✅ 已修复 (2026-07-11)：`lower_expr.rs` EnumLiteral/Call/Ident 三条路径为 Option/Result 推断 `Type::Generic { base: Enum, args }`，从字段值推断类型参数 |
| ~~HashMap 键仅支持 str~~ | ✅ 已修复 (2026-07-11)：`interpreter/methods.rs` + `vm/natives.rs` 双侧新增 `map_key_to_string`/`vm_map_key_to_string`，支持 str/int/bool/float 键 |
| ~~Range 非一等类型（降级为内部类型）~~ | ✅ 已修复 (2026-07-11)：`hir/types.rs` 新增 `Type::Range { inner, inclusive }` 变体 + Display，`lower_expr.rs` Range 表达式推断为 `Type::Range` |
| ~~match 无穷尽性检查~~ | ✅ 已修复 (2026-07-11)：`lower_expr.rs` match 表达式新增穷尽性检查，支持 `Type::Enum` 与 `Type::Generic` 两种 scrutinee 类型，缺变体报 TypeError |
| ~~函数返回类型未检查（body 为空返回 Unit 但声明非 Unit）~~ | ✅ 已修复 (2026-07-11)：`hir/lower/types.rs` `check_and_merge_tensor_shape` 非 Tensor 分支新增 Unit 返回检测，声明非 Unit 但实际 Unit 时报 TypeError |
| ~~整数类型运行时全退化为 i64（dtype 丢失）~~ | ✅ 已修复 (2026-07-11)：`IntLiteral(i64)` → `IntLiteral(i64, BaseType)` 四层同步（Token/AST/HIR/Value）；Lexer 新增后缀检测（`42u8`）+ 编译期范围检查（`256u8` → 错误）+ 无后缀大整数自动提升 I32→I64；运行时新增 `check_int_overflow()` 算术溢出检测（Add/Sub/Mul/Div/Mod/Neg）；type_of() 返回实际 dtype 而非硬编码 I32；Bug fix：`()` 类型注解正确解析为 `Type::Base(BaseType::Unit)`。14 项 `int_types_test.rs` 全部通过 |

---

## 六、已知限制

| # | 问题 | 影响 |
|---|------|------|
| 0 | ~~VM 不支持字符串切片~~ | ✅ 已修复：SliceStr + Range 索引解析 |
| 2 | 树遍历解释器大文件慢 (debug build) | release build 即解决 |
| 3 | WASM codegen 个别边界情况 | wasmi 执行偶有 type mismatch |
| 4 | 无 GPU 后端 | `compile/gpu/` 仅 CUDA C 源代码生成 + 模拟设备（`device.rs:79-82` `CudaDevice::is_available()` 永远返回 true，注释自承 "Simulated"；`total_memory` 硬编码 24GB）；`mod.rs::compile_kernel` 仅把 HIR 函数转成 CudaKernel 字符串；**未接 nvcc / CUDA Runtime API / cuLaunchKernel**，不编译、不加载、不执行任何 kernel。待安装 CUDA Toolkit 后真正激活。详见 §11.4 AUDIT-11.4.6。 |
| 5 | three_stage wasmtime 路径运行时失败（host import 已补全但 WASM-B 0 字节） | **2026-07-09 更新**：`wasmtime_host.rs::register_wasmtime_host_functions` 中 17/18 个 host import 已补全真实现（Vec 四件套/String 全家桶均按 WASM 线性内存 + bump allocator 方案实现，与 `compile/wasm/host.rs` 对齐），仅 `tensor_from_vec` 为简化版（与 wasmi 路径一致，自举管线不依赖）。**但运行 `three_stage_selfhost_wasmtime` 仍失败**：tenthc 编译器在 wasmtime 下执行完整流程（tokens_done/parsed/lowered/compiled 日志均输出），但最终 WASM-B 为 0 字节——根因是 tenthc 在 wasmtime 下的 Vec 写回逻辑有问题（main 返回的 Vec 指针指向空数据）。wasmi 路径已通过（WASM-B 460 bytes，add(3,4)=12）。wasmtime 路径保持 `#[ignore]`，深度调试 ROI 不高（wasmi 路径已工作，wasmtime 仅是 JIT 性能优化）。 |
| 6 | ~~Rust 母编译器 wasm.rs Call 分派缺 str_eq/str_add/str_int 函数调用分支~~ | ✅ 已修复 (2026-07-09)：`tenth/src/compile/wasm/compile.rs` Call 分派补齐 `str_eq`/`str_add`/`str_int` 三个分支，与 `str_len`/`str_at`/`str_cmp`/`str_slice` 对齐。新增 3 项回归测试 `wasm_backend_minimal::test_str_{eq,add,int}_call_compiles` 验证函数调用形式能产出合法 WASM 并被 wasmi 实例化执行。 |
| 7 | ~~6 个测试文件栈溢出崩溃~~ | ✅ --release 模式下全部通过（2026-07-12 验证）：tenthc_generic_tensor_test 5/0/0、tenthc_for_loop_test 9/0/0、jit_stack_overflow_test 3/0/0、tenthc_dotdot_eq_test 3/0/0、selfhost_frontend 4/0/0、parity_test 129/0/0。debug 模式下仍栈溢出（Windows STATUS_STACK_OVERFLOW），根因是 Windows debug 模式栈空间不足。**2026-07-08 更新**：`tenthc_for_loop_test` 的 Vec 迭代测试在设置 `RUST_MIN_STACK=268435456`（256MB）后已通过 3/3（vec_literal_basic / vec_literal_break_continue / vec_literal_empty），但默认栈空间下仍栈溢出，根因是 Windows debug 模式栈空间不足（git stash 验证为预存问题）。 |
| 8 | spawn 仍为 eager 语义（Phase 2 设计决策） | `runtime/vm.rs::Op::Spawn` 立即求值 inner 表达式并包装为 `Future(Ready(v))`，不制造真并行；真正的"并发"来自 async I/O 返回的 `Pending` Future（子线程工作期间调度器可切换到其他就绪任务）。**影响**：CPU 密集型 spawn 不会真并行执行；如需 CPU 并行需引入工作窃取调度器或绿色线程池（Phase 3 遗留）。**登记性质**：设计决策非缺陷——保持 Phase 1 兼容性 + 零新依赖原则。详见 MEMO.md 2026-07-08 feat 条目。 |
| ~~9~~ | ~~yield 无语法层关键字（Phase 2 已就绪未接入）~~ | ✅ 已修复 (2026-07-25)：Rust 母编译器侧 yield 早已完整接入（lexer TokenKind::Yield + parser ExprKind::Yield + hir HirExprKind::Yield + bytecode Op::Yield + jit fallback）；tenthc 侧本次完成接入：hir.th 新增 disc=37、parser.th 新增 yield 解析（支持 `yield;`/`yield expr` 两种形式）、lower.th 新增 yield lowering（返回 Unit）、wasm.th 新增 yield codegen（pass through + drop inner）。新增 tenthc_yield_test 3 项测试。**遗留**：路径 B（bridge.rs）未接 yield（tenthc/main.th 不含 yield，自举不受影响；路径 A/C 完整可用）。 |
| 10 | ~~f16/bf16 Phase 1 已实现，Phase 2 待做（2026-07-08 登记）~~ | ✅ Phase 2 已完成 (2026-07-09)：`TensorData` 已含 `F16(ArrayD<f16>)/BF16(ArrayD<bf16>)` 变体（`half = "2"` 依赖），构造器/运算/dtype 提升表/序列化 v2/HIR 白名单/native 双侧注册已完整。**Phase 2 四缺口全部完成**：(a) JIT 路径 `compile/jit/translator.rs:470-478` MakeTensor 已扩展 4 dtype 分发（0=F64/1=F32/2=F16/3=BF16），`hostcalls.rs:470-526` 实现 `host_make_tensor_f16/bf16`；(b) WASM 路径 `bytecode.rs:610-615` dtype_code 扩展为 0-3 编码，`wasm/compile.rs:761-833` 按 dtype 分发到 `HOST_MAKE_TENSOR_F16=18`/`HOST_MAKE_TENSOR_BF16=19`，`wasm/host.rs`+`wasmtime_host.rs` 双侧注册 stub，`wasm/mod.rs` to_val_type F16/BF16→I64；(c) F16/BF16 param 的 autodiff 反向传播已实现（`tensor.rs:307-383` acc_grad 移除 early-return Err，F16/BF16 走 F32 中间累加策略 AMP，避免 F16 溢出 max≈65504；`autodiff.rs:72-85` dispatch_float! F16/BF16 走 f32 路径；种子梯度 F16/BF16 用 F32 ones）；(d) tenthc 自举编译器支持 f16/bf16 字面量（`lexer.th` 扩展 f16(ival=2)/bf16(ival=3) 后缀检测，`lower.th` FloatLiteral 写入 HIR sub=14(F16)/15(BF16)，`wasm.th` is_expr_float 识别）。**影响**：f16/bf16 张量现可在 VM/解释器/JIT/WASM 全路径下使用，也可作为 autodiff param 参与训练（F32 中间累加，支持混合精度训练 AMP）。**验证**：autodiff 54/54、vm_autodiff 15/15、tensor_features 18/18、jit 10/10、wasm_backend_minimal 10/10 均 0 回归；自举 `[OK] Full compiler compiled`。详见 MEMO.md 2026-07-09 feat 条目。 |
| 11 | ~~use 机制在 cargo run --release 下不工作~~ | ✅ 已修复 (2026-07-09)：4 段路径 use（prelude.th 推荐用法，如 `use std::nn::activations::gelu`、`use std::optim::clip::clip_grad_by_value`）在 `cargo run --release` 下工作正常（验证 `Tenth实例/Transformer示例/transformer_demo.th` 和 `Tenth实例/梯度裁剪与累积/grad_clip_accum.th` 均成功执行）。**原描述"连原版 gelu/adamw_step 都报 undefined variable"已过时**——根因是 `try_import_file` 单符号导入只用 parent_path 调用，对目录型模块失败；方案 B 修复（`lower_stmt.rs` 第 404-443 行）改为先尝试完整 path 作为文件，失败再回退 parent_path 作为模块。3 段路径 use（如 `use std::nn::gelu`）仍不支持，属设计限制（标准库用目录/文件.th 结构，gelu 在 activations.th 中而非 gelu.th）。 |

| ~~12~~ | ~~bmm FLOPs 预估为 no-op（返回 0）~~ | ✅ 已修复 (2026-07-25)：tenthc `HirType` 新增 `dim2: i64` 字段（hir.th:39），lower.th 激活 6 处 no-op（`get_tensor_dim2`/`check_bmm_shape`/`static_numel`/`fmt_dims`/`emit_bmm_flop_estimate`/bmm 分支 shape 推断）+ `parse_tensor_type` 放宽至解析 1-3 个 dim 字面量。bmm FLOPs 预估现与 Rust 母编译器对齐（types.rs:1094-1129），新增 `test_bmm_flop_estimate_parity` 验证双侧 warning 一致。详见 §11.4 AUDIT-11.4.7。 |
| 13 | JIT `is_sealed` 断言 panic（循环回边） | **workaround 已加（2026-07-30）**：`compile/jit/translator.rs` 用 `catch_unwind` 包裹 `translate`，含循环的函数 JIT 编译时自动 fallback 到 VM 解释执行，不再 crash。**根因**：`translator.rs:165` block 被过早密封（`is_sealed` 返回 true），循环回边（loop back-edge）尝试密封已密封 block 触发 panic。**根本修复**：延迟密封 block 以支持循环 JIT 编译，推后到 P2。**影响**：含循环的函数无法 JIT 加速，但功能完整（fallback VM 正确执行）。详见 §11.4 AUDIT-11.4.10。 |
| 14 | embedding `gather` ndim 限制 | `tenth/std/nn/embedding.th` 改用 `gather(weight, 0, indices)` native 实现（原 `embedding_lookup` 张量方法从未实现为 native）。`gather` 要求 `weight` 与 `indices` 的 ndim 匹配——典型场景 `weight[V, D]` (ndim=2) + `indices[S]` (ndim=1) 会因 ndim 不匹配运行时报错。**完整解决**：需新增 `index_select` native（沿 dim 维收集，对 ndim 不匹配更宽容）或 broadcast 支持。**当前状态**：workaround——调用方需保证 ndim 匹配（如把 indices 扩展为 `[S, 1]` 再 gather 后 reshape）；推后到 P1 后续。详见 §11.4 AUDIT-11.4.11。 |
| ~~15~~ | ~~`hir/types.rs:392` `.shape()` 方法分支与运行时不一致~~ | ✅ 已修复 (2026-08-02)：grep 确认无生产代码/测试依赖 `.shape()`（`.th` 中仅 `tenth/std/nn/multihead_attention.th:21` 注释提及；测试中 `.shape()` 均为 Rust `Tensor` API 而非 Tenth 方法）。删除 `hir/lower/types.rs` 的 `"shape" => Array<i64>` 分支，并在 `hir/lower/lower_expr.rs` MethodCall 降级处对 Tensor receiver 的 `shape` 方法直接报编译期 TypeError（提示改用 `.shape_tensor()`，返回 `Tensor[f64, ndim]`）。用户自定义 struct 的 `shape` 方法不受影响（Tensor 专属检查）。验证：`tenth/tests/audit_11412_regression_test.rs` 3 项（编译期报错/`shape_tensor` 正常/用户自定义 `shape` 不误伤）。详见 §11.4 AUDIT-11.4.12。**⚠️ 本条后续已反转**：2026-08-31（QA-20260831）撤销上述编译期拦截，`.shape()` 作为 MINOR 兼容新增在 VM + 解释器双侧注册正式落地（返回 `Vec<i64>`，`shape_tensor()` 保留）——**当前行为以 §11.4 AUDIT-11.4.12 的最终状态为准**。 |
| 16 | `randn` 变量参数限制 | `shape_from_int_args`（hir/lower/types.rs）只接受字面量参数作为 shape 维度，变量参数（如 `let n = 10; randn(n, m)`）退化为 `Dim::Any`，丢失编译期 shape 信息。**影响**：使用变量参数的 `randn`/`zeros`/`ones` 调用无法享受编译期 shape 检查与内存预估。**修复计划**：P1 提升 `Dim::Symbol` 支持——变量参数生成符号维度（如 `Dim::Symbol("n")`），编译期通过约束求解保留信息。**当前状态**：✅ 已修复（2026-07-30 P1-1 完成）：`shape_from_int_args` 新增 `HirExprKind::Var` 分支，变量参数提升为 `Dim::Symbol(name)`；tenthc 侧 `HirType` 新增 `symbol_dims` 字段（方案 A）双侧同步。详见 §11.4 AUDIT-11.4.13。 |
| ~~17~~ | ~~VM 字节码：同一函数内连续多个带标签循环，第二个及之后的标签循环错乱（2026-08-02 登记）~~ | ✅ 已修复 (2026-08-02)：**根因**实为 `compile/bytecode.rs` 局部变量槽位查找用 `position`（首匹配）——两个标签循环共用变量名（如 `j`）时，第二个循环的 `j = j + 1` / `while j < 3` 读写第一个循环的 `j` 槽（残留值 3 使内层 while 立即退出 → `s2` 应为 26 实测 50）。`rposition`（最近绑定）+ 本批新增 match 臂编译期作用域（`scope_stack`，臂绑定在 `locals` 中臂结束后被 truncate）修复后，同函数多标签循环（不同标签名、同名标签、while/for/loop 混合嵌套）全部正确。**验证**：`tenth/tests/audit_17_18_regression_test.rs` 3 项标签循环回归（含同名标签、三循环混合），VM+解释器双路径 parity；`Tenth实例/标签循环/labeled_loop.th` 已合并为单函数 `demo_labeled` 运行正确（s=3、s2=26）。 |
| ~~18~~ | ~~VM 字节码：同一函数内连续两个泛型枚举 match，第二个（str 绑定）取值错误（2026-08-02 登记）~~ | ✅ 已修复 (2026-08-02)：**根因**实为 `compile/bytecode.rs` 变量槽位查找用 `position`（首匹配）——两个 match 都绑定同名 `x` 时，第二个 match 的 `Load(x)` 读到第一个 match 的 `x` 槽（残留 42 → `"hi"` 实测 42）。`rposition`（最近绑定）修复 + 本批新增 match 臂编译期作用域（`scope_stack`）修复后：同函数双 match（i64+str）、多字段（Pair<A,B>）、嵌套泛型（Wrap<Vec<i64>>）全部正确，且 match 臂绑定不再遮蔽/污染外层同名变量（此前臂绑定残留为"最近绑定"：解释器删外层变量报"未定义变量"、VM 读残留臂值）。**验证**：`tenth/tests/audit_17_18_regression_test.rs` 2 项泛型枚举回归（含同名绑定 shadow、多字段+嵌套），VM+解释器双路径 parity；`Tenth实例/泛型枚举/generic_enum.th` 已合并为单函数 `demo_both` 运行正确（42 / hi）。 |
| ~~19~~ | ~~运算符重载降级仅支持单层 `a + b`（2026-08-02 登记）~~ | ✅ 已修复 (2026-08-02)：**根因**：`hir/lower/lower_expr.rs` 运算符重载降级用 `resolve_method_type` 取返回类型，但 trait 方法（`impl Add for Point` 的 `add`）不在 inherent 方法表（methods）中，回退 `Unknown` → 链式 `(a + b) + c` 的复合 receiver 类型丢失，外层 `has_trait_impl_for_type("Add", Unknown)` 为 false → 断链，运行时"加法类型不匹配"。**修复**：新增 `trait_impl_method_ret_type()`（从 `trait_impls[trait][type][method].return_type` 取真实返回类型），二元与一元重载降级路径均优先使用，链式 receiver 保持为具体类型。**验证**：`tenth/tests/audit_19_regression_test.rs` 4 项回归（`(a+b)+c`、`a+b+c+d`、`a*b+c` 混合、链式结果参与 Eq 比较）解释器路径全绿；`Tenth实例/宏与自定义运算符/operator_overload.th` 新增链式示例运行正确（5.0 / 6.0）。**遗留**：VM 具体值 trait 方法分派缺口（`Value::Struct` 方法分派只做字段访问）为既有已知限制，链式仍仅解释器路径可用。 |

---

## 七、已知缺陷与不完整（历史参考 — C 后端已移除）

### 7.1 自举编译器 Tenth 源码中的已知问题

| # | 位置 | 问题 |
|---|------|------|
| ~~1~~ | ~~`tenthc/lexer/lexer.th:98,105`~~ | ~~**字面量值不解析** — 整数和浮点数 token 的 value 硬编码为 0~~ **已修复**：lexer.th:90 正确解析 `fval: fv + (ff / div)`，lexer.th:105-106 正确存储 `let fv: f64 = ival; ... fval: fv`（f32 后缀路径） |
| ~~2~~ | ~~`tenthc/parser/parser.th:269`~~ | ~~**字段名不存储** — `parse_postfix` 的 `.field` 访问把字段名丢了~~ **已修复**：parser.th:269 实为 `&` ref 处理（`if d == 40`），字段访问在 parser.th:628 `let field_name = field_tok.sval;` 正确存储 |

> ~~原 #1 (cgen 函数调用参数丢弃) 随 codegen 移除而消除。~~
> 2026-06-30 复核：#1/#2 均已修复，条目保留以记录历史。

### 7.2 语言功能缺口

| # | 位置 | 问题 |
|---|------|------|
| ~~3~~ | ~~`tenth/src/compile/lower.rs`~~ | ~~**Match pattern binding 未生成**~~ **已修复**：实现 match 表达式 struct 解构 pattern（如 `match p { Point { x, y } => ... }` 中的 x/y 绑定）。跨模块全链路：(1) `parser/ast.rs` + `hir/hir.rs` 新增 `Pattern::Struct { name, fields }` 变体；(2) `parser/parser.rs::parse_match_pattern` 加 `LBrace` 处理，支持 `Name { x, y }` 简写与 `Name { x: a, y: b }` 命名绑定；(3) `hir/lower/lower_expr.rs` `lower_pattern`/`bind_pattern_vars` 加 Struct 分支；(4) `runtime/interpreter/pattern.rs` 三处加 Struct 分支；(5) `runtime/vm.rs` 新增 `IsStruct(usize)` Op（opcode 46，与 `IsEnumVariant` 对称）；(6) `compile/bytecode.rs` 加 Struct 分支（IsStruct + LoadField）；(7) `compile/jit/translator.rs` 遇 IsStruct 返回 Err 触发 JIT→VM fallback。tenthc 不用 struct pattern，无需同步。测试：`tests/pattern_match_test.rs` 新增 6 项（4 解释器 + 2 VM），17/17 全绿，0 回归。 |
| ~~8~~ | ~~`tenthc/main.th:11`~~ | **登记陈旧失效（2026-09-16 总师复核）**：`tenthc/main.th:6-11` 用 `read_file` **运行期**逐一拼接 6 个文件（token/lexer/parser/hir/lower/wasm），第 11 行是 `read_file("tenthc/compile/wasm.th")`；全仓**不存在** `tenthc_combined.th`（仅 AUDIT.md 自身 2 处提及）。`.github/workflows/release.yml:62-65` 与 `scripts/release/` 均无手动拼接前置步骤 → 自举**不需要**该步骤。§8.1 中「添加 tenthc_combined.th 自动生成」的建议同属过时，已一并失效。 |

> ~~#5-#7/#10/#12 引用已删除的 C 代码生成文件 (`tenthc/codegen/`, `tenthc/runtime.c`)，随 C 后端移除而消除，不再罗列。~~

### 7.3 中优

| # | 位置 | 问题 |
|---|------|------|
| ~~9~~ | ~~`tenthc/parser/parser.th:631`~~ | ~~**无 for 循环解析** — lexer 识别 `for` 但 parser 未处理~~ **登记陈旧失效**（2026-07-06 复核）：for-in 解析已在 tenthc 全链路实现 — `parser.th:1100-1132`（`parse_stmt` 中 `disc==12` 分支）+ `lower.th:1258-1279`（`kind=="for"` 分支，lower 为 `disc=4`）+ `wasm.th:1404+`（`disc==4` 分支）。原引用行号 `parser.th:631` 实为 generic_call 创建 ident 节点逻辑，与 for 循环无关。tenthc 自举源码本身大量使用 for 循环（parser.th 中 13+ 处），自举路径 B/C 必须能编译 for 循环——这是功能可用的铁证。 |
| 11 | `tenthc/parser/parser.th:180` | **parse_unary 纯透传** — 一元运算在 parse_primary 内处理，此函数冗余 |

### 7.4 架构债务

| # | 问题 |
|---|------|
| ~~14~~ | ~~borrow checker 双向放宽~~ — 已恢复，`check_borrow_shared` 和 `check_borrow_mut` 现执行 ExclusiveRef/SharedRef 检查 |
| ~~16~~ | ~~Test 覆盖盲区~~ — 已修复。前端契约测试 `selfhost_frontend.rs` 改为严格 assert（原 println 不 fail 的问题已修）；执行覆盖由 `fixpoint_runtime.rs`（Wasmtime 端到端编译+执行）和 `parity_test.rs`（112 项 Rust/tenthc 一致性）提供 |

---

## 八、灵感与改进方向

### 8.1 短期 (解锁自举)

- **~~修复致命缺陷 #1-#4~~** → ✅ 已修复，自举闭环可达
- ~~**添加 `tenthc_combined.th` 自动生成** — build script 或 Makefile，而非手动拼接~~ → **过时（2026-09-16 总师复核）**：拼接已在 `tenthc/main.th:6-11` **运行期内化**（`read_file` 逐一读取 6 个文件后 `+` 拼接），仓库内无需也不存在独立拼接产物；本建议已无对象（见 §7.2 #8 同批复核）。
- **~~`tenthc_test.rs` 加执行测试~~** → ✅ 已由 `fixpoint_runtime.rs` 和 `parity_test.rs` 提供

### 8.2 中期 (质量加固)

- **VM 补全** — closure/generic call/match 仍偶有 fallback
- **~~恢复 borrow checker~~** — 已恢复，`check_borrow_shared`/`check_borrow_mut` 现执行完整检查
- **WASM Host import 真实现** — Vec/String 在 WASM 模块中仍以占位形式存在，待实现

### 8.3 长期 (生态)

- **激活死模块** — shape.rs (张量形状优化)、docgen.rs (API 文档生成)；~~autodiff.rs (自动微分训练)~~ ✅ 已完整集成（21 算子，张量级，已集成解释器）
- **tenthpm 包管理器** — `tenth/tools/tenthpm/` **完整实现**，支持 init/build/test/run/add/remove/list/clean/publish/install + Tenth.toml + Tenth.lock + .tenthpkg 打包 + path/git/registry 三种依赖类型
- **LSP 服务器** — `tenth/tools/lsp/` **完整实现**（文档同步/diagnostics/hover/completion/definition/documentSymbol/references/rename/signatureHelp/foldingRange/semanticTokens/formatting）
- **CUDA 后端** — `compile/gpu/` + `compile/optimizations/` 当前仅 CUDA C 源代码生成 + 模拟设备（`device.rs::is_available()` 永远返回 true，`total_memory` 硬编码 24GB），未接 nvcc / CUDA Runtime / cuLaunchKernel。待安装 CUDA Toolkit 后真正激活 kernel 编译与执行

### 8.4 过程改进

- **`.gitignore` 已更新** — 添加 `*.exe` / 构建产物排除
- **清理 19 个构建产物** — 15 个 .exe + 2 个空 .txt + tenthc.c + test_mini.c 已从 git 跟踪移除
- **MEMO.md 保持同步** — 作为动态状态文件，每次大改动后更新

---

## 九、清理记录

| 操作 | 数量 |
|------|------|
| 从 git 移除 .exe 文件 | 15 |
| 从 git 移除空 .txt | 2 |
| 从 git 移除构建 C 文件 | 2 (tenthc.c, test_mini.c) |
| 删除临时 .th | 1 (test_input.th) |
| 更新 .gitignore | 覆盖 *.exe, *.txt, tenthc.c, test_mini.c |
| 删除游离产物 (2026-06-15) | 3 (stderr.txt, stdout.txt, test_w.thw) |
| .gitignore 加固 (2026-06-15) | 新增 *.thw / stdout.txt / stderr.txt |
| 文档对齐 (2026-06-15) | 8 文件：版本号 0.3.0→0.3.3，测试数 134→350，算子数 19→21，LSP/tenthpm 状态脚手架→完整实现 |
| AUDIT §5/§7 重构 (2026-06-15) | 修复重复小节号 (5.2×2)，移除 6 条已废弃 C 后端条目，章节号六/七→八/九 |

保留的历史 C 文件 (tenthc_v3.c ~ v9.c, tenthc_dbg5.c, tenthc_dbg6.c, tenthc_fix.c, tenthc_analyze.c, tenthc_chk.c, tenthc_out.c, tenthc_rust.c) 作为自举进化见证，暂时保留不删。

---

## 十、安全审查记录（2026-06-29）

> 全面安全审查识别 25 项问题（2 致命 / 8 高危 / 8 中等 / 7 低危），分两轮全部修复。第一轮 17 项（C-1, C-2, H-1, H-3, H-5~H-8, M-1~M-8），第二轮 7 项（H-2, H-4, L-1, L-3~L-6）；L-2 在 H-7 中一并修复，L-7 在 H-2 中一并修复。审查报告 `security_review.md`，威胁模型披露 `SECURITY.md` 重写，变更记录见 `MEMO.md` 顶部 2026-06-29 条目。

### 10.1 已修复（第一轮 17 项）

| ID | 严重度 | 位置 | 修复方式 |
|----|--------|------|---------|
| C-1 | 致命 | `tenth/tools/tenthpm/src/{manifest,install,add}.rs` | 新增 `validate_package_name` / `safe_package_name_from_git` / `ensure_within` / `safe_to_remove_dir` 集中校验；所有 `target_dir` 计算 + `fs::remove_dir_all` 必须经过校验 |
| C-2 | 致命 | `SECURITY.md` | 重写：纠正"0 处 unsafe"失实声明（实际 41+ 处），公开真实威胁模型与 `--fs-root` 沙箱选项 |
| H-1 | 高危 | `tenth/src/compile/jit/hostcalls.rs` | 新增 `MAX_HOSTCALL_ARGS = 1<<20` 与 `safe_slice` 统一闸门；所有 `from_raw_parts` 改用；`host_make_map` / `host_new_struct` 加 `count.checked_mul(2)`；`host_make_tensor` 加 `rows.checked_mul(cols)` |
| H-3 | 高危 | `tenth/src/main.rs` | 新增 `parse_memory_config` 与 `run_file(path, config)`；fallback 路径用 `Interpreter::with_limits`；`--no-limits` 显式退出沙箱 |
| H-5 | 高危 | `tenth/src/main.rs` | `time_sleep_ms` 拒绝负数与 > 24h 的请求 |
| H-6 | 高危 | `tenth/src/main.rs` | JSON 解析器加 `JSON_MAX_DEPTH=256` 防栈溢出；`json_unescape` / `simple_json_split` 修复 `\"` 转义状态机 |
| H-7 | 高危 | `tenth/src/main.rs` | `random` 改用 `rand::thread_rng().r#gen()`（CSPRNG），替代可预测的 `DefaultHasher` |
| H-8 | 高危 | `tenth/src/compile/wasmtime_host.rs` | 新增 `safe_offset` / `read_cstr` / `MAX_ALLOC_BYTES=16MiB`；17 个 host import 全部改用；`tenth_alloc` 拒绝负数 size 与 > 16MiB 请求 |
| M-1 | 中危 | `tenth/src/compile/jit/context.rs` | `transmute` 前加 `assert_eq!(size_of::<*const u8>(), size_of::<JitFn>())` 尺寸断言 |
| M-2 | 中危 | `tenth/src/repl.rs:241-245` | `mem-strict` 模式不再 `panic!` 杀 REPL，改为返回 `TenthError::RuntimeError` |
| M-3 | 中危 | `tenth/Cargo.toml` | `[profile.release]` 加 `overflow-checks = true, panic = "unwind"`；`[profile.dev]` 加 `overflow-checks = true` |
| M-4 | 中危 | `tenth/src/compile/jit/hostcalls.rs` | 见 H-1（`host_make_tensor` 加 `rows.checked_mul(cols)`） |
| M-5 | 中危 | `tenth/src/compile/jit/context.rs` | impl Drop for JitContext 调 `cache.clear()` 显式清理 |
| M-6 | 中危 | `tenth/src/compile/jit/hostcalls.rs` | `invoke_jit` 包 `catch_unwind`，panic 时写 `Value::Unit` 并返回 false，防 FFI unwind UB |
| M-7 | 中危 | `tenth/src/runtime/arena.rs` | `alloc` 用 `checked_add` / `checked_mul`；`scope` 用 `saturating_sub` 防下溢 panic |
| M-8 | 中危 | `tenth/src/compile/wasmtime_host.rs` | 见 H-8（bump allocator 越界 panic 修复） |

### 10.2 已修复（第二轮 7 项，2026-06-29）

| ID | 严重度 | 位置 | 修复方式 |
|----|--------|------|---------|
| H-2 | 高危 | `tenth/src/runtime/{limits,vm,interpreter}.rs`、`main.rs` | 新增 `FsSandbox` 类型（canonicalize + starts_with 防路径穿越）；Vm/Interpreter 新增 `fs_sandbox` 字段，所有文件 I/O 原生函数必须经过 `check_read`/`check_write`；`--fs-root`/`--read-only`/`--fs-cwd` 命令行选项 |
| H-4 | 高危 | `tenth/src/runtime/{vm,interpreter}.rs`、`main.rs` | VM 主循环和 Interpreter tick() 新增独立 `loop_counter`/`tick_counter`，每 4096 步检查 `deadline_ms`（墙钟超时独立于 `step_budget`）；`--timeout <secs>` 命令行选项，`parse_timeout_ms` 用 checked_mul/checked_add 防溢出 |
| L-1 | 低危 | `tenth/tools/tenthpm/src/manifest.rs` | 验证确认 `extract_package_name` 仅在 `safe_package_name_from_git` 内被调用，所有调用方都经 `validate_package_name` 校验 |
| L-3 | 低危 | `tenth/src/main.rs`、`tenth/src/runtime/interpreter.rs` | `days_to_date` 入口加 `if days > u64::MAX - EPOCH_OFFSET { return (0,0,0); }` 防 `days + 719468` 溢出 UB |
| L-4 | 低危 | `tenth/tools/tenthpm/src/manifest.rs` | `is_git_url` 默认仅放行 `https://`，`http://`/`git://`/`ssh://`/`.git` 后缀需 `TENTH_ALLOW_INSECURE_GIT=1` 显式 opt-in |
| L-5 | 低危 | `tenth/tools/tenthpm/src/{manifest,install,add}.rs` | 三处 `git clone` 加 `--config protocol.file.allow=deny` 和 `protocol.git.allow=deny`；克隆后调用新增 `disable_hooks()` 将 `core.hooksPath` 指向空设备 |
| L-6 | 低危 | `DEPS.md` | 新增"供应链安全"章节，说明 `cargo audit` 和 `cargo deny` 用法及 CI 集成建议 |

> L-2（DefaultHasher）已在 H-7 中一并修复；L-7（compile_host 写文件）已在 H-2 中一并修复。

### 10.3 未修复（保留至下轮）

无。全部 25 项安全问题已修复。

### 10.4 验证

| 命令 | 结果 |
|------|------|
| `cargo build --manifest-path tenth/Cargo.toml` | ✅ 成功（178 warning 为项目原有 `unsafe-op-in-unsafe-fn`，非本次引入） |
| `cargo build --release --manifest-path tenth/Cargo.toml` | ✅ 成功（第二轮验证） |
| `cargo build --manifest-path tenth/tools/tenthpm/Cargo.toml` | ✅ 成功（第二轮 L-4/L-5 验证） |
| `cargo run --release --manifest-path tenth/Cargo.toml -- run tenthc/main.th` | ✅ 自举路径 A 验证通过（`tenthc_full.wasm` 生成） |
| `cargo test --manifest-path tenth/Cargo.toml --lib` | ✅ 10/10 通过（第一轮） |
| `cargo test --manifest-path tenth/Cargo.toml --features mem-debug --test memory_test` | ✅ 17/17 通过（含 `arena_scope_rolls_back_counter` / `arena_overflow_returns_none` 验证 M-7 修复） |
| `cargo test --manifest-path tenth/Cargo.toml`（全集成测试） | ⚠️ rustc 1.95.0 ICE（`wasm.rs:2077` 方法调用解析，仅在 `--test` 模式触发），非本项目代码问题，ICE 文件未被本轮修改 |

---

*文档由 2026-05-27 全项目审计生成，作为后续正式开工的参考基准。*

---

## 十一、论文披露的中等价值问题登记（2026-07-02）

> 本章节登记形式化分析论文与相关评审中披露的、尚未在历史章节登记的中等价值已知缺陷与健全性破口。每条含编号、标题、论文来源、影响、当前状态。**本批次为纯文档登记，未修改任何代码**——属于"诚实披露 → 缺陷登记"环节，修复方案分批推进。

### 11.1 形式化健全性缺陷（护城河相关）

| 编号 | 标题 | 论文来源 | 影响 | 当前状态 |
|------|------|---------|------|---------|
| AUDIT-11.1.1 | T19 定理 B6 跨语句借用 unsoundness | T19（形式化分析定理 B6） | `hir/lower/lower_stmt.rs:50/57/73` 与 `lower_expr.rs:437/439/441/464/656` 调用 `scope.release_borrows` 在语句边界过早释放借用；`lower_expr.rs:701/717` Deref 路径不检查别名。`scope.rs:67/81/113` 实现仅做单点 SharedRef/ExclusiveRef 检查，未跟踪跨语句别名。**存在 Tenth 接受但违反别名规则的程序**（如 `let r = &x; let m = &mut x; println(r);` 类构造）。 | ✅ 部分修复（2026-07-07 `borrow_holders` 机制堵住 B6 原始反例；2026-07-09 数理部论证健全性边界）。**定理 B6'（条件健全性）**：在 P1（persistent Let 记录 holder）/P2（无变量转义）/P3（holder 未被 Move 前不被新借用）前置条件下，活跃 holder 的 borrowed 不被释放。**定理 B7（剩余反例）**：borrow_holders 仍无法捕获 3 类变量转义——B7-1 Block 最终表达式为 Ident（`let s = { let r = &mut p; r };`）、B7-2 变量赋值转义（`let s = r; move(r);`）、B7-3 函数返回引用类型（`fn id(&mut T) -> &mut T`）。根源是 `collect_persistent_borrowed_idents` 不识别 Ident 与 Call 形态。**风险评估**：在 Tenth 当前约束（无 unsafe/无 FFI/无并发）下不构成可利用内存安全漏洞，仅语义健全性缺口。根治方案仍是 NLL（liveness 分析）。详见 MEMO.md 2026-07-09 docs 条目。 |
| AUDIT-11.1.2 | T20 PB2 四类反例未覆盖 | T20（property-based testing 反例集） | Call/If/Block/Match 初始化表达式中的 Ref/MutRef 未跳过 `release_borrows`，四类 AST 节点的借用释放语义与 Let 不一致。属 PB2 反例集披露的 4 类漏洞，可能让通过 borrow checker 的程序在运行时仍违反别名。 | ✅ 已修复（2026-07-07，`hir/lower/mod.rs` 新增 `expr_may_produce_ref` 递归判断 If/Block/Match/Move 是否产生 Ref 值；`creates_persistent_borrow` 改用此函数；新增 `collect_persistent_borrowed_idents` 收集多分支中的所有被借变量；`hir/lower/lower_stmt.rs` Let 处理用新函数支持多变量借用记录。验证：cargo check 通过 + lib 测试 16 passed（含 5 autodiff）+ integration_test 14 passed + 自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。详见 MEMO.md 2026-07-07 fix 条目） |
| AUDIT-11.1.3 | T42 LayerNorm per-feature γ + BatchNorm 多 channel dX 闭式 backward 缺陷 | T42（normalization 算子梯度推导） | LayerNorm 在 per-feature γ（每个特征独立缩放参数）场景下闭式 backward 不正确；BatchNorm 在多 channel dX 场景下闭式 backward 不正确。两者均属"已实现但有数值错误"——前向 ✅，反向在常见配置下 ✅，在论文 T42 披露的特定配置下产生错误梯度。 | ✅ 已修复（2026-07-07，`runtime/autodiff.rs` line 852-1015：(1) LayerNorm backward 把 `dX = γ·std_inv·(dY - mean(dY) - x̂·mean(dY·x̂))` 改为 `dX = std_inv·(gy - mean(gy) - x̂·mean(gy·x̂))`，其中 `gy = dY·γ`（γ 不能提到括号外）；(2) BatchNorm 重写为 per-channel 双遍循环，修正 d_gamma/d_beta shape（从 (N,C,H,W) 改为 (C,)）、mean 改为 per-channel、修正广播错误。验证：cargo check 通过 + autodiff 5/5 + lib 测试 16 passed + integration_test 14 passed + 自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。详见 MEMO.md 2026-07-07 fix 条目） |
| AUDIT-11.1.4 | T50 multihead_attention 实为 single-head 等价 | T50（标准库 API 与论文承诺一致性审计） | `tenth/std/nn/multihead_attention.th` 文件头部注释自承："Simplified...single-head-equivalent attention...True multi-head attention requires either: 1. 3D/batched matmul support, or 2. A loop over heads with per-head slices"。`n_heads` 参数被读取但仅用于计算 `d_k = d_model / n_heads` 后做单次 attention，未真正分头。**与对外 API 名 MultiheadAttention 不符**。根因：受仅 2D matmul 限制（见 AUDIT-11.1.5）。 | ✅ 已修复。`tenth/std/nn/multihead_attention.th` 已基于 bmm 重写为真正分头计算版本：q/k/v 投影后 reshape 为 (n_heads, seq_len, d_k)，scores = q_3d.bmm(k_3d.transpose()) * scale，out_3d = weights.bmm(v_3d)，最后 reshape 回 (seq_len, d_model) 做 w_o 投影。Reshape/MaskedFill 已接入 autodiff（TapeOp::Reshape/TapeOp::MaskedFill），w_q/w_k/w_v/x 梯度完整传播。`tenth/tests/multihead_attention_test.rs` 11 项测试覆盖前向 shape/有限性/n_heads=1 等价/autodiff 无错误/w_q/w_k/w_v 梯度非零/x 梯度传播/完整反向链。详见 MEMO.md 2026-07-06 test: MHA 完整梯度测试补强 条目。 |
| AUDIT-11.1.5 | T18 body 直接 clone 健全性破口（泛型实例化） | T18（泛型实例化健全性） | 泛型函数实例化时 body 直接 clone 但未做替换健全性检查，可能让类型变量在 body 内未被一致替换，导致实例化后语义偏移。与 T12 双侧破口（tenthc 缺 shape 检查）联合暴露。 | ✅ 已修复（2026-07-07，`hir/lower/mod.rs` 新增 `substitute_expr` 递归替换 HirExpr 中所有 Type 字段；新增 `check_generic_instantiation_soundness` 健全性检查；`hir/lower/lower_expr.rs` GenericCall 实例化路径调用这两个函数，确保 body 内类型变量被一致替换。验证：cargo check 通过 + lib 测试 16 passed（含 5 autodiff）+ integration_test 14 passed + 自举验证通过（`[OK] Full compiler compiled to tenthc_full.wasm`）。详见 MEMO.md 2026-07-07 fix 条目） |

### 11.2 自举编译器双侧破口（T12）

| 编号 | 标题 | 论文来源 | 影响 | 当前状态 |
|------|------|---------|------|---------|
| AUDIT-11.2.1 | tenthc 缺 shape 检查 | T12（自举双侧对齐审计） | tenthc 完全无 shape 检查，Rust 母编译器已有 Phase 1+2+3 实现。两侧语义不对齐：在 tenthc 自举路径下无法享受 shape 检查带来的早期错误发现，且对 shape 不匹配的程序两侧行为可能不一致。 | ✅ 已修复（2026-07-02，`tenthc/hir/hir.th` HirType 新增 dim_count/dim0/dim1 三字段，`tenthc/hir/lower.th` 新增 8 个 shape 辅助函数与 3 处调用点检查（binary/call/method_call）；最小子集：matmul 2D 内侧 K 检查、二元广播检查、zeros/ones/rand/randn 字面量 shape 推断、transpose 2D 维度互换；跳过 let 注解/分支兼容/返回值合并（需 Tensor 注解解析，tenthc 不支持）；错误处理非致命（push 到 p.errors）；首轮 parity 全挂后重构 tuple 返回为 3 个单值 getter（WASM 后端 tuple 支持有限）；详见 MEMO.md 2026-07-02 fix 条目） |
| AUDIT-11.2.2 | tenthc 缺错误恢复 (panic mode) | T12 | tenthc 完全无 panic-mode，遇首个解析错误即终止。Rust 母编译器有 `error_recovery_test.rs` 7 项测试覆盖续接能力。两侧错误恢复语义不对齐。 | ✅ 已修复（2026-07-02，`tenthc/parser/parser.th` 新增 is_sync_token/synchronize/record_error_and_recover/is_expr_start_token，Parser 结构加 errors 字段，parse_program 主循环对无法识别的顶层 token 应用 panic-mode 恢复；error_recovery_test 7/7 通过，详见 MEMO.md 2026-07-02 fix 条目） |
| AUDIT-11.2.3 | tenthc 缺 expect_gt，嵌套泛型 `>>` 被整个吞掉 | T12 | tenthc lexer/parser 缺 `expect_gt`，把 `>>` 整个吞掉，无法解析 `Vec<HashMap<K, V>>`、`Pair<Pair<T, U>, V>` 等嵌套泛型。Rust 母编译器有 `generic_test.rs` 11 项含 `>>` 拆分测试。 | ✅ 已修复（2026-07-02，`tenthc/parser/parser.th` 新增 expect_gt 函数（遇 Gt consume / 遇 Shr consume 后插入合成 Gt，通过重建 tokens 数组实现 Vec::insert 语义），替换 parse_postfix 与 parse_generic_params 2 处闭合调用点，详见 MEMO.md 2026-07-02 fix 条目） |
| AUDIT-11.2.4 | tenthc 缺 i64 溢出检测 | T12 | tenthc 字面量与算术路径缺 i64 溢出检测，Rust 母编译器有 `overflow-checks = true`（M-3 安全修复）。两侧数值健壮性不对齐。 | ✅ 已修复（2026-07-02，`tenthc/lexer/lexer.th` parse_int 与 lexer_next 数字解析加入溢出检测，用数学等价条件 `ival > (i64_max - d) / 10` 检测，溢出时返回 i64_max 饱和值并打印错误，详见 MEMO.md 2026-07-02 fix 条目） |
| AUDIT-11.2.5 | tenthc 无 lossy / taint 分析（护城河「静默失败防护」只在 Rust 路径生效） | 2026-09-16 总师跨模块影响分析发现（委派编译器部做 `AUDIT-11.4.48` 时按 §3.1/§4 检查 tenthc 同步） | **已核实证据**：`grep 'lossy\|taint\|Lossy' tenthc/**/*.th` 仅 2 文件 4 处命中——`tenthc/lexer/token.th:13` 的 `TokenKind::Lossy` 变体定义 + `tenthc/lexer/lexer.th:431-433` 的保留字注册（注释自承「自举源码不使用 lossy（纯编译期构造），仅注册为保留字避免误 lex」），**parser / hir / compile 三层全然不消费该 token**；Rust 侧 lossy 检查实现于 `hir/lower/taint.rs`（旁路污点分析，`lower_stmt.rs:1502` 单点调用）。**影响（已核实）**：双侧语义不对齐——① Rust 路径对「标量→低精度张量」若干形态报 lossy 错，tenthc **无此检查（更宽松）**；② `lossy(...)` 显式放行在 tenthc 侧因 parser 不消费该 token 而**不可用**（使用 `lossy` 的程序在自举路径 C 下无法编译）。**未核实（需探针）**：tenthc 是否支持 F16/BF16 dtype（若不支持，① 的实际暴露面受限）；`lossy expr` 在 tenthc 下的实际报错形态。**性质**：双侧破口（与已修复的 `AUDIT-11.2.1` tenthc 缺 shape 检查同类）；**注意**：本轮 `AUDIT-11.4.48` 的 Rust 侧 sink/插值补齐**已落地并把 ① 加宽**（2026-09-16：新增 `print`/`json_encode*`/普通串插值三类逃逸点 → 「Rust 拒、自举收」的程序集合变大）。 | ⚠️ 待排期（编译器部，自举同步类）：需先做探针定性（tenthc 的 F16/BF16 支持面 + `lossy` 报错形态），再定同步范围；`lexer.th:431-433` 的注释表明该不对称是「自举源码不用 lossy」的刻意取舍，但**其对用户程序的影响面此前未登记**。 |

### 11.3 双重 native 注册反模式（T37）

| 编号 | 标题 | 论文来源 | 影响 | 当前状态 |
|------|------|---------|------|---------|
| AUDIT-11.3.1 | VM 缺 17 项 native 函数 | T37（VM/解释器 native 注册对齐审计） | `main.rs::register_natives()` 注册到 VM 的 native 函数集合缺 17 项：`to_string`、`type_name`、`with_step_limit`、`with_timeout_ms`、`is_timeout`、`start_grad`、`f64_bits`、`f64_from_bits`、`sin`、`cos`、`ln`、`pow`、`save_weights`、`load_weights`、`format`、`parse_int`、`parse_float`。**`save_weights`/`load_weights` 为致命项**——VM 路径下模型保存/加载失效，与 2026-07-01 "护城河 D 演示补齐" 修复的 6 个构造函数（zeros/ones/rand 等）属同类问题的延续。 | ✅ 已修复（2026-07-02，`main.rs::register_natives()` 补全 17 项，save_weights/load_weights 复用 FsSandbox 校验，详见 MEMO.md 2026-07-02 fix 条目） |
| AUDIT-11.3.2 | 解释器缺 3 项 native 函数 | T37 | `interpreter/natives.rs` 注册集合缺 3 项：`print`、`to_f64`、`to_f32`。与 VM 路径不对称。 | ✅ 已修复（2026-07-02，`interpreter/natives.rs` 补全 print/to_f64/to_f32，并同步 HIR lower 白名单与解释器 Var fallback 白名单，详见 MEMO.md 2026-07-02 fix 条目） |

### 11.4 工程债务与潜在 UB

| 编号 | 标题 | 论文来源 | 影响 | 当前状态 |
|------|------|---------|------|---------|
| AUDIT-11.4.1 | T22 FV5 O(n²) 工程债务 | T22（闭包自由变量收集复杂度） | `hir/lower/closures.rs:9-15` 的 `free_vars_in` 用 `Vec<String>` + `sort` + `dedup` 实现，复杂度 O(n²)（每次 push 后 sort）。HashSet 实现可降到 O(n)。在深层嵌套闭包场景下编译期开销显著。 | ✅ 已修复（2026-07-06，`hir/lower/closures.rs` `free_vars_in`/`collect_free_vars`/`collect_free_vars_stmt` 改用 `HashSet<String>`，4 处 `push` 改 `insert`；API 兼容（仍返回 `Vec<String>` + `sort`），复杂度 O(n²)→O(n)，纯优化无语义影响。验证：autodiff 52/52 + 全量 0 回归。详见 MEMO.md 2026-07-06 fix 条目） |
| AUDIT-11.4.2 | T31 MAX_STACK_DEPTH=256 静默溢出潜在 UB | T31（VM/JIT 栈深度限制审计） | `compile/jit/translator.rs:32` `const MAX_STACK_DEPTH: u32 = 256;`，超过时静默截断而非报错，可能让 JIT 编译出的代码访问越界栈区域。属潜在未定义行为。VM 路径（`runtime/vm.rs`）的 `locals: Vec<Value>` 用 `resize` 安全增长，但 JIT 路径的固定 256 槽是硬上限。 | ✅ 已修复（2026-07-06，`compile/jit/translator.rs` 新增 `bump_sp()` 方法在 push 前检查 sp 上限，28 处 push 操作改为 `bump_sp()?`；超限返回 `Err` 触发既有的 JIT→VM fallback（`mod.rs:62-65`），不再静默截断。验证：jit_test 10/10 + 全量 0 回归。详见 MEMO.md 2026-07-06 fix 条目） |
| AUDIT-11.4.3 | T36 双重存储同步开销 | T36（解释器 scope 数据结构审计） | `runtime/interpreter/mod.rs:40` `pub scopes: Vec<HashMap<String, Value>>` 用 Vec+HashMap 双重存储，变量查找从最后一个 scope 向前遍历，在重嵌套场景下有平方风险。VM 路径用索引 `locals: Vec<Value>` 无此问题。 | ✅ 已修复（2026-07-08）。改为扁平化 `vars: HashMap<String, Vec<(usize, Value)>>` + `scope_depth` + `scope_vars: Vec<Vec<String>>`，`resolve_var`/`set_var` O(1)，`pop_scope` O(m)（m 为本层变量数） |
| AUDIT-11.4.4 | tenthc `..=` lexer 解析 bug | 回归测试发现（2026-07-06） | `tenthc/lexer/lexer.th:180-184` 解析 `.` 时 `if next == "."` 分支直接返回 `DotDot`，未检查第三个字符是否为 `=`。导致 `2..=4` 被错误分词为 `2` `..` `=` `4`。tenthc 路径下 inclusive range 实际不可用。 | ✅ 已修复（2026-07-06，`tenthc/lexer/lexer.th:180-190` 在 `if next == "."` 分支内 advance 后再 peek 一次检查 `=`，匹配则 advance 并返回 `DotDotEq`，与 Rust 侧 `lexer.rs` 对齐。验证：自举 + parity 129/129 + selfhost_frontend 4/4 + error_recovery 7/7；新增 `tenthc_dotdot_eq_test.rs` 3 项测试。详见 MEMO.md 2026-07-06 fix 条目） |
| AUDIT-11.4.5 | tenthc Vec 迭代未实现 | 回归测试发现（2026-07-06） | `tenthc/compile/wasm.th:1472` 明确注释 `// Non-range iterable: no-op for now`。tenthc WASM 后端不支持 `for x in [Vec]` 形式的迭代，仅支持 Range 迭代。 | ✅ 已修复（2026-07-06 codegen + 2026-07-08 测试 stub 补全。`tenthc/compile/wasm.th:1471-1546` For 语句新增 ArrayLiteral (disc=22) 分支，复用现有 `vec_new`/`vec_len`/`vec_push`/`vec_get` host imports 实现 Vec 迭代 codegen；后续修复：删除 `wasm.th:1492` 误加的 `wasm_drop`（type/drop 不匹配导致 wasmi 验证失败）。2026-07-08 补全 `tenth/tests/tenthc_for_loop_test.rs` 中 `env` 模块的 4 个 Vec host function stub（vec_new/vec_len/vec_push/vec_get），基于 WASM 内存 + bump allocator 实现真实 Vec 语义（24 字节 header = cap+len+dp，8 字节/元素，vec_push 含倍增扩容），与 `wasm/host.rs` 的 `Vec_new`/`Vec_push` 实现对齐。取消 2 项 `#[ignore]` 标记（tenthc_for_vec_literal_basic / tenthc_for_vec_literal_break_continue），3 项 Vec 迭代测试全部通过。验证：自举 + parity 129/129 + selfhost_frontend 4/4 + tenthc_for_loop_test Vec 迭代 3/3。ArrayLiteral 表达式本身 codegen 未改。详见 MEMO.md 2026-07-06/2026-07-08 fix 条目） |
| AUDIT-11.4.6 | CUDA 后端文档矛盾 | 基本功核查第 94 项（2026-07-25 复核） | `compile/gpu/` 模块在 AUDIT.md §六 #4 / §8.3 / CODE_WIKI.md §4.4 中描述为"脚手架已就绪，待 CUDA 环境安装"，易让读者误解为"只差环境配置即可工作"。实际：`device.rs:79-82` `CudaDevice::is_available()` 永远返回 true（注释自承 "Simulated: always report available for code generation purposes"），`total_memory: 24 * 1024 * 1024 * 1024` 硬编码模拟值；`mod.rs::compile_kernel` 仅把 HIR 函数转成 CudaKernel 字符串；`cuda_kernel.rs:48-119` `to_cuda_code()` 仅生成 CUDA C 源代码字符串；Grep `nvcc\|cuda_runtime\|cudaRun\|cuLaunch\|exec\|compile_binary\|build_kernel` 在 `compile/gpu/` 0 匹配——整个 gpu 模块只是 C 代码生成器 + 模拟设备，不编译、不加载、不执行任何 kernel。 | ✅ 已修复（2026-07-25，纯文档措辞修正，零代码改动）：AUDIT.md §六 #4 措辞改为"仅 CUDA C 源代码生成 + 模拟设备，未接 nvcc / CUDA Runtime API / cuLaunchKernel"；§8.3 同步修正；CODE_WIKI.md §4.4 GPU 后端章节代码示例与实际 `Device` trait 对齐（实际是 `name/device_type/memory_limit/is_available` 4 方法，非文档原写的 `device_type/allocate/launch` 3 方法）；`基本功核查.md` 第 94 项状态从 "✅ 严重" 升级为 "已解决（文档措辞修正）"。**性质**：文档失实风险消除，非代码 bug。**未触及代码**：gpu/ 模块实现保留现状（待 CUDA Toolkit 环境就绪后真正激活）。详见 MEMO.md 2026-07-25 docs 条目） |
| AUDIT-11.4.7 | tenthc bmm FLOPs 预估为 no-op | 已知限制 #12 登记（2026-07-12 Problem 21 批次4 发现） | `tenthc/hir/lower.th:351` `emit_bmm_flop_estimate` 原直接 return（no-op）。根因：tenthc `HirType` 仅有 dim0/dim1，无 dim2 字段，3D tensor shape 无法在编译期追踪，导致 bmm FLOPs 预估、3D shape 检查、3D numel/bytes 计算均为 no-op。**影响**：bmm 数值计算路径完整（运行时正确），仅编译期 FLOPs 预估与 3D shape 静态检查缺失（errors defer to runtime）。 | ✅ 已修复（2026-07-25）：(1) `tenthc/hir/hir.th:39` 新增 `dim2: i64` 字段（`..` 默认零初始化，不破坏现有 1D/2D 路径）；(2) `tenthc/hir/lower.th` 激活 6 处 no-op——`get_tensor_dim2`（读 `t.dim2`）、`check_bmm_shape`（编译期校验 batch/M/K/N）、`static_numel`/`fmt_dims` 的 `dc==3` 分支、`emit_bmm_flop_estimate`（完整实现 B*M*K*N*2 FLOPs 计算，分段乘法 + 范围检查防 i64 溢出，每维 < 10^6，累乘 < 10^18）、bmm 分支（3D shape 推断 `(B, M, N)` + 调用 `check_bmm_shape` + `emit_bmm_flop_estimate`）；(3) `parse_tensor_type` 放宽：支持 `Tensor[f64, 2, 3, 4]` 解析 1-3 个 dim 字面量；(4) 构造函数 `add_tensor_type`/`add_tensor_type_param`/`propagate_tensor_dtype` 加 `dim2` 参数。bmm FLOPs 预估现与 Rust 母编译器对齐（`types.rs:1094-1129`）。新增 `test_bmm_flop_estimate_parity`（`tenth/tests/native_parity_test.rs`）验证双侧 warning 一致。**字段语义**：dim_count=3 → dim0=B, dim1=M, dim2=K(bmm 输入) 或 N(bmm 结果)。**验证**：build 无新 warning、自举 `[OK] Full compiler compiled`、cargo test --release 全套全绿、bmm_test 11/11、native_parity_test 36/36、selfhost_frontend 4/4、three_stage 1/0/2。详见 MEMO.md 2026-07-25 fix 条目） |
| AUDIT-11.4.8 | tenthc yield 关键字未接入 | 已知限制 #9 修复同步（2026-07-25） | `tenthc/parser/parser.th`（原无 yield 解析）、`tenthc/hir/lower.th`（原无 yield lowering）、`tenthc/compile/wasm.th`（原无 yield codegen）。**根因**：tenthc lexer 已注册 "yield" 关键字（disc=82），但 parser/lower/wasm 三层未接入，导致 tenthc 无法解析含 yield 的源码。**影响**：tenthc 侧无法编译含 yield 表达式的 Tenth 源码（Rust 母编译器侧不受影响，早已完整实现）。 | ✅ 已修复（2026-07-25）：(1) `tenthc/hir/hir.th`：HirExpr disc 列表新增 `37: Yield`（left=inner expr, ty=Unit）；(2) `tenthc/parser/parser.th`：新增 yield (disc=82) 解析，支持 `yield;`（无值，left=0）和 `yield expr`（带值，left=inner）两种形式；(3) `tenthc/hir/lower.th`：新增 yield lowering（inner 若存在则 lower 保留副作用，返回 Unit 类型 disc=11）；(4) `tenthc/compile/wasm.th`：新增 yield (disc=37) codegen（inner 若存在则编译并 drop，yield 本身 no-op）；(5) 新增 `tenth/tests/tenthc_yield_test.rs`：3 项测试（无值形式/带值形式/多 yield 混合）。**遗留**：路径 B（bridge.rs）未接 yield（任务约束禁止修改），tenthc/main.th 不含 yield 故自举不受影响。**验证**：build 无新 warning · 自举 [OK] · cargo test --release 全套全绿 · yield_test 6/6 · selfhost 4/4 · three_stage 1/0/2 · tenthc_yield_test 3/3。详见 MEMO.md 2026-07-25 feat 条目） |
| AUDIT-11.4.9 | 标准库三项能力文档失实 | 文档部复核（2026-07-25） | `能力梳理/能力全梳理.md` §4.1（HashSet ❌）、§4.3（统计函数 ❌）、§4.10（断言 ❌）。**根因**：标准库扩展后未同步能力全梳理状态，导致文档标记与实际实现不符。**影响**：误导总师优先级评估（误将已实现能力列为扩展目标）。 | ✅ 已修复（2026-07-25）：(1) 统计函数：原有 6 函数（mean/median/variance/stddev/variance_sample/stddev_sample）+ 新增 6 函数（min/max/range/sum/product/percentile）+ test_stats.th（25+ 断言）；(2) HashSet：原有 8 函数（new/insert/contains/remove/len/is_empty/to_array/clear）+ 新增 5 函数（from_array/set_union/intersection/difference/is_subset）+ hashset_test 12 项行为测试；(3) assert/assert_eq：补 8 项行为测试（assert_test.rs）+ 修复解释器 Shared 包裹值问题（新增 deref_wrapped() 辅助函数）+ native 白名单同步（lower_expr.rs + eval.rs）；(4) 能力全梳理状态修正（§4.1 HashSet ❌→✅、§4.3 统计函数 ❌→✅、§4.10 断言 ❌→⚠️）+ 总结矩阵数字更新（标准库 ✅ 57→59、⚠️ 5→6、❌ 98→95；合计 ✅ 217→219、⚠️ 26→27、❌ 342→339）。**性质**：文档失实 + 测试补强 + 能力扩展。详见 MEMO.md 2026-07-25 feat 条目） |
| AUDIT-11.4.10 | JIT `is_sealed` 断言 panic（循环回边） | 第一波修复同步登记（2026-07-30） | `tenth/src/compile/jit/translator.rs:165` block 被过早密封（`is_sealed` 返回 true），循环回边（loop back-edge）尝试密封已密封 block 触发 panic。**根因**：当前 JIT translator 的 block 密封策略假设线性控制流，循环回边打破该假设。**影响**：含循环的函数 JIT 编译时 crash（影响训练循环、迭代算法等典型场景）。 | ⚠️ workaround 已加（2026-07-30）：`compile/jit/translator.rs` 用 `catch_unwind` 包裹 `translate`，含循环的函数 JIT 编译时自动 fallback 到 VM 解释执行，不再 crash。**根本修复**：延迟密封 block 以支持循环 JIT 编译（需要重构 block 密封时机，让回边能正确触发 block 合并），推后到 P2。**影响**：含循环的函数无法 JIT 加速，但功能完整（fallback VM 正确执行）。**性质**：crash 防护 + 待根本修复。详见 MEMO.md 2026-07-30 fix 条目） |
| AUDIT-11.4.11 | embedding `gather` ndim 限制 | 第一波修复同步登记（2026-07-30） | `tenth/std/nn/embedding.th` 改用 `gather(weight, 0, indices)` native 实现（原 `embedding_lookup` 张量方法从未实现为 native）。`gather` 原语要求 `weight` 与 `indices` 的 ndim 匹配——典型场景 `weight[V, D]` (ndim=2) + `indices[S]` (ndim=1) 会因 ndim 不匹配运行时报错。**根因**：`gather` 是通用原语（沿 dim 维收集，要求 index 与 base ndim 一致以保持 shape 推断确定性），不是为 embedding 场景定制的 index_select。**影响**：标准 embedding 用法（`weight[V,D]` + `indices[S]`）需调用方手动扩展 indices 维度。 | ✅ **已修复（2026-09-17，运行时部+编译器部 W6）**：新增 **`index_select(base, dim, index)` native 自由函数**（**非**方法——两后端方法表都无 `gather` 臂，方法形态是陷阱）；`index` 限 **1-D**（静态可知 ≠1 → **编译期 TypeError**）；`out.shape = base[..dim] + [index.len()] + base[dim+1..]`；dtype 跟随 base（4 臂）；**index 严格化**（NaN/非整数/越界、dim 越界、标量 base → **响亮报错**；空 index 返回含 0 维空张量不 panic）。**autodiff 同步 7 处**（`tape_op` / `autodiff::mod` 的 `Tape::index_select` / `backward`（**抽出共用内核 `scatter_add_along_dim` + `unflatten_index` 并让 Gather 委托它**）/ `grad` / `relation_debugger`（归 `Preserve`）/ `backward_shape_pass` / `backward_shapes`）；`gather_test` 13 项（含 4 个 backward）绿。**WASM 天然响亮**（`未定义函数 'index_select'`）。`tenth/std/nn/embedding.th` 已切新原语（4 参签名保留）。**总师独立复验**：`index_select(3×2, 0, [2,0])` → sum **14.0**、shape **[2.0, 2.0]**（两路径一致，手算核对：行 2 + 行 0 = 5+6+1+2 = 14 ✓）。（原登记：⚠️ workaround 已加（2026-07-30）：调用方可将 indices 扩展为 `[S, 1]` 再 gather 后 reshape，或保证 weight 与 indices ndim 匹配。**完整修复**：新增 `index_select` native（沿 dim 维收集，对 ndim 不匹配更宽容，专门用于 embedding 等场景）或为 gather 增加 broadcast 支持。推后到 P1 后续。**性质**：API 兼容性限制 + 待 native 扩展。详见 MEMO.md 2026-07-30 fix 条目） |
| AUDIT-11.4.12 | `hir/types.rs:392` `.shape()` 方法分支与运行时不一致 | 第一波修复同步登记（2026-07-30） | `tenth/src/hir/types.rs:392` 有一处 `.shape()` 方法分支（类型系统误标为返回 shape），但运行时**无对应 native 实现**——`.shape()` 方法不存在于 tensor 方法注册表。**影响**：用户写 `x.shape()` 类型检查能通过，运行时崩溃（"method not found" 或类似错误）。**正确路径**：取 shape 应使用 `.shape_tensor()` 方法（返回 `Tensor[f64, ndim]`）。 | ✅ **已落地（最终方案 2026-08-31）**：（1) grep 确认无生产代码/测试依赖 `.shape()`（`.th` 生产代码零依赖，仅注释提及；测试 `.shape()` 均为 Rust `Tensor` API）；(2) 删除 `tenth/src/hir/lower/types.rs` 张量方法分派的 `"shape" => Type::Array{..}` 分支（现落 `_ => Type::Unknown`）；(3) `tenth/src/hir/lower/lower_expr.rs` MethodCall 降级处新增 Tensor receiver + `shape` 方法 → 编译期 TypeError："张量没有方法 'shape()'——取形状请用 'shape_tensor()'（返回 Tensor[f64, ndim]）"。用户自定义 struct/trait 的 `shape` 方法不受影响（Tensor 专属检查）。**验证**：`tenth/tests/audit_11412_regression_test.rs` 3 项（`x.shape()` 编译期报错含 shape_tensor 提示 / `x.shape_tensor()` 返回 1D 维度张量正常 / 用户自定义 `shape` 方法不误伤）全绿；全量 0 回归。**性质**：类型系统遗留误标清理。详见 MEMO.md 2026-08-02 fix 条目。**⚠️ 以上 2026-08-02 段已作废（其编译期拦截已被撤销），以下为最终状态 —— 最终方案（2026-08-31，QA-20260831）**：`.shape()` 已作为 MINOR 兼容新增**正式落地**——VM + 解释器双侧注册（返回 `Vec<i64>`，`shape_tensor()` 保留），2026-08-02 的编译期拦截（临时缓解）已撤销（`types.rs` 恢复 `"shape" => Array<i64>` 推断、`lower_expr.rs` 撤销 Tensor 专属 TypeError，保留历史注记）；`audit_11412_regression_test` 更新为断言 shape() 正常工作。**同款模式「类型推断存在但运行时未注册」的 `Tensor::to_vec()`**（`types.rs` 有 `"to_vec" => Array` 推断但两侧方法表均未注册——留言登记的第四例）同轮一并双侧注册落地（行主序展平为一维 Vec，元素按 dtype f64→Float/f32→Float32），守护测试 `tensor_shape_method_test.rs` 11 项。详见 MEMO.md 2026-08-31 条目） |
| AUDIT-11.4.13 | `randn` 变量参数限制 | 第一波修复同步登记（2026-07-30） | `tenth/src/hir/lower/types.rs` `shape_from_int_args` 只接受字面量参数作为 shape 维度，变量参数（如 `let n = 10; randn(n, m)`）退化为 `Dim::Any`，丢失编译期 shape 信息。**根因**：Phase 1 shape 检查的 `Dim` 枚举仅有 `Known(i64)` 与 `Any` 两类，无法表达"运行时已知但编译期未知"的变量维度。**影响**：使用变量参数的 `randn`/`zeros`/`ones` 调用无法享受编译期 shape 检查与内存预估（`static_bytes` 依赖 `Dim::Known`）。 | ✅ 已修复（2026-07-30 P1-1 完成）：(1) Rust 侧 `types.rs:599-613` `shape_from_int_args` 新增 `HirExprKind::Var(name)` 分支，变量参数提升为 `Dim::Symbol(name.clone())`；(2) tenthc 侧 `hir.th:46-54` `HirType` 新增 `symbol_dims: str` 字段（方案 A，分号分隔 Symbol 维度名字，`dim=-1` 标记 Symbol），`lower.th` 的 `dims_count_from_args`/`dims_d0/d1/d2_from_args` 支持 Var，新增 `dims_symbol_names_from_args` 和 `add_tensor_type_with_symbols`，消费者加 -1 跳过检查；(3) 额外修复 transpose 3D shape 推断 bug（`types.rs:383-395`，改为对任意 ≥2D 都交换最后两维）；(4) bridge.rs 无需改动。**遗留**：tenthc 侧 transpose 仍保守跳过 3D（见 AUDIT-11.4.14）；表达式参数（如 `n*2`）仍退化为 Any（属 P2 层级二）。**验证**：shape_check_compile_test 77 passed、自举 [OK]。详见 MEMO.md 2026-07-30 feat P1-1 条目） |
| AUDIT-11.4.14 | tenthc 3D+ transpose shape 推断双侧不一致 | P1-1 修复同步登记（2026-07-30） | P1-1 修复 Rust 侧 transpose 3D shape 推断 bug 时，tenthc 侧未同步——tenthc 的 `lower.th` transpose 仍保持"非 2D 退化为 unknown"的保守行为。**影响**：双侧在 3D+ transpose 上不完全一致（Rust 侧正确交换最后两维，tenthc 侧保守返回 unknown 跳过检查）。**风险**：低——tenthc 侧保守跳过不会误报，只是放弃 shape 信息；tenthc 自举源码不依赖 3D transpose 的精确 shape。 | ⚠️ 已知限制（2026-07-30 登记）：tenthc 侧保持保守行为，待后续 tenthc transpose shape 推断对齐 Rust 侧（需在 `lower.th` 实现 3D+ 最后两维交换逻辑）。**性质**：双侧非完全一致 + 低风险保守跳过。详见 MEMO.md 2026-07-30 feat P1-1 条目） |
| AUDIT-11.4.15 | 泛型函数运行时不可用（tensor<f64>([...]) + randn<T>） | 用户反馈 + 诊断发现（2026-07-31） | 两个独立根因：(a) `tensor<f64>([1.0, 2.0, 3.0])` 语法经 HIR 编译为 `Call("tensor", [ArrayLiteral])`，但 `tensor` native 仅克隆参数返回 `Value::Vec`/`Value::Array`，后续 `t1 + t2` 在 `eval_binary` 找不到 `(Vec, Vec)` 分支报"加法类型不匹配"；(b) `hir/lower/lower_expr.rs` `NATIVE_GENERIC_CTORS` 分支强制要求类型参数是 `Type::Base(BaseType)`，泛型函数体内 `randn<T>(...)` 类型参数是 `Type::TypeParam`，触发 TypeError "类型参数必须是具体 BaseType"。**影响**：泛型函数体内调用张量构造 native 全部失效；`tensor<f64>([...])` 字面量语法实际不可用。 | ✅ 已修复（2026-07-31）：(1) `runtime/value.rs` 新增 `array_to_tensor(&Value) -> TenthResult<Value>`——递归处理 `Value::Vec`/`Value::Array`，`flatten_values` 计算形状与数据（嵌套数组递归 + 形状一致性校验 + 叶子元素 Float/Int/Float32/Bool→f64），`unpack_shared` 解包 `Value::Shared`/`Value::SharedBox`（返回 owned 副本规避 `RefCell::borrow()` 的借用冲突）；`runtime/natives.rs` + `runtime/interpreter/natives.rs` 双侧 `tensor` native 改为调用 `array_to_tensor`；(2) `hir/lower/lower_expr.rs` NATIVE_GENERIC_CTORS 分支扩展——`Type::TypeParam { name }` 时保留原始 `func_name` + ret_ty.dtype 保留为 TypeParam；(3) `hir/lower/mod.rs` `substitute_kind_in_place` 的 `HirExprKind::Call` 分支新增 native 构造函数名修正——TypeParam 替换为 BaseType 后按 (name, dtype) 改写 func_name（F32→`randn_f32`/`zeros_f32`/`ones_f32`/`rand_f32`，F16→`zeros_f16`/`ones_f16`，BF16→`zeros_bf16`/`ones_bf16`）。**测试**：`tenth/tests/fixtures/generic_diag.th` 验证两场景——`tensor<f64>([1,2,3]) + tensor<f64>([10,20,30])` = `[11,22,33]` + `make_noise<f32>(4)` 正确实例化 `randn_f32`。**验证**：cargo test --release 全套全绿（含 selfhost_frontend 4/4 + three_stage 1/0/2）。详见 MEMO.md 2026-07-31 fix 条目） |
| AUDIT-11.4.16 | NaN 比较静默 | 阶段2b lossy M2 建议登记（2026-07-31） | 浮点 NaN 与自身的比较（`NaN == NaN` → false、`NaN <= x` → false 等）静默通过，无编译期/运行时提示。**根因**：lossy 格把 NaN 归为 PossibleNaN 级别，M2 采用"PossibleOverflow/NaN 只传播不报"的防误报策略。**影响**：NaN 污染条件判断（`if x == x` 等）时逻辑错误静默，难以定位。 | ⚠️ 已知限制（2026-07-31 登记）：NaN 比较静默未处理——lossy 只传播 PossibleNaN 不报错；完整方案需 NaN 比较使用点检测（如 `x == x` 自比较、NaN 传播进 if 条件），推后。**性质**：静默算错路径 + 待增强。详见 MEMO.md 2026-07-31 护城河系列条目） |
| AUDIT-11.4.17 | JIT 整数溢出路径未覆盖 | 阶段2b lossy M2 建议登记（2026-07-31） | 登记时假设：`check_int_overflow()` 运行时溢出检测覆盖 VM/解释器路径，但 **JIT 编译路径**的整数算术（Add/Sub/Mul/Div/Mod）直接 emit，无溢出检查。**根因**：lossy M2 明确"整数组合暂不做"（既有 lexer 字面量范围检查 + check_int_overflow 运行时兜底），但该兜底在 JIT 路径不生效。**影响**：JIT 路径整数溢出静默回绕（i64 wrapping），与 VM 路径报错行为不一致。**2026-08-02 运行时部复核更正**：JIT translator 并非直接 emit——Add/Sub/Mul/Div/Mod/Neg 全部经 hostcall（`host_add` 等）调用与 VM 相同的算术原语（`vm.add_priv` 等），窄 dtype 范围检查本就在 JIT 生效；真根因是 `Cargo.toml [profile.release] overflow-checks=true` 下 i64 层溢出（`i64::MAX+1`、`i64::MIN/-1`）在算术原语直接 panic——VM 路径崩进程，JIT 路径 panic 穿越 `extern "C"` hostcall 边界触发 `panic_cannot_unwind` 直接 abort，三路径行为均错误（崩溃而非报错）。 | ✅ 已修复（2026-08-02，运行时部）：(1) 算术原语改用 `checked_*`——`runtime/vm/execute.rs` `add_priv`/`sub_priv`/`mul_priv`/`div_priv`/`Op::Mod`/`Op::Neg` + `runtime/vm/mod.rs` `vm.rem`/`vm.neg`（JIT hostcall 入口）+ `runtime/interpreter/binary.rs` `eval_binary` Add/Sub/Mul/Div/Mod + `eval_unary` Neg，i64 层溢出（`checked_*` 返回 None）与窄 dtype 范围溢出统一转为干净 RuntimeError；`runtime/value.rs` 新增 `int_overflow_err`/`int_dtype_name` 辅助；(2) JIT translator `emit_binop`/`emit_unop` 新增 `emit_err_check_abort`（`host_check_error` 检测 + 立即返回 ok=0），与 MethodCall 分支 B2 模式一致——binop 报错后立即中断，避免错误被后续操作覆盖（三路径错误消息一致）；(3) 解释器 Sub 对齐 VM `sub_priv`（保留左 dtype + 范围检查，原强制 I32 且无检查）。**验证**：新增 `tenth/tests/jit_int_overflow_test.rs` 13 项（加/减/乘/除/取负/i64 层溢出 + 链式中间错误 + 除零 + 正常算术 + 循环 fallback），VM/JIT/解释器三路径错误与结果一致；jit_test 10/10 + jit_enum_field 13/13 + 全量 0 回归 + 自举 [OK]。**性质**：路径覆盖缺口 + 行为不一致（修复后三路径一致）。**遗留**：i64 字面量后缀运行时丢失为 I32（既有 dtype 保留问题，非本 AUDIT 范围）；JIT 错误消息带"运行时错误 — 运行时错误 —"双前缀（既有装饰，非功能性）。详见 MEMO.md 2026-08-02 fix 条目） |
| AUDIT-11.4.18 | 标量→张量静默降级 natives 未全审计（**2026-09-16 审计已完成 → 见 §11.4.48，确认 5 处绕过**） | 阶段2b lossy M2 建议登记（2026-07-31）；2026-09-16 总师委派编译器部只读审计完成 | 标量×/±/÷张量 = 静默降级路径（scalar cast 到张量 dtype，如 `f16_tensor * f64 标量`）。lossy M2 已静态覆盖二元"标量 op 张量"路径（标量 F32/F64 ×/±/÷ Tensor[F16/BF16/F32] → Lossy，使用点报错 + `lossy(...)` 显式放行），但 **natives 内部标量/常量处理未逐行审计**（阶段2b §7.7 已披露）——softmax/reduce 等 natives 内部的隐式 dtype 收缩不在 taint 覆盖内。 | ⚠️ **审计已完成（2026-09-16）→ 详见 §11.4.48（确认 5 处绕过）**；原登记 ⚠️ 已知限制（2026-07-31）：lossy 已覆盖"标量 op 张量"二元路径；natives 内部标量/常量处理残留未覆盖降级路径，待逐算子审计（natives 内标量/常量处理未逐行审计为既有披露，非本次新发现）。**性质**：覆盖缺口 + 待审计。详见 MEMO.md 2026-07-31 护城河系列条目） |
| AUDIT-11.4.19 | parity_test / jit_stack_overflow_test 默认线程栈（2MB）栈溢出基线 | 测试环境问题（2026-07-31 登记；M1 提交 0a426ad 即存在，非新回归） | cargo test 默认线程栈 2MB 下，`parity_test` 与 `jit_stack_overflow_test` 触发 Windows STATUS_STACK_OVERFLOW（0xc00000fd）。**根因**：Windows 默认测试线程栈 2MB，全量 1511 项测试中部分套件（parity/jit_stack_overflow）递归深度超过 2MB。**验证**：设置 `RUST_MIN_STACK=33554432`（32MB，实测）后全量 1511 passed / 0 failed；64MB（268435456）亦可用。与 AUDIT.md §六 #7（debug 模式栈溢出预存）同源不同表现——本条目为默认线程栈基线问题。 | ✅ **已缓解（2026-09-16，测试部复验 + 总师独立复验）**：**本条描述的失败在当前 HEAD 不能复现**——不设 `RUST_MIN_STACK` 时 `parity_test`（129 passed）与 `jit_stack_overflow_test`（3 passed）**均通过**（总师独立复验：临时移走根 `.cargo/config.toml`、确证 ambient 变量未设后直跑，结果一致）。**但隐患真实且余量≈0**：`GetCurrentThreadStackLimits` 实测未设该变量时 libtest 工作线程栈**恰为 2.00 MiB**（非 PE 的 64 MiB），而 `jit_stack_overflow_test` 在 **1.5 MiB 即溢出** ⇒ 该用例是**踩着默认 2 MiB 刀刃通过**，任何栈用量增长都会让它翻车。**性质修正**：由「必须手动设 ≥32MB」改为「历史需手动设；当前版本踩着默认 2 MiB 通过、余量≈0」。**缓解已落地**：`RUST_MIN_STACK=33554432` 改由 cargo 配置 `[env]` 声明（仓库根 `.cargo/config.toml`），规范命令**无需手动设**（见 `AUDIT-11.4.49`）。**取舍披露（总师已确认）**：全局抬高栈使该用例原先「偶然」充当的 2 MiB 深递归金丝雀作用消失——判断为**错位的金丝雀**（该用例真正守护的是 JIT **虚拟栈** `MAX_STACK_DEPTH=64`，与 OS 线程栈无关；仓库另有 `jit_call_path_test` 17 项与 P-2 专门守护深递归），故接受此取舍。原状态 ⚠️ 环境依赖（2026-07-31）：验证命令需先 `$env:RUST_MIN_STACK="33554432"` 再 cargo test；影响 CI 与本地默认执行（不设置则 parity/jit_stack_overflow 栈溢出误报为失败）。**性质**：环境基线 + 非代码缺陷。详见 MEMO.md 2026-07-31 护城河系列条目） |
| AUDIT-11.4.20 | lossy 设计取舍（PossibleOverflow/NaN 只传播不报、递归保守 Exact、方法调用不查 callee） | 阶段2b lossy M2 设计取舍登记（2026-07-31） | 三项保守/防误报取舍：① **PossibleOverflow/NaN 只传播不报**——浮点溢出/NaN 的产生路径编译期不告警，仅 Lossy 级别使用点报错（防误报底线：静态不可判定的不报）；② **递归/互递归函数保守返回 Exact**——未做不动点，递归函数内的污点不跨调用传播，可能漏报；③ **方法调用不查 callee 返回污点**——只 receiver⊔args，方法返回值污点保守为 Exact，可能漏报。**影响**：三项取舍均可能导致 lossy 分析漏报（漏报方向安全，不会误报）。 | ⚠️ 已知限制（2026-07-31 登记）：三项取舍均为防误报/保守策略，如实记录。递归不动点求值 + 方法 callee 返回污点查询列为未来增强候选（架构设计文档 §3.3 已披露）。**性质**：设计取舍 + 潜在漏报。详见 MEMO.md 2026-07-31 护城河系列条目） |
| AUDIT-11.4.21 | VM `&mut` 写回顺序相关失效（静默错误值） | 手册示例验证（E 任务）发现（2026-08-02） | `runtime/vm/execute.rs` 写回路径：共享引用 `&x` 先声明、可变引用 `&mut y` 后写回（`*m = 20`）时值不更新——VM 输出 10、解释器输出 20（顺序相关：先 `&mut` 后 `&` 则正常）。**静默错误值**（无报错、结果错误），高危——引用语义与解释器不一致。最小复现 `.trae/tmp/manual_audit/m_ref_multi.th`（另 `d01_ref_move.th`）。手册 §10.1 引用示例受影响（手册示例需修/需等待修复）。 | ✅ 已修复（2026-08-03，运行时部）：VM 引用语义补齐——新增 opcode MakeRef(59)/MakeMutRef(60)/Deref(61)/DerefStore(62)（op.rs + chunk.rs emit/read_op + execute.rs 执行 + bytecode.rs 编译 + JIT fallback 双侧）；`&mut 变量` 经 Value::Shared 槽位 + Value::MutRef(Weak)，`*m = v` 写穿 Shared 不再硬编码 Store(0)，写回与声明顺序无关；算术/比较/取整前置解包 Shared（对齐解释器 eval_binary）。**验证**：`m_ref_multi.th`、`m_ref_slot1.th`（y 非槽 0 真实缺陷，修复前静默错值）VM=解释器一致；守护测试 `vm_ref_vec_methods_test` 19 项全绿；stdlib/ownership/vm 等针对性套件 291 项 0 回归。 |
| AUDIT-11.4.22 | VM 高阶函数/闭包作参数缺口 | 手册示例验证（E 任务）发现（2026-08-02） | `collections::iter::map/filter` 等闭包作参数的高阶函数在 VM 路径不可用（`m_map_diag.th`：map 结果无 len / 报未定义函数）；解释器正常。与批次2 A（a1 VM 闭包值调用）任务**重合**——VM 无法以闭包值作实参，修复随 a1 推进（登记关联）。手册 §12.7 代表性函数示例受影响。 | ✅ 部分修复（2026-08-03，a1 P4）：闭包值调用缺口随 a1（P1-P3）修复——`iter::map/filter/reduce`、`collections::any/all/find/count_if/partition`、`curry::partial/curry/compose`、`accumulate_loop<T>`、`runtime::run_with_limit/limit_or_default/run_with_timeout/timeout_or_default` 在 **VM 路径全部可用**（VM=解释器对拍一致，对拍用例 `.trae/tmp/a1_p4_verify/`）；`Tenth实例/闭包合集/closures.th`、`闭包捕获/closure_capture.th` VM 转绿；stdlib_smoke_test 4 个高阶用例（M29/M31/M49/M50）已翻转为 VM 路径（56/56 全绿）。**剩余**：`collections::flat_map` 被新发现独立缺陷（**VM Vec 方法分派缺 `extend`**，解释器有；同族缺口另有 VM Map 缺 `entries` 阻塞 `map_values/filter_map`）阻塞——非 a1 残余（闭包调用本身正常，t04e 证实闭包返回 Vec 可用）、非 AUDIT-11.4.23，属既有 VM/解释器方法对齐缺口，待另行安排（建议登记新 AUDIT）。`group_by` 仅存在于 collections.th 文档注释，无实现（非 a1 问题）。 |
| AUDIT-11.4.23 | 文件级模块限定调用不可用 | 手册示例验证（E 任务）发现（2026-08-02） | `use std::env;` 后 `env::get_or_empty(...)`、`use nn::activations;` 后 `activations::leaky_relu_select(...)` 在 VM 与解释器**均**报「未定义的函数」（两种限定调用模式均不可用）；正确写法是 `use std::env::get_or_empty;` 导入具体函数后直接调用。**手册 4 处示例**（§11.6 / §12.13 / §12.14.2 等）依赖此模式受影响（手册示例需修/需等待修复）。复现 `.trae/tmp/manual_audit/m_env2.th`、`m_activations_q.th`、`m_activations_direct.th`。 | ✅ 已修复（2026-08-03，编译器部）：**根因**：① `use std::env;`（末段是模块文件）被当作「导入单个函数 `env`」处理——模块函数虽已整体进 `self.functions`，但末段在模块内找不到同名函数时**不注册任何别名/作用域符号**，`env::get_or_empty` 只能解析为扁平名 `env::get_or_empty`（永远不存在）→ 报未定义；② 目录型模块（`use std::collections;`）连文件都加载不到（`try_import_file` 缺「目录/<末段>.th」分支，头部注释有但代码未实现）→ 静默 no-op。**修复**（`hir/lower/`）：(1) `try_import_file` 补「`<rel>/<last>.th`」目录模块分支；(2) `use` 处理区分「末段是函数 vs 模块」——末段非模块内函数且完整 path 命中模块文件时，注册**模块别名**（`module_aliases: alias→模块key`）+ 模块全部函数进作用域（裸名与限定调用均可，手册 §12.14.2 同时用 `env::get_or_empty` 与裸 `exit`）；(3) `lower_expr` `Ident("mod::fn")` 新增模块限定解析（枚举变体优先，其次模块别名→底层函数名），泛型也注册进 `generic_funcs` 供 `mod::fn<T>(...)`。**验证**：`env::get_or_empty`（手册 §12.14.2 完整示例含 `exit`）、`collections::flat_map`（目录模块）、`activations::leaky_relu_select_default` 均 VM=解释器一致；既有直接导入/glob 不回归。守护测试 `module_qualified_call_test` 13 项全绿；`stdlib_smoke_test` 56/56 + `stdlib_test` 147/147 + `module_test` 6/6 + `l23a_fix_test` 10/10 + 自举 [OK]。**遗留**：泛型函数**普通调用**（无显式类型参数，`activations::leaky_relu_select(x, 0.01)`）为既有语言限制（同文件泛型普通调用同样失败，非本 AUDIT 范围），需显式 `mod::fn<T>(...)` 或 `fn<T>(...)`——手册 §11.6/§12.13 示例仍需文档部按 D4 改写（`leaky_relu_select<f64>(...)`）。**tenthc**：无需同步（tenthc 无模块系统，use 解析后丢弃）。 |
| AUDIT-11.4.24 | Vec 相等比较 `==` 缺陷（元素相同返回 false） | 手册示例验证（E 任务）发现（2026-08-02） | Vec `==` 元素相同但比较结果为 false：`decoded == [72, 105]`（解码值 72/105 均正确）返回 false。**影响**：断言/条件判断静默错误，高危。复现 `.trae/tmp/manual_audit/m_b64_diag.th`（§12.15.4 Base64/Hex/URL 示例断言必失败——手册示例需修/需等待修复）。 | ✅ 已修复（2026-08-03，运行时部）：VM `vm_eq` 与解释器 `values_eq` 均补 Vec/Array 元素逐一相等（解包 Shared），`decoded == [72, 105]` 现返回 true；另修解释器 `base64_encode`/`hex_encode` 未解包 Shared 元素（数组字面量元素 Shared 包裹）导致读 0 的静默错值——现 VM=解释器输出完全一致（`SGk=`/72/105/true/`ff0080`/255/0/128）。守护测试 `vm_vec_eq_*` 通过。 |
| AUDIT-11.4.25 | 字符串不支持 `\u{...}` Unicode 转义 | 手册示例验证（E 任务）发现（2026-08-02） | lexer `read_string` 仅支持 `\n\r\t\\\"\{\}`，无 `\u`/`\xNN` 分支——`"cafe\u{0301}"` 原样输出 `cafeu{0301}`，§12.15.2 Unicode 规范化示例不可运行（手册示例需修/需等待修复）。复现 `.trae/tmp/manual_audit/m_escape.th`、`m_unicode_diag.th`。 | ✅ 已修复（2026-08-03，编译器部）：lexer `read_string` 新增 `\u{HEX}` Unicode 码点转义（UTF-8 编码，`\u{0301}`→U+0301 组合重音、`\u{1f600}`→😀，无效码点/非十六进制报 LexerError）与 `\xNN` 十六进制字节转义（与字节串一致）；`\u` 后非 `{` 保持原样向后兼容。验证：m_escape/m_unicode_diag 双路径 EXIT=0 输出一致（café 规范化断言 true）；string_encoding_test 守护通过。 |
| AUDIT-11.4.26 | format 进制说明符 `{:x}`/`{:X}`/`{:o}`/`{:b}` 未实现 | 手册示例验证（E 任务）发现（2026-08-02） | `format("0x{:x}", 255)` 输出 `0x255`（说明符被忽略，按普通格式处理）；手册 §12.15.1 表格明确列出进制格式，文档夸大（手册示例需修/需等待修复）。复现 `.trae/tmp/manual_audit/m_format_diag.th`。 | ✅ 已修复（2026-08-03，运行时部）：`apply_format_spec`（VM `runtime/natives.rs` + 解释器 `interpreter/natives.rs` 双侧）重构为接收 `&Value`，支持 `{:x}`/`{:X}`（十六进制）/`{:o}`（八进制）/`{:b}`（二进制）/`{:d}`（十进制），含宽度/补零/`#` 前缀（`{:08x}`→`000000ff`、`{#x}`→`0xff`）；非整数回退默认字符串；既有 `>5`/`<5`/`^5`/`.2f` 语义保持。验证：m_format_diag 双路径 EXIT=0（0xff/FF/377/11111111）；string_encoding_test 进制守护通过。 |
| AUDIT-11.4.27 | f-string 仅支持简单标识符插值，不支持表达式与格式说明符 | 手册示例验证（E 任务）发现（2026-08-02） | lexer 插值用 `is_valid_ident` 限制——`f"pi ≈ {3.14159:.2f}"` 原样输出 `{3.14159:.2f}`；手册 §2.3 / §12.15.1 称支持任意表达式与格式说明符，文档夸大（手册示例需修/需等待修复）。复现 `.trae/tmp/manual_audit/m_format_diag.th`、`a03_fstring.th`。 | ✅ 已修复（2026-08-03，编译器部）：lexer `read_string(is_fstring)` 对 f-string 做括号深度感知的 `{expr:spec}` 扫描（`{{`/`}}`→字面花括号，与 Python 一致）；`lower_expr.rs` FString 分支按首个顶层 `:` 拆分表达式+格式说明符，表达式经子 Lexer/Parser 重新解析并 lower（任意表达式 `{a+b}`/`{3.14}` 等），说明符随模板 `{:.2f}` 由 format native 统一解析；顺带修 VM 既有限制（bytecode InterpolatedString 字符串化 `format(x)`→`to_string(x)`，普通插值含数字不再报错）。验证：m_format_diag/a03_fstring 双路径 EXIT=0（pi ≈ 3.14、name=Alice, age=30）；string_encoding_test 表达式/说明符/花括号转义守护通过。 |
| AUDIT-11.4.28 | VM Vec/Map 方法分派与解释器不对齐（flat_map/map_values/filter_map 阻塞根因） | a1 P4 std 高阶解锁验证发现（2026-08-03） | `runtime/vm/natives.rs` VM 方法分派缺方法，解释器 `interpreter/methods.rs` 全有：**VM Vec 缺 10 个方法**（`index_of`/`reverse`/`slice`/`extend`/`sort`/`dedup`/`first`/`last`/`flatten`/`chunks`）、**VM Map 缺 `entries`**。**影响**：`std::collections::collections::flat_map`（依赖 Vec.extend）在 VM 报「Vec 没有方法 'extend'」、`map_values`/`filter_map`（依赖 Map.entries）报「Map 没有方法 'entries'」——三者解释器路径均正常。**非 a1 残余**（闭包值调用 + 闭包返回 Vec 均正常，`t04e_diag_retvec.th` 证实）、**非 AUDIT-11.4.23**（直接导入函数名调用仍不可用，按任务判定依据可排除）。同族属性：VM/解释器方法分派对齐缺口。复现 `.trae/tmp/a1_p4_verify/t04d_diag_extend.th`（extend 缺失）、`t11a_diag_entries.th`（entries 缺失）。**建议修复方向**：运行时部按解释器实现补齐 VM 方法分派（`vm/natives.rs`，参照 `interpreter/methods.rs` 对应实现，约 15 行/方法）。 | ✅ 已修复（2026-08-03，运行时部）：`runtime/vm/natives.rs` `call_method_priv` 补齐 **Vec 10 方法**（`index_of`/`reverse`/`slice`/`extend`/`sort`/`dedup`/`first`/`last`/`flatten`/`chunks`）+ **Map `entries`**，语义对齐解释器 `methods.rs`。**验证**：`t04d_diag_extend.th`（extend）、`t11a_diag_entries.th`（entries）VM=解释器一致；`std::collections::collections::flat_map`/`map_values`/`filter_map` VM 路径可用（对拍 `.trae/tmp/manual_audit/m_std_flatmap_vm.th` 一致）；守护测试 11 项通过。 |
| AUDIT-11.4.29 | `collections.th` `group_by` 文档注释误导（仅注释无实现） | a1 P4 std 高阶解锁验证发现（2026-08-03） | `tenth/std/collections/collections.th` 文档注释声称有 `group_by` 函数，但源码中**无实现**——引用时报「未定义的函数」，属文档/实现不一致（非 a1 问题，VM/解释器均无此函数）。**建议**：文档部修正 collections.th 注释（删除或标注未实现），或补实现。 | ✅ 已修复（2026-08-03，总师直做）：`collections.th` 文件头 usage 注释已修正——移除 `use ...group_by` 误导行，改为标注「group_by 未实现，见 AUDIT-11.4.29」。 |
| AUDIT-11.4.30 | a1 闭包值调用遗留三项：native 别名 VM 断 / 递归闭包前端拒绝 / JIT 报错行号不保留 | a1 遗留 + B 批 JIT 独立限制（2026-08-03 登记，任务 9 修复） | ① **native 别名**：`let p = println; p("x")` 在 VM 报「期望可调用值，得到 Unit」——bytecode/VM 无 native 名→FnRef 表（解释器 eval.rs 有硬编码表），`LoadGlobal` 未命中得 Unit（响亮非静默）；② **递归闭包**：`let fact = |n| ... fact(n-1)` 在 lowering 报「未定义变量 'fact'」——闭包体自引用未绑定（前端限制，a1 范畴外）；③ **JIT 报错行号**：JIT 路径 hostcall 用 `set_last_error(e.to_string())` 传字符串，行号不保留（B 批记录为 JIT 独立限制）。 | ✅ 已修复（2026-08-03，编译器部 + 运行时部，任务 9）：**① native 别名**——VM `execute.rs` opcode 9 LoadGlobal 与 JIT `host_load_global` 在 globals 未命中时查 `natives` 表构造 `Value::FnRef`（用户全局 shadow native），`let p = println; p("x")` VM=JIT=解释器一致；**② 递归闭包**——lowering 新增 `Lowerer.self_ref_lets` 栈（`let name = <闭包>` 时压入），闭包体同名引用解析为 `Var(name)`（不报未定义变量）且**排除出 captures**（创建时槽位未绑定，按值捕获得 Unit/旧值；运行时按名解析：VM 经 globals / 解释器经作用域链）；`let fact=|n| if n<=1 {1} else {n*fact(n-1)}; fact(5)=120` 与「递归+捕获混用」（f(4)=762）VM=解释器一致；**③ JIT 报错行号**——JIT translator 在每个 hostcall 前把当前指令行号（chunk 行号表 `line_at(op_start)`）写入 `vm.current_line`，hostcall 用新 `Vm::set_jit_error` 补行号（取裸 message，消除双重前缀），`run_jit` surface 带行号 RuntimeError；除零/取模/非可调用/reshape/切片/索引越界全部带行号且文案与 VM 对齐。**验证**：守护 `jit_test` 新增 4 项（native 别名 2 + 递归闭包 2）、新建 `jit_error_line_test` 7 项全绿；`jit_test` 25/25、`vm_error_line_test` 10/10、相关回归（module_qualified/toplevel_let/silent_failure/stdlib/smoke/l23a）262 项 0 失败；自举 [OK]。**遗留 → ✅ 已根治（2026-08-03，M1-S2 true letrec）**：递归闭包逃逸作用域 + 多实例共存的别名静默错值已由 **true letrec** 根治——自引用绑定做成**实例级可变 cell**（`Value::Shared(Rc<RefCell<Value>>)`）捕获：HIR `HirExprKind::Closure` 新增 `self_refs`，闭包创建时先建空 cell 再 `BindSelfCapture` 写自身，体引用经捕获槽位 Load 到 cell、调用时解包 Shared 后间接调用；每实例 cell 独立随闭包走（逃逸作用域后仍可用），不再按名/全局解析。VM 新增 opcode `MakeCell(63)`/`BindSelfCapture(64)`；`CallClosure`/`call_value`/解释器 `eval_call`/`apply_closure` 对 Shared 解包；`Value` Debug 改转发 Display（防自引用 cell `{:?}` 无限递归）。**验证**：`t9b_multi_instance.th` VM=解释器=**762769**（修复前 522769/未定义变量）；新增守护 `tenth/tests/letrec_test.rs` **10 项**（多实例独立/逃逸作用域/main 级回归/递归+捕获回归/三实例/尾递归 TCO/嵌套/ HOF JIT 路径/工厂多实例/Debug 有界）VM=JIT=解释器三方对拍全绿；回归 31 套件 ≈850 项 0 失败；自举 [OK]。**遗留（如实，非本 AUDIT 范围）**：互递归（`let f=...g...; let g=...f...`）与条件分支共享 letrec（`let x = if c { |n| x(n-1) } else { |n| n }`）仍非 true letrec 共享绑定（每闭包独立 cell）。 |

| AUDIT-11.4.31 | 解释器缺 broadcast_to 方法分派 + VM 内联 mod 限定调用缺口（M1-S3a 两项同批修复） | 手册示例验证（E 任务）发现（2026-08-03 登记，M1-S3a 修复） | ① **L14（解释器缺 broadcast_to）**：手册 §11.3 `t.broadcast_to(3, 4)`（位置参数形式）在解释器（TENTH_NO_VM=1）报「未知的张量方法: broadcast_to」——VM 可用（`runtime/vm/natives.rs` 已注册）、tensor 层 `Tensor::broadcast_to`（`runtime/tensor/methods.rs:1087`）存在，仅 `runtime/interpreter/methods.rs` 未接线（与 AUDIT-11.4.28 同型反向）；② **L5（VM 内联 mod 限定调用）**：内联 `mod { }` 块（手册 §9.1/9.2）定义的函数经 `mod_name::fn_name()` 限定调用在 VM 报「未定义的函数 'mod::fn'」——任务 5（AUDIT-11.4.23）修的只是**文件级**模块（use 注册 module_aliases）；解释器在运行时按 `::` 拆分查 `self.modules` 故可用，VM 无此机制。 | ✅ 已修复（2026-08-03，运行时部 + 编译器部，M1-S3a）：**① broadcast_to**——`runtime/interpreter/methods.rs` 张量方法分派补 `broadcast_to`（目标 shape 由整数位置参数收集，失败报 RuntimeError，语义与 VM natives.rs 完全一致）；**② 内联 mod**——`hir/lower/lower_expr.rs::try_resolve_module_qualified` 扩展：首段命中 `self.modules` 内联 mod 键（无需 use）即解析 `mod::fn(...)` → 底层函数名 `Var("fn")`，并把内联 mod 函数补入 `self.functions`（first-wins，与 use 导入一致）——后端（bytecode/解释器/wasm）按函数名解析即通。**验证**：§11.3 broadcast_to、§9.1/9.2 内联 mod（直呼 + `use math::add;` 限定 + 表达式位置）全部 VM=解释器一致；文件级模块不回归（`module_qualified_call_test` 13/13）；新增守护 `s3a_runtime_gap_test` 14 项全绿；`stdlib_smoke_test` 59/59；release 构建通过。**遗留（如实）**：① 内联 mod 函数体裸名调用私有 sibling 仍不支持（VM 与解释器一致地失败，§9.1 的 `helper` 未在 `add` 内调用）；② 顶层函数与内联 mod 函数同名时 first-wins 可能解析到顶层同名函数（与文件级模块「先注册者胜」既有语义一致）；③ 嵌套多段 `outer::inner::fn()` 限定调用不在本次范围（手册仅单层）；④ `module_test` 当前无法编译（并行任务 S2 true letrec 在改 closures 区域，`HirExprKind::Closure` 增 `self_refs` 字段后 `eval.rs`/`wasm/closures.rs` 模式匹配暂未更新），非本次改动引入。 |
| AUDIT-11.4.32 | WASM 后端 4 项缺口：函数内引用全局 / 标签 break / 自定义运算符 native / 泛型枚举布局（+match 不支持） | M1-S1 阶段 1 可用性最后缺口（2026-08-03 登记，M1-S1 修复） | ① **函数内引用全局**：globals 只做进 main 的 local 槽，函数体 `Var(g)` 报「未定义变量 'g'」（VM/解释器经 globals/作用域链可用）；② **标签 break/continue**：`if_depths` 无标签维度，带标签形式直接报「暂不支持 WASM 后端」（明确报错不静默）；③ **自定义运算符 native**：operator 定义体内调用标量 math native（sqrt 等）报「未定义函数 'sqrt'」——WASM 后端 resolve_func 只认固定 host 集合；④ **泛型枚举布局**：`build_struct_layouts` 只建 `program.enums` 不建 `generic_enums`，且 **Match 在 WASM 完全不支持**（compile.rs 无 Match 分支落入 `_ => Err`）、枚举内存无变体 tag（无法区分变体）。另：**M3.5 潜伏 bug**——compile_main 原优先 main_expr，顶层 let 提升后 main_expr 常为 Unit 空块，`wrap_to_i32(Unit)` 发 `Drop`（空栈）致 WASM 校验失败（此前被「函数内引用全局报错」掩盖）。 | ✅ 已修复（2026-08-03，编译器部，M1-S1）：**① 全局**——globals 改真 WASM mut i64 全局（emit_global_section 声明 + global_map），main 前 GlobalSet 初始化，函数体 Var/Assign/AssignOp 经 GlobalGet/GlobalSet + 按声明类型转 i64 位存储；**② 标签**——`if_depths` 增标签维度，带标签 break/continue 从栈顶搜索匹配标签按相对深度计算 Br（语义与 bytecode loop_stack 一致）；**③ native**——sqrt/abs 用 WASM 原生指令内联，sin/cos/ln/pow 新增 4 个 host 函数（host_sin/host_cos/host_ln/host_pow，sections.rs/host.rs/wasmtime_host.rs 双侧注册 + 8 个测试 linker stub）；**④ 枚举**——泛型枚举布局补齐（TypeParam 字段统一 8 字节 i64 位存储），枚举布局统一为 tag(i64)+字段，**新增 Match codegen**（按 tag 比较变体、绑定字段、wildcard、字面量模式、守卫），f64 字段位存储往返正确；**顺带**：compile_main 改与 VM 一致优先 `fn main` + Unit 分支不 Drop。**验证**：新建 `m1s1_wasm_gaps_test.rs` **15 项全绿**；WASM 回归 wasm_backend_minimal 10 / f32_wasm 14 / parity 129 / three_stage 1(+2 ignored) / tenthc 系列 18 全绿；定向套件（generic_enum/toplevel_let/custom_operator/labeled_break/module/selfhost_frontend）全绿；自举 `[OK]`。**遗留（如实，非本次范围）**：① **类型系统 `abs(i64)` 返回 F64**（`infer_scalar_dtype` 默认 f64）而 VM 运行时返回 Int——既有不一致，WASM 按类型系统产 F64（`fn->i64 { abs(...) }` 会 wasmi 校验报错，不静默错）；② **tenthc wasm.th 未接标量 math**——tenthc 编译调用 sqrt/abs/sin/cos/ln/pow 的程序 fallthrough 到错误 call_idx（自举源码不用，无实际影响，建议后续 tenthc wasm.th 显式报错）；③ **Match 模式覆盖**——Tuple/Range/Struct/Binding 模式报错不静默（枚举变体/字面量/wildcard 已覆盖）。 |
| AUDIT-11.4.33 | `?` 解释器 double-wrap（try 块捕获路径）+ 十进制字面量下划线 bug + 字节串仅词法层 | M1-S5 语言规范评审（2026-08-03 登记，M1 收尾修复） | 三项与规范/手册声称不符的实现缺口（M1-S5 评审发现）：① **`?` double-wrap**——披露于 `try_operator_test.rs` 注释：`?` 遇 `Err` 时解释器返回 `Result::Err(Result::Err(e))`（double-wrap），VM 返回单层。函数边界 `unwrap_return` 曾在 b0c9588 修好单层，但 **try 块捕获处**（`runtime/interpreter/eval.rs` TryBlock 分支）仍把 TryPropagate 携带的完整 `Result::Err(e)` 再包一层——`try { f()? }` 在解释器路径仍 double-wrap；② **十进制下划线**——规范 §2.6.1 声称「下划线分隔（任何进制均支持）」，但 lexer `read_number` 十进制路径消费 `_` 并入 s 后直接 `s.parse()`，`"1_000_000"` 解析失败报「无效的整数」（进制路径正确跳过下划线，十进制/浮点漏剥离）；③ **字节串半实现**——`b"..."` 仅词法层产出 `ByteString` token，parser/HIR/运行时未接线，作为表达式直接报「意外的标记」——规范 §2.6.8 声称「运行时 Vec<u8>」失实。 | ✅ 已修复（2026-08-03，运行时部 + 文档部，M1 收尾）：**① `?` 单层**——`runtime/interpreter/eval.rs` TryBlock 捕获分支对齐 `core.rs::unwrap_return` 逻辑：TryPropagate 的 err_val 已是完整 `Result::Err(e)` 则直接透传（不再包一层），非 Result 值才兜底包装——与 VM `Op::Try` 单层语义一致（VM try 块 catch 路径本就透传栈上 `Result::Err(e)`）。验证：`try_operator_test` 12→17 项（新增多层 `?`/try 块成功/捕获 + 解释器/VM 对拍严格单层断言），VM=解释器一致；**② 下划线**——`lexer.rs read_number` 三处 parse（整数/浮点/浮点带后缀）前 `s.replace('_', "")`，`1_000_000`/`1_000.5` 现合法（进制路径原已跳过）；**③ 字节串**——`parser/expr.rs parse_primary` 把 `ByteString` token 降为整数数组字面量（每字节 → `Int(b, I32)`，复用 ArrayLiteral 全管线），`b"..."` 现可作表达式：`.len()`/`hex_encode`/`base64_encode`/`write_bytes` 可用（运行时为 `Vec<i64>` 字节数组）。**验证**：release 构建通过；手册修订示例 v1_literals（进制/下划线/后缀/提升/字符/`\u`/`\x`/原始/多行/字节串）/ v3_precedence（`1 == 2 < 3` → false）/ v5_try（try 块单层）/ v5_async（async/await/spawn/yield）VM=解释器双路径 EXIT=0 一致；针对性套件 try_operator 17 / string_encoding 68 / int_types 14 / lexer 34 / yield 6 全绿。 |
| AUDIT-11.4.34 | match tuple 模式 + guard 缺陷（guard 失败后回退错乱 + JIT 编译 panic 逃逸） | M2-A5 VM-vs-JIT 一致性套件发现（2026-08-03 登记，部分修复） | ① **VM guard 回退错乱**：`match t { (a,b) if a>b => ..., (a,b) => ..., _ => ... }` 中 guard 失败后，VM 不尝试**下一条 tuple 臂**而是直接落 wildcard——`(2,3)`（guard false）VM 返回 `_ => -1` 而解释器正确返回 23；② **JIT 编译 panic 逃逸**：任何 tuple 模式 + guard 都会在 Cranelift 低化阶段（`finalize_definitions`，位于既有 catch_unwind 之外）触发 `TryFromIntError` panic 并**逃逸出 JIT**（进程级 panic 红线），二进制顶层捕获后回退 VM 才免于崩溃。**影响**：tuple 模式 + guard 组合不可靠（VM 静默错值方向 + JIT panic）。最小复现 `.trae/tmp/a5_tguard.th` / `a5_tguard2.th`（已删，用例模式见 jit_consistency_test 注释）。 | ✅ 已修复（2026-08-04，编译器部，1.0 前红线修复轮）：**① VM guard 回退错乱已根治**——`compile/bytecode.rs` match tuple 臂 lowering 对齐 EnumVariant/Struct 臂模式：有 guard 时在绑定前保留一份 scrutinee 副本（Dup），guard 通过后 Pop，guard 失败跳下一条臂时栈上仍留 scrutinee 供重试——guard 失败不再直接落 wildcard（此前 `(2,3)` VM 落 `_ => -1` 静默错值，现正确试下一条 tuple 臂返回 23）。**② 已于 M2-A5 根治**（JIT 编译 panic 逃逸 catch_unwind）。**验证**：新增 `tenth/tests/redline_overload_match_test.rs` 6 项 match guard 用例（guard 失败→下一条 tuple 臂 / 多 guard / guard+wildcard / 混合臂 / 循环内 / 绑定隔离），解释器=VM=JIT 三路径一致；既有相关套件（call_arg_type_check/typestate/try_operator/pattern_match/tuple/jit 全套/stdlib/instance_batch/WASM）全绿；自举 [OK]。**遗留（如实）**：VM 对 tuple 模式含**字面子模式**（如 `(0, 0) => ...`）仍不支持（`all_simple` 检查落 fall-through，VM 注释既有限制，与本次 guard 修复无关），解释器支持——非本次范围，另登记。 |
| AUDIT-11.4.35 | i64 注解小函数循环内联静默错值（分析预测 spec 而发射内联 → 循环回边 Load 读过期标量） | A2 内联静默错值红线 bug（总师实测 + 编译器部 IR 定位，2026-08-03 登记并修复） | JIT 路径 `emit_direct_call` 中**内联（A2）优先于**特化 ABI（A6），但标量分析只建模 spec 路径（结果 I32）：分析预测 `i64` 注解小函数调用结果 I32/spec，发射却内联产出 Value → 一般路径 `Store(i)` 只写 Value 槽、`local_scalars[i]` 残留过期标量 → 循环回边处分析把该局部重置为 I32，下一轮 `Load(i)` 重专用化读**过期标量槽** → **静默 0**（1/2/3/4 参内联循环均受影响，溢出检查同步失效）。**复现**：`s = f3(s,i,1)`（3 参 `i64` 小函数）循环 3M 次 JIT 静默返回 0，应累加并在 ~2.1e9 报「整数运算结果溢出 i32 范围」；解释器（TENTH_NO_VM=1）正确报错。既有测试用 `Int`（无 spec 签名）未覆盖此路径。 | ✅ 已修复（2026-08-03，编译器部）：**双层修复**——(1) **分析层** `analyze_scalar_kinds` transfer 的 `Call`/`CallN` 新增 `inline_eligible`（与发射端 `inline_analyze`/`try_inline_call` 条件逐一对应：指令数 ≤16、纯标量白名单、参数个数匹配、num_locals ≤64）→ **可内联调用预测 Unknown**（与内联 Value 结果一致），消除分析/发射漂移；(2) **发射层**一般路径 `Store(i)` 增加 `local_scalars.remove(&i)` 安全网（Value 槽权威时标量槽必须失效，防任何残留过期标量被 `Load` 重专用化）。内联失败静默回退 A1/特化 ABI 机制不动。**验证**：`c4.th`(3 参)/`c3.th`(4 参)/`c2.th`(2 参) 溢出 JIT=解释器一致报「溢出 i32 范围」（2147516415/2147516415/2147516416 逐字节一致）；正常值 3/4/2/1 参循环累加正确（55/75/5050/5250…）；`jit_consistency_test` 43→48（+5 回归：3/4 参循环累加、溢出行号对拍、2/1 参回归、混合字符串不内联对照）；8 个 JIT 套件（jit_test 62 / jit_consistency 48 / jit_spec_abi 17 / jit_error_line 12 / jit_int_overflow 13 / jit_enum_field 10 / jit_stack_overflow 3 / stdlib_smoke 59）全绿；自举 [OK]；fib(28) 9-10ms / loop 65-67ms 无倒退。详见 MEMO.md 2026-08-03 A2 条目。 |
| AUDIT-11.4.36 | JIT 分析/发射 binop 专用化集合漂移（Mul-I32 / Mod-F64 分析预测标量而发射不专用化 → spec 调用预测不一致 → 整函数回退 VM） | M2.6-P2 JIT 静默错值审计发现（2026-08-04 登记并修复） | `analyze_scalar_kinds.transfer` 对算术统一预测 `(I32,I32)→I32 / (F64,F64)→F64`，但发射端 `emit_binop` 的专用化集合是 **I32 = Add/Sub/Div/Mod（Mul 不专用化——checked_mul 复杂，保 hostcall 正确性）、F64 = Add/Sub/Mul/Div（Mod 不专用化——VM 报错）**——两处漂移：**Mul(I32,I32)** 分析预测 I32 而发射走通用 hostcall（结果无标量槽）、**Mod(F64,F64)** 同理。**后果（能力缺口 + 潜在静默错值隐患）**：`f(k * 10)`（spec 函数、实参为 Mul 结果）在循环内——分析把 `k*10` 预测为 I32 → 预测 spec 调用；发射期 `try_spec_call` 找不到该实参的标量槽 → 触发防御性 Err「JIT A6: 特化参数预测不一致（发射期缺标量槽）」→ **整个 main 整函数回退 VM**（结果仍正确——防御性检查兜底，但 JIT 不生效，且属分析/发射漂移类隐患）。**复现**：`fn f(x:i64)->i64{ 内部循环 }` + main 循环内 `t = t + f(k * 10)`——修复前 main 未 JIT 编译（`jit_compiled_main` 断言失败）。 | ✅ 已修复（2026-08-04，测试部 + 编译器部，M2.6-P2）：`transfer` 按发射端专用化集合**逐 opcode 对齐**——`Mul` 仅 `(F64,F64)→F64`（I32→Unknown）、`Mod` 仅 `(I32,I32)→I32`（F64→Unknown）、`Add|Sub|Div` 不变。修复后 `f(k*10)` 循环模式 main 恢复 JIT 编译（分析不再误预测 spec → 实参非标量走通用调用，值正确）。**验证**：新增 `tenth/tests/jit_silent_audit_test.rs` **34 项全绿**（D1-D5 审计维度 + 对抗形态：spec 体内嵌 native 方法调用/内部循环、通用体内嵌 spec 调用；进程内三路径对拍 + is_compiled/is_spec_compiled 覆盖断言 + 子进程字节级 stdout/exit 对拍）；既有 9 套件全绿（jit_test 62 / jit_consistency 48 / jit_spec_abi 17 / jit_skip_chunk_ctx 10 / jit_error_line 12 / jit_int_overflow 13 / jit_enum_field 10 / jit_stack_overflow 3 / stdlib_smoke 59）；自举 [OK]；perf_bench3 fib(28) 8-10ms / loop 59-61ms 无倒退（P1 成果保持）。详见 MEMO.md 2026-08-04 M2.6-P2 条目。 |
| AUDIT-11.4.37 | JIT 原生 F64 比较 `Eq`/`Neq` 用精确 `FloatCC::Equal/NotEqual`，而 VM/解释器浮点相等为 **epsilon 比较 `(x-y).abs() < 1e-10`**——既有潜伏静默分歧（f64 特化启用前被「无 f64 标量槽」掩盖） | M2.6-P3 f64 特化调研发现（2026-08-04 登记并修复） | 三路径浮点相等语义核对：解释器 `runtime/interpreter/binary.rs:354` 与 VM `runtime/vm/execute.rs:448/471` 均为 `(x-y).abs() < 1e-10`（Float32 为 1e-6），JIT `emit_native_cmp` 的 F64 分支此前用 `FloatCC::Equal`/`NotEqual` 精确比较。**分歧点**：`0.1 + 0.2 == 0.3`（精确不等、epsilon 相等）——VM/解释器返回 true，JIT 原生路径返回 false（静默错值）。**触发面**：任何两个 F64 标量槽做 `==`/`!=` 的 JIT 编译函数（A2 标量专用化路径已存在，但无 f64 特化时标量比较常被 hostcall 覆盖；f64 特化会把它暴露到热路径）。**NaN 语义差异**：`FloatCC::NotEqual` 对 NaN 为 true 而 VM `!((NaN).abs() < 1e-10)` 亦为 true（一致），但 `|a-b| == 1e-10` 恰好时 `NotEqual` 与 `!(<)` 分歧（边界）。 | ✅ 已修复（2026-08-04，编译器部，M2.6-P3）：`emit_native_cmp` F64 分支对 `Eq`/`Neq` 改发 **epsilon 比较**——`Eq = |a-b| < 1e-10`（fsub→fabs→fcmp LessThan）、`Neq = !(|a-b| < 1e-10)`（select 翻转，NaN 语义与 VM 一致：NaN 下比较为 false → Eq=false、Neq=true）；`Lt/Gt/Lte/Gte` 保持精确有序比较（与 VM `x < y` 等一致）。**验证**：新增 `tenth/tests/jit_spec_f64_test.rs` `spec_f64_epsilon_eq_regression`（0.1+0.2==0.3 → true、1.5==1.5 → true、1.5==1.6 → false 及 Neq 翻转，三路径逐位对拍）；既有 10 套件全绿。详见 MEMO.md 2026-08-04 M2.6-P3 条目。 |
| AUDIT-11.4.39 | 重载函数运行时分派 VM/解释器不一致（VM HashMap 后注册覆盖 vs 解释器取第一条同名 → 同一重载调用两路径可能选中不同签名，静默错值风险） | M3.2 G6 多重载缺口修复实证发现（2026-08-04 登记，⚠️ 待排期） | 实证（M3.2 修复验证临时脚本）：`fn g(x: i64, y: i64) -> i64` + `fn g(x: str) -> str`，`g(1, 2)`——**VM** `functions: HashMap<String, usize>`（`vm/mod.rs:625-630` `add_fn` 按名 insert，后注册覆盖先注册）→ 调用 g(str) chunk，传 [1,2] 静默返回 `"ONE_PARAM"`（错误值）；**解释器** `self.functions: Vec<HirFnDef>`（`interpreter/core.rs:105` 全量注册，`natives.rs:3171` `find` 取第一条同名）→ 调用 g(i64,i64) 返回 103（正确）。即**编译期 `resolve_fn_overload` 选中的签名与运行时实际调用签名可能不一致**。`overload_resolution_unchanged`（call_arg_type_check_test）只测解释器路径故未暴露。**性质**：静默错值方向 + 两路径不一致（护城河红线）。**M3.2 缓解**：编译期检查已拦截「类型确定不兼容」的重载调用（`g(1,"x")` 现在编译期报错），缩小了暴露面；但「类型正确、参数数量/签名不同的重载」运行时仍可能选错签名。 | ✅ 已修复（2026-08-04，编译器部，1.0 前红线修复轮，方案①编译期改写）：**新增 `hir/lower/overload_mangle.rs`**——lowering 完成后对顶层 HirProgram 做确定性重载 mangling：名字 ≥2 个不同签名 → 每个签名分配唯一 `__ovl_<name>_<idx>`（去重签名首现序）；定义改名（`HirFnDef.name`）+ 调用点改写（`Call`/`GenericCall` 按实参类型复用 resolve_fn_overload 匹配规则选中签名）+ 函数值引用改写（裸 `Var` 且 ty=FnType → 首个签名，与 lookup_fn 语义一致）。三后端（bytecode/VM/JIT/解释器/WASM）按 mangled 名解析天然一致，运行时零歧义零开销。**配套修复**：`types.rs resolve_call_type` 的 merge_return_shape 只匹配与已解析签名同参的函数定义（原取第一个同名函数返回类型，多重载下调用点静态返回类型被错误覆盖 → 下游 G6 误报/静态类型错误）。**验证**：新增 `tenth/tests/redline_overload_match_test.rs` 8 项（审计反例 arity 不同签名 / 同参不同类型（显式类型字面量）/ 递归经重载 / 函数值引用 / str 先声明 / 三签名 / mangled chunk 注册白盒 / 单签名不回归），解释器=VM=JIT 三路径对拍一致；既有相关套件全绿；自举 [OK]。**范围边界（保守，如实）**：仅处理顶层 program.functions 与 main_expr；跨文件模块重载按名 first-wins 去重（既有语义），模块内重载不完整存活到运行时分派集合，不在本次范围；泛型函数（generic_funcs）不经 define_fn 注册、调用走显式类型实参，无运行时歧义，不处理。 |

| AUDIT-11.4.40 | `Vec.get()/pop()` 类型系统误标——注册返回裸 `Enum("Option")`（`types.rs:685-686`），但运行时返回**裸元素**（越界报错而非 Option，`methods.rs:281-296`） | M3.4 静默失败"误用"拦截调研发现（2026-08-04 登记，⚠️ 待排期） | 类型系统对 `get`/`pop` 标注返回 `Enum("Option")`，与实际运行时语义（返回裸元素、越界报「无法索引」错误）不符；`toml.th` 实例 `parts.get(i).trim()` 依赖该"误标行为"（若真按 Option 实现则会编译/运行失败）。**M3.4 影响**：静默失败"误用"拦截对**裸 `Enum("Option")` 形态保守不报**（与 `parse_int`/`parse_float` 误标同形不可区分）——若放开会把 `get(i).trim()` 误报为误用。 | ⚠️ **部分缓解（2026-09-17，运行时部 W6，路线②）**：新增**真正的** `Option` 返回方法 `v.get_opt(i)` / `v.try_get(i)`（**是方法不是 native** ⇒ 三处齐改：VM 方法分派 + 解释器 `methods.rs` + 类型层方法表；返回 `Value::Enum{Option, Some/None}`，标注 `Generic{Enum("Option"), [inner]}`）。**`get`/`pop` 的语义与标注零改动**（三个文件 `git diff -U0` **无删除行**＝纯新增，另有语义/标注断言）⇒ **误标本身仍在**，M3.4 对裸 `Enum("Option")` 的跳过规则继续有效，**故本项不能标 ✅**。**总师独立复验**：`get_opt(0)`/`try_get(0)` → 元素；越界 `or_die(get_opt(7))` → **响亮** `or_die: 值失败` + exit 1（两路径一致）。路线①（改标注为真实元素类型）仍另案——`.get(` 在 std 有约 178 处调用，放开下游类型解析风险高。（原登记：⚠️ 待排期（独立工作包）：二选一——① 修正 `get`/`pop` 类型标注为真实返回类型（裸元素），并评估 `toml.th` 等依赖方；② 新增真正的 Option 返回方法（如 `get_opt`/`try_get`）并迁移。修复后可在 M3.4 误用拦截放开裸 `Enum("Option")` 形态。 |

| AUDIT-11.4.41 | 模块间 use 符号污染——主文件**通配导入多个「含内部 use 的模块」**时，先 use 的模块内部 use 符号被后 use 模块覆盖（静默 Unit / 未定义变量） | M4.3 标准库 AI 生态调研实证发现（2026-08-04 登记，⚠️ 待排期） | 实证（`.trae/tmp/m43_probe/`）：`use std::data::dataloader::*` + `use std::data::sampler::*`（两个模块内部各 `use std::random::random::shuffle`）——probe6c（dataloader 先）→ sampler 内部 `shuffle` 解析成**未定义**；probe6f（sampler 先）→ dataloader 内部 `shuffle` 未定义（**顺序相关**）；probe6e（sampler+csv，csv 无内部 use）→ sampler 的 `shuffle` **静默解析成 Unit**（函数返回 Unit 不报错）。单独通配导入任一模块均正常。**性质**：静默错值方向 + 顺序相关（护城河红线）。**M4.3 规避**：sampler/dataloader 内部不再 `use`，改直接调用内置 native `random_int`/`random_float` 内联实现 Fisher-Yates（模块内无内部 use 即无污染面）；**遗留**：任何「模块内部 use + 被多模块通配导入」的标准库模块组合仍可能触发（现有 nn/transformer 内部 use 等需排查）。 | ✅ **已修复（2026-09-17，编译器部 W2；详见 §11.4 表末「2026-09-17 W2 修复轮」）**。**⚠️ 根因更正（重要，与登记猜测不同）**：**不是**"模块加载时共享符号表 / 后加载模块覆盖先加载模块的内部 use 绑定"——真根因是 `import.rs:25-27` 把「已导入/循环」**复用为 `Ok(None)`**（与"文件不存在"同值）+ `import.rs:70-76` 只回流 `imported_files`、**从不回流 `sub_lowerer.modules`** ⇒ 随后 lowered 的**兄弟模块**其内部 `use` 命中去重守卫 → `Ok(None)` → 缓存又取不到 → 落到 `lower_stmt.rs:1304-1341` 的 inline-mod fallback **静默空转**（顺序相关：**后导入者失效**）。原登记理由保留追溯：⚠️ 待排期（编译器部，模块加载/use 作用域）：根因疑似模块加载时共享符号表、后加载模块覆盖先加载模块的内部 use 绑定。建议：① 模块内部 use 与主文件 use 隔离作用域；② 或 use 解析改为按模块隔离的符号表。修复前标准库新增模块遵循「无内部 use（用内置 native 内联）」约束。 |

| AUDIT-11.4.42 | VM `vm_eq` 对「交换后 Vec 元素」比较不稳定 + VM 索引结果链式方法调用受限（`gsum[0].sum()` 报「没有方法 'sum'」） | M4.3 标准库 AI 生态调研实证发现（2026-08-04 登记；2026-08-31 子项② 已随 QA-20260831 修复；**2026-09-16 子项① 判定不可复现**） | 两处 VM 限制（解释器路径均正常）：① **`vm_eq` 不稳定**——`v.set(i, v.get(j))` 交换（Fisher-Yates）返回的 Vec，其元素在「双层判定循环 + 后续代码」场景下 `==` 比较错误（实测 probe20/26/29：报告"有重复"，实际排列合法 `[4,0,3,1,2]`）；单层打印 / sum 求和 / 局部变量 vs 字面量比较均稳定；疑似 `set` 写入的 `Value::Shared` 元素在 `vm_eq` 中解析不稳定（与 AUDIT-11.4.24 同域）。② **索引结果链式方法**——`gsum[0].sum()` 报「张量没有方法 'sum'」（索引表达式结果的方法分派缺失），`gsum[0][0]` 双重索引正常（probe35）。**性质**：① 静默错值方向（比较错判）；② 能力缺口。**M4.3 规避**：测试/标准库用 sum（0..n-1 排列恒 n(n-1)/2）与 len 断言，规避元素间 `==`；张量取值用 `[i][j]` 双重索引。 | ✅ **本条可关闭（2026-09-16：子项① 判定不可复现 + 子项② 已修复）**；历史状态 ⚠️ 部分修复（2026-08-31 QA-20260831，运行时部 + 文档部实测）：**子项②（索引结果链式方法）✅ 已修复**——VM opcode 36 `IndexGet` 解包 Shared/MutRef（`runtime/vm/execute.rs`）后，索引表达式结果可直接链式方法调用；2026-08-31 文档部实测最小用例 `gsum[0].sum()`（gsum 为 2×2 张量）VM=解释器双路径均输出 3.0，`gsum[0][0]` 双重索引维持正常；instance_batch 的 bintree（无法索引）/queue 用例同根因转正常守护（VM_GAP 清单清空）。**子项①（vm_eq 交换后 Vec 元素比较不稳定）✅ 判定不可复现（2026-09-16 运行时部实测）**——按原文复刻 M4.3 场景（`use std::data::sampler::*` + `rand_seed(42)` + `shuffle_indices(5)`，输出排列恰为原文所记 `[4,0,3,1,2]`，证明复刻到位）后：双层判定循环重复对数 = 0、与字面量逐位比较 5/5、整体 `==` = true、跨 Vec 多重集 = 5、sum = 10；**200 seed 扫描 7000 次元素间 `==` 全部 dup=0 且多重集恒合法**；另 11 个构造用例 × 4 路径（纯 VM / JIT 默认 / 解释器 / CLI 默认 vs `TENTH_NO_VM=1`）输出逐字节一致。**本轮未改 `vm_eq`**（不复现即不猜着改）；**疑似**由 2026-08-03 `37b81df`（`vm_eq` 补 Vec 分支 + 入口 deref，即 11.4.24 修复）与 2026-09-08 `b49ca06`（`deref_wrapped` 单点收敛）顺带消除。**位置勘误**：`vm_eq` 实际在 `runtime/vm/execute.rs:2063`（原文写的 `runtime/vm/natives.rs` 系陈旧指针）；解释器 `values_eq` 在 `runtime/interpreter/binary.rs:348`。**遗留**：`tenth/std/data/` 侧 M61 sampler 测试注释仍写着该「已知限制」，现已不成立，待清理（属真理源/测试注释，本轮未动——**遗留项，待清理**）：M61 sampler 测试注释仍注明此限制存在；（**不再排查**：如上「子项① 判定不可复现」已闭环；此处原指针 `runtime/vm/natives.rs` 亦为陈旧路径，正确位置见上方「位置勘误」——`runtime/vm/execute.rs:2063`）对 Shared 包裹元素在复杂控制流下的稳定性（对齐解释器 `values_eq` 的解包语义）；M4.3 规避方案（sum/len 断言替代元素间 `==`）继续有效。 |

| AUDIT-11.4.43 | JIT 对 **Union 字段修改**（`FieldAssign` 到 Union 变量，如 `n3.integer = 100`）触发 Cranelift 低化 `TryFromIntError` panic（`cranelift-codegen-0.115.1 isa/x64/lower/isle.rs:75`）——stderr 打印 panic 噪音，功能靠 catch_unwind fallback 兜底 | M5.2 大规模回归实例批量扫描实证发现（2026-08-04 登记；✅ 已于 2026-08-31 QA-20260831 修复轮关闭） | `Tenth实例/Union类型/union_demo.th`（默认路径）每次运行 stderr 都打印 `thread 'tenth-main' panicked at ...cranelift-codegen...isle.rs:75:5: called 'Result::unwrap()' on an 'Err' value: TryFromIntError(())`，随后程序正常输出（exit=0）——panic 发生在 **Cranelift 低化（finalize_definitions）**，已被 M2-A5 的 `catch_unwind`（`compile/jit/context.rs`，translate+finalize+get_finalized_function 全链路）捕获 → 转 Err → JIT→VM fallback → 功能正确。**最小复现**（`.trae/tmp/union_min2.th`）：`union Number { integer: i64, float: f64 } fn main() { let mut n3 = Number { integer: 1 }; n3.integer = 100; println(n3.integer); }`——**触发**；对照 `union_min1.th`（只构造 + 字段访问，无修改）**不触发** → 根因锁定在 Union `FieldAssign`（bytecode `StoreField` + `Store` 写回）的 JIT 发射路径。**根因（2026-08-31 定位）**：Union `FieldAssign` 语句字节码为 `StoreField(净 -1) + Store(净 -1) + ExprStmt Pop`——表达式净 0，随后的 `Pop` 在 VM 里是对空栈的 no-op（`Vec::pop` 空返回 None 的容忍语义）；JIT 的**静态 sp** 却减到负偏移，低化 `stack_addr` 负 offset → ISLE `u32::try_from` 失败 → `TryFromIntError` panic。**修复**：JIT translator `Pop` 发射钳到 0（`translator.rs` Pop 臂，`if self.sp >= VALUE_SIZE { sp -= VS }`）——精确镜像 VM 帧栈基 0 不可下穿的容忍语义；修复后 union_demo 默认路径 exit=0、输出正确、**stderr 无 panic（JIT 直接编译执行，不再 fallback）**。**M5.2 守护同步**：`instance_batch_test.rs` 的 KNOWN_JIT_PANIC_STDERR 分类已移除，union_demo 按「正常」守护（stderr 禁 panic）。 | ✅ 已修复（2026-08-31，运行时部 QA-20260831 任务 A 顺手修复）：JIT `Pop` 空栈钳 0 + KNOWN_JIT_PANIC_STDERR 分类移除 + 本条状态更新。同轮顺带修复：JIT 控制流 block 管理（循环回边 seal 时序 + 死代码跳过，见 MEMO QA-20260831 记录）。 |

| AUDIT-11.4.44 | JIT 标量专用化「分析/发射资格漂移」系统性风险面——分析器（`analyze_scalar_kinds`）与发射端（`emit_binop`/`emit_native_cmp` 等）的专用化资格判定为**两处独立代码路径**，任何不对齐即静默错值（UB）或整函数回退 | QA-20260831 修复轮实证（2026-08-31 登记，⚠️ 待排期） | **同族第三例**（前两例：AUDIT-11.4.35 内联优先于特化 ABI 的预测漂移、AUDIT-11.4.36 binop 专用化集合漂移）：本轮 probe5——分析器对比较指令**无条件**预测 Bool 标量，发射端比较专用化资格更窄（仅同类 I32/F64→Bool）→ 分析预测某槽为标量而发射端未为其建标量槽 → `Load` 专用化读「仅由未执行分支创建」的**未初始化标量槽**（布局敏感 UB，main_expr 裸表达式 `&&` 链静默错值 false/true 翻转）。已修复（分析端比较预测与发射端逐一对齐），但**结构性风险仍在**——「两处独立资格表」意味着未来任何一端单独修改（新增 opcode 专用化、调整白名单）都可能再造同族缺陷；现有守护（`jit_silent_audit_test` 34 项组合矩阵 + `jit_consistency_test`）只覆盖已知组合，无法系统性防住新增漂移。**性质**：架构债务 + 静默错值风险面（历史同族：11.4.35 / 11.4.36 / 11.4.44）。 | ⚠️ **部分修复（2026-09-17，运行时部 W4＝M0 段；详见「2026-09-17 W4 修复轮」）**：D1 `Load` 资格守卫已落地（含"槽种类≠分析种类"分支，走既有 Err→整函数回退 VM 通路）、`TENTH_JIT_POISON` 已补毒化标量伴随槽（`spec_mode` 入口除外，见 `11.4.75`）；**D2 `Store` 侧守卫实测过度保守已回退**（见 `11.4.74`）。**M1–M5 单表化仍待做（W10）**（原登记：⚠️ 待排期（运行时部建议）：重构为**单一资格表**——专用化资格判定收敛为一个共享数据结构/函数（opcode + 操作数类型 → 是否专用化 + 预测类型），分析与发射两端共同消费同一张表，从结构上消除双源漂移；重构落地前，任何一端改动必须在另一端逐条核对，并全量跑 jit_silent_audit + jit_consistency + jit_spec_abi 套件。 |

| AUDIT-11.4.45 | 代码架构摸底发现的一批治理大项（涉及改指令格式 / 前端类型系统 / 自举三路径），经总师评估后**全部归档、暂不执行**（2026-09-08 用户决定） | 2026-09-08 代码架构摸底（code-arch-audit workflow，8 子代理并行）发现并评估；用户拍板全部归档暂不执行 | 5 项（详见 `.agents/tmp/big-items-registry.md` 原始评估留档）：①**A 后缀字面量 dtype 丢失**——`Op::PushInt` 只存 i64 不存 dtype，`42u8` 运行时实际 I32（违反手册 L166 承诺）；改需动 PushInt 指令格式、跨 bytecode/VM/解释器/JIT/WASM + 可能 tenthc（工作量较高、危险度中）。②**B tenthc 块注释错误信号**——tenthc lexer 返回 `Token` 直出无 Result，Rust 侧已修成返回 LexerError 但接口不对称无法照搬；改触及自举三路径 B/C（工作量、危险度最高，只影响错误提示优雅度、非正确性红线）。③**C VM 前端 char 字面量建模为整型码点**——`let ch='x'` 时 VM `to_string=120` 而解释器保留 `Value::Char`，双后端不一致；改需确认 char 语言语义（工作量中低、危险度中低）。④**D 重载分派两套兼容判定收敛**——`resolve_fn_overload`（scope.rs L227-297）用"精确==→数量兜底"而 `types_compatible`（types.rs L357-480）未用于兜底，形成两套口径；改会**改变重载解析行为**（兼容但不完全== 参数从报错变匹配成功），牵动 typestate/泛型/调用点检查、需同步 tenthc（工作量中、危险度**高**、改语义）。⑤**E native 注册一致性守卫测试**——native 名称在 `runtime/natives.rs`/`interpreter/natives.rs`/`hir/lower/types.rs` 多处手工同步无守卫，漏一处静默漂移；实测双端 native 集合有差异（VM 独有 8 个、解释器独有 4 个带下划线前缀，下划线可能是内部半公开命名）；实现守卫测试纯源码扫描易误报、需结构化 API（工作量低、危险度低）。**性质**：架构债务 / 治理建议（非当前正确性红线）。 | ⚠️ **已归档，暂不执行**（2026-09-08 用户决定，全部不做）。原始评估（工作量/危险度分级 + 每项各自建议）留档于 `.agents/tmp/big-items-registry.md`，供今后重启参考。**更新（2026-09-17）**：**① A 项已解冻并修复**（用户拍板「i64 必须做」⇒ 见 §11.4 表末「2026-09-17 i64 修复轮」：`Op::PushInt` 已携带 dtype）；其余 B/C/D 三项仍归档；**⑤ E 项（native 注册守卫）已重新排期**（用户 2026-09-17 授权总师自主推进小项，且原评估即「工作量低、危险度低」；审计实测同步点实为 **16 类**、并发现 `11.4.63` 活样本）。**2026-09-17 已完成（W5）**：常驻守卫 `tenth/tests/native_registry_guard_test.rs`（**10 用例**、进默认套件、无 `#[ignore]`）已落地并**经总师独立注入漂移验证能真红**；详见「2026-09-17 W5 修复轮」。 |

| AUDIT-11.4.46 | 同一函数体内链式两次调用同一 std 泛型函数编译期失败（符号维度 `[M,N]` 与 `[M,K]` 不统一） | 2026-09-09 性能基线巡检运行时部探针复现（`.agents/tmp/perf_profile_20260909.md` §7.3；2026-09-09 登记，⚠️ 待排期） | 实证：`tenth/std/nn/linear.th:14 linear<T>(x: Tensor[T,M,K], w: Tensor[T,N,K], b: Tensor[T,N]) -> Tensor[T,M,N]`——同一函数体内**链式两次调用** `linear<f64>`（linear → relu → linear）**编译期失败**：第一次调用返回类型带**符号维度** `[M,N]`，第二次要求 `[M,K]`，符号名 `N` ≠ `K` 被判为不兼容（维度符号未做上下文重命名/统一），报「函数 'linear_F64' 的第 1 个实参（x）类型不兼容」。**绕行（已实测编译+运行通过）**：中间结果加**具体维度标注** `let h: Tensor[f64, 64, 256] = linear<f64>(x, w1, b1);`。**性质**：类型推断局限——**响亮失败，非静默错值**（不影响正确性，影响可用性）。**相关既有工作**：`AUDIT-11.4.13`（`Dim::Symbol` 支持）、M3.1 shape 参数一致化。**交叉引用**：`docs/决策记录/2026-09-09-架构议题台账.md` ARCH-3（类型系统 / 符号维度架构，本轮仅登记不启动）。 | ⚠️ 待排期（编译器部议题，HIR 类型推断 / 符号维度统一）：ARCH-3 探讨要点——① 符号维度的统一表示与传播（泛型实例化后 shape 如何收敛）；② 与 `tenthc` 侧 shape 检查的双侧同步（自举一致性）；③ 与 shape 护城河边界（**收紧属改进、放宽属破坏**，见 `docs/API冻结清单.md` semver 约定）。**判据**：链式调用可用且既有 shape 检查不放松（shape 相关套件全绿）。 |

| AUDIT-11.4.47 | `elementwise_binary` 无条件物化全量副本（`broadcast().to_owned()`）→ 逐元素二元算子比归约慢约 **50×** | 2026-09-09 性能基线巡检实测（`tenth/src/runtime/tensor/methods.rs` 约 585-657 行；2026-09-09 登记，⚠️ 待排期） | `elementwise_binary` **无条件** `broadcast().to_owned()` 物化全量副本 + `ArrayD::zeros` + 两次 `zip_mut_with`——即使两操作数 shape 相同也走广播物化路径。**实测（2026-09-09，1M f64）**：`elementwise_mul_1k` ≈ 7-19ms（约 1.1-2 GB/s）vs `reduce_sum_1k` ≈ 0.11-0.27ms（约 38 GB/s），约 **50×** 差距（2026-09-09 权威基线重跑、每测试独立进程：`jit` 7.110/7.295ms vs 0.112/0.136ms ≈ **53×**，见 `docs/性能基线.md` §四 G2）。**性质**：性能缺陷——**正确性不受影响**（数值结果正确，仅慢）。数值口径与现行数据见 `docs/性能基线.md`（性能数据 SSOT）；G2 矩阵已含 `elementwise_mul_1k` / `reduce_sum_1k` 两场景。**交叉引用**：`docs/决策记录/2026-09-09-架构议题台账.md` ARCH-2（tensor 内核分层架构，本轮仅登记不启动）。 | ⚠️ **部分修复（2026-09-16，运行时部波次 1）**：同 shape **无广播 / 无操作数物化副本**快路径已落地（`runtime/tensor/methods.rs:611`，F64/F32/F16/BF16 四分支，`Zip::map_collect` 直出、dtype 匹配时 `Cow::Borrowed` 零拷贝；**shape 不同时广播路径逐行未变**；未做 in-place/SIMD/并行/BLAS）。**根因澄清**：原实现贵在 `as_f64_view()` **对 F64 数据执行 `clone()`（是拷贝不是视图）** → 每次运算付 4 次全量拷贝 + 3 次写 + 1 趟零填充、3 次额外分配。**权威数据（`docs/性能基线.md` §四 G2，2026-09-16 局部重跑，每测试一个独立进程）**：jit 列 **7.110/7.295 → 1.604/1.806 ms**（min/median，**4.43× / 4.04×**）、vm 列 **11.723/12.290 → 3.060/3.391 ms**（**3.83× / 3.62×**）；与 `reduce_sum_1k` 的 median 比值 **53.6× → 13.4×**。同场次 jit 列复现性 **<1%**；同场次未受影响场景整体慢 6-13%（**场次漂移、非回归**）⇒ 用同场次对照场景归一化后收益 **4.4-4.8×**，与原始值一致。**判据「内存带宽 ≥60%」仍未达成**（剩余为输出分配本身，属本议题内核分层范围）；VM 列另有组间污染问题（见 `AUDIT-11.4.52`）。验证：`elementwise_fastpath_test` `10 passed; 0 failed`（`to_bits()` 逐位 + 强制广播路径对拍 + NaN/±Inf/-0.0 + dtype 提升 + 操作数未被就地修改）+ 全量 `2436 passed / 0 failed`。历史状态 ⚠️ 待排期（运行时部议题）：ARCH-2 探讨要点——① 内核分派分层（**同 shape 无拷贝快路径** / 广播惰性 stride 视图 / in-place 路径）；② SIMD 与 autovectorization、分块与并行；③ BLAS 接入（GEMM 当前 33-48 GFLOPS 单线程）；④ **红线**：in-place 与 autodiff tape 的交互（写坏梯度是静默错值）。**判据**：逐元素算子达到内存带宽 ≥60%，既有 autodiff 对拍套件全绿。 |
| AUDIT-11.4.48 | lossy 格覆盖面缺口——「标量→张量静默降级」的编译期唯一防线**只挂在二元算符上**（审计完成，确认 5 处绕过） | 2026-09-16 总师委派编译器部只读审计（`AUDIT-11.4.18` 的后续落地）；总师独立 grep 核对行号一致 | **前置事实**：静态类型层不兜底——`hir/lower/types.rs:137-159` 的 `infer_binary_type` 对「Tensor × 标量」直接返回**原 dtype**（不报错、不降级提示），故 `hir/lower/taint.rs` 的旁路污点分析是**唯一**防线；而 taint 的算子副作用只挂在 `HirExprKind::Binary`（`taint.rs:322-326` 是 `op_effect` 全仓唯一调用点，另两处 119/123 为定义）。**已确认 5 处绕过**：**G1** `+=`/`-=`/`*=`/`/=` 复合赋值（`taint.rs:388-396` 明示「保守只取右值污点」；`lower_expr.rs:1230-1236` 复合赋值不跑 `infer_binary_type`，结果类型写死 `Type::unit()`）→ 逐元素落位回低精度 dtype，**静默丢精度且结果被当确定值继续使用**；**G2** 闭包体整体不求值（`taint.rs:382` `Closure { .. } => Lossiness::Exact`；闭包 HIR 内联、不建 `HirFnDef`）；**G3** 方法调用实参只传播不检查（`taint.rs:331-341`，如 `t.clamp(0.0,1.0)`、`masked_fill`）；**G4** f-string 插值体不求值（`taint.rs:438`）；**G5** sink 名单硬编码 9 项且缺 `print`/`json_encode`/`json_encode_pretty`（`taint.rs:99-105` vs `natives.rs:2123-2126`/`1349`/`1356`）。**std 不依赖该行为**（搜索证据：`tenth/**/*.th` 与 `tenth/std/**` 全树 `+=`/`-=`/`*=`/`/=` **0 命中**；std 无 F16/BF16 张量算式）→ **修复代价低**。**性质**：护城河「静默失败防护」的宣称 vs 实际落差（对外宣称 lossy 拦隐式精度降级时，复合赋值与闭包是可被一句话反驳的反例）。**交叉引用**：`AUDIT-11.4.18`（原登记）。 | ✅ **已修复（2026-09-17，编译器部 W2）——G1/G2/G3 已补齐，lossy 防线不再只挂在二元算符上**（原状态：⚠️ **部分修复（2026-09-16，编译器部波次 2）**：已完成 **G5**（sink 名单补 `print`/`json_encode`/`json_encode_pretty`）+ **G4**（**落点更正**：`f"..."` 在 lower 期早已脱糖为 `format(...)`（`lower_expr.rs:1677-1721`）而 `format` 本就在 sink 名单内——**f-string 从来不是缺口**；真缺口是**普通双引号串插值 `"{x}"`**（`lower_expr.rs:1666-1672`，原 `taint.rs:438` 不遍历 parts），已纳入分析）。`taint.rs` 单文件 +50/-2。**已补永久回归守护**：`tenth/tests/lossy_test.rs` 第 8 节 11 个用例（6 正向 / 4 零误报 / 1 残留缺口绊线）+ 总师另补 1 个 **f-string 脱糖绊线**（防脱糖目标改名致防线静默失效）；并实测「stash 掉 `taint.rs` 修复 → 6 条正向全部失败；恢复 → 全绿」= **改前红、改后绿，非恒真断言**；三套 lossy 合计 **65 passed**，全量 **2448 passed / 0 failed**。**仍未修**：G1/G2/G3（G1 取 target 静态类型可能需改 HIR → 跨模块；G2 闭包语义未定、有误报风险）→ 待拍板。**本轮新增遗留**：`"{a.b}"` **点路径插值仍漏报**（`InterpPart::Expr` 存的是带点字符串、非变量表键；退化为按根变量取污点会误伤 `"{t.shape}"` 这类精确字段访问 → 真修需 HIR 改存表达式，跨模块，另议）。原状态 ⚠️ 待排期（编译器部，单文件，估 40-80 行）：**低风险可先做**＝G5 sink 名单 + G4 f-string 遍历 parts；**需总师先拍板**＝G1（`HirExprKind::AssignOp` 只存变量名字符串，取 target 静态类型可能需改 HIR → 触发 bytecode/wasm/bridge + tenthc 同步）/ G2（闭包体内 lossy 是否算「外泄」未定，扩大分析面有误报风险）。**探针规格（待执行，需 cargo）**：P4 `let x = zeros_f16(2,2)*1.5; println(f"{x}");`、P5 `print(x)`、P3 `t.clamp(0.0,1.0)`（t=F16）——当前应**编译通过**（证明漏报），修复后应报 lossy 污点。**（2026-09-17 实况）**：P4 / P5 已由 G4 / G5 拦下、**P3 已由 G3 拦下**（`lossy_test` 56 项、含 G3 正向 7 项）；**残留**＝`DerefAssignOp`（`11.4.69`）与「类型判定 ⇒ 精确字面量也报」固有属性（`11.4.70`）。 |
| AUDIT-11.4.49 | `tenth/.cargo/config.toml` 的 `[target.'cfg(test)']` 是**死配置**——无法实现其注释宣称的「给测试加大栈」，属**失效的缓解措施** | 2026-09-16 总师独立实测（非代理产出） | 文件内容为 `[target.'cfg(test)']` + `rustflags = ["-C","link-args=/STACK:33554432"]`（注释称 Increase stack size for tests (32 MB)）。**实测**：分别以**仓库根**与 **`tenth/`** 为 cwd，强制重编译 `tests/tensor_shape_method_test.rs`，`cargo ... --no-run -v` 的 rustc 命令行中**始终只有** `build.rs` 的 `-C link-arg=/STACK:67108864`，`33554432` **从未出现**（`cargo metadata` 亦无任何警告）；PE 头实测 `tenth.exe` 与该测试 exe 的 `SizeOfStackReserve` **均为 67108864**。**根因**：`cfg(test)` 不是 `[target]` 表可求值的 cfg（该表按**目标平台**求值），故该键**无论 cwd 都不生效**。**更关键**：即便生效也无效——PE 栈保留只决定**主线程**栈，而 libtest 的每个用例跑在**派生线程**上，其栈由 `RUST_MIN_STACK` 决定（默认 2MiB），这正是 `AUDIT-11.4.19` 需要手动设该变量的原因。**性质**：失效的缓解措施——比没有更糟（让人误以为已处置）。**交叉引用**：`AUDIT-11.4.19`。 | ✅ **已修复（2026-09-16，测试部执行 + 总师独立复验）**：删除 `tenth/.cargo/config.toml`（连带空目录），改为**仓库根** `.cargo/config.toml` 用 `[env] RUST_MIN_STACK = "33554432"`（cargo 按 cwd 及其父目录发现**并合并**配置 ⇒ 根一份即覆盖「仓库根」与「`cd tenth`」两种 cwd，合 SSOT）。**新配置生效证据（三重，均不设 env 且不经槽位锁脚本——该脚本自身会注入该变量，用它验证等于作弊）**：① 临时探针从仓库根读到 `Ok("33554432")`；② 从 `tenth/` cwd 同样读到（证明祖先合并生效）；③ 实测测试线程栈 **33554432 B = 32.00 MiB**。**验收**：规范命令、不设 env 下 `jit_stack_overflow_test` 3 passed / `parity_test` 129 passed。**语义风险已排除**：该用例守护的是 JIT **虚拟栈**（`compile/jit/translator.rs:79 const MAX_STACK_DEPTH: u32 = 64`），直接探针证实 n=300 触发 `Err("JIT stack overflow…")`，且在 unset/2/32/128 MiB 四档输出完全一致 ⇒ 与全局线程栈无关、**未被架空**，故无需在用例内改小栈。原状态 ⚠️ 待修复（低风险，工程/测试部）：候选＝改用 `[env] RUST_MIN_STACK = "33554432"`（须先验证 cwd 发现规则——规范命令从仓库根执行，故应置于**仓库根** `.cargo/config.toml`），并删除/改正这条死配置。**语义风险（必须先验证）**：`jit_stack_overflow_test` 是「故意测栈溢出」的用例，抬高全局 `RUST_MIN_STACK` 可能改变其判定 → 需在该用例内显式设回小栈，不得以全局值替代。**验证**：**不设 env** 直接跑 `parity_test` 与 `jit_stack_overflow_test`（`AUDIT-11.4.19` 口径）。 |
| AUDIT-11.4.50 | VM `vm_eq` 缺 `(Tuple, Tuple)` 分支——**元组相等比较在 VM/JIT 恒 `false`，解释器为 `true`**（跨后端静默错值） | 2026-09-16 运行时部波次 1 顺带发现（同域于 11.4.42①）；总师独立读源码核对确认 | 最小复现：`let t = (1, 2); let u = (1, 2); println(t == u);` → 默认路径（JIT/VM）输出 `false`，`TENTH_NO_VM=1`（解释器）输出 `true`；3 元组、嵌套元组同；**不等**元组两侧均 `false`（该侧正确）。**根因（总师已核对）**：`runtime/vm/execute.rs:2063-2093` 的 `vm_eq` 在 Int/Float/Float32/Bool/Char/String/Unit/Vec 分支之后直接落 `_ => false`，**无 Tuple 分支**；而解释器 `runtime/interpreter/binary.rs:365-368` 的 `values_eq` **有** Tuple 分支（长度相等 + 逐元素递归）。**性质**：与已修复的 `AUDIT-11.4.24`（Vec `==` 同样缺分支）**同类**——VM 比较缺复合值分支 → 静默错值，以解释器为基准。**交叉引用**：`AUDIT-11.4.24`（Vec 同域，已修）、`AUDIT-11.4.42`（同域）。 | ✅ **已修复（2026-09-17，运行时部；详见 §11.4 表末「2026-09-17 修复轮」）**：`vm_eq` 已补 `(Value::Tuple(a), Value::Tuple(b))` 分支（对齐解释器 `values_eq` 的「长度相等 + 逐元素递归」）。**排查提示**：`Value::Map`/HashMap 等其余复合值是否同样缺分支，本次**未查**，宜一并 grep 对照解释器分支集合。**（2026-09-17 已闭环：与解释器 `values_eq` 的分支集合已对照，`Map`/`Array` 等不修并给出理由；双路径对拍守护已落地，见下方「2026-09-17 修复轮」）**建议同时补**双路径对拍**守护测试（`(1,2)==(1,2)` 在 VM 与解释器结果一致）。 |
| AUDIT-11.4.51 | `jit_stack_overflow_test` 两处问题：**注释失实**（写 `MAX_STACK_DEPTH=256`，源码实为 **64**）+ **Test 2/3 未断言 fallback 真的发生**（测试静默失效面） | 2026-09-16 测试部执行 `AUDIT-11.4.49` 时顺带发现（总师登记） | **① 注释失实**：该测试文件注释多处写 `MAX_STACK_DEPTH = 256`，而 `compile/jit/translator.rs:79` 实为 `const MAX_STACK_DEPTH: u32 = 64`（不影响判定方向，属文档漂移）。**② 覆盖缺口（性质更重）**：Test 2/3 只断言结果值正确（`1+2+…+300`），而该值在 **JIT 路径与 fallback 路径下都正确** ⇒ 这两条用例**并未断言 fallback 真的发生**。若将来 `MAX_STACK_DEPTH` 被调到 >300，用例仍会「通过」（此时走 JIT），而它自称守护的 fallback 已不再被覆盖——**测试静默失效**，与项目「静默失败防护」的立意直接冲突。**证据**：直接调 `JitContext::get_or_compile` 的探针显示 n=64 → `Ok`、n=65/256/300 → `Err("JIT stack overflow: …")`，即溢出边界正好 n>64。 | ✅ **已修复（2026-09-17，测试部 W5；详见 §11.4 表末「2026-09-17 W5 修复轮」）**（原登记：⚠️ 待排期（运行时部/测试部，低风险，纯测试改动）：① 把注释 `256` 修正为 `64`；② 将探针那一行固化为断言——断言 `get_or_compile(300)` 返回 `Err` 且信息含 `JIT stack overflow`，使「fallback 确实被触发」成为**被守护的事实**而非默认假设。 |
| AUDIT-11.4.52 | `perf_vm_reference` 单进程跑 6 个组 → **VM 列存在组间状态污染**（测量方法缺陷，数据可信度问题） | 2026-09-16 总师 G2 局部重跑时发现（`AUDIT-11.4.47` 落地后的数据复核） | **问题**：`tenth/tests/perf_baseline_test.rs:1402-1433` 在**同一进程**内先跑完全部组的 JIT（作对拍基准）再跑完全部组的 VM ⇒ §二「规范跑法＝每测试一个独立进程」所要规避的「前一组堆状态污染后续组」**在该进程内部重新出现**，而 VM 列恰恰由这一个进程产出。**证据（2026-09-16 实测）**：`reduce_sum_1k` 的 vm 由 9/9 的 0.115/0.124 变为 **0.216/0.239（+88%）**，而**归约路径不含 `elementwise_binary`、当次改动不可能影响它**，且同场次其 jit 仅 +10%；VM 列漂移量级远超 jit 列、方向亦不一致（`matmul_1024` vm −2.5% vs `reduce_sum_1k` vm +88%）。**影响**：**VM 列数据不可用于跨场次比较与回归判断**（jit 列不受影响，同场次复现性 <1%）。**性质**：测量方法缺陷——按 `工作规范.md` §12.4「测量方法本身的缺陷 → **立即修**，否则数据不可信」。 | ✅ **已修复（2026-09-16，总师执行，分两步完成）**：① **按组拆分**（`perf_vm_reference` → 每组一进程）——跨组污染消除：G2 `matmul_512` 的 vm 由 9/9 的 14.854ms 降到 5.488ms（≈ jit）。② **进一步按场景拆分**（`perf_vm_scenario` + `PERF_VM_SCENARIO=<G>:<name>`，每场景一进程）——因按组**仍不够**：实测**同一** `matmul_512` 在 G2 进程 vm=5.488ms、在 G6 进程 vm=**15.830ms**（后者前面跑了 18s 的 VM 长循环），跨进程差 **2.9×**。**修复判据＝同场景跨进程收敛**：`matmul_512` vm 14.854（9/9 原始）→ 5.488（按组）→ 5.293（按场景，G6），且 ≈ jit 5.482/5.742（**物理正确**：两路径都委托同一 ndarray dgemm）；G6 `jit/vm` 比值由被污染的 0.396 回到 **≈1.05**。**污染量级**：按场景隔离后 G1 的 vm 变化达 **−47% ~ −58%**（`method_vec_1e6` 553.865→233.656、`tail_rec_1e6` 418.337→178.381、`fib28` 368.916→195.056），而 jit 列同场次仅 ±15% ⇒ 通则：**JIT 按组隔离够用，VM 必须按场景隔离**。**已同步**：`docs/性能基线.md`（§二/§三/§四/§六/§七 方法学与全量重采数据）、`.github/workflows/perf.yml`（两段循环）、测试文件头与注释。**连带更正**：① 9/9 发布的 vm 列整体偏高（最高约 2×），已由本次全量重采覆盖；② 测试实现的 `ratio_vs_vm_median` 输出行因 jit 分母是**进程内首个测量**而偏大（实测 G6 `matmul_512` 进程内 7.52ms vs 独立测量 5.74ms）⇒ §四 G6 表比值改由表内 median 自行计算。**遗留**：`G3:tape_overhead` 是 autodiff↔手写**成对测量 + 派生比值**，非单场景，仍由 `perf_g3_autodiff` 在其进程内相邻测 jit/vm（已在 §四 G3 与 §七 标注）。原状态 ⚠️ 待修（应立即修，测试部/运行时部）：把 `perf_vm_reference` **按组拆分为独立进程** |
| AUDIT-11.4.53 | **整型算术被硬限在 i32**：任何结果超过 `i32::MAX`（2 147 483 647）的整数运算在运行时报「溢出 i32 范围」并中止——**后缀 / 标注 / 形参 / 返回类型全部无效**，与《语言参考手册》承诺相悖 | 2026-09-16 总师做 Python/C 性能对照时写 Monte Carlo π 触发（`s * 1103515245` 报错），随后以 8 个探针定位 | **手册承诺**（`docs/语言参考手册.md:166`）：「无后缀整数默认 `i32`；后缀 `i8 i16 i32 i64 u8 u16 u32 u64` 显式指定 dtype，超出范围报编译期错误；无后缀且超出 i32 范围自动提升为 `i64`」——即 **i64 是对外承诺的能力**。**实测探针（均可复现）**：① `let x: i64 = 2000000000; x * 100` → ❌ 报「溢出 i32 范围」（**标注无效**）；② `fn scale(x: i64) -> i64 { x * 100 }` 传 `2000000000` → ❌ 同（**形参与返回类型均无效**）；③ `let a = 3000000000; a + a` → ❌（6e9；**无小字面量、变量间运算同样中招**）；④ `let a = 3000000000; a * 2` → ❌；⑤ `2000000000i64 * 100i64` → ❌（**显式 `i64` 后缀也无效**）；⑥ 对照 `let a = 3000000000; println(a)` → ✅ 正常打印 `3000000000`（**字面量能承载 i64 值，一参与运算就按 i32 检查**）；⑦ `fn mul(x: i64) -> i64 { x*100 }` 传 `20000000`（结果 2e9 < 2^31）→ ✅ 正常。**双路径一致**：默认路径（VM/JIT）与 `TENTH_NO_VM=1`（解释器）报错完全相同。**根因（已定位）**：`runtime/value.rs:396` 的 `check_int_overflow(result, dtype)` 以 `dtype = I32` 执行（错误文案即该行格式串），而整型值的 dtype 标签**一律默认 I32**——源头即 `AUDIT-11.4.45` A 项（`Op::PushInt` 只存 i64、不存 dtype）。**性质**：**响亮失败**（非静默错值，不触护城河红线），但构成**能力封锁**：i64 算术实际不可用，且与对外权威手册（`语言参考手册.md`）相悖。**交叉引用**：`AUDIT-11.4.45` A（同根因，本条为其**严重度升级**）、`AUDIT-11.4.17`（JIT 整数溢出路径）。 | ✅ **已修复（2026-09-17，编译器部 + 运行时部，24 文件；详见 §11.4 表末「2026-09-17 i64 修复轮」）**（原登记：编译器部 + 运行时部；**严重度高于原 11.4.45 A**——原项仅记录「后缀字面量 dtype 丢失（`42u8` 运行时实为 I32）」这一语义缺陷，**未记录其导致 i64 算术整体不可用**这一后果）。**修复方向**：让 dtype 真正贯穿（`PushInt` 携带 dtype、值携带真实宽度），或溢出检查改用**提升后的结果 dtype** 而非操作数默认标签；跨 `bytecode`/VM/解释器/JIT/WASM（可能含 `tenthc`），工作量与危险度评估沿用 11.4.45 A（较高/中）。**验收口径（最小）**：上述探针 ①②⑤ 应输出正确值（2e11 / 2e11 / 2e11），且 ⑥ 行为不变；既有整数相关套件（`int_types_test` / `jit_int_overflow_test` / `overflow` 相关）全绿。 |
| AUDIT-11.4.54 | VM `Vec` 越界索引**静默返回 `()` 且 exit 0**（解释器正确报错）——**跨后端静默错值** | 2026-09-16 `tenth-lens` P0 探针顺带发现（其 `GAP-009`）；**总师独立复验** | **实测（总师复验，探针 `tenth-lens/probes/check_vec_oob.th`）**：`let v = Vec::new(); let x = v[7]; println(x);` → **默认路径输出 `()`、exit 0**（静默错误值）；`TENTH_NO_VM=1` → `第 4 行第 14 列：运行时错误 — Vec 索引 7 越界`、exit 1。**根因（代理定位，指向 `runtime/vm/execute.rs:950` 一带；总师未逐行核对）**：Vec 分支以 `unwrap_or(Value::Unit)` 兜越界，**仅 Vec 漏边界检查**（Tensor 分支与 natives 路径均正确报错）。**性质**：**静默错值 + 跨后端分歧（护城河红线）**——与 `AUDIT-11.4.24` / `11.4.50`（VM 比较缺分支、兜底成中性值）**同族**。**交叉引用**：`11.4.50`、`11.4.24`、`tenth-lens/GAPS.md` GAP-009。 | ✅ **已修复（2026-09-17，运行时部；详见 §11.4 表末「2026-09-17 修复轮」）**：VM Vec 索引越界现与解释器/natives 一致的**响亮报错**（去掉 `unwrap_or(Value::Unit)` 兜底，加边界检查 + 报错）。**验收**：上述探针两路径输出一致（均报错、exit 1）；既有 `vm_ref_vec_methods_test` / `vm_tensor_iter_test` / `parity_test` 全绿。 |
| AUDIT-11.4.55 | VM **丢失函数内对全局变量的写入**（该全局同时被主函数写过时）——**跨后端静默错值** | 2026-09-16 `tenth-lens` P0 守卫轮探针发现（其 `GAP-012`）；**总师独立复验** | **实测（总师复验，探针 `tenth-lens/probes/gap012_vm_global_write.th`）**：同一全局既被 `main` 写、又被函数写时，**VM 侧的函数写入被吞**——默认路径输出 `N1=1 N2=1 N3=1 N4=2 N5=2 N6=2`，解释器输出 `N1=1 N2=1 N3=2 N4=3 N5=4 N6=2`（差异自 N3 起累积）；**两条路径 exit 均为 0**（静默）。**性质**：**静默错值 + 跨后端分歧（护城河红线）**；与 `AUDIT-11.4.21`（VM `&mut` 写回顺序相关失效，曾修复）/ `11.4.24` / `11.4.50` / `11.4.54` **同族**——VM 侧写入/兜底语义与解释器不一致。**注意**：这是 `tenth-lens` 探针在**同一轮**内发现的**第 2 条红线级跨路径静默分歧**（第 1 条为 `11.4.54`）。**交叉引用**：`11.4.54`、`11.4.21`、`11.4.24`、`tenth-lens/GAPS.md` GAP-012。 | ✅ **已修复（2026-09-17，运行时部；详见 §11.4 表末「2026-09-17 修复轮」）**（原登记理由：**建议优先级高于 `11.4.54`**——`11.4.54` 是越界边角，本条影响**任何"函数修改全局状态"的正当程序**，且静默）。**修复方向**：对齐 VM 与解释器的全局写回语义（`StoreGlobal` 路径 + 写回时机），参照 `11.4.21` 的修法。**验收**：上述探针两路径输出**一致**；既有 `parity_test` / `vm_*` 套件全绿。 |

### 11.4.38（**编号说明：本节不是条目编号**）P 系列（M2.6-P1~P4）遗留难题登记（10 项，2026-08-04 P5 收尾留档）

> **编号消歧（2026-09-17）**：`11.4.38` **不存在对应条目**——该编号被用作**下方 P 系列小节的标题编号**；§11.4 正式条目序列为 `11.4.1` ~ `11.4.37`、`11.4.39` ~ `11.4.62`。P 系列 10 项（P-1 ~ P-10）与 11.4 正式条目**分列计数**（其中 P-3 与 `AUDIT-11.4.34`、P-4 与 `AUDIT-11.4.32` 遗留② 属同项重复登记）。若今后需给该小节正式编号，另开新号并**保留本小节标题不变**（改动编号会牵动 §11.5 五处引用）。

> 来源：P 系列 P1-P4 汇报中如实记录的遗留难题汇编（提交 e973391 / 6bf9bac / 03c9dae / 939dc94，全量 2298 passed / 0 failed，自举 OK）。纯文档登记，无代码修改；后续动作建议 M3 或后续里程碑排期。每项含现状（带行号证据或提交引用）+ 为何留 + 建议。

| 编号 | 名称 | 现状（证据） | 为何留 | 建议后续动作 |
|------|------|-------------|--------|-------------|
| P-1 | Await/Yield 的 JIT 支持 | ❌ 未支持——`compile/jit/translator.rs:2522-2526` `Await\|Yield` → Err「async suspend opcode not supported」整函数 fallback VM；async 挂起需保存整个 VM 调度器栈并挂起（Pending Future）、Yield 保存调用栈让出控制权，JIT 机器码帧无法序列化/恢复（M2-A3 评估） | 语义正确优先；不做运行期 hostcall 混合（会把合法挂起变成错误） | M3 或后续：async 状态机改写（对应未来 API 规划 §1.4 async/await A 级）或 JIT 帧序列化/恢复机制 |
| P-2 | 深递归栈 | ⚠️ 三子项：① 解释器树遍递归 >~1e4 层原生栈溢出（`jit_call_path_test.rs:472` 注释预存限制，故深尾递归守护只做 VM/JIT 双路径）；② JIT 非尾深递归原生栈帧 ~7KB/帧（`MAX_STACK_DEPTH` 64、`size_of::<Value>()`=112B、A1 修复 256MB 工作线程）——深尾递归 200000 层安全是因 TailCall 走解释器 TCO（P4 实测）；③ VM TailCall 真 TCO 帧复用 depth 100000 输出 49965——**经核算 = sum(1..100000) mod 1000000007 数学正确值，非缺陷**（测试注释 jit_call_path_test.rs:470 与断言 :482 一致） | 架构性限制：树遍解释器用原生递归；JIT 深递归需 stack probe/堆栈切换；测试基线受 AUDIT-11.4.19（默认 2MB 线程栈）影响 | M3 或后续：JIT stack probe 或堆分配栈；解释器 trampoline 化；RUST_MIN_STACK 基线文档化 |
| P-3 | AUDIT-11.4.34（match tuple 模式 + guard） | ✅ 已修复（2026-08-04，1.0 前红线修复轮）：② JIT 编译 panic 逃逸已于 M2-A5 根治；① VM guard 回退错乱已修复（bytecode.rs tuple 臂保留 scrutinee 副本供下一条臂重试，解释器=VM=JIT 三路径一致） | —— | 已关闭（详见 AUDIT-11.4.34 修复记录） |
| P-4 | WASM abs 类型系统 vs 运行时返回类型不一致 | ✅ **已修复（2026-09-17，运行时部 W4）**：WASM 整数 `abs` 保 i64（去 `F64ConvertI64S`）+ 静态类型对齐（新 `infer_abs_dtype`：整数→同型、f32→f32、其余→f64）+ 运行期 dtype **跟随输入**（VM/解释器 `abs` 均 `Int(n.abs(), *dt)`，修掉同类硬编码 I32）；`m1s1_wasm_gaps_test` 新增 3 条 wasmi 用例（`fn f(x:i64)->i64{abs(x)}` 得 7/5/0、`abs(a)+abs(b)=7`）。**总师未逐条核**（原登记：⚠️ 已登记 AUDIT-11.4.32 遗留②（P 系列确认仍开放）：`compile/wasm/compile.rs:587-610` WASM abs——f32→F32Abs、f64→F64Abs、i64→位技巧 abs（`(x^(x>>63))-(x>>63)`）后 F64ConvertI64S 转 **F64**（因类型系统 `infer_scalar_dtype` 默认 f64，compile.rs:594 注释）；VM/解释器 `abs` native（`runtime/natives.rs:2129`、`interpreter/natives.rs:1044`）`Value::Int` → **Int** | WASM 按类型系统产 F64，`fn->i64 { abs(...) }` wasmi 校验报错（不静默错，安全） | M3：对齐类型系统 `infer_scalar_dtype`（abs(i64)→Int）或明确 abs 语义 |
| P-5 | P1 剩余每次调用 ~5ns 固定开销 | ⚠️ P1 后 fib(28) 9→8ms（~11%），目标 6ms 未达成；剩余开销（MEMO.md P1 条目如实遗留）：特化指针表双重 load + 空值检查分支、8 寄存器 ABI（fib 仅 1 实参）、结果合并槽内存往返、错误标志 load | 纯优化非正确性问题；已满足 bench gate（fib<100ms/loop<200ms） | M 系列优化候选（M3 或后续）：特化表单 load、寄存器 ABI 瘦身、合并槽消除、错误标志并入返回值 |
| P-6 | 递归闭包（MakeCell/BindSelfCapture）整函数 fallback | ⚠️ `compile/jit/translator.rs:2534-2543` MakeCell/BindSelfCapture → Err 整函数 fallback VM（M1-S2 true letrec 决策，P4 确认未触及：`jit_call_path_test` 递归闭包用例「只对拍」）；闭包体本身（Load+CallClosure）仍可 JIT（host_call_indirect 走 call_value 支持 Shared 解包） | 仅闭包创建点（顶层 let 递归闭包）含此二指令，非优先级；A3 评估 | M3 或后续：JIT 支持 MakeCell/BindSelfCapture（cell 创建 + 自绑定发射） |
| P-7 | >8 参不特化 | ⚠️ `compile/jit/context.rs:19` `MAX_SPEC_ARGS=8`；`from_hir`（context.rs:72）`params.len() > MAX_SPEC_ARGS → None`；`JitFnSpec` 固定 `(vm, i64×8) -> i64`（context.rs:84-88） | P3 评估：放宽需改 Rust fn 指针类型 + 大量机械工作；x64 SysV 仅 6 GPR（f64 走 XMM）；std/测试中 >8 纯标量参数函数占比极低 | 保持现状；如需改 struct-return 或内存传递 ABI |
| P-8 | MethodCall 无用户 chunk 目标 | ⚠️ `compile/jit/translator.rs:2167` MethodCall → host_method_call → `call_method_priv`（纯 native 按接收者运行期类型分派，无用户函数 chunk 目标——`jit_call_path_test.rs:13-14` 注释、P4 记录） | 方法本身已是原生 Rust 调用，JIT-to-JIT 直接调用不适用 | 无（设计正确，保持 host_method_call） |
| P-9 | TailCall 不特化 | ⚠️ `compile/jit/translator.rs:2483-2498` TailCall → host_call + 立即返回（不内联不走 A1 直接调用）；VM opcode 55 真 TCO 帧复用（execute.rs:1260）；守护 `jit_call_path_test.rs:465-485` 100000 层 VM/JIT 双路径 49965 不崩溃 | JIT-to-JIT 每尾调用压原生栈帧 ~7KB → 深尾递归 0xC0000005（安全行为变崩溃）；当前 host_call→解释器 TCO 迭代帧池安全 | 保守保持；未来 JIT 真 TCO（帧复用）需在机器码层实现，高风险 |
| P-10 | f32/Float32 不纳入特化 | ⚠️ `compile/jit/context.rs:54/61-69` `Type::Base(F32)` → 不推导（None）；`ScalarAbiKind` 仅 I64/F64（context.rs:23-27） | P3 决策：f32 热路径需求低于 f64；特化需 f32 标量槽 + 位打包 | M 系列候选（f32 标量热路径需求明确时） |
| P-11 | **i64 标注函数不再走 JIT 标量特化**（2026-09-17 i64 修复的正确性代价） | ⚠️ 为消除「裸 i64 寄存器特化 ABI 按 i32 检查」而移除 I64 资格（`jit/context.rs`）；`ChunkSig` 特化现仅 `Int`/f64 | **正确性优先**：i64 语义正确 > 该路径 JIT 加速；已有反向守护 `spec_i64_not_eligible_and_i64_semantics_hold` | 恢复条件＝特化 ABI 承载 dtype（标量槽带 tag）。**已核实（总师 grep，2026-09-17）**：G1 的 `fib(n: i64)` / 闭包 `|x: i64|` / `tr(n: i64, acc: i64)` 与 G6 的 `mha_block(..., n_heads/seq_len/d_model: i64)` **均带 i64 注解形参** ⇒ 这些场景**已失去 JIT 标量特化、实测数字必然改变**（`docs/性能基线.md` 相关场景已标**待重采**）。**已提为 W10（JIT 单表 / ABI 收敛）的第一优先项** |

**P 系列确认状态**：P-1 ~ P-10 全部 ⚠️ 已登记（纯文档留档，待 M3 或后续排期）。P-2 中「VM TailCall depth 100000 出 49965」经核算为**数学正确值非缺陷**（sum(1..100000) mod 1000000007 = 49965）；P-3 关联 AUDIT-11.4.34；P-4 关联 AUDIT-11.4.32 遗留②。**新发现**：无（10 项均为 P1-P4 如实记录的遗留，P5 仅汇编留档）。

### 11.4.56 ~ 11.4.59（2026-09-17 登记）

> 来源：2026-09-17「修三条跨后端红线 + 补常驻守护」轮。`11.4.56` 由运行时部修复 `11.4.55` 时**顺带发现**、总师独立复验；`11.4.57~11.4.59` 由编译器部**只读审计**发现（总师核对了 `11.4.57` 的 parser 承重断言，`11.4.58/11.4.59` 为代理定位、未逐行核对）。

| 编号 | 名称 | 性质 | 证据 | 状态 / 后续 |
|------|------|------|------|------------|
| AUDIT-11.4.56 | **闭包体内写全局：VM 写穿全局表 vs 解释器按值捕获（写丢失）** | 红线（静默错值 + 跨后端分歧）——但**语义未定义** | 最小程序：顶层 `let mut H: i32 = 0` + `let f = \|_u: i32\| { H = H + 1; };` → 默认路径 `C1=1 C2=2 C3=3`、`TENTH_NO_VM=1` `C1=0 C2=0 C3=1`，**两侧 exit 均 0**（总师独立复验；1.0 发布版二进制亦可复现 ⇒ **非本轮回引入**） | ⚠️ **待语义裁定（阻塞）**——**2026-09-17 用户裁断：暂不裁定，维持登记待排期**（分歧已由 ratchet 守护钉住，不会静默；裁定前不得改代码）：`docs/语言规范.md:956,966` 定「捕获**按值**内联」，`docs/语言参考手册.md:949,954` 只承诺「同文件**函数**可读写全局」——**两条均未覆盖「闭包体内写全局」这一交叉情形**（编译器部只读审计结论）。按项目原则**不替手册补语义**：先裁定语义，再对齐两侧。当前由 `tenth/tests/backend_parity_guard_test.rs` 的 **ratchet 用例**钉住（分歧一旦消失即报红，倒逼移除台账并重定后端哨兵） |
| AUDIT-11.4.57 | **tenthc 无法表达程序级全局：顶层 `let` 被静默丢弃（`Parser.errors` 无出口）** | 能力封锁 + 静默失败（自举路径） | `tenthc/parser/parser.th:2033-2036` 把 `Let`（disc=5，**不在** `is_expr_start_token` `:117-131` 之列）交给 `record_error_and_recover` 记入 `p.errors`（`:111`），而返回的 `Program`（`:2046-2060`）**无 errors 字段**、全 tenthc 无人读取 ⇒ **声明与错误一并静默丢弃**（总师已核对 `:117-131` / `:2033-2036` / `:2046-2060`）；`HirProgram`（`:223-258`）无 globals 字段。后果：**路径 B 响亮失败**（`bridge.rs:68-92` 把 main 语句包成合成 `fn main`，Rust 前端报「未定义变量」），**路径 C 静默错值**（见 `11.4.58`） | ✅ **已修复（2026-09-17，编译器部 W7＝响亮化；详见 §11.4 表末「2026-09-17 W7 修复轮」）**：tenthc **两条错误通道全死**（`Parser.errors` + `HirProgram.errors` 的 17 处 `push_error` 均无读取方）⇒ 物理出口只有"**让产物变空**"：`Program`/`Parser` 各加 `error_count`、经 `HirProgram` 透传到 `compile_to_wasm` 入口判非零 ⇒ `println("[tenthc] 编译中止：…")` + **`return Vec::new()`**（空字节流无 `\0asm` 魔数 ⇒ 下游校验立刻失败）。**总师独立核验**：自举 `[OK]`；`[WASM] compile` 刷屏 **0** 行；`parity_test` 129 全绿。（原登记：⚠️ 待排期（编译器部）。**非 `11.4.55` 同款**：tenthc 无字节码后端、无全局表（`tenthc/compile/wasm.th:2186-2189` 全局段仅 1 个 bump pointer）⇒ `11.4.55` 的修复**无需**向 tenthc 同步 |
| AUDIT-11.4.58 | **tenthc WASM codegen：未绑定名的读/写静默落 local 0（＝第一个形参 / temp 槽）** | 红线（静默错值）——**限路径 C** | `tenthc/compile/wasm.th:610-682`（Var）/ `:792-831`（Assign disc=14）三级查找全失败后**不报错**，`local_idx` 默认 0 直接 `local_get/set`（`:672` / `:830`）；Let 槽位＝`temp_local+1+local_count`（`:1890`）⇒ local 0 = 第一个形参。最小复现：顶层 `let mut H: i32 = 0` + `fn bump() { H = H + 1; }`（`H` 的声明已被 `11.4.57` 丢弃） | ✅ **已修复（2026-09-17，编译器部 W7）**：新增 `resolve_local`（**未命中返 -1**；查找顺序**必须** let → capture → param——闭包捕获的可能正是**外层形参**）+ `find_capture_slot` / `current_fn_name` / `closure_body_binds_local`，**4 处兜底点共用**（Var 读 / Assign 写 / **AssignOp** / 闭包 env 装载）⇒ 未命中**响亮报错**（不再默认 local 0）。设计稿曾建议的 `i64.const 0; drop` 占位是**错的**（净 0 ⇒ RHS 仍留栈上＝栈失衡），实改为 Var 压 `i64.const 0` 占位、env 装载压常量保 `i64.store` 双操作数，**四者产物均通过 wasmi 校验**。（原登记：⚠️ 待排期（编译器部；**代理定位，总师未逐行核对**）。属**通用**静默失败机制（任何未绑定名，不止全局）） |
| AUDIT-11.4.59 | **tenthc WASM codegen：`Assign` 不查 captures（闭包体内写自由变量 → 写 `env_ptr`）** | 红线（静默错值）——**限路径 C** | `Assign` 分支不查 captures（对比 Var 的捕获分支 `tenthc/compile/wasm.th:629-660`），而 `tenthc/hir/lower.th:2325-2333` 把自由变量收进 captures ⇒ 闭包内 `H = H + 1` **读走 env 捕获槽、写落 local 0（＝`env_ptr`）**：自读自写不一致 + 覆写环境指针 | ✅ **已修复（2026-09-17，编译器部 W7）**：复用同一 helper；命中 captures ⇒ **报错 + 不发 `set`** —— Assign 用 **`wasm_drop`** 消耗已压栈的 RHS、AssignOp 在发射任何字节前 return（净栈效应 0）⇒ **栈平衡未破**（产物 `wasmi 校验通过`，len 589/576）；另有 4 条**正常用例**（读外层形参 / 读本地捕获 / for+赋值 / 块体闭包局部 `let`）断言**不报错且 VALID**（防误报）。（原登记：⚠️ 待排期（编译器部；**代理定位，总师未逐行核对**）。与 `11.4.56` **成因不同**（后者是 Rust 侧两后端分歧），故**不并入同一断言**） |

### 11.4.60 ~ 11.4.62（2026-09-17 登记，来源 `tenth-lens/GAPS.md` 未转入条目）

> 编号由总师 2026-09-17 定号（承接 `11.4.59`）。三条均为「用 Tenth 写 `tenth-lens` 时真实撞上」的缺口，按 §11.4 体例转入登记。

| 编号 | 名称 | 性质 | 证据 | 状态 / 后续 |
|------|------|------|------|------------|
| AUDIT-11.4.60 | **`use` 路径段不能含 `-`**——仓库目录名带连字符的 Tenth 项目无法把自己拆成多模块（`tenth-lens/src/*.th` 不可被 `tenth-lens/main.th` 导入） | 能力封锁（**响亮编译期报错**，非静默错值） | 实测：`use tenth-lens::src::probe_mod::hello` → 「第 1 行第 20 列：语法错误 — 意外的标记：::」（总师 2026-09-17 独立复现）；根因：`tenth/src/hir/lower/import.rs:19-57` 只按 `<搜索目录>/<路径>.th` 查找，而 `tenth/src/main.rs:118-152` 给出的搜索目录仅 **cwd** / exe 同级 `std/`、`tenth/`、`tenth/std`——**不含脚本自身目录**；登记原文 `tenth-lens/GAPS.md:145-151`。**本项目无独立探针文件**（以单文件 `main.th` 绕行） | ✅ **已修复（2026-09-17，编译器部 W3；详见 §11.4 表末「2026-09-17 W3 修复轮」）**（原登记：⚠️ 待排期（编译器部，`use` / 模块搜索路径）。**绕行**：单文件 `tenth-lens/main.th` + 函数前缀分层（`rec_*`/`obs_*`/`grd_*`）。**修复方向**：让 `use` 接受含 `-` 的路径段，或使搜索路径包含**脚本自身所在目录**） |
| AUDIT-11.4.61 | **解释器：容器取出的「临时值」丢运行时类型标签**（`Vec.get()`/`HashMap.get()` 返回值 `type_name` → `unknown`） | **红线**（静默错值 + 跨后端分歧；**限解释器路径**） | 探针 `tenth-lens/probes/gap013_interp_tag_loss.th`：VM `X2\|容器元素标签=string` vs 解释器 `X2\|容器元素标签=unknown`；解释器把该值当 `HashMap` 键 → 「运行时错误 — HashMap 键类型不支持: alpha（仅支持 str/int/bool/float）」，VM 同表达式返回 1；`X4`「先 `let local = v.get(0);` 再使用」两路径均 `string` 且可作键（即绕行办法）；另作 native 实参会被吞（`command_arg(h, v.get(i))` 收不到参数、子进程退回用法提示、**退出码随之改变**）；登记原文 `tenth-lens/GAPS.md:164-173` | ✅ **已修复（2026-09-17，运行时部 W4；详见 §11.4 表末「2026-09-17 W4 修复轮」）**（原登记：⚠️ 待排期（运行时部，解释器方法分派）。**绕行**：容器取值先绑局部再使用（`main.th` 的 `total_notes`/`total_noise`、`tests/selftest.th` 的 `spawn_*` 已如此）。**修复方向**：解释器对 `Vec.get()`/`HashMap.get()` 的返回值保留运行时类型标签，与 VM 对齐。**注**：根因具体行号待运行时部补 |
| AUDIT-11.4.62 | **`parse_float` / `parse_int` 对非法输入静默返回 0（无失败信道）** | **静默失败**（0 是合法数值，无法与「解析失败」区分） | 探针 `tenth-lens/probes/gap014_parse_float_silent.th`（总师 2026-09-17 独立复现）：`parse_float("n/a")=0.000000`、`parse_float("")=0.000000`、`parse_float("abc")=0.000000`、`parse_int("abc")=0`（**两路径一致**）；登记原文 `tenth-lens/GAPS.md:175-181`；影响面：任何解析外部文本（日志 / 配置 / CSV）的程序，对「守卫 / 差分」类工具尤其致命 | ✅ **已修复（2026-09-17，运行时部 W6）**：新增 `parse_int_or(s) -> Result<i64, str>` / `parse_float_or(s) -> Result<f64, str>`（双侧逐字同构 `ok_result`/`err_result`）；类型标注为 **`Type::Generic{base: Enum("Result"), args:[T, str]}`**（**非**裸 `Enum`，故 `or_die`/`assume_ok` 可抽回内型并激活 M3.4 告警）；**旧 `parse_int`/`parse_float` 语义与标注逐字未变**（有断言）。`parse_or_test` 13 项。**总师独立复验**：`or_die(parse_int_or("42"))` → `42`（两路径一致）。（原登记：⚠️ 待排期（运行时部）。**绕行**：调用方显式判 `n/a` / `na` / 空串（`tenth-lens/main.th` 的 `rec_make`）。**修复方向**：提供显式失败信道（`Result`/`Option` 版本，或 `parse_float_or` 之类的变体） |

### 11.4.63 ~ 11.4.68（2026-09-17 二批登记）+ P-11

> 来源：2026-09-17 四路只读审计（native 注册清单 / i64 影响面 / JIT 双表 / tenthc 响亮化）。三条均为审计实测发现。

| 编号 | 名称 | 性质 | 证据 | 状态 / 后续 |
|------|------|------|------|------------|
| AUDIT-11.4.63 | **解释器路径缺 4 个公开 encoding native 的注册臂**（`to_utf8` / `to_utf16` / `utf16_to_str` / `bytes_to_str`） | 能力封锁（**响亮** `undefined function`，非静默错值） | VM 侧已注册（`runtime/natives.rs:3332/3342/3076/3134`），`tenth/std/prelude.th:212-216` 与手册均列为**公开** built-in；解释器侧只有 `_to_utf8`/`_to_utf16`/`_utf16_to_str`/`_bytes_to_str`（`interpreter/natives.rs:3036/3045/2808/2856`）且**全仓无别名映射** ⇒ `TENTH_NO_VM=1` 下落 `natives.rs:3117 _ => {}` → `:3164 undefined function 'to_utf8'`。**定性**：曾做「公开名去下划线」重命名——VM 侧错误文案仍残留旧内部名（`runtime/natives.rs:3073/3119/3145/3339/3350`，**本身也是用户可见文案 bug**），解释器只改文案未改臂名。**守护缺口**：`string_encoding_test.rs` **只跑 VM**（无 `TENTH_NO_VM`）⇒ 套件全绿下长期存在 | ✅ **已修复（2026-09-17，运行时部 W4；详见「2026-09-17 W4 修复轮」）**：解释器 4 个公开名改为**规范名**（**不留 `_` 别名**，避免"解释器有 VM 无"的反向双端差异）；VM 侧旧内部名文案修 **15 处**（题面 5 + 同类残留 10）；`string_encoding_test.rs` 补 **12 条双路径**断言（80 passed）。**总师独立复验**：`TENTH_NO_VM=1` 下 4 个公开名可用且与 VM 逐字一致 |
| AUDIT-11.4.64 | **i64 dtype 强制残差**：注解只对**字面量**生效，非字面量实参不强制 | 能力残差（**响亮报错**，非静默错值；双路径一致） | i64 修复轮遗留（见下方修复轮）：`fn m(x: i64, y: i64) -> i64 { x * y }` 以**非字面量** i32 实参调用 → 形参运行期仍 I32 ⇒ 报「溢出 i32 范围」。单字面量实参（`scale(2000000000)`）与体内含字面量（`x * 100`）**已正确**。同族：方法调用实参不走形参 dtype 强制；`LazyIterator::from_range` 元素恒标 I32。**修法**：帧建立期按 `Chunk.param_dtypes` 强制（VM 4 个帧点 + 解释器 + JIT 内联体）或引入 HIR cast 节点 | ⚠️ 待排期（编译器部+运行时部）。**本轮不做**（改动面跨帧建立，风险高于收益） |
| P-11 | **i64 标注函数不再走 JIT 标量特化**（修复的正确性代价） | 性能（**正确性优先**，非缺陷回退） | i64 修复轮为消除「裸 i64 寄存器特化 ABI 按 i32 检查」而**移除 I64 特化资格**（`jit/context.rs`；`ChunkSig` 特化现仅 `Int`/f64）。后果：带 `i64` 注解形参的函数走通用 ABI ⇒ 失去该路径的 JIT 加速。已有反向守护 `spec_i64_not_eligible_and_i64_semantics_hold`。**恢复条件**：特化 ABI 承载 dtype（标量槽带 tag）后重新纳入 | ⚠️ 待排期（运行时部）。**已核实（总师 grep，2026-09-17）**：G1 的 `fib(n: i64)` / 闭包 `|x: i64|` / `tr(n: i64, acc: i64)` 与 G6 的 `mha_block(..., n_heads/seq_len/d_model: i64)` **均带 i64 注解形参** ⇒ 这些场景已失去 JIT 标量特化，`docs/性能基线.md` 相关场景**待重采**；恢复＝dtype-aware 特化，**已提为 W10 第一优先项** |
| AUDIT-11.4.65 | **`let` 元组解构不分解元素类型**（逐名赋**整体** `Type::Tuple`）→ **静默错型**；且 `r.0` 元组索引未实现 | 静默失败（类型层；值级正确） | `hir/lower/lower_stmt.rs:212-214` 把 init 的**整体类型**逐个赋给每个绑定名；**只有 `match` 模式按位分解**（`lower_expr.rs:1897-1908`）⇒ `let (y, m, d) = date_from_unix_days(...)` 的 `y/m/d` 静态类型全是 `Type::Tuple([i64×3])`——**今天已如此**，仅因 `infer_binary_type:286` 的 `_ => l.clone()` 与 `check_binary_shape_compat:2605`（只查 Tensor）掩盖而未报错。另：`r.0` 元组索引**未实现**（`lower_expr.rs:970` Field 分支对 `Type::Tuple` 落 `_ => Unknown`；全仓 `Op::TupleGet` 仅 `bytecode.rs:772`(match) 与 `:1136`(let) 两个发射点） | ✅ **已修复（2026-09-17，编译器部 W6；详见 §11.4 表末「2026-09-17 W6 修复轮」）**：`lower_stmt.rs` 的解构改为**按位分解**（`Type::Tuple(elems)` 且 `names.len() >= 2 && elems.len() == names.len()` 时 `names[i]` 取 `elems[i]`，否则维持整体类型；与 match 的 `bind_pattern_vars` **对称**）。新增 `audit_11465_regression_test.rs` 4 项（date 三变量各 I64 / 扁平三元组 / 异质 4 元组逐位 / **单名 `let t = tuple` 仍取整体类型**的反向守护）。**残留**：`r.0` 元组索引**仍未实现**（本轮未顺手做）。（原登记：⚠️ 待排期（编译器部，**W6 第一步**：+6 行按位赋型，与 match 分支对称）。**影响**：`command_output_ex` 等**返元组的新 API** 解构后会拿到错误静态类型（值级正确）⇒ **必须先修**，否则新 API 的类型链路接不上 |
| AUDIT-11.4.66 | **`collect_tensor_op` 对字面量实参直接 `return None` ⇒ `gather` 的"编译期反向 shape 校验"是假覆盖** | 静默失败（校验假绿） | `hir/lower/backward_shape_pass.rs:413-414` 一带对**字面量实参**直接返回 `None` ⇒ 该算子不进编译期反向 shape 校验集合；而 **embedding 用的正是字面量 dim**（`gather(weight, 0, indices)`）⇒ 今天 `gather` 的编译期反向 shape 校验在最常见用法下**根本没跑**。影响面：凡带字面量参数的张量算子（gather / scatter / select 等） | ✅ **已修复（2026-09-17，编译器部 W11）**：字面量实参按位置占位（名 `#lit{pos}`）参与收集 + 新增 `input_int_consts`（`Literal → int 常量`）；`gather` 加「index 与 base 必须同秩」、`index_select` 加「`dim` 必须落在 base 秩内」（判据与运行时同源）。**注入漂移实证**：把字面量回退 `return None` ⇒ 红色用例**编译通过**（只在运行期报错）。**并且发现更深一层⇒另立 `11.4.83`（整个 pass 原本是死代码）** |
| AUDIT-11.4.67 | **`?` 对 `Option::None` 直通**（不解包、不早退）⇒ `let x = v.get_opt(i)?;` 把**整个 Option** 绑给 `x` | **红线**（静默错值） | VM opcode 52 只认 `enum_name == "Result"`（`runtime/vm/execute.rs:1308-1311`，非 Result 走 `:1343-1353` 原样压栈）；解释器只认 Result（`runtime/interpreter/binary.rs:426-441`）；类型层 `?` 对 Option 不脱壳（`hir/lower/lower_expr.rs:403-415`）。**诱导面**：编译期「Result/Option 被忽略」warning 的文案把 `?` 与 `or_die` **并列推荐**（`docs/语言参考手册.md:1069`），且 `:1071` 声称「被 `?` 消费」不触发 warning——**对 Option 两条均不成立** | ✅ **已修复（2026-09-17，编译器部 W11；详见 §11.4 表末「2026-09-17 W11/W12 修复轮」）**：新增 `check_try_operand_not_option`（操作数静态类型名为 Option 的**三形态**——`Generic` / 裸 `Enum("Option")` / 裸 `TypeParam("Option")` ⇒ `TypeError`），**Result 的 `?` 分支一行未动**（`try_operator_test` 22/22）；warning 文案按 Result/Option **拆开**（对 Option 不再推荐 `?`）。**总师独立核验**：`?`-on-Option 编译期报错；grep 全仓 **`?`-on-Option 现存用法 = 0**（纯收紧、零破坏）；手册与 `prelude.th` 文案已同步。（原登记：⚠️ 待排期（编译器部；**代理定位，总师未逐行核对**）。**总师裁定**：**不发明早退语义**，先让它**响亮**——`?` 作用于 Option 时**编译期报错**（提示用 `match`/`or_die`），并把丢弃 warning 的文案按 Result / Option **分开**；**手册 `:1069`/`:1071` 待代码改完后同步**（本轮不改，否则文档与实现不符）。实现前先 grep 全仓是否有 `?`-on-Option 现存用法并报总师。**未来候选**：Option 的 `?` 早退（需推断外层返回类型），另案 |
| AUDIT-11.4.68 | **`gather`/索引的 `*v as i64` 静默截断**（`NaN → 0`、`1.7 → 1`）⇒ **静默错行** | 静默错值 | index 取值走 `*v as i64`（代理定位；`gather` 同病）⇒ 非整数 / NaN 指数被**静默取整**，取到**另一行**而无声。新 `index_select` 若不处理会继承同病 | ⚠️ 待排期（运行时部）。**总师裁定**：新建的 `index_select` **采用严格化**（NaN / 非整数指数 → **响亮报错**，新 API 无既有调用方故零破坏）；`gather` 的现状**待统一**（本项即统一入口）。**2026-09-17 更新**：`index_select` 已按严格化落地（**总师独立复验**：非整型/越界 index 均响亮）；`gather` 的 `*v as i64` 静默截断**仍在**（新原语**不继承**该行为）⇒ 本项继续作为"统一入口"待排期 |

### 11.4.69 ~ 11.4.82（2026-09-17 三批登记）

> 来源：2026-09-17 W2 轮（`11.4.48` 残余 G1/G2/G3 + `11.4.41`）的**残留与附带发现**，由执行者如实上报、总师登记。

| 编号 | 名称 | 性质 | 证据 | 状态 / 后续 |
|------|------|------|------|------------|
| AUDIT-11.4.69 | **`DerefAssignOp`（`*p += 标量`）绕过 lossy 检查**（漏报） | 静默失败（漏报） | W2 修 G1 时发现：`AssignOp` 已纳入「左变量类型 × 右值类型」判定，但**解引用赋值的写回目标不可追踪** ⇒ 仍漏报。本轮**有意不引入"立即报错"新语义**（会误伤合法解引用写） | ⚠️ 待另案（编译器部）：修法需追踪解引用目标的静态类型 |
| AUDIT-11.4.70 | **lossy 类型判定对"精确可表示的常量"也报**（`t * 1.0`、`t.clamp(0.0, 1.0)`） | 可用性（**响亮、非静默**） | 既有 lossy 设计**固有属性**：判定看**静态类型**而非**值**，故 `1.0`/`0.0` 这类精确可表示常量也触发；G3 只是把该属性扩到方法实参。实测 `tenth/std/**` + `Tenth实例/**` + `tenthc/**` 三目录**零影响**（`SWEEP_WARNINGS 0`） | ⚠️ 待另案（值级常量判定属**新机制**，非本轮范围）。**性质**：误报方向，不伤正确性 |
| AUDIT-11.4.71 | **`use` 空转响亮化的残留静默点**：模块内 / inline mod 内的**非函数名**（类型 / 常量导入）形态**不报** | 静默失败（残留） | W2 已把「整条 use 空转」改为编译期警告，但**类型导入**（如 `tenth/std/test_date.th` 的 `use std::date::Date`）属"模块加载成功、只是无同名函数"，一并报会**误报** ⇒ 该类形态仍不报。真修需把类型/常量导入也纳入合并 | ⚠️ 待另案（编译器部）。**W2 有意取"警告而非硬错误"**正是为避免该误报 |
| AUDIT-11.4.72 | **`compile/wasm/compile.rs:22` 无条件 `eprintln!("[WASM] compile …")`** | 工程噪声（stderr 刷屏） | 无条件打印，是**自举 stderr 刷屏**的来源；污染用户与 CI 日志 | ✅ **已修复（2026-09-17，编译器部 W7）**：改为 **`TENTH_WASM_VERBOSE` 门控**（**未删除**，零破坏）。**总师独立核验**：自举命令下 `[WASM] compile` 输出 **243 → 0** 行；程序口径 stdout 2 行 / stderr 0 行。**口径约定**：今后提行数须给**确切命令 + stdout/stderr 分列**（W7 首轮报的"253→11 行"是 **cargo 合并口径**且未分列，已更正） |
| AUDIT-11.4.73 | **REPL 与 `compile_host` 无 `search_paths`**（仍用 `Lowerer::new()`）⇒ **文件模块在 REPL 里不可达** | 能力缺口（既有，非本轮引入） | W3 把搜索路径接进 `main.rs::source_to_hir` 的 4 个调用点后，发现**另有两类入口没有搜索路径**：REPL（`repl.rs:447/472/539` 一带）与 `compile_host`（桥/宿主编译入口）仍用 `Lowerer::new()`（空 search_paths）⇒ 在 REPL 里 `use` 文件模块仍解析不到。**注**：W3 brief 明确限定范围，故本轮未扩 | ⚠️ 待排期（编译器部，低风险小改）：把 `with_search_paths` 接到 REPL / `compile_host` 两处入口。**影响面**：交互式开发体验（非正确性） |
| AUDIT-11.4.74 | **JIT `Store` 侧（D2）块入口级资格守卫实测过度保守** ⇒ 未纳入，D2 未闭合 | 工程（守卫精度；**非静默错值**） | W4 做 M0 时给 `Store` 侧补了同型块入口守卫，但实测**回退正确语料**（`audit_d2_dup_pop_block_boundary`，ip=164 / local 3）⇒ **过度保守会把正确程序从 JIT 打回解释器**。故 W4 **未纳入**该守卫（宁缺勿滥），并如实登记。D1（`Load` 侧）守卫已落地且为**防御性**（143 目标全绿未触发） | ⚠️ 待排期（运行时部）：D2 的精确闭合依赖 **M1–M5 单表化**（`SpecPlan.per_ip` 计划化后，Store 可按计划判定而非块入口保守推定）——归 W10 |
| AUDIT-11.4.75 | **`TENTH_JIT_POISON` 的标量槽池化在 `spec_mode` 入口被排除** ⇒ 毒化覆盖有盲区 | 调试设施缺口（**该子集内 UB 仍不可探测**） | W4 实现 D10（毒化标量伴随槽）时实测：**池化 + 非零毒值**会使 `spec_f64_recursive_mixed` 崩 `0xC0000005`，且**与毒值内容相关**（`0`→绿、`42`/`2^40`→崩、不池化→绿）⇒ 为保证可用性，`spec_mode` 入口**排除**池化。故该入口下的标量槽 UB **仍不可探测**。已有可观测证据：`TENTH_JIT_POISON=1` 一轮使 `jit_spec_abi` 0.21s→23.9s（毒化确实生效） | ⚠️ 待排期（运行时部）：需查清"毒值与 `spec_f64_recursive_mixed` 崩溃"的因果（疑与未初始化槽被当真值参与递归有关），再把池化覆盖到 `spec_mode` |
| AUDIT-11.4.76 | **`11.4.61` 家族剩余 ~30 处"按裸变体 match 实参"的解释器 native** + **`math_*` 的 `Float32` 双端分叉** | ① 多为**响亮**（低危）② Float32 分叉为**跨后端分歧**（需定性） | W4 顺手排查 46 处，其中**静默错值 9 处已修**（字节数组元素读 `_=>0`、`math_*` 15 臂 `_=>0.0`、`random_seed`/`random_int` 静默 0、`env_set` 静默 no-op —— **总师已独立复验两条**：`bytes_to_str(Vec)` 现 `AB`、`math_floor(v.get(0))` 现 `2.0`，两路径一致）。**剩余 ~30 处**（BigInt/Complex/Decimal/tcp/fs 等）形态为**响亮报错**，登记为观察项；另发现 **`math_*` 的 `Float32` 双端分叉**：解释器只认 `Float`、VM 认 `Float32` | ⚠️ 观察项（运行时部）：① 剩余 ~30 处按需逐个确认"是否真的响亮"；② `math_*` 的 Float32 分叉需定性（若可复现即升为红线） |
| AUDIT-11.4.77 | **`resolve_builtin` 有缺口：部分 native 名未被该表覆盖** ⇒ 其静态返回类型落 `Unknown`（**数字不硬编码**——见下"证据"格的口径说明，以守卫棘轮台账为准）；且存在同族不对称（**`scatter` 缺而 `gather` 有**） | 静默（类型层）+ 能力不对称 | W5 立常驻守卫时发现（**数字一律不硬编码**：派单说 71、W4 快照说 128、W8 实测说「VM 194 名 / 缺口 78」—— **三个口径不一致** ⇒ 只记口径与 `as-of`，**以常驻守卫 `tenth/tests/native_registry_guard_test.rs` 的棘轮台账为准**）：`hir/lower/types.rs` 的 `resolve_builtin` 是 native 静态返回类型的**权威表**，未覆盖者落 `Unknown`（**静默**）；未覆盖者集中在 `math_*` / `time_*` / 编码与哈希族，以及 **`scatter`（`gather` 有而它没有）**。兜底是 `_ => Ok(Type::Unknown)` ⇒ **漏项是静默的**（不报错、只是类型推断退化为 Unknown） | ⚠️ 待排期（编译器部+运行时部）：按族还债（先 `scatter`/`gather` 对称，再 `math_*`/`time_*`/编码哈希族）。**守则**：`11.4.45E` 的守卫已含 `KNOWN_NO_STATIC_RETURN_TYPE` **双向棘轮台账**（新增漏项报红、台账陈旧也报红）⇒ 还债时台账会逼着同步，不会静默漂移 |
| AUDIT-11.4.78 | **`let v: Vec<i64> = Vec::new()` 的注解不收窄 inner** ⇒ `get_opt` 的解出内型为 `Unknown` | 静默（类型层退化为 Unknown） | W6 实现 `get_opt`/`try_get`（返回 `Option<T>`）时发现：`let v: Vec<i64> = Vec::new()` 的**元素类型注解不参与推导** ⇒ `v.get_opt(i)` 的 `T` 推不出、落 `Unknown`（运行时值正确，仅静态类型退化）。与 `11.4.65`（`let` 元组解构不分解元素类型）**同族**：都是"`let` 注解/结构的元素级信息没有被用起来" | ⚠️ 待排期（编译器部，**建议独立批次**——W11 已定位机制）：`hir/types.rs` 的 `TA::Generic` 只特判 `Box`/`Rc`/`Arc`/`Pin`/`Weak` ⇒ `Vec<i64>` 注解得到 `Type::Generic{base: TypeParam("Vec"), args:[i64]}`，**不是** `Type::Array{inner: i64}`；`lower_stmt.rs` 的注解合并对非 Tensor 直接 `annot.clone()` 不收窄 inner ⇒ Array 方法表（含 `get_opt`）不适用 ⇒ 解出 `Unknown`。**修法要动"注解 → Type"的全局映射**（会放开 std 全部 `Vec<T>` 注解的下游解析）⇒ 与 `11.4.65`（元组解构按位分解）**同族但不是同一机制**，独立批次更稳 |
| AUDIT-11.4.79 | **泛型实例化函数内的 native 报错被记为"待处理错误"后继续执行**（错误只在进程末尾浮出，函数已返回 `Unit`） | 静默（错误延迟/降级；**既有行为，非 W6 引入**） | W6 排查 `index_select` 时发现：`embedding<f64>` 内 `index_select` 报错后**并未立即中断**，而是记入待处理错误、函数返回 `Unit`、错误在进程末尾才浮出；用 `gather<f64>` **同样复现**（与本次改动无关）。**影响**：泛型实例化体内的运行时错误**不是立即响亮**的，调用方可能先拿到 `Unit` 继续用下去 | ⚠️ 待排期（编译器部+运行时部）：定性并让泛型实例化体内的 native 错误**立即响亮**（或至少不把 `Unit` 当正常返回值交回）。**代理定位，总师未逐行核对** |
| AUDIT-11.4.80 | **tenthc 值语句不 drop** ⇒ `g(seed); tail` 形态产出 `values remaining on stack`（WASM 校验失败） | 静默失败（编译产物非法） | W7 响亮化过程中实测（临时探针，已删）：表达式语句（函数调用作语句）**不发射 drop** ⇒ 栈上残留值 ⇒ wasmi 校验报 `values remaining on stack`；**改前改后同样 INVALID**（对照组无捕获无报错）⇒ **既有缺陷、非 W7 引入**。影响：任何"把有返回值的调用当语句"的 tenthc 编译路径 | ⚠️ 待排期（编译器部）。**代理定位，总师未逐行核对**；修法＝语句位置的值表达式后补 drop（注意与 `11.4.59` 的 RHS drop 是**两件事**） |
| AUDIT-11.4.81 | **tenthc `collect_free_vars` 作用域盲**：闭包体内 `let` 进 captures（**伪捕获**，env 槽永不读）；**块体闭包**读外层变量时 captures 为空/错位（改前**静默读 `env_ptr`**、改后响亮报"未绑定变量"） | 红线方向（静默错值 → 已部分转响亮） | W7 过程中实测：① 闭包体内的 `let` 被误收进 captures（伪捕获）；② 块体闭包读外层变量时 captures 为空/错位——**W7 之前是静默读 `env_ptr`（错值）**，W7 之后变成**响亮**「未绑定变量」（**方向正确，但根因未修**）。W7 已对"伪捕获"加抑制以免误报 | ⚠️ 待排期（编译器部）。**代理定位，总师未逐行核对**；根因＝`collect_free_vars` 缺作用域感知（需按块/嵌套作用域收集），修它要动 tenthc 的捕获分析 |
| AUDIT-11.4.82 | **两个既有 selfhost 测试的源码含 tenthc 无法编译的构造，测试仍全绿**（测试静默失效面） | 测试静默失效（掩盖 tenthc 缺口） | W7 响亮化后，`generic_enum_selfhost_test` / `typestate_selfhost_test` 的运行输出出现 **12 行 `[tenthc]` 报错**，但**测试仍全绿**：① match 模式绑定名（`v`/`a`/`c`）**未登记为 local**；② 泛型 struct 字面量 `File<Open>{path: …}` 被**误降级为 Var**（`File/Open/path`）。⇒ 这两个构造**未被 tenthc 路径真正覆盖**（测试只断言了别的面） | ⚠️ 待排期（测试部+编译器部）。**代理定位，总师未逐行核对**；两件事：① 上述两个 tenthc 缺口另立修复项；② 该两测试应**断言 tenthc 确实编译成功**（否则永远是"绿但没覆盖"） |
| AUDIT-11.4.85 | **默认（JIT）路径二元运算读到陈旧操作数槽**：形如 `i < f()`（`f` **不可内联**、语句在**循环里**）**报「无法比较」**，而 `i+f()`/`i-f()`/`i!=f()` **静默算错**；解释器正确 | **红线**（跨后端分歧 + **可静默中止**） | tenth-lens P1 首扫发现（`tenth-lens/GAPS.md` GAP-018），夹具 `tenth-lens/tests/fixtures/divergence_callsite_compare.th`。**总师独立复现**：默认路径 → `M\|start / DIRECT\|k=0 / M\|returned` + stderr「VM 运行时失败：第 45 行：运行时错误 — **无法比较**」、**rc=1**；解释器 → `DIRECT\|k=1 / DIRECT\|done / M\|returned` 完整、rc=0 ⇒ 循环体被 VM **提前中止**。对照夹具 `gap018_controls.th`（先绑局部）**两路径逐行一致、rc 均 0** ⇒ 差异确实由"返回值直接作比较操作数"引起 | 🔧 **修复中（J1 波，2026-09-17）**。**⚠️ 本行口径已被 L2-B 只读审计更正（原登记两处误判）**：① **不是"跨模块"问题**——**15 行单文件**即复现；② **不是"返回值派生自局部"**——与字面量/派生无关。**收敛后的必需要素**：默认路径＝**JIT**、调用是运算符的**第二操作数**、语句**在循环里重复求值**、被调函数**不可内联**、保持栈槽布局；**无关**：跨模块、循环位置、`TENTH_JIT_POISON`。**★面比登记宽**：`i+f()` 静默 `2/4/6`（真值 2/3/4）、`i-f()` `-2/-4/-6`、`i!=f()` 恒 `true`、`let s=i+f()` 报「+ 类型不匹配」⇒ **比较只是唯一响亮形态，其余是默认路径上的静默错值**（性质＝**红线**）。**根因嫌疑（未证，需 cargo 闭环）**：`compile/jit/translator.rs:3169-3181` 快路径 **`:3177` 声明 3 个 `ptr` 参数、`:3179` 传 4 个实参**（ABI 实为 `(vm,*const Value,usize,*mut Value)->bool`，`jit/mod.rs:5`）；次号＝偏移键标量伴随槽记账（`:450/:1341`、`Call/CallN` 只清结果槽 `:2338-2361`、结果覆盖 a 槽 `:3758`）。**同族更正**：与 `11.4.61`/`79`/`24`/`50` **不同根因**，应归 **`11.4.44`/`11.4.35` 的"JIT 标量槽/调用点记账"族**。**绕行**：先绑局部 `let k = f();`（或对含此类调用者回退解释器）。**裁决（总师）**：**正确性优先于性能**——若对齐 ABI 的改动面波及所有快路径，**先用"强制回退 VM"兜住正确性**，ABI 修复留给 `W10`（JIT 单表化）。审计全文：`.agents/tmp/prep_audit_11485.md` |
| AUDIT-11.4.86 | **模块级顶层 `let` 不按模块隔离**：不同模块的同名全局**静默别名**（实际是同一个变量） | **静默错值**（且**两路径一致地错**） | tenth-lens 多文件拆分时实测（`GAPS.md` GAP-015）：两个模块各写 `let mut N: i32 = 0`，写入互相串（实为同一全局）。**⚠️ 关键后果**：因为**两路径一致地错**，**差分工具永远看不见它** —— 只有金标准/人工断言能抓 ⇒ 本条的发现方式是"人工在拆分时撞上"，不是工具 | ⚠️ 待排期（编译器部，模块系统/作用域）。**绕行**：全项目给模块级全局加模块前缀（tenth-lens 已如此）。**性质**：静默错值 + 模块系统语义缺失 |
| AUDIT-11.4.87 | **解释器在 tenth-lens 的 P0 全量 6 源清单上栈溢出**（`thread 'tenth-main' has overflowed its stack`，rc `0xC0000409`） | 能力/稳定性（**既有，非本轮引入**） | tenth-lens P1 轮实测（`GAPS.md` GAP-019）：`--manifest manifest_all_sources.txt` 在 `TENTH_NO_VM=1` 下崩；**`git show HEAD:tenth-lens/main.th`（拆前单文件版）同样崩** ⇒ 既有缺陷。与 `P-2`（解释器树遍历原生递归）**同域** | ⚠️ 待排期（运行时部）。**影响**：P0 的全量 6 源诊断**只能在默认路径跑**；用 `TENTH_NO_VM=1` 复现会崩 ⇒ 已在 `tenth-lens/corpus/README.md` 记录用法约束 |
| AUDIT-11.4.88 | **`json_encode(HashMap)` 键序逐进程随机** ⇒ **输出不可复现** | 可复现性缺陷（**与后端无关**） | tenth-lens P1 首扫"归因不明"项，**总师独立复现并加强**：3 键 HashMap 连跑 **8 次出现 5 种键序**（`{"beta":2,"gamma":3,"alpha":1}` / `{"gamma":3,...}` / …）；**解释器路径 4 次出现 3 种** ⇒ **非跨后端分歧**（差分工具判 `NONDET` 而非 `DIVERGE`，**这个判定是对的**）。**后果**：① 任何含 HashMap 的 JSON 输出**不能作对拍/金标准基线**（tenth-lens 已用 `--repeat` 判 NONDET）；② 用户程序输出不可复现（破坏 golden 测试 / 缓存 / 签名 / 可复现构建） | ⚠️ 待排期（运行时部）：修法＝**稳定序**（对 map 键**排序**，或改用插入序容器——取实现代价低者；两方案都可复现）。**注**：JSON 对象键序在语义上无意义，但**可复现性**是本项目既有要求（fuzz/对拍/基线） |
| AUDIT-11.4.89 | **无子串/切片原语**（工具作者被迫"按行定位"绕行） | 能力缺口 | tenth-lens P1 实测（`GAPS.md` GAP-017）：写差分检查器时需要从 `PERF\|` 行里取字段/子串，语言侧无 `substr`/切片原语 ⇒ 只能按行/分隔符绕行。**同族**：`GAP-016`（`str.len()` 是**字符数**非字节数——语义问题，已留在 `GAPS.md`，未升级为缺陷） | ⚠️ 待排期（标准库部/编译器部）：给 `str` 补最小切片/子串原语（并明确"字符 vs 字节"语义） |

| AUDIT-11.4.90 | **tenth-lens P1 默认单次采样把"非确定程序"误判为 `DIVERGE`**（**假红**）——且**红数本身随机** | 工具/方法论缺陷（**假红**） | L2-A 全量扫发现、**总师独立复现**、L2-C 修正：默认 `--repeat 1` 对全量实例清单给出 `CASES\|72 DIVERGE\|11`，而 `--repeat 2` 后变 `DIVERGE\|0 NONDET\|11`。**三次独立测量的红数互不相同**（部门 11/10、总师 11/11、L2-C 9/10）⇒ **单次采样的判决不可信**；`--repeat 2` 后**两次逐行一致**。**根因**：被测程序自身含随机初始化（张量 `randn/rand`，9 条）/ `HashMap` 迭代序（3 条）/ 墙钟或随机数（2 条）⇒ 单次采样把"同侧噪声"读成"跨后端分歧"。**同族**：与 `11.4.52`（测量方法缺陷，"立即修"）同类 | ✅ **已修复（2026-09-17 L2-C）**：① `--repeat` **默认 1→2**（确定性复核＝默认步骤）；② 显式 `--repeat 1` 必打 `WARN\|REPEAT=1：…DIVERGE 可能来自被测程序自身随机性…` 并保留 `REPEAT\|1` 留痕；③ **首个 `DIVERGE` 自动复跑确认**（不一致即降级 `NONDET`；因缺 `env_unset` 只能覆盖解释器侧，见 `11.4.92`，报告如实标注）；④ **退出码三态**：全 PASS＝**0** / 有 `DIVERGE`＝**1** / 用法输入错＝**2** / **只有"无法判定"（NONDET/TIMEOUT/EXPECT_FAIL）＝3**（另提供 `--fail-on-unverified` 把第三态抬成 1）；`VERDICT\|` 分 `GREEN`/`RED`/`UNVERIFIED` 三态。自检 **59→78 项**（其中 4 项按三态新语义改期望）。**总师复验**：全量实例清单默认连跑两次**逐行一致**、`DIVERGE\|0 NONDET\|11 REPEAT\|2` **rc=3** |
| AUDIT-11.4.91 | **张量随机不可种子**：`random_seed(n)` 只覆盖标量 RNG，`rand()/randn()` 不走该路径 ⇒ **输出不可复现** | 可复现性/可测试性缺口 | L2-A 遗留、**总师定位**：可种子机制在 `runtime/natives.rs:1383-1406`（`set_seeded_rng`/`next_seeded_u64/f64`，仅 `random_int`/`random_float` 消费）；而张量侧 `natives.rs:2251/2281/2324` **直接调 `Tensor::randn`/`Tensor::rand`**，不经该路径。**后果**：① 8 条随机初始化实例（`xor_net`/`classifier`/`cnn`/`transformer_demo`…）**天然无法字节级对拍**（差分工具只能判 `NONDET`）；② 用户无法为含随机初始化的程序写 **golden 测试**；③ 与 `11.4.88`（`HashMap` 键序随机）**同族**：都是"不可复现 ⇒ 不可断言" | ⚠️ 待排期（运行时部，**第 1 步＝核实是否已有 tensor 侧 seed 入口**——代理未核实；若无，建议把 `random_seed` **同时**作用于张量 RNG（API 最一致），或新增 `tensor_seed`/`randn_seed`） |
| AUDIT-11.4.92 | **缺 `env_unset` / per-child env** ⇒ 差分工具的"确定性确认轮"**只能覆盖解释器侧** | 能力缺口（影响工具对称性） | L2-C 实测：因 `env_set` 之后无 `env_unset`，确认轮无法在**同一进程内**重跑"默认路径" ⇒ 两侧对称确认不可得（报告已如实标注）。**同源**：`tenth-lens/GAPS.md` **GAP-008**（`TENTH_NO_VM` 用 `is_ok()` 判定、设成 `0` 也算已设置；且无 `env_unset`）——工具用"两阶段跑 + 父进程 env 硬失败 + 阳性对照"三层绕过，但**对称性**仍缺 | ⚠️ 待排期（运行时部 + 标准库部）：补 `env_unset`（或 per-child env 的 `command_env`）。**价值**：不仅是工具，任何"同一进程内跑两种配置再对比"的程序都需要 |

### 2026-09-17 L 轮：tenth-lens P1（工具做厚 + 用它撞语言）

> 用户 2026-09-17 定向："继续做下去，既收获有用的工具，又探索 tenth"。总师出 brief（`.agents/tmp/brief_l1.md`）→ 单代理实施（**零 Rust 改动**）→ **总师独立抽查（含两条自跑复现）**。

**工具侧交付**：
- **多文件拆分成功**：`main.th` 748 → **49 行**（只调度）+ `tenth-lens/src/` **6 模块 1546 行**（`util`/`record`/`observe`/`guard`/`runner`/`diff`），全部用 **`use src::模块::名字`**（即本会话 W3 刚解锁的能力；模块间亦如此互引）。
- **P1 `--diff <清单>`**：逐条**两次 spawn**（默认 / `TENTH_NO_VM=1`），**各自一次 spawn 同源取三轴**（`command_output_ex`），比 stdout 内容 + 退出码 + stderr 非空性；判 `PASS` / `DIVERGE` / `TIMEOUT` / `NONDET` / `EXPECT_FAIL`；退出码 0/1/2；`--timeout-ms` / `--repeat` / `--stderr-contains` / `--out`。
- **防假绿三层（本轮最有价值的设计）**：① 先读 `env_get("TENTH_NO_VM")`，**为 `Ok(_)`（含 `"0"`/空串）⇒ 硬失败 exit 2**（`main.rs` 用 `is_ok()` 判定 ⇒ 否则会"双解释器自比自"报假绿）；② 因**无 per-child env / `env_unset`**（`GAP-008` 仍有效）⇒ 分**两阶段**跑（先全部默认路径，再 `env_set` 跑解释器路径）；③ **阳性对照**：已知必分歧夹具（`AUDIT-11.4.56` 闭包写全局）**若报"一致"则硬失败** —— 这条**验的是行为而非环境**，比 ① 更强。
- 报告首行 `CLAIM|` 明写「**PASS ＝ 两路径一致，不等于两侧都正确**（同源同 bug 两侧一起错，本工具看不见）」。
- 自检 `tenth-lens/tests/selftest.th` **22 → 59 项全过**（原 22 项**一字未改**）；新增 `corpus/README.md`（记 P0 `PERF|` 日志的重新生成方式）＋ P0 模式**清单引用日志缺失 ⇒ FATAL exit 2**（原为判红 exit 1）。

**总师独立抽查（亲跑）**：`SELFTEST|59|PASS|59|FAIL|0`；P0 `g2_repeat` → `KEYS|14 OK|14 GREEN` rc=0（不回归）；P1 正例 → `CASES|10 PASS|10` 且 **`CONTROL|DIVERGED|两侧确为不同后端（依据 AUDIT-11.4.56）`**（阳性对照真的在跑）；**父进程设 `TENTH_NO_VM=0` ⇒ `[FATAL] PARENT_ENV_DIRTY` + rc=2 + 无任何 VERDICT**（防假绿成立）；GAP-018 夹具**两路径分歧复现**（见 `11.4.85`）；`json_encode(HashMap)` 键序**8 跑 5 态复现**（见 `11.4.88`）；对照夹具两路径逐行一致（我先前一次 `rc=-1` 是**管道被 `Select-Object -First` 掐断**的假读数，已更正）。

**首扫产出**：小批 23 条（`probes/*.th` 10 + `Tenth实例/` 13，含 2 条 `error`）**全部两路径一致**；**1 条真分歧**（`11.4.85`）；**1 条非确定性**（`11.4.88`）。**⇒ "工具 + 语言探索"这条路的产出效率再次被验证**：一次首扫就带回 1 条红线 + 1 条可复现性缺陷 + 3 条能力/语义缺口。

**新登记 5 条**：`11.4.85`~`11.4.89`（见上表）。

### 11.4.83 ~ 11.4.92（2026-09-17 四批登记）

| 编号 | 名称 | 性质 | 证据 | 状态 / 后续 |
|------|------|------|------|------------|
| AUDIT-11.4.83 | **Phase 2 跨算子反向 shape pass 对最常见写法是死代码**（且既有 "pass case" 测试全部**空跑**） | 假覆盖（比 `11.4.66` 更深一层） | W11 修 `11.4.66` 时发现：`Block` lowering 把函数体**最后一条 Expr** 提升为 `final_expr`（`hir/lower/lower_expr.rs:1096-1099`），而 `backward_shape_pass.rs` 的 `find_grad_regions` **只遍历 `stmts`** ⇒ 对 `fn main(){ …; backward(loss); }` 这种**最常见写法**梯度区域**永远闭不上** ⇒ **整个 pass 从未真正运行**。**注入漂移实证**：把 `final_expr` 从识别中移除 ⇒ W11 的红色用例**编译通过**（只在运行期报错）⇒ 证明该识别承重 | ✅ **已修复（2026-09-17，编译器部 W11）**：把 `final_expr` 合成 stmt 参与 grad 区域识别；并新增红色用例断言**编译期**报错（此前为空跑）。**教训**：pass 修"活"后立刻暴露了 `11.4.84`（见下）—— 死代码掩盖了过度严格 |
| AUDIT-11.4.84 | **`shape_compatible` 要求秩相等 ⇒ 对"左补 1 可还原"的广播链路误报**（pass 修活后首次可触发） | **假阳性**（拒绝合法程序；响亮但错误） | W12 发现、W12b 定性：运行时 `backward.rs::unbroadcast`（`:1388` 左补 1、`:1393` 对 `param==1` 的轴求和、`:1402` 元素数相等才 reshape）⇒ **可还原 ⟺ grad 秩 ≥ param 秩 且右对齐后每维 `param==1` 或 `param==grad`**；而编译期却要求**秩相等** ⇒ `param(zeros(4)) + zeros(1,4) → sum → backward` **被编译期拒绝**。**W12b 的硬证据**：同一算子图加 `for i in 0..1`（使 Phase 2 丢区、不做校验）⇒ **运行期成功**、梯度还原为 `[1.0,1.0,1.0,1.0]` ⇒ **运行期能力就是 unbroadcast 可还原集合，编译器过严成立** | ✅ **已修复（2026-09-17，编译器部+运行时部 W12b）**：`shape_compatible` 改为「**方向守卫**（grad 秩 ≥ param 秩）+ 委托运行期的 `unbroadcast_feasible`」——**判据单一权威＝运行期**；`unbroadcast_feasible` 提升为 `pub(super)`。**未退化成恒真**：仍拒绝秩不足 / 两个 >1 维不等 / `param>1` 而 `grad=1`。新增 **5 条绿用例（含数值断言，元素数 4 证明真 unbroadcast）** + 2 条 lib 单元测试钉住接受/拒绝集；**W11/W12 的 3 条红用例仍红**。**总师独立复验**：原构造两路径均 `exit 0` 跑通 |





### 2026-09-17 W2 修复轮（`11.4.48` 残余 G1/G2/G3 + `11.4.41`）

**改动 6 文件**：`hir/lower/taint.rs`、`hir/lower/import.rs`、`hir/lower/lower_stmt.rs`、`runtime/interpreter/core.rs`、`tests/lossy_test.rs`、`tests/import_order_test.rs`（新增）。

- **G1**：`taint.rs::AssignOp` 增**静态类型表**（let / 形参绑定、赋值跟随右值、分支合并）；复合赋值改判「左变量类型 × 右值类型」；抽出 `op_effect_ty` 与二元算子**共用同一判定**（不再手写第二份）。
- **G2**：`taint.rs::Closure` 克隆作用域分析闭包体（捕获继承污点、形参 Exact、`return` 只进闭包自身），**闭包值仍 Exact**（不把函数值当污点值误报）。
- **G3**：`taint.rs::MethodCall` 每实参加 `method_arg_effect(receiver.ty, arg.ty)`（复用 `scalar_tensor_contraction`）。
- **`11.4.41`**：① `try_import_file` 改**三态** `ImportResolution::{Resolved, AlreadyImported, NotFound}`（不再与"文件不存在"共用 `Ok(None)`）；② `load_and_compile_file` 的 `modules` **双向传播**（下传父缓存命中、回流子缓存；**只限 `imported_files` 键**，避免内联 mod 命名空间泄漏）+ 子模块 warnings 上浮；③ 新增 `warn_unresolved_use`（use 整条空转 → 编译期警告）。附带：`merge_module_globals` 保留"先胜"但同名且类型/可变性不同时**警告**；`interpreter/core.rs` 多段 use 回填改「末段＝函数名 + 最长命中模块键前缀」。

**验收（代理实测 + 总师独立抽查）**：`lossy_test` **56 passed**（37→56，+19）；新增 `import_order_test` **9 passed**（两序 × 两路径 VM / `TENTH_NO_VM=1`，stdout 一致、不可解析 use 两路径均响亮）；**零误报总检**：150 文件（`tenth/std/**` + `Tenth实例/**` + `tenthc/**`）`SWEEP_ERRORS 6` **与改动前逐字节相同**、`SWEEP_WARNINGS 0`（总师核对两个证据文件）；全量 **2508 passed / 0 failed / 23 ignored**（142 targets）；自举 `[OK]`；`backend_parity_guard_test` **17 passed**。**总师另验**：`lower_stmt.rs` **无 UTF-8 BOM**（该文件曾被 PowerShell 批量替换误写 BOM、代理用 .NET 还原——总师核首字节 `117,115,101`＝`use`，确认干净）；临时探针 `tenth/tests/tmp_zz_sweep.rs` **已删**。

### 2026-09-17 W3 修复轮（`AUDIT-11.4.60`：`use` 搜索路径）

**改动 5 文件**：`tenth/src/main.rs`、`tenth/src/hir/lower/import.rs`、`tenth/src/parser/decl.rs`、`tenth/src/error.rs`、`tenth/tests/module_search_path_test.rs`（新增 7 项）。

- **(a) 脚本自身目录入搜索路径**：`main.rs::source_to_hir` 新增 `source_path: Option<&str>` 形参（4 个调用点传 `Some(path)`）；搜索序＝**脚本目录 → cwd → exe 同级 `std/` → `tenth/` → `tenth/std`**；抽 `push_unique_search_path` 去重（防「脚本目录 == cwd」假歧义）。
- **(a′) 子 lowerer 前置被加载模块文件所在目录**：`import.rs::child_search_paths(path)`，`load_and_compile_file` 用它建子 lowerer（该目录去重后插到最前）⇒ **模块可以导入与自己同目录的模块**。
- **(d) 非法路径段可操作提示**：`parser/decl.rs` 的 `Use` 分支重写（`use_path_segment_error`）+ `error.rs::suggestion()` 新增「路径段」分支，给三条出路（改目录名 / 放搜索路径内 / 保持单文件）。
- **失实注释更正**：`main.rs` 原注释声称搜索路径含「脚本自身目录」而代码无此条 —— 已改为与实现一致的五条序。

**总师定的语义（已实测落地）**：**脚本目录优先**（副作用＝项目自己的 `std/` 会遮蔽内置 std，由下一条兜住）；**多处命中 → 响亮警告**（列出**全部候选 + 最终采用者**，取搜索顺序第一个，**不许静默**）。

**解析目标零变化（红线，已核）**：`try_import_file` 重构为 `collect_candidates(rel_path, form)`，形态序仍 Direct → DirMod → DirNamed；候选序＝搜索目录序、取首个 ⇒ 对任意「目录 × 形态」命中组合的选择结果与旧实现**一致**，新增的只有「命中 >1 时告警」。实测：`tenth-lens/main.th` 三条 `use std::…` 仍解析（实跑出 MANIFEST/SOURCES 行）、`import_order_test` 9/9、`backend_parity_guard_test` 17/17。

**验收（代理实测 + 总师独立全量复验）**：脚本+同目录模块（含「b 内部再 use helper」）VM 与解释器均输出 `42`/`63`；同名遮蔽实测采用**脚本目录**并在多处命中时打出 2 候选 + 采用者；`use tenth-lens::src::x` 仍失败但提示可操作；全量 **2515 passed / 0 failed / 23 ignored**（143 targets）；自举 `[OK]`。**契约已写入 `docs/语言参考手册.md` §9.3**（此前文档只写「搜索 `std/` 目录」，与实现不符）。

### 2026-09-17 W4 修复轮（`11.4.61` / `11.4.63` / `11.4.44`-M0 / `P-4`）

**改动 9 文件（+635/−91）**，四项独立小任务：

- **`11.4.61`**（红线，限解释器）：**只在三个只读消费点 peel**——`type_name`、`map_key_to_string`（覆盖 insert/get/remove/contains 四处键转换）、`command_arg`（原 `if let` 不匹配 `Shared` 即 `Ok(Unit)` ⇒ **实参被静默吞**）；统一用既有权威解包 `deref_wrapped`（`runtime/value.rs:596`）。**未删 `Value::Shared` 包装**（`index.rs` 写穿透依赖它）、**未下沉**到 `Vec.get`/`Map.get`。**同族排查 46 处"按裸变体 match 实参"**：其中**静默错值 9 处一并修**（字节数组元素读 `_=>0` ⇒ 曾静默得 `"\0\0"`；`math_*` **15 臂** `_=>0.0`；`random_seed`/`random_int` 静默按 0；`env_set` 静默 no-op）。**总师独立复验两条**：`bytes_to_str(Vec)` → `AB`（原 `"\0\0"`）、`math_floor(v.get(0))` → `2.0`（原 `0.0`），**两路径均一致**；`gap013` 探针两路径逐字一致（`X2|容器元素标签=string`）。
- **`11.4.63`**：解释器 4 个公开 encoding native 改**规范名**（`to_utf8`/`to_utf16`/`utf16_to_str`/`bytes_to_str`，**不留 `_` 别名**以免造成反向双端差异）；旧内部名**用户可见文案修 15 处**（题面 5 + 同类残留 10）；`string_encoding_test.rs` 补 **12 条双路径**断言（80 passed）。
- **`11.4.44` M0**：`Load` 臂补 **D1 资格守卫**（含"槽种类≠分析种类"分支，走既有 Err→整函数回退通路）；`TENTH_JIT_POISON` **补毒化标量伴随槽**（`spec_mode` 入口除外，见 `11.4.75`）；**D2 `Store` 侧守卫实测过度保守 ⇒ 回退未纳入**（见 `11.4.74`）。**D1 无已知触发**（143 目标全绿）⇒ 守卫为**防御性**，已如实标注。
- **`P-4`**：WASM 整数 `abs` 保 i64 + `infer_abs_dtype` 类型对齐 + 运行期 dtype 跟随输入。

**计数更正（实测）**：`jit_silent_audit_test` = **33**（本表此前记 34，**错**）、`jit_consistency_test` = **49**（记 48）、`jit_spec_abi_test` = **18**（记 17）。

**验收（代理实测 + 总师独立全量复验）**：全量 **2535 passed / 0 failed / 23 ignored**（143 目标）；自举 `[OK]`；6 个 JIT 套件全绿**并另加 `TENTH_JIT_POISON=1` 一轮全绿**（`jit_spec_abi` 0.21s→23.9s 为毒化生效的可观测证据）；不回归 `backend_parity_guard_test` 17 / `import_order_test` 9 / `module_search_path_test` 7。**总师另核**：临时探针与中间日志已删、泄漏扫描 `found 0`。

### 2026-09-17 W5 修复轮（`11.4.51` 测试静默失效 + `11.4.45E` native 注册守卫；**只动测试**）

**`11.4.51`**：① 注释按源码更正（`MAX_STACK_DEPTH` 实为 **64**，`compile/jit/translator.rs:81`；`bump_sp` 在 `translator.rs:2820-2830`、fallback 分支 `compile/jit/mod.rs:86`；原文写 256 **错**）；② 把「fallback 确实发生」从**默认假设**变成**被断言的守护事实**：新增 `run_jit_observed`（读公开的 `vm.jit_ctx.is_failed(chunk)`）+ `jit_translate_main`（直接驱动 `JitContext::get_or_compile`）；**Test1（n=10）断言"未回退"**（`Ok` + `is_failed == false`），**Test2（n=300）断言"确实回退"**（`Err` 含 `JIT stack overflow` **且** `is_failed(main) == true`）。

**`11.4.45E`**：新增常驻守卫 `tenth/tests/native_registry_guard_test.rs`（**10 用例**、默认套件、无 `#[ignore]`）：
- **A2 主守卫**：VM 侧取**结构化** `vm.natives.keys()`（零扫描）；解释器侧因 `call_named_fn` 是 `match`、**无法结构化枚举**（空参探测会阻塞/退进程）⇒ 用**严格锚点**扫描 `interpreter/natives.rs` 的 `match name` 臂（与 `match` 同缩进 + 行首引号/`|` + **只取 `=>` 之前**），规避前轮实证的"错误文案里的 `_to_utf8` 被误当注册项"。断言 **解释器 ⊇ VM**。
- **三份双向棘轮台账**：`KNOWN_VM_ONLY` / `KNOWN_INTERP_ONLY` / `KNOWN_NO_STATIC_RETURN_TYPE` —— **新差集报红 + 台账陈旧（差异已消失）也报红**。
- **`VM ⊆ resolve_builtin`** 覆盖断言（该表是 `pub(super)`，测试用 `include_str!` 源码扫描）。
- **行为对拍**：1 条语料 12 个调用（含 W4 修的 4 个 encoding native），**仅 2 次 spawn**，比 stdout 原始字节 + 退出码 + 金标准。

**双端差集现状（实测，守卫绿测试本身即证据）**：VM **190** / 解释器 **186** / **VM-only 4**＝`async_sleep_ms`/`async_tcp_read`/`async_tcp_write`（解释器硬编码拒 async，**既定设计**）+ `str_add`（f-string 内部 codegen，非公开 API）/ **interp-only = ∅**（W4 已把 4 个 `_` 残留改公开名）；`resolve_builtin` 覆盖 **127**、未覆盖 **71**（见 `11.4.77`）。

**总师独立验证（关键一步）**：**自己注入漂移**——把解释器臂 `"println"` 改名为 `"println_zz_drift"` ⇒ 守卫 **FAILED（6 passed / 4 failed）**；`git checkout --` 还原后 **ok 10 passed**。⇒ **守卫是真守卫、不是装饰**（对照反面教材 `11.4.51` 自身与 `11.4.66`）。

**验收**：全量 **2545 passed / 0 failed / 23 ignored**（144 目标）；自举 `[OK]`；不回归 `backend_parity_guard_test`(17) / `import_order_test`(9) / `module_search_path_test`(7)；泄漏扫描 `found 0`。

### 2026-09-17 W6 修复轮（native 批：`11.4.65` / `11.4.62` / `11.4.40` / `11.4.11` / `command_output_ex`）

**本波是新增能力批**（跨编译器/运行时/标准库），新增 **63 项**测试；全量 **2608 passed / 0 failed / 23 ignored**；自举 `[OK]`。

- **`11.4.65`（前置）**：`let` 元组解构**按位分解**元素类型（与 match 的 `bind_pattern_vars` 对称）；单名 `let t = tuple` 仍取整体类型（反向守护）。⇒ 使返元组的新 API 解构后能拿到**正确静态类型**。
- **`11.4.62`**：`parse_int_or` / `parse_float_or` 提供**显式失败信道**（返 `Result<T, str>`），类型用 **`Generic`**（非裸 `Enum`）；旧 API 不变。
- **`11.4.40`（部分缓解）**：新增真 `Option` 方法 `get_opt`/`try_get`（三处齐改）；**`get`/`pop` 零改动**（纯新增、无删除行）⇒ 误标仍在，**不能标 ✅**。
- **`11.4.11`**：新增 **`index_select`** native 自由函数（1-D index、严格化报错、可微）+ autodiff **7 处**同步（抽出 `scatter_add_along_dim` 共用内核，Gather 委托）+ WASM 天然响亮 + `embedding.th` 切换。
- **`command_output_ex`**：新增 native，**一次 spawn 同源返回 4 元组** `(stdout, stderr, exit_code, timed_out)`。**关键设计**：`timeout_ms` **不接 `deadline_ms`**（后者是**协作式**、只在主循环每 4096 指令/tick 检查，**native 内部不 tick**）⇒ 实现为 **分块读线程（边读边投递）+ mpsc + `try_wait` 轮询 + 到期 `kill()`/`wait()`**；**超时表达＝值级 `timed_out` + 保留部分输出**（不走 `Err`）。

**总师独立复验（写探针亲跑，两路径逐字一致）**：卡死子进程（`echo PARTIAL & ping -n 30`，timeout 600ms）→ stdout 含 **`PARTIAL`**、`exit_code=-1`、**`timed_out=true`**；`or_die(parse_int_or("42"))` → `42`；`get_opt(0)`/`try_get(0)` → 元素、越界 `or_die(get_opt(7))` → **响亮** exit 1；`index_select(3×2, 0, [2,0])` → sum **14.0**、shape **[2.0, 2.0]**（手算核对 ✓）。**注**：我的首版探针误把 `index` 传成 Vec 且 base 用 `zeros`（返回 Vec）⇒ **两路径分别响亮报错**（"期望 base/index 为张量" / "没有方法 'sum'"），本身也印证了"传错类型是响亮的"。

**文档**：W6 未自行改手册（真理源由总师单点写入）——4 个 API 的手册条目建议文本已留档 `.agents/tmp/manual_updates_w6.md`，**由 W8 文档轮带新锚点落地**（含"优先新增 §11.12 而不重排既有编号"的裁定）。`tenth/std/prelude.th` 已登记 4 个 API（源码，W6 已改）。

**未做（如实登记）**：LSP 补全表（`tenth/tools/**` 不在 W6 文件权限内）、`native_parity_test.rs` 手抄 native 副本、`taint.rs` 逃逸点（`command_output_ex` 的返回属外部输入＝污点源另一机制，与既有 `command_output` 保持一致未加）、`main.rs::vm_run` dead code。

### 2026-09-17 W7 修复轮（tenthc 响亮化：`11.4.57` / `11.4.58` / `11.4.59` + `11.4.72`）

> **只做"响亮化"**（把静默失败改成响亮失败），**不补功能**；`11.4.14`（3D transpose 保守）按总师裁定**跳过另案**（它不是静默失败）。

**关键前提（已核实）**：tenthc 的**两条错误通道全死**（`Parser.errors` 与 `HirProgram.errors`——后者有 **17 处 `push_error` 调用**——**都没有读取方**；宿主只看 `compile_host` 的 0/1 返回码）⇒ **「响亮化」的物理出口只有一个：让产物变空/失败**。

**改动（4 处）**：
- **`11.4.57`**：`tenthc/parser/parser.th`（`Program.parse_error_count` / `Parser.error_count`）→ `hir/hir.th` → `hir/lower.th` 透传 → `compile/wasm.th` 入口判非零 ⇒ `println("[tenthc] 编译中止：…")` + **`return Vec::new()`**（**空字节流无 `\0asm`** ⇒ 下游 parity/wasmi/落盘立刻失败）。
- **`11.4.58`**：新增 `resolve_local` / `find_capture_slot` / `current_fn_name` / `closure_body_binds_local`，**4 处兜底点共用**（Var 读 / Assign / **AssignOp（登记册原先漏记）** / 闭包 env 装载）⇒ 未命中**响亮**。
- **`11.4.59`**：`Assign`/`AssignOp` 命中 captures ⇒ **报错 + 不发 `set`**，Assign 用 **`wasm_drop`** 消耗 RHS、AssignOp 提前 return ⇒ **栈平衡未破**。
- **`11.4.72`**：`tenth/src/compile/wasm/compile.rs` 的 `eprintln!` 改 **`TENTH_WASM_VERBOSE` 门控**。

**永久回归测试（总师退回补做后落地）**：`tenth/tests/tenthc_loud_failure_test.rs`（**5 用例**、默认套件、无 `#[ignore]`）。**为什么必须退回**：三条响亮化行为首轮**只有临时探针验过、探针已删** ⇒ 零回归守护（与早先 lossy 轮同一教训）。**测试关键技术**：tenthc 的错误走 `host.println` → Rust `println!`，而 **libtest 会吞掉通过用例的 stdout** ⇒ 进程内无法断言报错文本；解法＝**父测试 spawn `current_exe()` 自执行子进程**（文件重定向 stdout/stderr，子进程输出 `W7-BEGIN/LEN/VALID/END` 结构化标记，父测试读回分段断言）。

**红/绿实证（部门注入 3 处变异，总师复核其断言原文）**：红 ⇒ `FAILED. 2 passed; 3 failed`（57「要求产物为空：实际 len=496」/ 59「未出现响亮化特征串」/ 72「实际 248 行刷屏」；**58 未变异 ⇒ ok**，证明守卫**按站点独立**、不是装饰）；还原后 ⇒ `ok. 5 passed`。**总师独立复跑该套件：5 passed / 0 failed。**

**验收**：全量 **2613 passed / 0 failed / 23 ignored**（150 targets）；自举 `[OK]`；不回归 `backend_parity_guard_test` 17 / `native_registry_guard_test` 10 / `import_order_test` 9 / `module_search_path_test` 7；泄漏 `found 0`。

**顺手登记 3 条既有缺陷（均为 W7 过程中发现，非本波引入）**：`11.4.80`（**值语句不 drop** ⇒ `values remaining on stack`）、`11.4.81`（`collect_free_vars` **作用域盲**：闭包体内 `let` 成**伪捕获**、块体闭包捕获错位——**改前静默读 `env_ptr`、改后响亮**）、`11.4.82`（**两个既有 selfhost 测试的源码含 tenthc 无法编译的构造而测试全绿**＝测试静默失效面：match 绑定名未登记 local / 泛型 struct 字面量被误降级为 Var）。

### 2026-09-17 W11/W12 修复轮（`11.4.67` + `11.4.66`/`11.4.83` + `11.4.84`）

**三波连续**：W11（编译器部：`?`-on-Option 响亮化 + 反向 shape 假覆盖）→ W12（测试部+文档部：`#[ignore]` 占位转正 + `?` 契约文案 6 条）→ W12b（编译+运行时：广播链路误报对齐）。

- **`11.4.67`（红线）**：`?` 作用于 `Option` ⇒ **编译期报错**（新增 `check_try_operand_not_option`，覆盖 `Generic` / 裸 `Enum("Option")` / 裸 `TypeParam("Option")` 三形态），文案可操作（用 `match` / `or_die`）；**Result 的 `?` 逐字未变**；warning 文案按 Result/Option 拆开。**grep 全仓确认零现存用法** ⇒ 纯收紧。
- **`11.4.66`**：字面量实参参与收集（`#lit{pos}` 占位 + `input_int_consts`），`gather` / `index_select` 的判据与运行时同源。
- **`11.4.83`（新发现，更深一层）**：`find_grad_regions` 只遍历 `stmts`，而 `Block` lowering 把函数体**最后一条 Expr** 提升为 `final_expr` ⇒ **整个 Phase 2 pass 对 `fn main(){…; backward(loss); }` 原本是死代码**（既有 pass case 测试**全部空跑**）⇒ 已修（`final_expr` 合成 stmt 参与识别）。
- **`11.4.84`（新发现，由 `11.4.83` 修活后暴露）**：`shape_compatible` 要求**秩相等** ⇒ `param(zeros(4)) + zeros(1,4) → sum → backward` 被**编译期误拒**，而运行时 `unbroadcast`（左补 1 + 对 `param==1` 轴求和 + 元素数相等才 reshape）**本可还原**。**W12b 的硬证据**：同图加 `for i in 0..1` 使 Phase 2 丢区 ⇒ **运行期成功**、梯度还原为 `[1,1,1,1]`。**修法＝判据单一权威化**：`shape_compatible` 改为「方向守卫（grad 秩 ≥ param 秩）+ 委托运行期 `unbroadcast_feasible`」，**未退化成恒真**（仍拒绝秩不足 / 两 >1 维不等 / `param>1` 而 `grad=1`）。
- **测试**：`autodiff_backward_shape_test.rs` 的 `#[ignore]` **占位转正**（改为真 fail case：上游 `reshape` 改秩 → `gather` 秩不匹配，与既有红用例不重复）⇒ **ignored 23 → 22**；另新增 5 条绿用例（含**数值**断言）+ 2 条 lib 单元测试钉住判据接受/拒绝集；**3 条红用例仍红**。
- **文档**：`11.4.67` 的 6 条文案落笔（§5.13 新增段落 + 报错原文 / §10.6 核心命题拆分 / 触发文案拆 Result·Option / 「不触发」标注仅 Result / 两处「将改为」→「**现为**」/ `tenth/std/prelude.th` 注释）。

**总师独立核验**：原被误拒的构造**两路径均跑通**（`W12B_OK|loss=[0.0]`，exit 0）；`autodiff_backward_shape_test` **59 passed / 0 failed / 0 ignored**；全量 **2635 passed / 0 failed / 22 ignored**（150 目标）；自举 `[OK]`；5 个守卫（17/10/9/7/5）全绿；泄漏 `found 0`。

**⚠️ 本波最值得记的一条**：`11.4.83`（死代码）**掩盖**了 `11.4.84`（过度严格）——pass 修"活"的同一刻，过严的判据才第一次对用户可见。**"假绿"与"假红"是同一个病的两面**：都不告诉你真实行为。

### 2026-09-17 i64 修复轮（`AUDIT-11.4.53`，解冻 `11.4.45A`）

> 用户 2026-09-17 拍板「i64 整型算术必须要做」⇒ 保留手册承诺、改实现。改动 **24 文件**（15 源码 + 9 测试），新增 **20 项**测试。

**根因链（已闭环）**：`lexer.rs:291`（十进制超范围提升已有）→ `parser/expr.rs:27` → `lower_expr.rs:76` → **`compile/bytecode.rs:188`（`Literal::Int(n, _)` 丢弃 dtype）** → `runtime/vm/execute.rs:267`（补回硬编码 I32）→ `runtime/value.rs:396`（以 I32 报溢出）。**信息只丢在字节码这一层**。

**改动**：① `Op::PushInt(i64)` → **`PushInt(i64, BaseType)`**（编码 8B 值 + 1B tag；`vm/op.rs:9`、`vm/chunk.rs:84/126/166`）+ 全消费者（`bytecode.rs:188/681/1083` + 7 处合成常量、`vm/execute.rs:265`、`interpreter/eval.rs:48/31`、`jit/hostcalls.rs:124` 新增 `host_make_int_dtype`、`jit/translator.rs:827/1903`、`bridge.rs:679`）；② **注解驱动 dtype 强制**（`hir/lower/types.rs` 新增 `coerce_int_dtype`/`int_fits`/`int_base_of`；`lower_stmt.rs:177/1364`、`lower_expr.rs:509`）；③ **整数混合提升＝宽度优先且可交换**（rank `i8/u8 < i16/u16 < i32/u32 < i64/u64`；同宽异号 → 下一个更宽有符号；`types.rs::promote_float_dtype` + VM 10 处 + 解释器 5 处）；④ **lexer radix 字面量对齐十进制**（`lexer.rs:154`）。**tenthc 无需同步**（4 条独立证据：0 处 `PushInt`、无字节码后端、tenthc lexer 早已实现提升、整型统一 i64）；`compile/wasm/**` 免改（输入为 HIR）。

**验收（总师亲跑 + 代理实测）**：`let x: i64 = 2000000000; x * 100` → **`200000000000`**；`let a = 3000000000; a + a` → `6000000000`；`2000000000i64 * 100i64` → 正确；**交换律** `x+1 == 1+x`；**i32 溢出仍响亮**（`let c: i32 = 2000000000; c * 2` → 「溢出 i32 范围」exit 1）；两路径逐字节一致；`backend_parity_guard_test` **17/17**；全量 **2480 passed / 0 failed / 23 ignored**（141 目标）；自举 `[OK]`。

**R3（把缺陷当契约的测试）处置**：8 处期望改动**逐条留痕**（如 `jit_consistency_test.rs:353` 原断 i64 循环溢出 → 改名断同值 `4500001500000`，**并新增 i32 变体保留响亮守护**；`jit_spec_abi_test`/`jit_spec_f64_test`/`jit_skip_chunk_ctx_test` 的 i64 特化断言改 `Int`，**并新增反向守护**）。**i32/i8 溢出正向守护全部保留**（6 既有 + 2 新增 + 2 条 `ParityLoud`），守护未削弱。

### 2026-09-17 修复轮：三条跨后端红线 + 常驻跨后端对拍守护

> 对应 `11.4.50` / `11.4.54` / `11.4.55`。运行时部执行、测试部补守护、编译器部只读审计，总师抽查并单点写入真理源。

**修复（5 文件 +55/−7）**：

- `AUDIT-11.4.55` 根因**不在 `StoreGlobal`，而在编译期 local 阴影**：`tenth/src/compile/bytecode.rs:369-378` 的 `Assign` 对「程序全局名 + 当前 chunk 无同名 local」**仍新建 local 槽** ⇒ main 之后读写全落该槽、函数写全局对 main 不可见。改为命中全局时**只写全局表、不建 local**（闭包 chunk 共享 `global_names`，故同修）。
- `AUDIT-11.4.54`：`runtime/vm/execute.rs:950` Vec 分支 `unwrap_or(Value::Unit)` → 越界报错；JIT 侧 `runtime/vm/mod.rs:612` 同修；同族**字符串索引越界**（`execute.rs:955` / `vm/mod.rs:616`）与 `Vec.get` 越界（`vm/natives.rs:135`）一并修；JIT `compile/jit/translator.rs` 的 IndexGet 后补 `emit_err_check_abort()`（修前 hostcall 报错后 JIT 继续执行、先静默输出 `()`）。
- `AUDIT-11.4.50`：`vm_eq` 补 `(Tuple, Tuple)`（长度相等 + 逐元素递归）。**同族闭环**：与解释器 `values_eq` 的分支集合已对照——`Map/Map` 两侧一致为 `false`、`Value::Array` 在 VM 侧不可达 ⇒ **不修**（理由见修复报告）；60+ 处 `stack.pop().unwrap_or(Value::Unit)` 属 VM 内部不变量、无解释器对应语义 ⇒ 不修（避免无关重构）。

**验收（总师亲跑，非转述）**：① `tenth-lens/probes/gap012_vm_global_write.th` 两路径 `N1=1 N2=1 N3=2 N4=3 N5=4 N6=2`、exit 0，**逐字节一致**；② `tenth-lens/probes/check_vec_oob.th` 两路径**均 exit 1、stdout 为空**、stderr 含「Vec 索引 7 越界」；③ 元组（2 元 / 3 元 / 嵌套 / 混合类型，含 `!=`）两路径逐行一致；④ 全量 **2460 passed / 0 failed / 23 ignored**（141 个测试目标；代理实测并留证据文件，总师抽查了守护套件与三条探针）；⑤ 自举 `[OK] Full compiler compiled to tenthc_full.wasm`。

**新增常驻守护**：`tenth/tests/backend_parity_guard_test.rs`（**12 用例，进默认套件、0 `#[ignore]`**）——每个后端**一次 spawn 同源**取三轴（`env!("CARGO_BIN_EXE_tenth")` + `Command::output()`；默认路径显式 `env_remove("TENTH_NO_VM")`，专防 `main.rs:194` 的 `is_ok()` 陷阱造成「双解释器假绿」）；**双保险**＝差分断言（`stdout` 原始字节 / `stderr` 非空 / 退出码全等）＋**金标准断言**（输出必须等于语言语义应有值 ⇒「两条路径都错成一样」亦报红）。**总师独立红验证**：`git stash` 掉本轮 5 个源文件 → 该套件 **FAILED 3 passed / 9 failed**（正是 9 条红线语料）→ `git stash pop` 后 **ok 12 passed**、三条探针复跑仍正确 ⇒ **是真守卫、不是装饰**（对照反面教材 `AUDIT-11.4.51`）。

**补上的盲区**：此前 137 个测试文件**无一条**常驻断言「VM/JIT = 解释器，且失败必须响亮」——`11.4.50/54/55` 三条红线全部由一次性探针发现、探针用完即弃。

### 11.4 开放项分诊（2026-09-17，总师）

> **用途**：把"待排期"里**该长期接受的**挑出来（附理由），让待办表**可执行**。
> **原则**：**不删条目、只改处置** —— B 类条目仍留在本册（附理由），但不再占"待排期"的注意力。**本表是分诊的唯一视图**（不在各条目行内二次改写状态，避免双源漂移）。
> 口径：**A＝已排波次（本轮动手）**；**B＝长期接受（附理由）**；**C＝需用户裁定（架构/语义/对外承诺）**。

#### A · 已排波次（2026-09-17 会话内动手）

| 编号 | 一句话 | 波次/状态 |
|---|---|---|
| `11.4.53` | i64 整型 dtype 贯通 | ✅ 已修（W1） |
| `11.4.48` 残余 | lossy 防线 G1/G2/G3 | ✅ 已修（W2） |
| `11.4.41` | 导入顺序失效（**根因更正**） | ✅ 已修（W2） |
| `11.4.60` | `use` 搜索路径含脚本自身目录 | ✅ 已修（W3） |
| `11.4.61` | 解释器容器取值丢标签（+ 同族 9 处静默错值） | ✅ 已修（W4） |
| `11.4.63` | 解释器缺 4 个公开 encoding native | ✅ 已修（W4） |
| `11.4.44` | JIT **M0**（D1 守卫 + 毒化标量槽） | ⚠️ 部分修复（W4）；M1–M5 排 W10 |
| `P-4` | WASM `abs(i64)` 返 F64 | ✅ 已修（W4） |
| `11.4.51` | 测试静默失效（fallback 未断言） | ✅ 已修（W5） |
| `11.4.45E` | **native 注册一致性常驻守卫** | ✅ 已落地（W5） |
| `11.4.65` | `let` 元组解构不分解元素类型 | ✅ 已修（W6） |
| `11.4.62` | `parse_*` 无失败信道 | ✅ 已修（W6） |
| `11.4.40` | `Vec.get/pop` 类型误标 | ⚠️ **部分缓解**（W6 路线②：新增真 Option 方法；误标仍在） |
| `11.4.11` | embedding `gather` ndim → `index_select` | ✅ 已修（W6） |
| `11.4.57/58/59` | tenthc 静默→**响亮** | ✅ 已修（W7）；仍在补永久回归测试 |
| `11.4.72` | WASM 无条件 `eprintln` 刷屏 | ✅ 已修（W7，改 `TENTH_WASM_VERBOSE` 门控） |
| `11.4.66` | 反向形状校验**假覆盖**（字面量实参） | ⚠️ 待办（W11） |
| `11.4.67` | `?` 对 `Option` 直通（静默错值） | ⚠️ 待办（W11：**先响亮化**） |
| `11.4.68` | `gather` 索引 `*v as i64` 静默截断 | ⚠️ 待办（`index_select` 已严格化；gather 待统一） |
| 手册/ADR | 4 个新 API 的手册条目 + 对外行为决策 ADR | ✅ 已完成（W8） |
| `11.4.85` | **默认（JIT）二元运算读到陈旧操作数槽 ⇒ 报「无法比较」/静默算错**（**红线**；口径已更正） | 🔧 **修复中（J1 波）**；审计已收敛触发面（`.agents/tmp/prep_audit_11485.md`） |
| `11.4.90` | tenth-lens 默认单次采样 ⇒ 非确定程序被误判 `DIVERGE`（**假红**） | ✅ 已修（L2-C：`--repeat` 默认 2 + 退出码三态 0/1/2/3） |
| `11.4.91` | **张量随机不可种子**（`random_seed` 只管标量）⇒ 输出不可复现/不可 golden | ⚠️ **新立案**（运行时部；第 1 步＝核实有无 tensor seed 入口） |
| `11.4.92` | 缺 `env_unset`/per-child env ⇒ 差分工具确认轮**只能覆盖解释器侧** | ⚠️ **新立案**（运行时部+标准库部；＝GAP-008 后果） |
| `11.4.86` | 模块级同名全局**静默别名**（两路径一致地错 ⇒ 差分工具看不见） | ⚠️ **新立案**（编译器部） |
| `11.4.87` | 解释器在 P0 全量 6 源清单上栈溢出（与 `P-2` 同域） | ⚠️ **新立案**（运行时部） |
| `11.4.88` | `json_encode(HashMap)` 键序逐进程随机 ⇒ 输出不可复现 | ⚠️ **新立案**（运行时部；**修法＝稳定序**） |
| `11.4.89` | 无子串/切片原语（工具作者反复绕行） | ⚠️ **新立案**（标准库部/编译器部） |

#### B · 长期接受（**附理由**，不再占"待排期"注意力）

| 编号 | 一句话 | 接受理由 |
|---|---|---|
| `11.1.1` | T19 借用 unsoundness（B7 三类变量转义未被 `borrow_holders` 捕获） | 登记原文即「仅语义缺口，**当前约束下不可利用**」；修它要改借用检查器内部模型，收益比不成立 |
| `11.2.5` | tenthc 无 lossy/taint | **限路径 C（半冻结）**；补齐＝把整个 lossy 格移植进 tenthc。**解冻路径 C 时立刻回到 A** |
| `11.4.10` | JIT 循环回边 `is_sealed` panic（现 `catch_unwind` 兜底） | 根修需重构 block 密封时机（架构级）；功能正确，仅损失含循环函数的 JIT 加速 |
| `11.4.45` | 治理大项 5 项 | 其中 **A 项已解冻并修复（W1）**、**E 项已落地（W5）**；其余 B/C/D 三项用户 2026-09-08 已拍板归档 |
| `11.4.14` | tenthc 3D+ transpose shape 保守 | **不是静默失败**（不误报、不产错码）；对齐依赖未验证的 dim 同序约定，且 tenthc 源码不用张量 ⇒ 自举覆盖不到、风险裸奔 |
| `11.4.70` | lossy 类型判定对"精确可表示常量"也报 | **误报方向**（不伤正确性）；值级常量判定属**新机制**，另案 |
| `11.4.71` | `use` 空转响亮化的**残留静默点**（类型/常量导入形态） | 一并报会**误报**（`use std::date::Date` 是类型导入）；真修需把类型/常量导入纳入合并，另案 |
| `11.4.76` | 剩余 ~30 处"按裸变体 match 实参"的解释器 native | 形态**多为响亮**（低危）；按需逐个确认即可 |
| `11.4.79` | 泛型实例化内 native 报错被记录后继续执行 | **既有行为、非本会话引入**；定性成本高，另案 |
| `P-1` `P-2` `P-5` `P-6` `P-7` `P-9` `P-10` | Await/Yield 无 JIT · 深递归栈 · 残余 ~5ns · 递归闭包 fallback · >8 参 · TailCall · f32 不特化 | 均为已明确"**保守保持**"的性能/架构项；**正确性不受影响**。`P-2` 中 `depth 100000 出 49965` 已核算为**数学正确值非缺陷** |
| `P-11` | i64 标注函数不再走 JIT 标量特化 | **正确性优先**的代价；恢复＝dtype-aware 特化，**已排 W10 第一优先**（非"接受"） |

#### C · 需用户裁定（架构 / 语义 / 对外承诺）

| 事项 | 为什么不能由总师自定 |
|---|---|
| `11.4.46` 符号维度统一（= `ARCH-3`） | 类型系统架构；且**收紧属改进、放宽属破坏护城河**（semver 边界） |
| `ARCH-2` tensor 内核分层（`11.4.47` 剩余收益"内存带宽 ≥60%"挂此） | 影响 tensor 全部算子的内核架构 |
| **i64 窄化策略**（`let x: i32 = <i64 表达式>`：隐式 / 报错 / 必须 `lossy()`） | 改**对外语义**，且牵动 lossy 护城河 |
| `11.4.56` 闭包体内写全局的语义 | 已裁「暂缓」；ratchet 守卫钉住（不会静默） |
| `11.4.16`/`11.4.20` lossy「只传播不报」是否加**可提醒信道** | lossy 格的**取舍哲学**（设计层） |
| `11.4.60` 方案 (b) `use "字符串路径"` 语法新增 | **对外语法新增**（ADR 已记，待批） |
| `A7`：`tenthc/main.th` 是否切到 tenthc 自身管线 | 里程碑级：等于要求 tenthc 能编译自己（从未验证过），须单独一轮 |
| 全自举路线解冻条件 | ADR 冻结中 |

### 11.5 登记元数据

| 字段 | 值 |
|------|----|
| 登记日期 | 2026-07-02（第 1 批）/ 2026-07-06（11.4.4/11.4.5 新增）/ 2026-07-25（11.4.6 / 11.4.7 新增）/ 2026-07-25（11.4.8 新增）/ 2026-07-25（11.4.9 新增）/ 2026-07-30（11.4.10 / 11.4.11 / 11.4.12 / 11.4.13 新增）/ 2026-07-30（11.4.14 新增）/ 2026-07-31（11.4.15 新增）/ 2026-07-31（11.4.16 / 11.4.17 / 11.4.18 / 11.4.19 / 11.4.20 新增）/ 2026-08-03（11.4.21 ~ 11.4.27 新增）/ 2026-08-03（11.4.28 / 11.4.29 新增）/ 2026-08-03（11.4.30 新增）/ 2026-08-03（11.4.31 新增）/ 2026-08-03（11.4.32 新增）/ 2026-08-03（11.4.33 新增）/ 2026-08-03（11.4.34 新增）/ 2026-08-03（11.4.35 新增）/ 2026-08-04（11.4.36 新增）/ 2026-08-04（11.4.37 新增）/ 2026-08-04（P 系列小节登记，**非条目编号**，见 `### 11.4.38`）/ 2026-08-04（11.4.39 登记 + 11.4.34/11.4.39 修复，1.0 前红线修复轮） / 2026-09-08（11.4.45 新增）/ 2026-09-09（11.4.46 / 11.4.47 新增）/ 2026-09-16（11.4.48 / 11.4.49 / 11.4.50 / 11.4.51 / 11.4.52 / 11.4.53 / 11.4.54 / 11.4.55 新增；11.4.19 性质修正、11.4.42① 判定不可复现、11.4.47 / 11.4.48 / 11.4.49 / 11.4.52 状态更新）/ 2026-09-17（11.4.56 / 11.4.57 / 11.4.58 / 11.4.59 / 11.4.60 / 11.4.61 / 11.4.62 新增；11.4.50 / 11.4.54 / 11.4.55 修复）；2026-09-17 二批（11.4.63 / 11.4.64 新增、P-11 登记、**11.4.53 修复**、11.4.45A 解冻落地） |
| 登记批次 | 第 1 批（纯文档登记，无代码修改）+ 2026-07-06 回归测试发现新增 2 条 + 2026-07-25 基本功核查发现新增 1 条（11.4.6）+ 已知限制 #12 修复同步新增 1 条（11.4.7）+ 已知限制 #9 修复同步新增 1 条（11.4.8）+ 2026-07-25 文档部复核新增 1 条（11.4.9）+ 2026-07-30 第一波修复同步新增 4 条（11.4.10 JIT is_sealed / 11.4.11 embedding gather ndim / 11.4.12 .shape() 类型系统不一致 / 11.4.13 randn 变量参数）+ 2026-07-30 P1-1 修复同步新增 1 条（11.4.14 tenthc 3D+ transpose 双侧不一致）+ 2026-07-31 用户反馈修复同步新增 1 条（11.4.15 泛型函数运行时不可用）+ 2026-07-31 护城河系列同步新增 5 条（11.4.16 NaN 比较静默 / 11.4.17 JIT 整数溢出路径 / 11.4.18 标量→张量静默降级 natives / 11.4.19 默认线程栈 2MB 栈溢出基线 / 11.4.20 lossy 设计取舍）+ 2026-08-03 手册示例验证（E 任务）同步新增 7 条（11.4.21 VM `&mut` 写回顺序相关失效 / 11.4.22 VM 高阶函数/闭包作参数缺口 / 11.4.23 文件级模块限定调用不可用 / 11.4.24 Vec `==` 比较缺陷 / 11.4.25 字符串 `\u` 转义未实现 / 11.4.26 format 进制说明符未实现 / 11.4.27 f-string 仅标识符插值）+ 2026-08-03 a1 P4 std 高阶解锁验证同步新增 2 条（11.4.28 VM Vec/Map 方法分派缺口 / 11.4.29 group_by 文档注释误导）+ 2026-08-03 任务 9（a1 遗留三项补齐）同步新增 1 条（11.4.30 a1 闭包值调用遗留三项）+ 2026-08-03 M1-S3a 同步新增 1 条（11.4.31 解释器 broadcast_to 分派缺口 + VM 内联 mod 限定调用缺口）+ 2026-08-03 M1-S1 同步新增 1 条（11.4.32 WASM 后端 4 项缺口）+ 2026-08-03 M1 收尾（M1-S5 评审）同步新增 1 条（11.4.33 `?` 解释器 double-wrap + 十进制下划线 bug + 字节串半实现）+ 2026-08-03 M2-A5 VM-vs-JIT 一致性套件同步新增 1 条（11.4.34 match tuple 模式 + guard 缺陷：VM guard 回退错乱 + JIT 编译 panic 逃逸）+ 2026-08-03 A2 内联静默错值修复同步新增 1 条（11.4.35 i64 注解小函数循环内联静默错值：分析预测 spec 而发射内联 → 循环回边 Load 读过期标量）+ 2026-08-04 M2.6-P2 JIT 静默错值审计同步新增 1 条（11.4.36 JIT 分析/发射 binop 专用化集合漂移：Mul-I32 / Mod-F64 分析预测标量而发射不专用化 → spec 调用预测不一致 → 整函数回退 VM）+ 2026-08-04 M2.6-P5 P 系列收尾同步新增 1 条（P 系列小节登记 10 项遗留难题，纯文档；**非条目编号**，见 `### 11.4.38`）+ 2026-09-08 代码架构摸底（code-arch-audit）同步新增 1 条（11.4.45 治理大项全部归档）+ 2026-09-09 性能基线巡检同步新增 2 条（11.4.46 泛型链式调用符号维度 / 11.4.47 elementwise_binary 物化副本）+ 2026-09-16 tenth-lens 立项与 P0 轮 + 跨后端红线轮同步新增 8 条（11.4.48 lossy 绕过 / 11.4.49 死配置 / 11.4.50 VM 元组相等 / 11.4.51 jit_stack_overflow_test 注释失实 / 11.4.52 perf_vm_reference 组间污染 / 11.4.53 i32 硬限 / 11.4.54 Vec 越界静默 / 11.4.55 VM 丢全局写入）+ 2026-09-17 跨后端红线修复轮同步新增 7 条（11.4.56 闭包写全局 / 11.4.57 tenthc 程序级全局 / 11.4.58 tenthc 未绑定名落 local 0 / 11.4.59 tenthc Assign 不查 captures / 11.4.60~11.4.62 由 GAPS.md 转入）+ 2026-09-17 二批同步新增 3 条（11.4.63 解释器缺 4 个公开 encoding native / 11.4.64 i64 dtype 强制残差 / P-11 i64 标量特化移除）+ **i64 修复轮**（11.4.53 修复、11.4.45A 解冻落地，24 文件） |
| 论文来源范围 | T12 / T18 / T19 / T20 / T22 / T31 / T36 / T37 / T42 / T50（共 10 个论文条目）+ 回归测试发现 2 条 + 基本功核查发现 1 条 + 已知限制同步 2 条 + 文档部复核 1 条 + 第一波修复同步 4 条 + P1-1 修复同步 1 条 + 用户反馈修复同步 1 条 + 护城河系列同步 5 条 + 手册示例验证（E 任务）同步 7 条 + a1 P4 验证同步 2 条 + 任务 9（a1 遗留三项）同步 1 条 + M2.6-P2 审计同步 1 条 + **非论文来源批次（2026-09-08 ~ 09-17）**：代码架构摸底 1 条（11.4.45）+ 性能基线巡检 2 条（11.4.46 / 11.4.47）+ tenth-lens P0 与跨后端红线轮 8 条（11.4.48 ~ 11.4.55）+ 2026-09-17 登记 7 条（11.4.56 ~ 11.4.62）+ 二批 3 条（11.4.63 / 11.4.64 / P-11） |
| 总登记条目数 | **114**（11.1 ×5、11.2 ×5、11.3 ×2、**11.4 ×91**、P 系列 ×11；2026-09-17 总师实测 + 新转入 11.4.60 ~ 11.4.62 + 二批 11.4.63 ~ 11.4.68 + 三批 11.4.69 ~ 11.4.82 + 四批 11.4.83 ~ 11.4.92 + P-11）。**本字段此前长期失实**：原记「55（11.4 ×43）」为 2026-09-08 前旧数，未随 09-08/09-09/09-16/09-17 四个批次更新；且「P 系列 10 项是否计入」原先无定义（`### 11.4.38` 是 P 系列小节标题、不是条目编号）。**P-3=11.4.34、P-4=11.4.32 遗留② 属同项重复登记** ⇒ 去重后**唯一条目 78**；再计入 11.4.60 ~ 11.4.92 与 P-11 后 ⇒ 去重前 **114**、去重后**唯一条目 112** |
| 开放项分诊 | 见 **§11.4 开放项分诊（2026-09-17）**：A＝已排波次 / B＝长期接受（附理由）/ C＝需用户裁定。**本字段是分诊的唯一视图** |
| 拆分说明 | T12 双侧破口在 11.2 拆 4 条（shape/panic-mode/expect_gt/i64 溢出）；T37 双重 native 在 11.3 拆 2 条（VM 缺 17 项/解释器缺 3 项）；其余 8 个论文条目各 1 条；2026-07-06 新增 11.4.4（tenthc `..=` lexer bug）和 11.4.5（tenthc Vec 迭代未实现）属回归测试发现；2026-07-25 新增 11.4.6（CUDA 后端文档矛盾）属基本功核查第 94 项发现；2026-07-25 新增 11.4.7（tenthc bmm FLOPs 预估为 no-op）属已知限制 #12 修复同步登记；2026-07-25 新增 11.4.8（tenthc yield 关键字未接入）属已知限制 #9 修复同步登记；2026-07-25 新增 11.4.9（标准库三项能力文档失实）属文档部复核发现；2026-07-30 新增 11.4.10（JIT `is_sealed` 断言 panic）+ 11.4.11（embedding `gather` ndim 限制）+ 11.4.12（`hir/types.rs:392` `.shape()` 方法分支与运行时不一致）+ 11.4.13（`randn` 变量参数限制）属第一波 5 项修复同步登记；2026-07-30 新增 11.4.14（tenthc 3D+ transpose shape 推断双侧不一致）属 P1-1 修复同步登记；2026-07-31 新增 11.4.15（泛型函数运行时不可用 — `tensor<f64>([...])` Array→Tensor 转换 + `randn<T>` TypeParam 实例化）属用户反馈修复同步登记；2026-07-31 新增 11.4.16（NaN 比较静默）+ 11.4.17（JIT 整数溢出路径未覆盖）+ 11.4.18（标量→张量静默降级 natives 未全审计）+ 11.4.20（lossy 设计取舍三项）属阶段2b lossy M2 建议登记；11.4.19（parity_test / jit_stack_overflow_test 默认线程栈 2MB 栈溢出基线）属测试环境问题登记（M1 提交 0a426ad 即存在，非新回归）；2026-08-03 新增 11.4.21（VM `&mut` 写回顺序相关失效）+ 11.4.22（VM 高阶函数/闭包作参数缺口）+ 11.4.23（文件级模块限定调用不可用）+ 11.4.24（Vec `==` 比较缺陷）+ 11.4.25（字符串 `\u` 转义未实现）+ 11.4.26（format 进制说明符未实现）+ 11.4.27（f-string 仅标识符插值）属手册示例验证（E 任务）发现登记（报告 `.trae/tmp/manual_example_audit.md`；D1-D9 手册示例 bug 属文档问题不登记）；2026-08-03 新增 11.4.28（VM Vec/Map 方法分派与解释器不对齐）+ 11.4.29（collections.th `group_by` 文档注释误导）属 a1 P4 std 高阶解锁验证发现登记（报告 `.trae/tmp/a1_p4_verify/report.md`）；2026-08-03 新增 11.4.34（match tuple 模式 + guard 缺陷）属 M2-A5 VM-vs-JIT 一致性套件发现登记（JIT 编译 panic 逃逸已根治，VM guard 回退错乱待排期） |
| 真理源同步 | `能力梳理/能力全梳理.md` 同步新增能力条目；`MEMO.md` 顶部新增 2026-07-02 与 2026-07-06 与 2026-07-25 与 2026-07-30 与 2026-07-31 变更记录；2026-07-31 护城河系列条目同步新增 11.4.16-11.4.20；2026-08-03 手册示例验证（E 任务）条目同步新增 11.4.21-11.4.27；2026-08-03 a1 P5 收尾条目同步新增 11.4.28-11.4.29；2026-08-03 M1-S3a 条目同步新增 11.4.31；2026-08-03 M1-S1 条目同步新增 11.4.32；2026-08-03 M1 收尾（M1-S5 评审）条目同步新增 11.4.33；2026-08-03 M2-A5 条目同步新增 11.4.34（match tuple 模式 + guard 缺陷）；2026-08-03 A2 条目同步新增 11.4.35（i64 注解小函数循环内联静默错值——分析/发射 spec-内联不一致）；2026-08-04 M2.6-P2 条目同步新增 11.4.36（JIT 分析/发射 binop 专用化集合漂移——Mul-I32/Mod-F64 分析预测标量而发射不专用化）；2026-08-04 M2.6-P5 条目同步新增 P 系列小节（`### 11.4.38`，**非条目编号**）登记 10 项遗留难题；2026-09-08 条目同步新增 11.4.45；2026-09-09 条目同步新增 11.4.46 / 11.4.47；2026-09-16 条目同步新增 11.4.48 ~ 11.4.55；2026-09-17 条目同步新增 11.4.56 ~ 11.4.62 |
| P 系列遗留（小节 `### 11.4.38`，**非条目编号**） | 2026-08-04 M2.6-P5 收尾登记 10 项（Await/Yield JIT / 深递归栈 / AUDIT-11.4.34 / WASM abs / P1 ~5ns 开销 / 递归闭包 fallback / >8 参 / MethodCall / TailCall / f32 特化）——纯文档留档，待 M3 或后续排期（详见 11.4.38） |
| 修复方案 | 分批推进：第 1 批纯登记；2026-07-06 修复 11.4.1/11.4.2/11.4.4（✅）+ 11.4.5 codegen 修复；2026-07-08 补全 11.4.5 测试 host function stub（✅）；2026-07-08 修复 11.4.3（✅ 解释器 scope 扁平化索引）；2026-07-25 修复 11.4.6（✅ 纯文档措辞修正，零代码改动）；2026-07-25 修复 11.4.7（✅ tenthc HirType 加 dim2 + 6 处 no-op 激活 + parse_tensor_type 放宽，含 parity 测试）；2026-07-25 修复 11.4.8（✅ tenthc 侧 yield 关键字 parser/lower/wasm 三层接入 + 3 项测试）；2026-07-25 修复 11.4.9（✅ 能力全梳理状态修正 + 总结矩阵数字更新 + 测试补强 + 能力扩展，纯文档同步）；2026-07-30 修复 11.4.10（⚠️ catch_unwind workaround + 根本修复推后 P2）/ 11.4.11（⚠️ gather ndim 限制 workaround + index_select native 推后 P1 后续）/ 11.4.12（⚠️ 待清理 hir/types.rs:392 .shape() 分支，未清理避免破坏潜在依赖）/ 11.4.13（❌ P1 计划项 Dim::Symbol 支持）；2026-07-30 P1-1 完成 11.4.13（✅ Dim::Symbol + tenthc symbol_dims 双侧同步）+ 登记 11.4.14（⚠️ tenthc 3D+ transpose 保守跳过已知限制）；2026-07-31 修复 11.4.15（✅ array_to_tensor Vec/Array→Tensor 转换 + NATIVE_GENERIC_CTORS 接受 TypeParam + substitute_kind_in_place 函数名修正）；2026-07-31 登记 11.4.16-11.4.18 + 11.4.20（⚠️ lossy 系列已知限制，待后续处理）+ 11.4.19（⚠️ 环境依赖：RUST_MIN_STACK≥32MB）；2026-08-02 修复 §六 #17/#18/#19 与 11.4.12（✅ 编译器部 4 项：字节码槽位 position→rposition 族修复 + match 臂编译期作用域 scope_stack（#17/#18，含同名绑定 shadow 污染）+ 运算符重载链式返回类型从 trait_impls 取（#19）+ .shape() 误标分支删除并编译期报错引导 shape_tensor（**该缓解已于 2026-08-31 撤销**，见 §11.4 AUDIT-11.4.12 最终状态）（11.4.12）；新增 audit_17_18/audit_19/audit_11412 三个回归测试套件共 12 项，全量 0 回归，自举 [OK]）；2026-08-02 修复 11.4.17（✅ 运行时部：算术原语 checked_* 化 + JIT emit_binop/emit_unop 错误立即中断 + 解释器 Sub 对齐 VM；新增 jit_int_overflow_test 13 项，三路径一致，全量 0 回归，自举 [OK]）；2026-08-03 登记 11.4.21-11.4.27（⚠️ 手册示例验证（E 任务）发现的 7 项语言/运行时缺陷，纯文档登记待修复；D1-D9 手册示例 bug 属文档问题不登记，待手册修订）；2026-08-03 登记 11.4.28-11.4.29（⚠️ a1 P4 std 高阶解锁验证发现的 2 项缺陷——11.4.28 VM Vec/Map 方法分派缺口建议运行时部补齐分派、11.4.29 group_by 文档注释误导建议文档部修正，纯文档登记待修复）；2026-08-03 修复 11.4.35（✅ A2 编译器部：分析期可内联调用预测 Unknown + 发射层一般 Store 失效 local_scalars 双层修复；jit_consistency_test 43→48 含 +5 回归，8 个 JIT 套件全绿，自举 [OK]，fib/loop 无倒退）；2026-08-03 修复 11.4.31（✅ M1-S3a 运行时部 + 编译器部：解释器 methods.rs 补 broadcast_to 分派 + lower_expr.rs try_resolve_module_qualified 扩展内联 mod；新增 s3a_runtime_gap_test 14 项，VM=解释器一致，module_qualified_call_test 13/13 不回归，release 构建通过）；2026-08-03 修复 11.4.32（✅ M1-S1 编译器部：WASM 后端 4 项缺口——函数内引用全局（真 WASM mut i64 全局）、标签 break/continue（if_depths 增标签维度）、自定义运算符 native（sqrt/abs 内联 + sin/cos/ln/pow 新 host 函数）、泛型枚举布局 + Match codegen（tag 布局 + 变体匹配）；新增 m1s1_wasm_gaps_test 15 项，WASM 回归全绿，自举 [OK]）；2026-08-03 修复 11.4.33（✅ M1 收尾运行时部 + 文档部：`?` 解释器单层——eval.rs TryBlock 捕获分支对齐 unwrap_return 直接透传完整 `Result::Err(e)`，try_operator_test 12→17 项含多层 `?`/try 块严格单层对拍；十进制下划线——lexer read_number 三处 parse 前剥离 `_`；字节串——parser 把 ByteString 降为整数数组字面量接通全管线；手册修订示例双路径 EXIT=0，针对性套件全绿）；2026-08-03 部分修复 11.4.34（⚠️ M2-A5 编译器部 + 运行时部：**② JIT 编译 panic 逃逸已根治**——compile/jit/context.rs 把整个编译链路（translate + finalize_definitions 低化 + get_finalized_function）纳入 catch_unwind，Cranelift 低化 panic（tuple 模式 + guard 的 TryFromIntError）统一转 Err → 既有 fallback，不再逃逸进程；**① VM match guard 回退错乱未修复（待排期）**；顺带修 VM 溢出错误行号一致性缺口——execute.rs R2 快速路径（Add/Sub/Mul/Div/Mod/Neg 共 7 处）checked_* 溢出错误补 with_line，对齐 JIT 标量路径（此前 VM 溢出报错无行号、JIT 有行号）；新增 jit_consistency_test 43 项（VM=JIT 对拍：互递归/多参/字符串返回/循环内直接调用/内联多形态/标量全比较集/溢出除零取模取负/Try 多层链式循环/tuple 深层 OOB/struct 字段链循环写/Spawn eager/TailCall 链深递归/错误行号跨路径一致/热函数按需编译覆盖断言）+ bench_gate_test 2 项（D4 基准固化 CI 门槛，默认 #[ignore]，release 下断言 fib<100ms/loop<200ms/matmul<20ms）；2026-08-04 修复 11.4.36（✅ 测试部 + 编译器部，M2.6-P2：`analyze_scalar_kinds.transfer` 按发射端 binop 专用化集合逐 opcode 对齐——Mul 仅 (F64,F64)→F64（I32→Unknown）、Mod 仅 (I32,I32)→I32（F64→Unknown）、Add/Sub/Div 不变；修复 spec 函数实参为 Mul 结果时「特化参数预测不一致」整函数回退 VM 的能力缺口；新增 jit_silent_audit_test 34 项，9 个 JIT 套件全绿，自举 [OK]，fib(28) 8-10ms / loop 59-61ms 无倒退）；2026-09-08 登记 11.4.45（✅ 全部归档暂不执行）；2026-09-09 登记 11.4.46 / 11.4.47（⚠️ 待排期）；2026-09-16 登记 11.4.48 ~ 11.4.55 并落地 11.4.47 部分修复（快路径 4.43×）、11.4.48（G4/G5）、11.4.49（✅ 死配置根治）、11.4.52（✅ 按场景拆分）、11.4.19（性质修正）；2026-09-17 修复 11.4.50 / 11.4.54 / 11.4.55（✅ 三路径一致 + 常驻 `backend_parity_guard_test` 12 用例），并登记 11.4.56 ~ 11.4.62（⚠️ 待排期）；**2026-09-17 i64 修复轮**：11.4.53 ✅ 已修复（24 文件、新增 20 项测试、全量 2480/0/23、自举 [OK]、总师抽查 5 项全过），解冻并落地 11.4.45A；二批登记 11.4.63 / 11.4.64 / P-11（⚠️ 待排期）；**2026-09-17 W2 修复轮**：`11.4.48` 残余 G1/G2/G3 ✅（`taint.rs`，`lossy_test` 37→56）、`11.4.41` ✅（**根因更正**：import 三态化 + `modules` 双向传播 + 空转响亮警告；新增 `import_order_test` 9 项），全量 2508/0/23、自举 [OK]、三目录零误报；三批登记 11.4.69 ~ 11.4.72（⚠️ 待另案）。**注意**：本节 11.4.12 的旧记载（「删 `.shape()` + 编译期报错」）已作废，见冲突 3 的落点（§11.4 AUDIT-11.4.12） |

