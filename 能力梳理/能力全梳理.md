# 完整编程语言 + Tenth 定位所需能力全梳理

> 版本：v0.5.0 | 日期：2026-08-03
>
> 从**通用语言基础设施**和 **Tenth 特定需求**两个维度，穷举式罗列所有能力，并标注当前状态。
>
> 状态图例：✅ 已完成 | ⚠️ 部分/脚手架 | ❌ 未实现

---

## 目录

1. [语言核心设计](#一语言核心设计)
2. [编译器与工具链](#二编译器与工具链)
3. [运行时系统](#三运行时系统)
4. [标准库](#四标准库)
5. [Tenth 特定需求（AI 原生语言）](#五tenth-特定需求ai-原生语言)
6. [通用编程生态](#六通用编程生态tenth-作为通用语言)
7. [语言生态与社区](#七语言生态与社区)
8. [平台支持](#八平台支持)
9. [总结矩阵](#九总结矩阵)
10. [关键洞察](#十关键洞察)

---

## 一、语言核心设计

### 1.1 词法与语法

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 词法分析器 | 源码 → Token 流 | ✅ 已完成 |
| 语法分析器 | Token → AST | ✅ 已完成 |
| 注释系统 | 行注释、块注释（嵌套） | ✅ 已完成 |
| 字符串字面量 | 普通字符串、转义、原始字符串、多行字符串 | ✅ 已完成（2026-07-13 补齐：原始字符串 `r"..."`（lexer RawString + read_raw_string 无转义）、多行字符串 `"""..."""`（lexer MultiLineString + read_multiline_string 三引号闭合），Rust 侧与 tenthc 侧全同步；f"..." 模板字符串亦于 2026-07-13 B批 #41 补齐；2026-08-03 补 `\u{HEX}` Unicode 码点转义（UTF-8 编码）与 `\xNN` 十六进制字节转义，AUDIT-11.4.25） |
| 数字字面量 | 整数、浮点、十六进制、二进制、下划线分隔 | ✅ 已完成（`0x`/`0b`/`0o` 进制字面量 + 下划线分隔 + 整数后缀 `42u8`/`42i64` 等 + 编译期范围检查 + 无后缀大整数自动提升 I32→I64，2026-07-11 Problem 5；**2026-08-03 AUDIT-11.4.33 补全**：十进制/浮点下划线此前 parse 前未剥离（`1_000_000` 报「无效的整数」），现 lexer read_number 三处 parse 前 `replace('_',"")`，十进制/浮点下划线全部合法） |
| 字符字面量 | `'a'` | ✅ 已完成（2026-07-13 B批 #1 全链路接通：lexer CharLiteral token + parser→HIR→VM（PushChar 指令 opcode 54）→interpreter→WASM→JIT，新增 `Value::Char`） |
| 字节串字面量 | `b"..."` | ✅ 已完成（2026-07-13 A批 #12：lexer ByteString(Vec<u8>) + read_byte_string + `\xNN` 转义，Rust 侧与 tenthc 侧同步；**2026-08-03 AUDIT-11.4.33 补全**：原仅词法层存在、parser 不认作表达式（「意外的标记」），现 parser 把 ByteString 降为整数数组字面量（每字节 → Int(b, I32)），复用 ArrayLiteral 全管线——`b"..."` 可作表达式，`.len()`/`hex_encode`/`base64_encode`/`write_bytes` 可用） |
| 运算符重载 | 自定义 `+` `-` `*` 等行为 | ✅ 已完成（2026-07-13 F批 #11：hir/lower/lower_expr.rs `try_binary_op_overload`/`try_unary_op_overload` 按 trait impl 降为 MethodCall）；**2026-08-03 批次2 C 补注**：运算符重载在 VM 具体值路径 ✅（`operator_overload.th` 实例转绿——具体值 trait 方法编译期改写 `__dyn_*` 路由） |
| 自定义运算符 | 用户定义新运算符 | ✅ 已完成（2026-08-02 M3.1 最小可行版本：`operator <op> = fn(a: T, b: T) -> R { ... }` 声明 + `a <op> b` 使用。字符集为 `@`/`$`/`~` 连续组合（如 `@@`、`@~`）——三字符在 lexer 中原本无用途，与全部内置 token 零冲突（`<|>` 等含内置运算符字符的组合不在最小版本开放，见 MEMO M3.1 遗留）；lexer 新增 `CustomOperator(String)` token + `operator` 关键字；parser 新增 `ExprKind::CustomBinary`（默认优先级 4，与 `+`/`-` 同级、左结合）；lower 把绑定函数以合成名 `__custom_op_<op>` 注册为普通函数，`a <op> b` 降级为对该函数的普通调用 → VM/解释器/WASM 天然支持；未声明即使用/重复声明编译期 TypeError；**双侧同步**：tenthc token.th（Operator disc 91 / CustomOp disc 92）+ lexer.th + parser.th（parse_operator_def + parse_binary CustomOp 中缀）+ lower.th（custom binary→调用）全部对齐，tenthc 编译含 `operator` 的程序端到端可运行；`custom_operator_test.rs` 15 项 + `tenthc_custom_operator_test.rs` 3 项；全量 1744 passed 0 回归，自举 [OK]。**顺带修复既有 bug**：`has_trait_impl_for_type` 不识别 `Type::TypeParam`（struct 字面量的类型表示），导致用户 struct 的 `a + b` 运算符重载此前永不触发——已修复（含在 M3.1）。**2026-08-03 M1-S1 补注**：operator 定义体内调用标量 math native 已在 WASM 后端接通——sqrt/abs 用 WASM 原生指令内联（F32Sqrt/F64Sqrt/F32Abs/F64Abs/I64 位技巧），sin/cos/ln/pow 走新增 host 函数（host_sin/host_cos/host_ln/host_pow，f64 签名）；`m1s1_wasm_gaps_test.rs` P4 4 项（含既有 operator 回归）） |
| 宏/元编程 | 代码生成代码 | ✅ 最小可行版本（2026-08-02 M3.3：声明式宏 `macro name(param1, param2) { body_expr }` + 调用 `name(args)`（与函数调用同形）。编译期在 parse 完成后、lower 前做 AST 展开 pass：调用点 AST 替换为 body（参数按名代入），展开后与手写代码等价 → VM/解释器/WASM 天然支持。支持：多参数、0 参（声明可省略括号）、嵌套调用（实参为宏调用）、宏体内调用其他宏（递归展开 + 深度上限 64 防死循环）、宏先使用后定义（收集与使用顺序无关）。边界：参数个数不匹配/重复定义/递归超深 → 编译期 ParseError；未定义名调用照常走函数调用路径（lower 报未定义函数）；展开后类型错误正常报。**不做（标注遗留）**：hygiene（标识符捕获，参数/局部变量同名时按名整体替换）、模式匹配宏、过程宏、`name!(args)` 语法、tenthc 语法层（自举编译器不解析宏——tenthc lexer 有 Macro token 但 parser 未处理，自举源码无 macro 故自举安全）；宏与函数同名时调用点宏优先展开（文档标注）。`macro_test.rs` 18 项 + CLI 端到端验证；全量 1795 passed 0 回归（1 项 weak_test 预先存在失败与本次无关），自举 [OK]） |
| 语法扩展 | 编译插件、过程宏 | ❌ 未实现（**M4 决策（2026-08-01）**：声明式宏已做（M3.3）；过程宏接口标远期——需编译插件机制，非"正常语言"门槛） |
| lossy 关键字 | `lossy expr` 前缀一元表达式，标记"算错可能可接受" | ✅ 已完成（2026-07-31 lossy M2：lexer `TokenKind::Lossy` + parser `ExprKind::Lossy`（parse_unary 前缀，参照 move/await 先例）+ HIR `HirExprKind::Lossy` + bytecode/wasm/interpreter 编译为 inner=no-op（运行时 no-op，纯编译期构造）；配合 `hir/lower/taint.rs` 污点旁路分析，在 Lossy 使用点（println/eprintln/eprint/to_string/format/write_file/write_bytes/save_weights）显式放行；tenthc token 级同步（lossy 保留字 disc 90），parser 未扩（自举不依赖）记录遗留；`lossy_test.rs` 25 项测试） |

### 1.2 类型系统

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 基础类型 | 整数、浮点、布尔、字符串、字符、单元 | ✅ 已完成 |
| 整数类型 dtype 保留 | `i8/i16/i32/i64/u8/u16/u32/u64` 全管线 dtype 保留（Token/AST/HIR/Value 四层同步） | ✅ 已完成（2026-07-11 Problem 5：`IntLiteral(i64)` → `IntLiteral(i64, BaseType)` 四层同步；Lexer 后缀检测；Parser/HIR lower dtype 传播；type_of() 返回实际 dtype；`int_types_test.rs` 14 项测试） |
| 整数溢出检测 | 编译期字面量范围检查 + 运行时算术溢出检测 | ✅ 已完成（2026-07-11 Problem 5：编译期 `256u8` → LexerError；运行时 `check_int_overflow()` 检测 Add/Sub/Mul/Div/Mod/Neg 溢出）→ **2026-08-02 AUDIT-11.4.17 补全**：算术原语改用 `checked_*`（`runtime/vm/execute.rs` + `vm/mod.rs` + `runtime/interpreter/binary.rs`），i64 层溢出（`i64::MAX+1`、`i64::MIN/-1`）与窄 dtype 范围溢出统一转干净 RuntimeError——VM/JIT/解释器三路径一致（JIT 整数算术本就走 hostcall → 同一原语；JIT `emit_binop`/`emit_unop` 新增错误立即中断防级联覆盖）；`jit_int_overflow_test.rs` 13 项三路径一致；详见 AUDIT-11.4.17 |
| 张量类型 | `Tensor[f64/f32, M, N]` | ✅ 已完成（运行时，f32 Phase 5.6 真泛型改造完成） |
| 编译期 Shape 检查 | shape 不匹配编译期报错 | ⚠️ Phase 1+2+3+4 已实现（matmul/broadcast/构造函数 shape 推断 + 编译期报错 + 符号维度同名等价 + let/参数 shape 传播 + sum/mean axis 降维 + reshape 字面量推断 + permute/broadcast_to/cat/argmax/gelu/masked_fill 算子覆盖；cross_entropy/reshape 反向 shape 检查（护城河 A 深化 Phase 1）；跨函数 shape 求解 Phase 4 已实现（2026-07-28：函数体精确 shape 传播到调用方，多 return 路径 join，递归/泛型函数跳过；`join_return_dims` + `current_fn_return_shapes` 字段；`cross_fn_shape_test.rs` 测试覆盖）；**函子化重构完成**（2026-07-31 b76c1a4：删除 `Lowerer.current_fn_return_shapes` 全局可变收集器，`types.rs` 新增纯函数 `collect_return_tensor_dims`——跨函数 shape 组合性自动涌现，嵌套 return/三层 `f(g(h()))`/泛型实例化/闭包隔离由 IR 结构递归成立；附带修复闭包内 return 不再泄漏进外围函数签名；`shape_check_compile_test` 83 passed（+6 项函子测试，重构前 77）+ `cross_fn_shape_test` 21 + `type_inference_test` 40；已知断点：前向引用顺序无关性未成立、符号维度 `Dim::Symbol` 不参与 unify，详见 docs/程序代数架构设计.md §2.3/§4 与 MEMO.md 2026-07-31 护城河系列条目；**M3.1 补注（2026-08-04）**：param() 类型直通（shape 黑洞修复——param(x) 恒等直通参数类型，`param(zeros(3,4))` 保持 `Tensor[f64,3,4]`）+ tensor 字面量编译期 shape 追踪（`tensor[[...]]` 直通 TensorLiteral 的 Known 维度，字面量 matmul/广播编译期拦截）+ 不规则字面量（行长度不一致）编译期 TypeError + 嵌套 3D 字面量保守退化 [Any]（不误报 2D）+ matmul FLOPs 预估 ×2 因子修正（每乘加 2 FLOP，与 bmm 口径一致，Rust + tenthc 双侧同步），详见 MEMO.md 2026-08-04 M3.1）） |
| 运行时 Shape 校验 | autodiff backward 梯度 shape 不匹配时报错 | ✅ 已实现（护城河 A：消除 autodiff.rs 5 处 silent squeeze——acc_grad/unbroadcast/matmul_2d/MatMul 1D squeeze/Conv2D 零填充兜底全部改为报错；backward/propagate_grad 全链路 Result 传播；10 项测试 autodiff_shape_test）+ 编译期反向 shape 验证（单算子级 + 跨算子传播）——护城河 A 深化完成（2026-07-10）。Phase 1：`backward_shapes.rs` 规则表覆盖 10+ 算子（add/sub/mul/div/matmul/bmm/sum/mean/cross_entropy/reshape/scatter/gather/masked_fill/select）+ `unbroadcast_feasible` + `numel_compatible`；Phase 2：`backward_shape_pass.rs` HIR 分析 pass 做跨算子梯度 shape 传播（find_grad_regions + propagate_backward）。把运行时 RuntimeError 提升到编译期 TypeError。48 项 autodiff_backward_shape_test（31 Phase 1 + 18 Phase 2，1 #[ignore]） |
| 符号维度 | `M`/`K`/`N` 编译期追踪 | ✅ Phase 2 已实现同名等价检查（matmul 内侧 Symbol 同名兼容、不同名报错）；跨函数 unify 留待 Phase 3 |
| 编译期内存/算力预估 | 大 tensor/大 matmul 编译期 warning | ✅ 已实现（护城河 D：`Type::static_numel`/`static_bytes` + `TenthWarning` 系统；≥1GB tensor 或 ≥1 GFLOP matmul 发 warning；构造函数/函数调用/方法调用 4 个插入点；护城河 A 复用：`param(t)` ≥1GB 时发梯度 OOM warning；35 项测试 32 passed + 3 ignored） |
| 编译期警告系统 | `TenthWarning` 非致命提示 | ✅ 已实现（独立于 `TenthError`，携带 line/col/message + `display_with_source` 源码定位） |
| 秩多态 | `Tensor[f64, ..]` 任意秩 | ❌ 未实现（**M3.2 决策（2026-08-01）**：归入 shape 检查护城河战略规划——与编译期 shape 推断强耦合，需数理部论证任意秩与编译期 shape 检查的兼容性后，在护城河路线图中推进，不属语言核心独立任务） |
| 结构体 | 命名字段、默认值 `..` | ✅ 已完成 |
| 枚举 | 单元/元组/记录变体 | ✅ 已完成 |
| 元组类型 | `(a, b)` | ✅ 已完成（2026-07-08 全管线实现：`Value::Tuple(Vec<Value>)` + `Type::Tuple(Vec<Type>)` + `ExprKind::Tuple`/`Pattern::Tuple` AST 节点 + 4 个 VM Op `MakeTuple(n)`/`IsTuple(n)`/`TupleGet(i)`/`Try`(opcode 49-52)；支持元组创建/解构 `let (a,b) = pair`/match 解构/函数返回多值 `-> (i64, i64)`；空元组 `()` 编译为 `Value::Unit` 向后兼容；WASM 遇新 Op 返回 Err 触发 fallback；`tuple_test.rs` 12 项测试覆盖。**2026-08-03 M2-A3 补注**：JIT 已支持 MakeTuple/IsTuple/TupleGet——`host_make_tuple`/`host_is_tuple`/`host_tuple_get`（语义与 VM opcode 49-51 逐一对齐：弹 n 逆序装 Tuple/长度匹配 Bool/取元素越界 Unit），元组构造/解构/match 不再整函数 fallback；`jit_test` 新增 Tuple 对拍 6 项（构造解构/match 长度匹配与不匹配/跨函数返回/嵌套/越界）+ 覆盖断言） |
| 联合类型 / Union | 带 active_field 的 tagged union | ✅ 已完成（2026-08-01 M1.2 全链路接通：`union Name { f: T }` 声明 + `U { field: value }` 构造（恰好激活一个字段，运行时 `Value::Union{name, active_field, value}`）+ `u.field` 字段访问/修改（tagged 语义：只允许读写当前 active 字段，访问/修改非活跃字段报错「当前活跃字段是 …」）+ 编译期类型检查（多字段构造/未知字段/`..` 默认填充编译期 TypeError）+ Display `union U { a: 42 }` + 三路径一致（解释器 eval_field / VM get_field+StoreField+NewUnion 指令 opcode 56 / JIT host_new_union+host_load_field+host_store_field / WASM 沿用共享布局）；`union_test.rs` 25 项测试；全量 1654 passed 0 回归；自举 [OK]。注意：Tenth 的 union 是带 active_field 的 tagged union，**非** C 风格内存重叠；`MyUnion::Field(v)` 变体 match 模式留待 M2（Binding 模式 `match u { x => … }` 可用）；WASM 路径为内存共享布局（active_field 标签不落内存），与 VM/解释器的 tagged 语义存在差异（已如实记录）） |
| 类型别名 | `type Foo = Bar` | ✅ 已完成（关键字存在） |
| 泛型函数 | `<T>` 参数化 | ✅ 已完成（2026-07-31 修复泛型函数运行时不可用：`tensor<f64>([...])` 语法经 HIR 编译为 `Call("tensor", [ArrayLiteral])`，原 native 仅克隆参数返回 `Value::Vec`/`Value::Array` 导致后续张量运算失败；新增 `runtime/value.rs::array_to_tensor` 递归转换 Vec/Array→Tensor（含 `unpack_shared` 解包 Shared 包装 + `flatten_values` 形状一致性校验），VM + 解释器双侧 `tensor` native 调用该转换。同时修复 `randn<T>`/`zeros<T>`/`ones<T>`/`rand<T>` 在泛型函数体内的实例化——`hir/lower/lower_expr.rs` `NATIVE_GENERIC_CTORS` 分支接受 `Type::TypeParam`（保留原始 func_name + TypeParam dtype），`hir/lower/mod.rs` `substitute_kind_in_place` 在 TypeParam 替换为 BaseType 后按 (name, dtype) 改写 func_name 如 `randn`+F32→`randn_f32`。测试：`tenth/tests/fixtures/generic_diag.th` 验证 `tensor<f64>([1,2,3]) + tensor<f64>([10,20,30])` = `[11,22,33]` + `make_noise<f32>(4)` 正确实例化 `randn_f32`） |
| 泛型结构体 | `Pair<T, U>` | ✅ 已完成（类型实参在 StructLiteral 中保留——2026-08-01 typestate G1：`File<Open> { ... }` 字面量构造 `Type::Generic { base, args }`，状态随字面量与方法返回类型传播；见「类型状态 typestate」条目） |
| 类型状态 typestate | `impl File<Open>` 按类型实参特化 + 状态参数实例化 | ✅ 已完成（2026-08-01 阶段2a M2 主干 G1+G2+G3+G4：StructLiteral 保留泛型实参（G1）；`impl File<Open>`/`impl File<Closed>` 方法按（类型名+实参）分键注册、mangled `__File_Open_read`，同名方法互不覆盖，裸 `impl File` 通用回退（G2）；方法解析按 receiver 状态过滤（特化优先、裸名回退）、`close` 返回 `File<Closed>` 状态传播、泛型 struct 实例上方法缺失 → 编译期 TypeError（G3）；状态转换方法消费 receiver 标记 Moved（G4，状态参数检查层面）；tenthc 语法层跳过 `impl File<Open>` 的 `<...>`（语义不特化，遗留）；`typestate_test` 15 项 + `typestate_selfhost_test` 1 项，全量 1541 passed，自举 [OK]；demo `Tenth实例/类型状态Typestate/`。File 场景端到端：`File<Closed>` 上调用 `.read()` 编译期拦截——非法状态表达不出来；**G6 调用点参数类型检查（2026-08-01）**：单签名函数/泛型实例化/用户方法调用点校验实参与形参类型兼容，不兼容编译期 TypeError（带行/列）；`File<Closed>` 传 `File<Open>` 形参编译期拦截（非法状态实参表达不出来）；数值类型（i32/i64/f32/f64/整数↔浮点）按运行时值语义互相兼容放行；未声明 TypeParam/Unknown/Never/内置泛型（Vec/Option/Result）/未覆盖类型保守放行防误报；变参/默认参数不触发。`call_arg_type_check_test` 24 项，全量 1584 passed / 0 failed，自举 [OK]。**2026-08-04 M3.2 补注**：G4/G6 现状核实 + 2 个缺口修复——① **多重载兼容回退也校验实参**（`resolve_call_type` 统一对 resolve_fn_overload 选中签名检查，`fn g(i64,i64)`+`fn g(str)` 下 `g(1,"x")` 编译期报「第 2 个实参（y）类型不兼容」，此前漏到运行时且 VM/解释器两路径不一致）；② **参数数量不同的重载按实参数量选签名**（`find_fn_def_for_call`，`g("hi")` 正确匹配 1 参签名不再误报「缺少必需参数」）；`call_arg_type_check_test` 27 项 + `typestate_test` 17 项（close 后传参报「使用了已移动的值」/ 链式临时值双路径不误报）；登记 AUDIT-11.4.39（重载运行时 VM/解释器分派不一致，待排期）。**2026-08-04 M3.3 补注**：审计核实 G1+G2+G3 已落地（HEAD `7346886` 主干）——impl 特化注册 `lower_stmt.rs:971-1038`（方法表键 `File<Open>` + mangled `__File_Open_read`，裸 impl 通用回退）、方法解析按 receiver 状态特化优先/裸名回退 `lower_expr.rs:748-756` + `find_inherent_method` 三查 `lower_expr.rs:2667`、StructLiteral 保留泛型实参 `lower_expr.rs:1347-1362`；本次补三路径 JIT 对拍 + 覆盖断言 + 字面量状态推断专项（`typestate_test` 17→25：合法链/状态互不覆盖/字面量状态推断 interpreter+VM+JIT 一致、`File<Closed>` 字面量调 Open 专属 read 编译期拦截三路径一致、JIT 覆盖断言非 fallback、双状态同名方法双 chunk 并存；体平凡标量方法被 A2 内联进 main 不单独编译——内联同样 JIT 执行，覆盖断言按「main 编译 + 双 chunk 存在」判定）；自举 [OK]，67 实例扫描零新增编译错误） |
| 泛型枚举 | `Option<T>` | ✅ 已完成（2026-08-01 M2.1 显式 `<T>` 声明语法全链路：`enum Box<T> { Value(T), Empty }` 声明 + `Box<i64>::Value(42)` 显式类型实参构造 + `Box::Value(42)` 推断构造（从变体字段类型推断实参，`Vec<T>`/`Option<T>`/`[T]`/`&T` 嵌套位置提取）+ match 解构（元组/命名/单元变体，绑定变量类型替换为实例化后的具体类型）+ 嵌套泛型 `Wrap<Vec<i64>>`（`>>` Shr 处理）+ 泛型函数交互（泛型函数参数/返回值带泛型枚举，实例化后实参替换）+ 穷尽性检查在泛型枚举上仍工作 + 用户 `enum Option<T>` shadow 内置 Option（声明时清除同名内置条目）+ mangled 命名沿用泛型函数/struct 同模式；tenthc 语法层可解析 `enum X<T>` 声明（parse_generic_params 跳过，语义不特化，参照 typestate 先例）；`generic_enum_test.rs` 14 项 + `generic_enum_selfhost_test.rs` 1 项，全量 1687 passed 0 回归，自举 [OK]。**2026-08-03 M1-S1 补注**：WASM 后端泛型枚举布局已补齐——枚举布局统一为 tag(i64) + 字段（8 字节 i64 位存储），EnumLiteral 存 tag、Match 按 tag 比较变体并绑定字段（含 f64 字段位存储往返）；非泛型枚举 match 同步可用；`m1s1_wasm_gaps_test.rs` P2 4 项） |
| Trait 定义与实现 | 接口抽象 | ✅ 已完成（**2026-08-03 批次2 C 补注**：具体值 trait 方法分派 ✅——lower_expr.rs MethodCall 分支编译期改写，恰一 trait 命中 → `Call("__dyn_{trait}_{type}_{method}")`，0/≥2 不改写保持响亮报错；VM/JIT/WASM/解释器四路径同通，无新指令、tenthc 无需同步。**2026-08-03 批次2 C P3 补注**：泛型 `<T>` 内 trait/inherent 方法运行时兜底 ✅——泛型模板 body 定义时以 `TypeParam("T")` lower 查表不命中，实例化点 `rewrite_inst_method_calls` 对替换后 body 递归应用与普通 MethodCall 分支同源改写（inherent `__{Type}_{method}` 优先、trait `__dyn_*` 恰一命中其次、都不命中保持响亮报错）；VM 与解释器一致，无静默错值；`generic_trait_dispatch_test` 9 项） |
| Trait 约束 / where 子句 | `T: Add` | ✅ 已完成 |
| 关联类型 | `type Item;` in trait | ✅ 已完成（2026-07-13 基本功核查 #24 误判纠正：parser/decl.rs 已解析 associated_types） |
| 默认 Trait 方法 | trait 中提供默认实现 | ✅ 已完成（2026-07-13 基本功核查 #23 误判纠正：parser/decl.rs `TraitMethod.body: Option<Expr>` 支持 trait 中默认方法体） |
| Trait 对象 / 动态分发 | `dyn Trait` | ✅ 已完成（2026-08-01 M1.3 全链路接通，方案 A 运行时按类型分派、复用 trait_impls 无真 vtable：值表示 `Value::Dyn { trait_name, type_name, value }`；类型注解驱动升级——`let d: dyn Draw = rect;` 编译期改写为 `into_dyn(rect, "Draw")` native（lowerer 白名单 + resolve_builtin + 解释器/VM 双重注册），升级前编译期 trait impl 检查（具体类型未实现 → TypeError「类型 'X' 未实现 trait 'Y'，无法升级为 dyn Y」；Unknown/未声明 TypeParam/泛型保守放行防误报）；方法调用 `d.method()` 按 trait_name+type_name 查 trait_impls[trait][type]（解释器直接查表 + eval HIR body；VM 通过 lowerer 注册的 `__dyn_{trait}_{type}_{method}` 字节码函数 + `call_method_priv` 的 Value::Dyn 分支调用，JIT 走 host_method_call 自动覆盖）；`resolve_method_type` 对 Type::Dyn 接收者查 trait 定义签名返回类型；`dyn_trait_test.rs` 10 项测试（升级/双类型动态调用/字段访问型方法/未 impl 编译报错/无该方法运行报错/Display/函数传参/VM+解释器+JIT parity/多 trait 多类型矩阵）；全量 1664 passed 0 回归；自举 [OK]。已知边界：trait 默认方法体不注册 `__dyn_*`（dyn 上调默认方法报错，与具体值路径一致）；dyn 不可 Copy、不自动 drop（编译期既有处理）。**2026-08-03 批次2 C 补注**：具体值 trait 方法分派 ✅——lower_expr.rs MethodCall 分支编译期改写 `__dyn_*` 路由，VM/JIT/WASM/解释器四路径同通，具体值路径（trait_demo/operator_overload）转绿） |
| 高阶类型 (HKT) | `Functor<F>` | ❌ 未实现（**M4 决策（2026-08-01）**：远期研究——高阶 kind 系统，绝大多数语言没有，非"正常语言"门槛） |
| 生命周期标注 | `'a` 显式生命周期 | ⚠️ 部分/脚手架（**M1.4 决策（2026-08-01）**：`&'a T` 生命周期注解可解析并透传（`Type::Ref/MutRef` 第二参数 `Option<String>`），但**不参与借用检查语义**——Tenth 借用检查是语句粒度保守近似（T19 论文），此为设计取舍（与 GC/goto 同列）；函数级 `<'a>` 泛型参数不支持；`lifetime_test.rs` 8 项测试守护语法透传 + 借用检查不变） |
| 生命周期省略 | 自动推断生命周期 | ❌ 未实现（**M4 决策（2026-08-01）**：随 M1.4——Tenth 无显式 lifetime 语义检查，故无省略需求） |
| 可空类型 / Option | `Option::Some` / `Option::None` | ✅ 已完成（2026-07-11 真泛型化：`Type::Generic{base, args}` 承载 `Option<T>` 泛型实参） |
| Result 类型 | `Ok` / `Err` | ✅ 已完成（基础 + I/O native 错误处理：Stage 1-4 所有 I/O native 返回 `Result<T>`，`ok_result`/`err_result` 辅助函数在 `main.rs` 与 `interpreter/natives.rs` 双侧定义；详见 §4.4/§4.5/§4.7） |
| Future 类型 | `Future<T>` 异步值 | ✅ Phase 1 已实现（`Type::Future(Box<Type>)` + `Value::Future(Box<Value>)`，spawn 包装/await 解包） |
| Never 类型 / 发散 | `!` 永不返回的类型 | ✅ 已完成（2026-07-13 基本功核查 #28 误判纠正：hir/types.rs `Type::Never` + unify 到任何类型） |
| Newtype 模式 | `struct Meters(f64)` | ✅ 已完成（2026-07-13：parser/decl.rs:124 `StructKind::Tuple`，tuple struct 已解析；**2026-08-01 M2.2 全链路接通**：`Meters(3.5)` 构造（lower_expr.rs Call 分支按 `tuple_structs` 集合改写为 StructLiteral，字段 `_0/_1/...`，与 named struct 共用运行时表示）、`m._0` 字段访问、函数参数/返回值、与 named struct 并存、嵌套 Newtype；类型检查：裸 `f64` 传 `Meters` 形参编译期报错、`Meters` 传 `Meters` 通过；`.0` 语法不支持（须用 `._0`）；newtype_test.rs 12 项测试（解释器 + VM parity）） |
| 类型推断 | 局部变量自动推断 | ✅ 已完成 |
| 类型检查 | 编译期类型错误 | ✅ 已完成 |
| 协变/逆变 | 泛型子类型关系 | ❌ 未实现（**M4 决策（2026-08-01）**：远期研究——需完整方差推断表，工作量/风险高，非"正常语言"门槛） |
| Phantom 类型 | 幻影类型参数 | ✅ 已完成（2026-08-02 M3.5：侦察确认 Tenth 当前**已允许**未使用类型参数——`struct Marker<T> { x: i64 }` 可声明/构造/访问且无报错/警告（lower 仅注册字段类型，不要求每个类型参数被字段引用），因此本项为补测试确认 + 文档标注 Phantom 用法；`phantom_type_test.rs` 10 项：声明/构造/访问、多未使用参数、不同类型实参共存、无显式实参推断、空字段 phantom、方法与函数交互、typestate 协同（`File<Open>`/`File<Closed>` 状态由未使用类型参数承载）；与 typestate 护城河协同（G6 调用点检查使非法状态传参编译期拦截）；全量 1744 passed 0 回归） |
| GAT (Generic Associated Types) | 泛型关联类型 | ❌ 未实现（**M4 决策（2026-08-01）**：远期研究——依赖关联类型（已实现）+ 生命周期联动，研究向） |
| Const 泛型 | 编译期常量泛型参数 | ❌ 未实现（**M4 决策（2026-08-01）**：归入 shape 检查护城河战略——与符号维度/秩多态一起在护城河路线图推进） |

### 1.3 控制流

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| if / else if / else | 条件分支 | ✅ 已完成 |
| if 表达式 | `let x = if cond { 1 } else { 0 }` | ✅ 已完成 |
| match / 模式匹配 | 代数数据类型匹配 | ✅ 已完成（2026-07-11 穷尽性检查：枚举 match 无通配符且未覆盖全变体时编译期报 TypeError） |
| match 守卫 | `n if n > 0 =>` | ✅ 已完成 |
| while 循环 | 条件循环 | ✅ 已完成 |
| loop 无限循环 | `loop { ... }` | ✅ 已完成 |
| for-in 循环 | Range/Vec/Tensor 迭代 | ✅ 已完成 |
| break / continue | 循环控制 | ✅ 已完成 |
| break 带值 | `break val` | ✅ 已完成（2026-07-13 B批 #5：`StmtKind::Break(Option<Expr>)` 全栈） |
| 标签 break | `break 'outer` | ✅ 已完成（2026-08-01 M2.3：`'label: while/for/loop/do` 循环标签 + `break 'label` / `continue 'label` 嵌套循环跳转；复用 Lifetime token（`'name`），与 `&'a T` 类型注解通过上下文区分（语句起始 `'name : <循环关键字>` 是标签，类型注解由 type_parser 消费）；全栈实现——parser（`StmtKind::Break{label,value}`/`Continue{label}` + 循环节点 `label: Option<String>`）、HIR（`HirStmtKind` 同形）、lower（`loop_labels` 标签栈校验：未定义标签 / 标签在循环外 → 编译期 TypeError）、bytecode（loop_stack 携带标签，`break 'label` 从栈顶向下搜索匹配标签取跳转目标）、解释器（`LabeledBreakSignal`/`LabeledContinueSignal` 携带标签逐层传播，循环处理器按标签匹配吞掉或上抛）；带值+标签组合 `break 'outer val` 支持；嵌套循环同标签 shadow（内层优先）；无标签 break/continue 行为不变；**2026-08-03 M1-S1 补注**：WASM 后端已支持带标签 break/continue（if_depths 增标签维度，从栈顶向下搜索匹配标签按相对深度计算 Br；嵌套/多层 if/三层跳最外层/continue 外层 for 均验证）；tenthc 语法层不解析标签（自举源码不用，参照 typestate 先例）；`labeled_break_test.rs` 13 项 + `m1s1_wasm_gaps_test.rs` P1 4 项测试，全量 0 回归，自举 OK；详见 MEMO.md 2026-08-01 M2.3） |
| return | 函数返回 | ✅ 已完成 |
| try / catch / 异常 | 异常处理机制 | ✅ 已实现（**M2.4 决策（2026-08-01）**：try 块 = Result 错误捕获语法糖——`ExprKind::TryBlock` + `?` 操作符 + `Op::Try`/`TryPropagate`；错误处理以 **Result 优先**（所有 I/O native 返回 `Result<T>`，`or_die`/`assume_ok`/丢弃 warning 全链路）；**不引入 throw/unwind/catch 对象**——设计取舍，与 GC/goto 同列；测试：try_operator_test 12 + stdlib_test try 块 4 + type_inference try 块 + silent_failure 2） |
| try 表达式 / `?` 操作符 | 错误传播简写 | ✅ 已完成（2026-07-08 全管线实现，方案 B 新增 `Op::Try` 指令：`Question` token + `ExprKind::Try(Box<Expr>)` / `HirExprKind::Try(Box<HirExpr>)` AST/HIR 节点；类型推断从 `Result<T>` 提取 T；VM `Op::Try` 通过 frame 恢复实现 early return；解释器通过 `TryPropagate` 实现错误传播；`try_operator_test.rs` 覆盖 `?` 成功/错误传播/链式/I/O 模拟。**2026-08-03 AUDIT-11.4.33 修复**：解释器 `?` Err 传播单层一致——函数边界 `unwrap_return` 与 try 块捕获处均直接透传完整 `Result::Err(e)`（不再 double-wrap，与 VM 一致）；测试 12→17 项，含多层 `?`/try 块成功/捕获 + 解释器/VM 对拍严格单层断言。**2026-08-03 M2-A3 补注**：JIT 已支持 `Op::Try`——`host_try_pop`（Err → 写完整 `Result::Err` 到 out 返回 1，调用方 early return；Ok → 解包返回 0；非 Result 透传，与 VM/解释器单层一致），`?` 不再整函数 fallback；try 块内 `?` 走 IsEnumVariant+JmpFalse+EnumGetField 路径本就 JIT 可编译；`jit_test` 新增 Try 对拍 6 项（Ok 解包/Err 早退 Debug 对拍/多层传播/链式/try 块成功+捕获）+ `jit_error_line` 早退非错误用例） |
| do-while 循环 | 后测试循环 | ✅ 已完成（2026-07-13 B批 #10：Do 关键字 + `StmtKind::DoWhile` 全链路，tenthc 同步） |
| goto | 无条件跳转 | ❌ 未实现（通常不需要） |
| yield / 协程 | 生成器、协程 | ✅ Phase 2 已实现 + 2026-07-17 快赢批次第 93 项语法层全链路接通（VM `Op::Yield`(opcode 53) + `run_scheduler` 协作式调度器；spawn/await 真正挂起/恢复基于 `FutureState::Pending`/`Ready` + 每任务独立 Frame.operand_stack + YieldReason Completed/Suspended/Yield；**语法层 yield 关键字已接通**——`parser/ast.rs` 新增 `ExprKind::Yield(Option<Box<Expr>>)` 变体、`parser/expr.rs:699-713` `parse_unary` 新增 Yield 分支（前缀一元语义，与 Await/Spawn 一致）、`hir/hir.rs` 新增 `HirExprKind::Yield(Option<Box<HirExpr>>)` 变体、`hir/lower/lower_expr.rs:1100-1110` Yield lowering 分支（类型推断为 Unit）、`hir/lower/closures.rs:171-175` 自由变量收集、`hir/lower/backward_shape_pass.rs:191-193` 控制流检测、`hir/lower/mod.rs:450-454` 类型替换、`compile/bytecode.rs:634-645` Yield 编译分支（`Op::Pop` 丢弃 inner + `Op::Yield` + `Op::PushUnit`）、`runtime/interpreter/eval.rs:600-604` 解释器路径报错"yield 不支持解释器路径"；**关键设计**：yield 类型推断为 Unit（VM `Op::Yield` 不消费栈顶、不 push 返回值），`yield expr` 中 expr 被求值但结果丢弃；tenthc 侧 2026-07-25 已接入（hir.th disc=37 + parser.th yield 解析支持 `yield;`/`yield expr` + lower.th yield lowering 返回 Unit + wasm.th yield codegen pass through；tenthc/main.th 不含 yield 故自举不受影响；路径 B bridge.rs 未接，路径 A/C 完整可用；详见 AUDIT-11.4.8 与 MEMO.md 2026-07-25 feat 条目）；WASM 路径 `compile/wasm/compile.rs:881-883` `_ => return Err(...)` 兜底自然落入；JIT 路径 `compile/jit/translator.rs` 对 `Op::Yield` 返回 Err 触发 JIT→VM fallback；`tenth/tests/yield_test.rs` 6 测试覆盖；详见 §3 运行时系统协程/绿色线程与 async runtime 条目。**2026-08-04 M2.6-P5 补注**：JIT 对 Await/Yield 保持整函数 fallback（挂起需保存整个 VM 调度器栈，机器码帧无法序列化/恢复），列 P 系列遗留①（详见 AUDIT-11.4.38）） |
| 尾调用优化 | TCO | ✅ 已完成（2026-07-13 D批 #20：`Op::TailCall`（opcode 55）+ 帧复用 + bytecode.rs 检测 return 位置调用发射 TailCall。**2026-08-04 M2.6-P4 补注**：VM `Op::TailCall`（opcode 55）为真 TCO（execute.rs 帧复用）；**JIT 侧保守不特化**——TailCall 走 host_call→解释器 TCO 迭代帧池（深尾递归 100000 层 VM/JIT 双路径正确 49965 不崩溃，守护 `jit_call_path_test`）；JIT-to-JIT 会压原生栈帧 ~7KB/帧致 0xC0000005（P5 遗留⑨）） |
| `or_die` / `assume_ok` 显式解包 | 强制解包 Result/Option | ✅ 已完成（2026-07-31 阶段1-静默失败 M1：`or_die(x, msg)` — Ok/Some→取内部值、Err/None→panic（消息含自定义 msg）；`assume_ok(x)` — 不检查直接取内部值（用户负责）；VM `register_all_natives` + 解释器 `call_named_fn` 双侧 native；HIR 白名单三处同步（closures.rs/lower_expr.rs/types.rs）+ 解释器 eval.rs；types.rs 注册返回类型=Result/Option 泛型参数 args[0]（兼容 Enum 与 TypeParam base）） |
| Result/Option 丢弃编译期 warning | 静默丢弃值编译期警告（Rust must_use 同款路线） | ✅ 已完成（2026-07-31 阶段1-静默失败 M2：Result/Option 值被静默丢弃（Block 非 final Expr 语句 + Loop 体检查）→ 编译期 `TenthWarning`（非 error）；函数末表达式/let 绑定/`?`/or_die/assume_ok/match 消费不触发；`silent_failure_test.rs` 25 项测试） |

### 1.4 函数与闭包

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 函数定义 | `fn name(params) -> Ret` | ✅ 已完成 |
| 隐式返回 | 最后表达式作为返回值 | ✅ 已完成 |
| 闭包 | `\|x\| x + 1` | ✅ 已完成（**2026-08-03 a1 VM 闭包值调用完成**：`CallClosure`/`TailCallClosure` 指令（opcode 57/58）+ `Vm::call_value`——闭包值在 local/非 Var 表达式上的调用 VM 路径可用，捕获内联（`Value::FnRef.captures`）保证多实例独立；`Tenth实例/闭包合集/closures.th` VM 转绿；全量 1927+ passed，自举 OK。**2026-08-03 M1-S2 true letrec**：递归闭包自引用为实例级 cell（`Value::Shared`）捕获——逃逸定义作用域 + 多实例共存不再别名静默错值（`make_fact(2,10)`/`make_fact(3,1)` 各自递归正确 762769，VM=解释器一致）；HIR `Closure.self_refs` + opcode `MakeCell(63)`/`BindSelfCapture(64)`；守护 `letrec_test` 10 项三方对拍全绿。**2026-08-04 M2.6-P4 补注**：CallClosure JIT-to-JIT **直接调用**——`host_jit_call_indirect`（A1 慢路径等价物）对用户函数 chunk 且可 JIT 编译的目标 → `Vm::jit_call_chunk` → **闭包体直接执行 JIT 机器码**（不再逃逸解释器），native/未定义/recording/编译失败完整回退 call_value（语义零变化）；递归闭包（MakeCell/BindSelfCapture）创建点整函数 fallback 保持（P5 遗留⑥，闭包体 Load+CallClosure 仍可 JIT，host_call_indirect 走 call_value 已支持 Shared cell 解包）） |
| 闭包捕获 | 自动捕获环境变量 | ✅ 已完成（**2026-08-03 a1 捕获内联完成**：`Op::MakeClosure` 扩展为（params, captures_count, name_idx），捕获值内联进 FnRef，删除全局名 hack——`make_adder(5)`/`make_adder(10)` 独立（add5(3)=8 / add10(3)=13，P3 前 add5 静默返回 13）；嵌套闭包 chunk 注册修复；`Tenth实例/闭包捕获/closure_capture.th` VM 转绿） |
| 高阶函数 | 函数作为参数/返回值 | ✅ 已完成（**2026-08-03 a1 std 高阶解锁**：`iter::map/filter/reduce`、`collections::any/all/find/count_if/partition`、`curry::partial/curry/compose`、`accumulate_loop<T>`、`runtime::run_with_limit/limit_or_default/run_with_timeout/timeout_or_default` 全部 VM 路径可用（VM=解释器对拍一致）；`flat_map`/`map_values`/`filter_map` 阻塞于新登记 AUDIT-11.4.28（VM Vec/Map 方法分派缺口）） |
| 柯里化 / 部分应用 | `f(a)(b)` | ✅ 已完成（2026-07-13 E批 #18：std/curry.th——partial/curry/compose；**2026-08-03 a1 后 VM 路径可用**：多实例独立 add5/add7 互不污染，VM=解释器对拍一致） |
| 默认参数 | `fn f(x: i64 = 0)` | ✅ 已完成（2026-07-13 C批 #6：`Param::default_value` + `HirFnDef::param_defaults` + process_call_args 填充） |
| 命名参数 | `f(x = 1)` | ✅ 已完成（2026-07-13 C批 #7：`ExprKind::NamedArg` + 按签名匹配） |
| 可变参数 | `fn f(...args)` | ✅ 已完成（2026-07-13 C批 #17：DotDotDot 令牌 + `Param::variadic` 标记 + 多余实参收集为 Array） |
| 函数重载 | 同名不同签名 | ✅ 已完成（2026-07-13 F批 #16：hir/lower/scope.rs functions 改 `HashMap<String, Vec<签名>>` + `resolve_fn_overload` 类型匹配） |
| 方法定义 | `impl` 块中定义方法 | ✅ 已完成 |
| 静态方法 | 类型关联函数 `Vec::new()` | ✅ 已完成 |
| 构造函数 / 析构函数 | `new` / `drop` | ✅ 已完成（`new` 经静态方法约定（如 `Vec::new()`）；析构——Drop trait/RAII：**2026-08-01 M2.5 修复验证**——`type_impls_drop` 原不处理 `Type::TypeParam`（struct 字面量类型），RAII 永不触发；修复后作用域退出/函数返回调用 `__dyn_Drop_{Type}_drop`（VM/解释器双路径），drop_test.rs 8 项测试） |
| async / await | 异步函数 | ✅ Phase 1 已实现（`async fn` + `await expr`/`spawn expr` 前缀语法 + `Type::Future(Box<Type>)` + VM `Op::Await`/`Op::Spawn` 同步调度；Phase 2 引入真正协程调度） |
| 生成器 / yield | 惰性序列 | ❌ 未实现 |
| 尾递归 | 递归优化 | ❌ 未实现 |
| 模块级全局（顶层 let） / use 导入模块状态 | 顶层 `let` = 模块级全局常量/状态，同文件函数可见；`use` 导入随模块带入 | ✅ 已完成（2026-08-02 M3.5：修复「顶层 let 对函数不可见」+「模块级状态不随 use 导入」——此前 `let PI: f64 = 3.14; fn get_pi() -> f64 { PI }` 编译报"未定义变量 PI"、`use std::math::constants::*` 后函数内用 PI 报错。**语言语义**：① 顶层单名 `let` = 程序级全局（`HirProgram.globals: Vec<HirGlobal{name,ty,mutable,init}>`），同文件函数可见，运行时在 main 之前初始化；② 仅**被函数引用**（含传递引用）的顶层 let 提升为全局，**未被函数引用的顶层 let 保留在 main_expr 原位**（保护执行顺序，如 autodiff 测试 `new_grad(); let y = x.abs(); backward(y)` 的 tape 顺序）；③ `use path::*`/`use path::name` 导入时模块顶层 let（含 mutable 状态）合并为导入方全局（同名先注册者胜，本地优先）；④ 模块模式（use 导入文件与 REPL 行）提取全部顶层单名 let；⑤ 函数内 `let PI = 99` shadow 不覆盖全局（VM 按 global_names 判定）；⑥ 可变全局跨模块可读写（`use std::logging::logging::*` 后 `set_level`/`log_level` 生效）。**实测**：constants/logging/lr_schedule 三模块由"完全不可用"转可用（`PI + TAU*2 + E = 18.42624509640801`）。**测试**：`toplevel_let_test.rs` 10 项解释器/VM 双路径；全量 1836 passed 0 回归，自举 [OK]。**遗留**：~~WASM 后端函数内引用全局不支持~~（**2026-08-03 M1-S1 已修复**：globals 改为真 WASM mut i64 全局，函数体 GlobalGet/GlobalSet，main 前初始化，f64 全局存储转换正确）；use 跨模块同名可变全局先注册者胜。详见 MEMO.md 2026-08-02 M3.5。**S4b-2（2026-08-03）**：无 init let 语义明确——顶层无 init（含带注解）→ 编译期报错「全局必须带初始值」；函数内 `let x: T;`（带注解）→ 类型零值默认（str→""、i64→0…）；`let x;`（无注解）→ 编译期报错） |

### 1.5 内存与所有权

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 栈分配 | 局部变量栈上分配 | ✅ 已完成 |
| 堆分配 | 动态分配 | ✅ 已完成 |
| 引用 / 借用 | `&` / `&mut` | ✅ 已完成 |
| 移动语义 | `move x` | ✅ 已完成 |
| 借用检查 | 编译期验证引用安全 | ✅ 已完成 |
| 跨语句借用健全性 (B6) | 跨语句的引用释放与 Deref 别名检查 | ⚠️ 部分（T19 定理 B6 披露 unsoundness：`hir/lower/lower_stmt.rs:50/57/73` 与 `lower_expr.rs:701/717` 调用 `scope.release_borrows` 在语句边界过早释放，Deref 路径不检查别名；存在 Tenth 接受但违反别名规则的程序。详见 AUDIT.md §11.1） |
| 生命周期 | 引用有效范围 | ⚠️ 部分/脚手架（**M1.4 决策（2026-08-01）**：显式 `&'a T` 标注可解析透传，借用检查为语句粒度保守近似（T19），不消费 lifetime——设计取舍，见「生命周期标注」条目） |
| GC 垃圾回收 | 自动内存回收 | ❌ 未实现（用 arena + RC 代替） |
| Arena 分配器 | 区域分配 | ✅ 已完成 |
| 内存限制 | 上限保护 | ✅ 已完成 |
| 智能指针 | `Box`/`Rc`/`Arc` | ✅ 已实现（2026-08-01 M1.1 全链路接通：`Type` HeapBox/SharedBox/AtomicBox/Pin；`Value` HeapBox/SharedBox/Pin；`Box::new`/`Rc::new`/`Arc::new`/`Pin::new` native 双侧注册（runtime/natives.rs 问题29 段 + interpreter/natives.rs:2563）；类型注解 `Box<T>`/`Rc<T>`/`Arc<T>` 映射（hir/types.rs `from_annotation`，恰 4 名 + 恰 1 实参，`Lowerer::annotation_type` has_struct 防护防与用户 `struct Box` 冲突）；方法 `deref()`/`deref_mut()`（返回内部值）/`clone()`（Box/Pin 深拷贝重包装、Rc 用 Rc::clone 共享）双侧（VM call_method_priv + 解释器 eval_smart_ptr_method）；native 白名单 + resolve_builtin 返回类型；smart_ptr_test.rs 28 项覆盖解释器/VM/JIT 三路径） |
| 弱引用 | `Weak<T>` | ✅ 已实现（2026-08-02 M3.4 全链路接通：`Type::Weak(Box<Type>)`（hir/types.rs，注解 `Weak<T>` 映射 + Display + has_struct 防护）；`Value::Weak(Weak<RefCell<Value>>)`（runtime/value.rs，存 `std::rc::Weak` 才能 upgrade，type_of/as_float/as_int/is_truthy/Display 全处理，悬垂时 type_of→Unknown、Display→`Weak<dangling>`）；native 双侧注册（runtime/natives.rs + interpreter/natives.rs）——`Weak::new(rc)`（从 Rc/Arc 值创建，非 Rc 值报错）、`weak_upgrade(w)`（成功 `Option::Some(Rc 值)`、原 Rc 释放后 `Option::None`）、`weak_strong_count`/`weak_weak_count`（i64 辅助）；HIR 白名单三处同步（lower_expr.rs/closures.rs/types.rs + 解释器 eval.rs）；resolve_builtin 返回类型（`Weak::new`→`Weak<T>`、`weak_upgrade`→`Option<Rc<T>>`）；方法 `clone()`（Weak::clone 共享弱句柄）双侧（VM call_method_priv + 解释器 eval_smart_ptr_method）；prelude.th 文档；weak_test.rs 20 项覆盖解释器/VM/JIT 三路径（upgrade 成功/临时 Rc 释放后 None/函数边界 drop 后 None/弱引用不阻止释放/计数/注解/非 Rc 报错/Display）。**S4b-1（2026-08-03）**：VM Let 全局镜像消除（仅顶层写全局，函数内 let 只存 local）——函数边界 `let r = Rc::new(42); Weak::new(r)` 返回后 Rc 正确释放、upgrade→None，VM=解释器一致（weak_test +2：函数边界释放 + 对拍）；残留分歧仅 VM 计数 native 受操作数栈残留影响不可靠（计数测试保留在解释器）） |
| Drop trait / RAII | 资源释放即析构 | ✅ 已完成（**2026-08-01 M2.5 修复**：`impl Drop for T { fn drop(self) }` 全链路可用——修复 `type_impls_drop` 不识别 `Type::TypeParam`（struct 字面量类型）导致 drop 永不触发的缺口；drop 调用改为直接调用 `__dyn_Drop_{Type}_drop` 函数（VM/解释器/JIT 均可执行）；块退出/函数返回/嵌套块正确触发；`move` 后变量不再 drop（防双重释放）；修复 Block 作用域恢复 bug（原恢复到块自身作用域，块内变量泄漏到块外，嵌套块变量被二次 drop）；缺 drop 方法 → 编译期 TypeError；drop_test.rs 8 项测试（HIR 结构性断言 + VM 运行时时序）；**遗留**：多变量 drop 顺序依赖 HashMap 迭代序（非严格 LIFO）；元组/数组/Box 内嵌 Drop 类型的 drop 仅解释器可用） |
| Copy trait | 值类型自动复制 | ✅ 已完成（**2026-08-01 M2.6 修复**：`is_copy_type` 补 `Type::TypeParam` 处理（struct 字面量类型）——自动派生（字段全 Copy）/显式 `impl Copy` 生效，`move p` 后原变量仍可用；非 Copy（含 Vec 字段）`move` 后编译期报「使用了已移动的值」；Copy 与 Drop 互斥（实现 Drop 的类型不自动派生 Copy，防双重释放）；`move x` 对 Copy 类型解包为值拷贝（修复解释器运行时无条件标 Moved 的误判）；顺带修复 VM 纯路径 `move` 接收端 bug（bytecode 原只发射 no-op MoveOp，非 Copy `let y = move x` 在纯 VM 拿到垃圾值，改为求值 inner）；`let q = p` 为隐式克隆语义（不移动，手册 §10.2）；`str` 字段视为 Copy（深拷贝安全，设计取舍）；copy_trait_test.rs 9 项测试） |
| 手动内存管理 | `alloc`/`free` | ❌ 未实现（项目策略排除） |
| 内存对齐 | `#[repr(align)]` | ❌ 未实现（远期） |
| Pin | 自引用安全 | ✅ 已实现（2026-08-01 M1.1 全链路接通：`Type::Pin`/`Value::Pin`；`Pin::new` native 双侧注册；类型注解 `Pin<T>` 映射（hir/types.rs `from_annotation` + has_struct 防护）；方法 `deref()`/`deref_mut()`/`clone()` 双侧（VM call_method_priv + 解释器 eval_smart_ptr_method）；smart_ptr_test.rs 覆盖 Pin 解释器/VM/JIT 三路径） |

---

## 二、编译器与工具链

### 2.1 编译管线

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| Lexer | 词法分析 | ✅ 已完成 |
| Parser | 语法分析 | ✅ 已完成 |
| AST | 抽象语法树 | ✅ 已完成 |
| HIR | 高层中间表示 | ✅ 已完成 |
| MIR / IR | 中层中间表示 | ❌ 未实现 |
| LIR | 低层中间表示 | ❌ 未实现 |
| 类型推断 | HIR 阶段 | ✅ 已完成 |
| 借用检查 | HIR 阶段 | ✅ 已完成 |
| 常量折叠 | 编译期常量计算 | ❌ 未实现 |
| 死代码消除 | 移除不可达代码 | ❌ 未实现 |
| 内联优化 | 函数内联展开 | ✅ 已实现（**2026-08-03 M2-A2**：JIT 调用点小函数内联——`compile/jit/translator.rs` `Call(i)` 处对被调用 chunk 做白名单分析（体 ≤16 条指令、仅标量/局部/简单算术与跳转、无递归/字符串/全局/复杂 op），满足则把 body 内联到调用方机器码（不 emit call），Ret→写 out+跳 cont；不满足静默回退 A1 直接调用（不报错、行为零变化）。配合**标量专用化快路径**（must 分析 + 伴随槽 + 原生算术/比较/条件，I32/F64/Bool），loop 1e7 488→66ms（7.4×）。详见 MEMO 2026-08-03 M2-A2 条目 |
| 循环优化 | 展开向量化等 | ❌ 未实现 |
| 尾调用优化 | TCO | ❌ 未实现 |
| 编译期求值 | `const fn` | ❌ 未实现 |
| 增量编译 | 只重编译变更部分 | ❌ 未实现 |
| 并行编译 | 多线程编译 | ❌ 未实现 |
| 编译缓存 | 缓存中间结果 | ❌ 未实现 |
| tenthc 错误恢复 (panic mode) | 自举编译器解析错误后续接能力 | ✅ 已实现（2026-07-02 补齐，`tenthc/parser/parser.th` 新增 is_sync_token/synchronize/record_error_and_recover/is_expr_start_token，Parser 结构加 errors 字段，parse_program 主循环对无法识别的顶层 token 应用 panic-mode 恢复；与 Rust 侧 `parser.rs` SYNC_TOKENS 对齐；error_recovery_test 7/7 通过。详见 AUDIT.md §11.2 AUDIT-11.2.2） |
| tenthc expect_gt / 嵌套泛型 `>>` | 自举编译器对嵌套泛型右尖括号的分词 | ✅ 已实现（2026-07-02 补齐，`tenthc/parser/parser.th` 新增 expect_gt 函数（遇 Gt consume / 遇 Shr consume 后插入合成 Gt，通过重建 tokens 数组实现 Vec::insert 语义），替换 parse_postfix 与 parse_generic_params 2 处闭合调用点；与 Rust 侧 `parser.rs:54-86` expect_gt 对齐。详见 AUDIT.md §11.2 AUDIT-11.2.3） |
| tenthc shape 检查 | 自举编译器编译期张量 shape 推断与不兼容报错 | ✅ 已实现（2026-07-02 补齐，`tenthc/hir/hir.th` HirType 新增 dim_count/dim0/dim1 三字段（0=未知跳过/1=1D/2=2D），`tenthc/hir/lower.th` 新增 8 个 shape 辅助函数（add_tensor_type、get_tensor_dim_count/dim0/dim1、check_matmul_shape、check_binary_broadcast、dims_count_from_args/dims_d0_from_args/dims_d1_from_args）与 3 处调用点检查（binary/call/method_call）；最小子集：matmul 2D 内侧 K 检查、二元广播（NumPy 从右对齐）、zeros/ones/rand/randn 字面量 shape 推断、transpose 2D 维度互换；跳过 let 注解/分支兼容/返回值合并（需 Tensor 注解解析，tenthc 不支持）；错误处理非致命（push 到 p.errors）；与 Rust 侧 `hir/lower/types.rs` check_and_merge_tensor_shape/check_binary_shape_compat/check_method_shape 对齐。详见 AUDIT.md §11.2 AUDIT-11.2.1） |
| tenthc for-in 循环解析 | 自举编译器对 `for x in iter { ... }` 语句的解析/HIR/WASM 全链路支持 | ✅ 已实现（2026-07-06 复核确认，AUDIT.md §7.3 #9 原登记"无 for 循环解析"为陈旧失效。全链路实现：`tenthc/parser/parser.th:1100-1132` `parse_stmt` 中 `disc==12` 分支解析 for-in 语法 → `tenthc/hir/lower.th:1258-1279` `kind=="for"` 分支 lower 为 `disc=4` HIR 节点 → `tenthc/compile/wasm.th:1404+` `disc==4` 分支 WASM codegen。tenthc 自举源码本身大量使用 for 循环（parser.th 中 13+ 处），自举路径 B/C 必须能编译 for 循环——功能可用的铁证。原引用行号 parser.th:631 实为 generic_call 创建 ident 节点逻辑，与 for 循环无关。详见 AUDIT.md §7.3 #9） |
| tenthc inclusive range (`..=`) lexer 解析 | 自举编译器对 inclusive range `..=` 的分词 | ✅ 已实现（2026-07-06 修复，`tenthc/lexer/lexer.th:180-190` 在 `if next == "."` 分支内 advance 后再 peek 一次检查 `=`，匹配则 advance 并返回 `DotDotEq`，与 Rust 侧 `lexer.rs` 对齐。验证：自举 + parity 129/129 + selfhost_frontend 4/4 + error_recovery 7/7；新增 `tenthc_dotdot_eq_test.rs` 3 项测试。详见 AUDIT.md §11.4 AUDIT-11.4.4） |
| tenthc 泛型 Tensor 类型注解 `Tensor[T, ..]` | 自举编译器对复合 Tensor 类型注解的解析/HIR/类型推断全链路支持 | ✅ 已实现（2026-07-06 f32/f64 parity 路线图阶段 7 完成，`tenthc/hir/hir.th` HirType 新增 `dtype_ty: i64` 字段（0=用 sub 字段，非 0=指向 type_nodes 索引，承载 TypeParam dtype）；`tenthc/parser/parser.th` 新增 `parse_type_annotation(p, term1, term2, term3)` 函数带方括号深度跟踪的复合类型收集器（产出 `"Tensor[T,..]"` 形式），改造 7 处类型注解收集点；`tenthc/hir/lower.th` 新增 `parse_tensor_type`/`add_tensor_type_param`/`get_tensor_dtype_ty`/`propagate_tensor_dtype` 四个 helper，改造 `parse_type`/`substitute_type`/`is_type_f64`/`is_type_f32`/`is_type_str`，7 处 `add_tensor_type` 调用改为 `propagate_tensor_dtype`（binop/matmul/bmm/transpose）；`tenth/src/compile/bridge.rs` `parse_type_annotation` 加复合类型识别 + 新增 `parse_dim_specs`；与 Rust 侧 `hir/types.rs` `Type::Tensor.dtype: Box<Type>` 对齐。**关键约束**：tenthc 仅暴露 str_eq/str_at/str_slice/str_len/str_cmp/str_int/str_add 7 个字符串 host 函数，但 Rust 母编译器 wasm.rs Call 分派对 str_eq/str_add/str_int 缺函数调用分支（仅 == 操作符路径可用），parse_tensor_type 用 == / != 操作符做字符串比较规避。**性质**：能力扩展（tenthc 自举源码不使用此注解，阶段 3 标准库泛型化后双侧能力不对称），非修复 f32/f64 不对称点。验证：自举 + selfhost_frontend 4/4 + parity 129/129 + shape_check_compile 74/74 + 新增 tenthc_generic_tensor_test 5/5；0 回归。详见 MEMO.md 2026-07-06 阶段 7 条目） |
| tenthc Vec 迭代 For codegen | 自举编译器 WASM 后端对 `for x in [Vec]` 形式的迭代 codegen | ⚠️ 部分实现（2026-07-06 部分修复，`tenthc/compile/wasm.th:1471-1546` For 语句新增 ArrayLiteral (disc=22) 分支，复用现有 `vec_new`/`vec_len`/`vec_push`/`vec_get` host imports 实现 Vec 迭代 codegen；后续修复：删除 `wasm.th:1492` 误加的 `wasm_drop`（type/drop 不匹配导致 wasmi 验证失败）。验证：自举 + parity 129/129 + selfhost_frontend 4/4。**遗留**：测试 host function stub 是空实现（`vec_len` 永远返回 0），2 项非空 Vec 迭代测试 `#[ignore]`——tenthc 编译器层面 codegen 正确，仅测试基础设施局限；ArrayLiteral 表达式本身 codegen 未改。详见 AUDIT.md §11.4 AUDIT-11.4.5） |
| JIT 栈深度溢出检测 | JIT translator 超过 `MAX_STACK_DEPTH` 时的安全 fallback | ✅ 已实现（2026-07-06 修复，`compile/jit/translator.rs` 新增 `bump_sp()` 方法在 push 前检查 sp 上限，28 处 push 操作改为 `bump_sp()?`；超限返回 `Err` 触发既有 JIT→VM fallback（`mod.rs:62-65`），不再静默截断。验证：jit_test 10/10 + 全量 0 回归。详见 AUDIT.md §11.4 AUDIT-11.4.2。**2026-08-03 M2-A1 补注**：`MAX_STACK_DEPTH` 256→64（每 JIT 帧虚拟栈槽 28KB→7KB，`size_of::<Value>()`=112B）——JIT-to-JIT 直接调用后递归在原生栈展开（不再逃逸解释器堆栈），28KB/帧 + Cranelift 无 stack probe 会跨 guard page 以 0xC0000005 崩溃；需更深虚拟栈的罕见函数经 bump_sp 优雅回退解释器） |
| HIR 闭包自由变量收集复杂度 | `free_vars_in` 等函数的算法复杂度 | ✅ 已优化（2026-07-06 修复，`hir/lower/closures.rs` `free_vars_in`/`collect_free_vars`/`collect_free_vars_stmt` 改用 `HashSet<String>`，4 处 `push` 改 `insert`；API 兼容（仍返回 `Vec<String>` + `sort`），复杂度 O(n²)→O(n)，纯优化无语义影响。验证：autodiff 52/52 + 全量 0 回归。详见 AUDIT.md §11.4 AUDIT-11.4.1） |
| 编译期跨函数 shape 组合推导（函子化） | 跨函数 shape 组合性自动涌现，无需全局收集器 | ✅ 已完成（2026-07-31 b76c1a4：删除 `Lowerer.current_fn_return_shapes` 全局可变收集器，`types.rs` 新增纯函数 `collect_return_tensor_dims`（函子 Φ 的构造性定义）；嵌套 return/三层 `f(g(h()))`/泛型实例化/闭包隔离全部由 IR 结构递归自动涌现；附带修复闭包内 return 不再泄漏进外围函数签名。验证：shape_check_compile_test 83 passed（新增 6 项函子测试，重构前 77）/ cross_fn_shape_test 21 / type_inference_test 40 / stdlib_test 141；自举 [OK]。已知断点：符号维度 `Dim::Symbol` 不参与 unify（如实记录）；**前向引用顺序无关性已由断点 4.2 实施（2026-08-03，见下条）**。详见 docs/程序代数架构设计.md §2 与 MEMO.md 2026-07-31/2026-08-03 护城河条目） |
| 前向引用 shape 不动点（断点 4.2） | 调用后定义函数的调用点编译期拿到 body 精化 shape（顺序无关） | ✅ 已完成（2026-08-03 编译器部，收敛性论证见 docs/shape-check-roadmap/前向引用断点收敛性论证.md 2026-08-02）：`lower_program` 尾部 `run_forward_ref_shape_fixpoint`——同文件常规函数**无环全量不动点 + 轮数上限 2n+1 兜底回退**（递归震荡超限回退首轮状态，不引入误报）；纯 shape 重推导复用 resolve_method_type / infer_binary_type / collect_return_tensor_dims / join_return_dims / check_and_merge_tensor_shape + let/赋值变量类型绑定传播 + 断点 4.1 实参代换；`join_return_dims` 改 **n 元共识语义**消除折叠顺序依赖；收敛后对精化函数体重跑 matmul/二元/分支 shape 校验，新拦截「注解 vs 前向引用 body」冲突与 matmul 内侧不匹配（main 先于 make 时 `[3,4]@[5,6]` 编译期拦截）。验证：forward_ref_shape_test **26 项** + 既有 shape 套件全绿 + 自举 [OK]。**tenthc 无需同步**（tenthc 无同款跨函数返回 shape 精化机制）。详见 MEMO.md 2026-08-03 条目） |
| lossy 污点传播（旁路分析） | 编译期"算错可能性"格分析（方案 C，Type 零侵入） | ✅ 已完成（2026-07-31 0a426ad+0887eb5：`hir/lower/taint.rs` 新增——Lossiness 格 = 链 Exact < PossibleOverflow < PossibleNaN < Lossy，join=max；var_taint+expr_taint 结构递归；跨函数传播 `fn_call_taint` 实参敏感 memo（memo_call 键=(name,args_taints)），`f(g(h()))` 三层嵌套证明 Phi_lossy 函子成立；静态 Lossy 来源：隐式标量→张量 dtype 收缩（标量 F32/F64 ×/±/÷ Tensor[F16/BF16/F32]）；使用点（println/eprintln/eprint/to_string/format/write_file/write_bytes/save_weights）报错提示 `lossy(...)`；防误报底线：PossibleOverflow/NaN 只传播不报、变量除数不 speculate、递归/互递归保守 Exact（未做不动点）、方法调用不查 callee 返回污点、泛型实例化 body 不参与（均登记 AUDIT-11.4.20）；`lossy_test.rs` 25 项 + `lossy_lattice_test.rs` 9 项。详见 docs/程序代数架构设计.md §3.3 与 MEMO.md 2026-07-31 护城河系列条目） |
| 编译期零除数检测 | 字面量零除数编译期报错 | ✅ 已完成（2026-07-31 lossy M1：`types.rs` 新增 `is_statically_zero` + `check_binary_static_divzero`——Div/Mod + 字面量零（含 -0）编译期 TypeError，此前 `1.0/0.0` 静默 inf；lower_expr.rs Binary 两个默认分支（无 trait 重载）接入；方案 C（lowering 旁路分析）零侵入 Type/HIR/bytecode/wasm；`lossy_lattice_test.rs` 9 项全绿） |
| tenthc Lexer 全量同步 | 自举编译器 TokenKind 与 Rust 母编译器对齐 | ✅ 已实现（2026-07-12 Problem 21 批次1：TokenKind 67→82 变体，新增 11 行为 token + 10 关键字；IntLiteral 加 dtype 字段（ival 复用存 dtype，0=F64/1=F32/2=F16/3=BF16）；修复 `.= ` 词法 bug（DotAssign 前瞻判断修正）；tenthc/lexer/token.th + lexer.th 双侧同步） |
| tenthc Parser 全量同步 | 自举编译器 AST 节点与 Rust 母编译器对齐 | ✅ 已实现（2026-07-12 Problem 21 批次2：5 ExprKind 新增（Tuple/StructLiteral/InterpolatedString/ClosureExpr/IndexExpr 扩展）+ 1 Pattern（RangePattern）+ 4 StmtKind（ForIn/While/Break/Continue）+ 3 TypeAnnotation（NamedType/GenericType/RefType）；lower.th dtype 透传；locals 容量 256→512 修复） |
| tenthc HIR 全量同步 | 自举编译器 HIR Type 变体与 Rust 母编译器对齐 | ✅ 已实现（2026-07-12 Problem 21 批次3：全 7 Type 变体补齐（Never/Range/Tuple/Generic/Ref/MutRef/Future），tenthc/hir/hir.th + lower.th 类型系统对齐。4 项 P0 能力：(1) generic instantiation——泛型函数实例化 mangled name 生成；(2) 函数返回 Unit——Type::Base(Unit) 正确推导；(3) match 穷尽性检查——编译期穷尽性验证；(4) borrow_holders——借用持有者追踪） |
| tenthc 编译期反向 shape 验证 | 自举编译器 backward_shape + 内存/FLOPs 预估 | ✅ 已实现（2026-07-12 Problem 21 批次4：backward_shape Phase1 实现 11 算子编译期反向 shape（MatMul/Add/Sub/Mul/Div/Neg/Transpose/Reshape/Sum/ReLU/Softmax）；Phase2 跨算子梯度 shape 传播；内存预估（estimate_memory）+ FLOPs 预估（estimate_flops）+ unbroadcast_feasible 检查。新增 autodiff_backward_shape_test 49 用例 + memory_estimate_test 35 用例。**bmm FLOPs 预估已激活**（2026-07-25 AUDIT #12 修复：`tenthc/hir/hir.th:39` 新增 `dim2: i64` 字段，lower.th 激活 6 处 no-op——`get_tensor_dim2`/`check_bmm_shape`/`static_numel`(dc==3)/`fmt_dims`(dc==3)/`emit_bmm_flop_estimate`(B*M*K*N*2 分段乘法防溢出)/bmm 分支 shape 推断 (B,M,N)；`parse_tensor_type` 放宽至解析 1-3 个 dim 字面量；构造函数 `add_tensor_type`/`add_tensor_type_param`/`propagate_tensor_dtype` 加 `dim2` 参数。bmm FLOPs 预估现与 Rust 母编译器对齐（`types.rs:1094-1129`），新增 `test_bmm_flop_estimate_parity` 验证双侧 warning 一致。详见 AUDIT.md §11.4 AUDIT-11.4.7） |
| tenthc WASM host 模块名统一 | 自举编译器 WASM import 段 host 模块名与 Rust 侧对齐 | ✅ 已实现（2026-07-12 Problem 21 批次5：host 模块名统一 env→host（wasm.th + wasm.rs 双侧对齐）；bridge.rs handlers 补齐（新增 Type 变体 + StmtKind + ExprKind 的 bridge 转换 handlers）；StructLiteral 三路径类型感知（Rust 前端 + tenthc 前端 + WASM 后端 struct literal 类型推导对齐）；修复 tenthc_for_loop_test） |

### 2.2 执行后端

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 树遍历解释器 | AST 直接执行 | ✅ 已完成 |
| 字节码 VM | 栈式虚拟机 | ✅ 已完成（45 指令） |
| JIT 编译 | Cranelift 热点编译 | ✅ 默认启用，保守策略（autodiff/复杂操作回退 VM）。**2026-08-03 M2-A1**：JIT-to-JIT **直接调用**——translator 对 `Call`/`CallN` 的用户函数目标生成直接调用（快路径 Cranelift `call_indirect` 已编译机器码，函数指针表 `JitContext.table` 按 chunk_idx 索引；首次遇未编译函数走 `host_jit_call` trampoline 编译注册）；递归不再逃逸解释器（fib(28) 180ms→**28ms**，~6.4×）；`MethodCall` 目标为 native 保持 host_method_call fallback；`TailCall` 保持 fallback（D2）；未注册/编译失败函数保持 host_call fallback（正确性优先）。**2026-08-03 M2-A2**：调用点小函数**内联**（≤16 指令白名单，失败静默回退 A1）+ **标量专用化快路径**（must 分析 + 伴随槽 + 原生算术/错误检查，I32/F64/Bool）——loop 1e7 488→**66ms（7.4×）**、loop-inc 226→34ms、loop-mod 495→66ms；fib(28) 26→28ms（+8% 轻微回归，标量路径物化开销，可后续优化）；`jit_test` 41/41、`jit_error_line_test` 9/9、自举 [OK]。**2026-08-03 M2-A3**：opcode 覆盖扩展——`MakeTuple/IsTuple/TupleGet/Try/IsStruct/Spawn` 从整函数 fallback → **JIT 可编译**（6 个新 hostcall，语义与 VM opcode 46/48/49/50/51/52 对齐）；`TailCall/TailCallClosure` 按 D2 保守走 host_call + 立即返回（不再整函数 fallback，不激进 TCO 帧复用）；`Await/Yield`（调度器挂起无法 JIT 序列化）+ 引用/letrec cell opcodes 保守保持 fallback；**顺带修复潜伏静默错值 bug**——JIT `StoreGlobal` 推回结果与 VM 消费语义不一致（被「含 TupleGet 函数整体 fallback」掩盖，A3 暴露：顶层 tuple 解构后续 TupleGet 读到残留错位值 → stdlib_smoke optim/adam/adagrad/rmsprop 回归），已对齐 VM（消费不推回）；`jit_test` **62/62**（+21）、`jit_error_line` 12、stdlib_smoke **59**、自举 [OK]、fib ~30ms/loop ~63ms 与 A2 持平。**2026-08-03 M2-A5**：VM-vs-JIT **一致性扩展**——新增 `jit_consistency_test` **43 项**系统性对拍（直接调用/内联/标量专用化/Try/Tuple/Struct/Spawn/TailCall 各类 ≥3，含错误路径与跨路径行号一致）；修复 2 个一致性缺口（① VM R2 快路径溢出错误缺行号——execute.rs 7 处补 with_line，对齐 JIT；② JIT 编译期 panic 逃逸——context.rs 把整个编译链路含 finalize 低化纳入 catch_unwind，tuple+guard 的 Cranelift TryFromIntError 不再逃逸进程）；**热分层分析**——默认 JIT 路径直接调用可达函数首次调用即编译（阈值=1），无需调用计数，分层后置；**基准固化（D4）**——新增 `bench_gate_test` 2 项（默认 #[ignore]，release + `--ignored` 跑 perf_bench3 镜像，fib<100ms/loop<200ms/matmul<20ms 阈值带 3× 裕量）；实测 fib 中位数 30ms / loop 64ms / matmul 0-1ms。**2026-08-03 M2.5-A6**：**入参标量 ABI（scalar-specialized call ABI）**——纯 i64 标量函数特化入口 `(vm, i64 x8) -> i64`（参数/返回走寄存器，跳过 Value 装箱链）。签名推导：BytecodeCompiler 编译期从 HIR `FuncDecl` 推导 `Chunk.scalar_sig`（`ChunkSig`，v1 仅显式 `i64` 注解、≤8 参、排除变参/默认参）；特化入口（`get_or_compile_spec`，与通用入口双轨惰性编译）参数种类**预置 seed**（i64→I32 标量）→ 函数体参数走 A2 原生路径；调用点 `try_spec_call`（内联之后）实参全为 I32 标量槽 → 特化指针表 `call_indirect`（慢路径 `host_jit_call_spec` 编译注册）；错误经调用后 `host_check_error`（B2，不失效栈标量）+ 按本函数返回约定中止；分析期预测 call_spec_results 防发射期漂移（不一致 → 整函数回退解释器，静默错值红线）。**fib(28) 29ms→9ms（中位数，~3.2×，M2 完全达成线 <10ms 达成）**；loop 65ms 持平无回归；新增 `jit_spec_abi_test` **17 项**（递归 fib/多参/互递归/混合签名 f64 走通用/str 返回不特化/错误带行号/双入口/函数值间接调用/含 hostcall/native 参数回退/链式特化/参数跨分支）；顺带修复 `find_leaders` Ret leader 偏移（结束→起始）——TailCall 后尾 Ret 不再双重 return（此前被整函数 fallback 掩盖）。**遗留**：f64 特化 v2（`ScalarAbiKind::F64` 已预留）、`Int` 别名不特化、chunk_idx 切换保留、TailCall 保守走通用、>8 参不特化。详见 MEMO.md 2026-08-03 M2.5-A6 条目。**2026-08-04 M2.6-P1（纯标量固定开销消除）**：① **特化调用点跳过 `current_chunk_idx` 保存/切换/恢复**——`compute_skip_chunk_ctx` 判定（分析/发射共用：chunk 有 `scalar_sig` + 体仅安全白名单 op + 每个 Call/CallN 站点内联或预测特化）为纯标量的函数，快路径省 3 次内存操作；含 PushStr/MethodCall/TailCall（TCO）/张量等 → 不跳过（保持切换）。② **`host_check_error` 内联化**——`Vm` 新增 `jit_error_flag` 字段（`set_last_error`/`set_jit_error` 置 1、`take_last_error` 清 0，与 `has_last_error()` 恒等），特化调用点直接 load 该标志 + 比较（省一次函数调用，B2 红线保留）。**基准**：fib(28) **9ms → 8ms**（同机 5 次中位数；每次调用 ~10.8→9.6ns），loop 65ms 持平；目标 ~6ms 未完全达成（剩余开销：特化指针表空值检查 + 8 寄存器 ABI + 结果合并槽内存往返 + 错误标志检查，~5ns/call，P2 候选）。对拍全绿（新增 `jit_skip_chunk_ctx_test` 10 项：fib/even-odd/add3 跳过正确性、除零/溢出跳过切换后行号正确、PushStr/MethodCall/TailCall 不跳过回归）、自举 [OK]。详见 MEMO.md 2026-08-04 M2.6-P1 条目。**2026-08-04 M2.6-P2**：**JIT 静默错值系统性审计**——新增 `jit_silent_audit_test` **34 项**（D1-D5 审计维度 + 组合矩阵 + 3 对抗形态；进程内三路径对拍 + 路径化覆盖断言 `is_compiled`/`is_spec_compiled` + 子进程字节级 stdout/exit 对拍）；**发现并修复 AUDIT-11.4.36**——分析/发射 binop 专用化集合漂移（分析对 `Mul(I32,I32)`/`Mod(F64,F64)` 预测标量，发射端 I32 Mul / F64 Mod 不专用化 → spec 调用实参 `f(k*10)` 缺标量槽 → 防御性 Err → 整函数回退 VM）；修复：`transfer` 按发射端专用化集合逐 opcode 对齐（Mul 仅 F64→F64、Mod 仅 I32→I32）。对拍全绿（9 套件 + 新 34 项）、自举 [OK]、fib(28) 8-10ms / loop 59-61ms 无倒退。详见 MEMO.md 2026-08-04 M2.6-P2 条目。**2026-08-04 M2.6-P3（f64 特化 + `Int` 纳入 + >8 参评估）**：A6 特化从「仅显式 i64」扩展——① **f64 特化**：`ChunkSig::from_hir` 推导 `Base(F64)` → F64（参数/返回混合签名 [I64.., F64..]）；机器 ABI 保持 `(vm, i64×8) -> i64` **位打包**（f64 经 Cranelift `bitcast` 塞 i64 寄存器，快/慢路径 ABI 统一）；调用点 `try_spec_call` 按签名种类预读实参槽（F64 槽 → bitcast）、合并槽按 ret_kind（F64→F64 槽）、返回 bitcast；特化入口 `init_spec_args` F64 参数 bitcast + `host_make_float`（F64 签名 helper）；Rust 慢路径 `jit_call_chunk_spec` 按 param_kinds 打包（F64 参收 Float→bits）+ 按 ret_kind 解包（F64→`Value::Float(from_bits)`）；新增 `host_value_to_f64`（严格解包）。② **`Int` 别名纳入**：`TypeParam("Int")` 语义 = `i64`（同为 `Value::Int`/I32 槽、body 编译类型无关）→ 推导为 I64 kind，扩大 i64 类覆盖（调用点 gate 保证非 Int 实参走通用）。③ **>8 参评估：不做**（固定 8 槽 ABI + Rust fn 指针类型约束，收益低，如实记录）。**静默错值修复**：VM/解释器浮点 `Eq`/`Neq` 为 epsilon 比较 `(x-y).abs()<1e-10`，JIT `emit_native_cmp` F64 此前用精确 `FloatCC::Equal/NotEqual`（既有潜伏分歧）→ 对齐 epsilon（Neq 用 select 翻转，NaN 语义与 VM 一致）。新增 `jit_spec_f64_test` **17 项**（混合递归/双递归/多参/循环/返回消费/嵌套链/除零→inf/NaN 三路径逐位对照/epsilon 回归/有序比较/Int 纳入/混合签名/f32 不特化/str 返回不特化/慢路径首调）；既有 10 套件全绿（含因纳入而更新的断言：silent_audit D5 general 类别与 d3/d4 general 用例改 `i32` 注解、jit_spec_abi 混合签名改断言 f64 特化、consistency 热函数用例改 `i32`）；bench_gate 2/2、自举 [OK]。**基准**：fp_fib(26) 10-11ms→**3-4ms（~3×）**、fp_poly(1e6) 39-43ms→**6-7ms（~6×）**、fp_multi 持平（可内联）、fib(28) **8ms 不倒退**。详见 MEMO.md 2026-08-04 M2.6-P3 条目。**2026-08-04 M2.6-P4（调用路径扩展：CallClosure JIT-to-JIT + MethodCall/TailCall 保守评估）**：① **CallClosure JIT-to-JIT**——新增 `host_jit_call_indirect`（A1 慢路径等价物：`call_value` 语义逐一对齐——letrec Shared 解包 / FnRef 按名 / 捕获追加 / native 直名 / 非可调用带行号报错，但对用户闭包 chunk → `Vm::jit_call_chunk` → **闭包体直接执行 JIT 机器码**，不再逃逸解释器；失败完整回退 `call_value`）。闭包 opcode 不带名字（callee 是运行期值）→ 无 A1 机器码 call_indirect 快路径，保持 hostcall trampoline。② **MethodCall 不做**——目标全为 native（`call_method_priv` 按接收者运行期类型分派，无用户函数 chunk），JIT-to-JIT 不适用。③ **TailCall 不做**——JIT-to-JIT 使深尾递归原生栈溢出崩溃（A1 教训 7KB/帧）；当前保守 host_call→解释器 TCO 迭代帧池安全（实测 200000 层 VM/JIT 正确不崩溃）。基准：闭包体含循环 5000 次调用 **75-84→12-14ms（~6×）**、薄闭包 1e6 次 356→~280ms、fib(28) 7-8ms / fp_fib 3ms 不倒退；新增 `jit_call_path_test` 17 项三路径对拍 + 覆盖断言。详见 MEMO.md 2026-08-04 M2.6-P4 条目。**2026-08-04 M2.6-P5（P 系列收尾：遗留难题登记）**：P 系列遗留 **10 项**登记（详见 AUDIT-11.4.38）：① Await/Yield JIT 挂起不支持（整函数 fallback）；② 深递归栈（解释器树遍 >1e4 层原生栈溢出 / JIT 非尾深递归 ~7KB/帧上限 / AUDIT-11.4.19 默认 2MB 线程栈基线；VM TailCall depth 100000 出 49965 经核算为数学正确值非缺陷）；③ AUDIT-11.4.34 VM match guard 回退错乱待排期；④ WASM abs 类型系统 vs 运行时返回类型不一致；⑤ P1 剩余每次调用 ~5ns 开销；⑥ 递归闭包 MakeCell/BindSelfCapture 整函数 fallback；⑦ >8 参不特化；⑧ MethodCall 无用户 chunk 目标；⑨ TailCall 不特化（守护固化）；⑩ f32/Float32 不纳入特化。 |
| AOT 编译到原生码 | 编译为 `.exe`/ELF | ❌ 未实现 |
| WASM 编译 | 编译为 `.wasm` | ✅ 已完成 |
| GPU 编译 | CUDA kernel 生成 | ⚠️ 脚手架（无实际 kernel） |
| 跨编译 | 在平台 A 编译平台 B 的产物 | ❌ 未实现 |
| 链接器 | 静态/动态链接 | ❌ 未实现 |
| LLVM 后端 | 编译到 LLVM IR | ❌ 未实现 |
| Cranelift 后端 | 编译到原生码 | ⚠️ 依赖已引入但未使用 |
| TPU / NPU 后端 | AI 专用硬件 | ❌ 未实现 |

### 2.3 开发工具

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| REPL | 交互式环境 | ✅ 已完成 |
| LSP 服务器 | 语言服务协议 | ✅ 已完成（13 项功能：文档同步/diagnostics/hover/completion/definition/documentSymbol/references/rename/signatureHelp/foldingRange/semanticTokens/formatting） |
| 调试器 | 断点、单步、变量查看 | ⚠️ 部分实现（护城河 F：张量关系调试器 FormalExplain + 5 类错误分类 + 边级归因已实现，backward 失败时输出结构化根因分析；通用断点/单步调试未实现） |
| 性能分析器 | Profiler / flamegraph | ❌ 未实现 |
| 代码格式化 | `tenth fmt` | ✅ LSP 中有 formatting |
| Linter | 静态检查 / 代码风格 | ⚠️ LSP diagnostics 部分 |
| 文档生成 | 从注释生成 API 文档 | ❌ 未实现 |
| 语法高亮 | 编辑器插件 | ❌ 未实现（无 VSCode 扩展） |
| 代码片段 / Snippets | 编辑器模板 | ❌ 未实现 |
| 交互式教程 | Tour / Playground | ❌ 未实现 |
| 在线 Playground | Web 端运行 | ❌ 未实现 |
| 语法树可视化 | AST 图形化 | ❌ 未实现 |
| 字节码反汇编 | 查看 VM 指令 | ❌ 未实现 |
| WASM 调试 | WASM 模块检查 | ❌ 未实现 |

### 2.4 包管理与构建

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 包管理器 | tenthpm | ✅ 已完成（完整实现，10 个子命令：init/build/test/run/add/remove/list/clean/publish/install） |
| 包初始化 | `tenthpm init` | ✅ 已完成 |
| 添加依赖 | `tenthpm add` | ✅ 已完成 |
| 移除依赖 | `tenthpm remove` | ✅ 已完成 |
| 构建项目 | `tenthpm build` | ✅ 已完成 |
| 运行项目 | `tenthpm run` | ✅ 已完成 |
| 测试 | `tenthpm test` | ✅ 已完成 |
| 清理 | `tenthpm clean` | ✅ 已完成 |
| 发布 | `tenthpm publish` | ✅ 已完成 |
| 安装 | `tenthpm install` | ✅ 已完成 |
| 列出包 | `tenthpm list` | ✅ 已完成 |
| 包注册中心 | 中央仓库 | ❌ 未实现 |
| 版本锁定 | lockfile | ✅ 已完成（Tenth.lock 含 checksum） |
| 依赖解析 | semver 版本范围 | ❌ 未实现 |
| 工作空间 | monorepo 多包管理 | ❌ 未实现 |
| 构建脚本 | `build.rs` 等价物 | ❌ 未实现 |
| 条件编译 | `#[cfg]` 平台/特性条件 | ❌ 未实现 |
| Feature flags | `--features foo` | ❌ 未实现 |
| 增量构建 | 只重编译变更 | ❌ 未实现 |
| 交叉编译支持 | target 配置 | ❌ 未实现 |

---

## 三、运行时系统

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 内存分配器 | arena + 堆 | ✅ 已完成 |
| 内存限制 | 上限保护 | ✅ 已完成 |
| 垃圾回收 | 自动回收 | ❌ 未实现（RC + arena） |
| 线程 / 并发 | 多线程执行 | ❌ 未实现（有 `spawn` 关键字） |
| 协程 / 绿色线程 | 轻量并发 | ✅ 已完成（2026-07-08 Phase 2 VM 调度器实现协作式协程：`run_scheduler` 主循环 + `run_until_yield` 返回 `YieldReason`（Completed/Suspended/Yield）+ `ready_queue`/`suspended` 双队列 + 每任务独立 `Frame.operand_stack` + `task_id` 标识；spawn 立即求值包装为 Ready Future，await Pending 时挂起当前调用栈并加入 Future 等待列表，Future 就绪后任务回到 `ready_queue`；详见 MEMO.md 2026-07-08 feat 条目） |
| async runtime | 事件循环 | ⚠️ 部分实现（2026-07-08 Phase 2 VM 调度器：`run_scheduler` 主循环 + `AsyncIoState` thread_local + mpsc channel 轮询；调度器每轮调用 `ASYNC_IO.with(\|s\| s.poll())` 检查 I/O 完成并把已就绪的 Future 标记为 Ready；非完整 runtime——无工作窃取、无优先级、无取消语义；spawn 仍为 eager 包装，真正并发仅来自 async I/O 子线程；解释器路径拒绝 async） |
| 通道 / 消息传递 | `channel`/`mpsc` | ❌ 未实现 |
| 互斥锁 / 读写锁 | `Mutex`/`RwLock` | ❌ 未实现 |
| 原子操作 | `AtomicU64` 等 | ❌ 未实现 |
| 条件变量 | `Condvar` | ❌ 未实现 |
| 线程局部存储 | TLS | ❌ 未实现 |
| 信号处理 | SIGINT/SIGTERM | ❌ 未实现 |
| 异常处理运行时 | unwind | ❌ 未实现 |
| Panic / Abort | 致命错误处理 | ⚠️ 有错误传播但无 panic 机制 |
| 栈溢出检测 | stack guard | ❌ 未实现 |
| FFI 外部函数接口 | 调用 C/Rust 库 | ❌ 未实现 |
| 动态加载 | `dlopen` 加载 `.so`/`.dll` | ❌ 未实现 |
| 内联汇编 | `asm!` | ❌ 未实现 |
| SIMD 向量化 | `std::simd` | ❌ 未实现 |
| 异常安全 | panic 时资源安全 | ❌ 未实现 |
| 双重 native 注册一致性 (VM vs 解释器) | VM 与解释器对 native 函数注册集合的对齐 | ✅ 已对齐（2026-07-02 补齐 VM 17 项 + 解释器 3 项，save_weights/load_weights 经 FsSandbox 校验，三层白名单同步，详见 MEMO.md 2026-07-02 fix 条目；原 T37 披露见 AUDIT.md §11.3） |

---

## 四、标准库

### 4.1 核心类型与集合

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| Vec 动态数组 | `push`/`pop`/`get`/`len` | ✅ 已完成 |
| HashMap | `insert`/`get`/`len` | ✅ 已完成（2026-07-11：键类型扩展支持 str/int/bool/float，`map_key_to_string`/`vm_map_key_to_string` 双侧 helper；解释器 + VM 均覆盖） |
| String | `len`/`+`/切片 | ✅ 已完成 |
| Option | `Some`/`None` | ✅ 已完成 |
| Result | `Ok`/`Err` | ✅ 已完成 |
| 数组 / 定长数组 | `[T; N]` | ❌ 未实现 |
| HashSet | 去重集合 | ✅ 已完成（基础 8 函数 + 5 集合运算 from_array/set_union/intersection/difference/is_subset；set_union 用下划线前缀避保留字；hashset_test 12 项行为测试） |
| BTreeMap | 有序映射 | ❌ 未实现 |
| LinkedList | 双向链表 | ❌ 未实现 |
| VecDeque | 双端队列 | ❌ 未实现 |
| BinaryHeap | 优先队列 | ❌ 未实现 |
| 元组 | `(a, b, c)` | ✅ 已完成（2026-07-08 全管线实现，见 §1.2 元组类型条目） |
| Range 类型 | `Range`/`RangeInclusive` | ⚠️ 隐式支持 `0..10` |
| 迭代器 trait | `Iterator` trait | ❌ 未实现（有 iter 工具函数） |
| 字符 / Char | Unicode 字符 | ⚠️ 有类型无字面量 |
| 字节 / Bytes | 字节序列 | ❌ 未实现 |
| 大整数 | BigInt | ❌ 未实现 |
| 复数 | Complex | ❌ 未实现 |
| 小数 / Decimal | 精确小数 | ❌ 未实现 |

### 4.2 字符串与文本

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 字符串拼接 | `+` | ✅ 已完成（2026-08-02 L2.3a：补 VM `str_add` native 注册，f-string/`json::parse`/`toml::parse` 的字符串拼接 VM 路径可用；此前仅解释器路径可用） |
| 字符串切片 | `s[0..5]` | ✅ 已完成 |
| 字符串长度 | `.len()` | ✅ 已完成 |
| split / trim / replace | 字符串操作 | ⚠️ 部分在 std |
| 正则表达式 | regex 引擎 | ✅ 已完成（基于 Rust `regex = "1"` crate，6 个 native：`regex_compile`/`regex_match`/`regex_find`/`regex_find_all`/`regex_replace`/`regex_split`；handle table 模式与 `tcp_streams` 一致；VM + 解释器双侧注册；`std/regex.th` 6 个 wrapper；2026-07-08 实现） |
| Unicode 规范化 | NFC/NFD | ✅ 已完成（2026-07-13 B批；`unicode_nfc(s)`/`unicode_nfd(s)` native，基于 unicode-normalization crate；VM + 解释器双侧注册） |
| 字符串格式化 | `format!` / `sprintf` | ✅ 已完成（2026-07-13 B批；`format(template, ...args)` 支持位置占位符 `{}`/`{:>5}`/`{:.2f}`/`{0}` 与命名占位符 `{name}`/`{name:spec}`；`count_placeholders` 正确扫描模板；越界返回 `Result::Err`；2026-08-03 补整数进制说明符 `{:x}`/`{:X}`/`{:o}`/`{:b}`/`{:d}`（含宽度/补零/`#` 前缀，如 `{:08x}`/`{#x}`），VM+解释器双侧，AUDIT-11.4.26） |
| 字符串构建器 | StringBuilder | ✅ 已完成（2026-07-13 A批 `std/string/string_builder.th`） |
| 编码转换 | UTF-8/UTF-16/GBK | ✅ 已完成（2026-07-13 B批；8 个 native：`str_to_utf16`/`utf16_to_str`/`str_to_bytes`/`bytes_to_str`/`to_utf8`/`to_utf16`/`from_utf16`/`to_gbk`/`from_gbk`；基于 encoding_rs crate；VM + 解释器双侧注册） |
| 模板字符串 | `f"hello {name}"` | ✅ 已完成（2026-07-13 B批；lexer FString token + parser ExprKind::FString + HIR lowering 编译为 `format("hello {}", name)` 调用；tenthc 双侧同步 disc=89；VM + 解释器双路径；2026-08-03 升级为任意表达式插值 `{a+b}`/`{3.14}` + 格式说明符 `{3.14:.2f}`/`{n:>5}`（含 `{{`/`}}` 花括号转义），AUDIT-11.4.27；tenthc 仍仅标识符插值，记录遗留） |
| Base64 编解码 | | ✅ 已完成（2026-07-13 B批；`base64_encode(vec)`/`base64_decode(s)` native，基于 base64 crate；VM + 解释器双侧注册） |
| Hex 编解码 | | ✅ 已完成（2026-07-13 B批；`hex_encode(vec)`/`hex_decode(s)` native；VM + 解释器双侧注册） |
| URL 编解码 | | ✅ 已完成（2026-07-13 B批；`url_encode(s)`/`url_decode(s)` native，基于 percent-encoding crate；VM + 解释器双侧注册） |
| HTML 转义 | | ❌ 未实现 |
| CSV 解析 | | ❌ 未实现 |
| XML 解析 | | ❌ 未实现 |
| YAML 解析 | | ❌ 未实现 |
| Markdown 解析 | | ❌ 未实现 |

### 4.3 数学与数值

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 基础算术 | `+`/`-`/`*`/`/`/`%` | ✅ 已完成 |
| 数学函数 | `abs`/`sqrt`/`sin`/`cos`/`ln`/`exp`/`pow` | ✅ 已完成 |
| 三角函数 | `tan`/`asin`/`acos`/`atan`/`atan2` | ✅ 已完成 |
| 双曲函数 | `sinh`/`cosh`/`tanh` | ✅ 已完成 |
| 对数 | `log10`/`log2` | ✅ 已完成 |
| 取整 | `floor`/`ceil`/`round` | ✅ 已完成 |
| 数学常量 | `PI`/`E`/`TAU` 等 | ✅ 已完成 |
| 随机数 | `rand`/`randn`/`random_int` | ✅ 已完成（native 层；std::random 模块 `rand_int`/`rand_float`/`rand_range`/`choice`/`shuffle`/`rand_bool`/`sample`/`rand_seed` 亦已修复可用——L2.2 修 `choice` 旧语法，L2.3b 勘误注释：`choice` 返回随机索引、取元素用 `sample`；**M1-S4a（2026-08-03）`shuffle` 已实现真 Fisher-Yates 就地交换**（此前仅生成随机下标未写回），新增 `rand_seed(n)` 确定性种子（同 seed 可复现，VM=解释器一致）） |
| 统计函数 | 均值/方差/标准差 | ✅ 已完成（mean/median/variance/stddev/variance_sample/stddev_sample + min/max/range/sum/product/percentile，共 12 函数；test_stats.th 25+ 断言；2026-08-02 L2.3a：sum/product 解释器路径随 eval_binary Shared 解壳修复，双路径可用） |
| 线性代数 | 矩阵分解/SVD/特征值 | ❌ 未实现 |
| FFT | 快速傅里叶变换 | ❌ 未实现 |
| 复数运算 | | ❌ 未实现 |
| 有理数 | | ❌ 未实现 |
| 数值积分 | | ❌ 未实现 |
| 插值 | | ❌ 未实现 |

### 4.4 文件与 I/O

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 读文件 | `read_file` | ✅ 已完成 |
| 写文件 | `write_file` | ✅ 已完成 |
| 读字节 | `read_bytes` | ✅ 已完成 |
| 写字节 | `write_bytes` | ✅ 已完成 |
| 路径操作 | `path_join`/`path_exists`/`path_is_file`/`path_is_dir` | ✅ 已完成 |
| 创建目录 | `mkdir` | ✅ 已完成 |
| 列目录 | `list_dir` | ✅ 已完成 |
| 文件大小 | `file_size` | ✅ 已完成 |
| 删除文件 | `remove_file` | ✅ 已完成 |
| 复制文件 | `copy_file` | ✅ 已完成 |
| 重命名 | `rename_file` | ✅ 已完成 |
| 文件流 / 缓冲 I/O | `BufReader`/`BufWriter` | ❌ 未实现 |
| 标准输入 | `read_line()` | ✅ 已完成（2026-07-07 Stage 1 新增 `read_line() -> Result<String>` native，EOF/错误返回 `Err`；标准库 `std::io.th` 提供 `read_line_result` 包装；`io_test.rs` 4 项测试覆盖） |
| 标准错误输出 | `eprint` / `eprintln` | ✅ 已完成（2026-07-07 Stage 1 新增 `eprint(value) -> Unit`（无换行）与 `eprintln(value) -> Unit`（带换行）两个 native，stderr 输出；标准库 `std::io.th` 索引） |
| 文件锁 | 文件级锁 | ❌ 未实现 |
| 文件权限 | `chmod` | ❌ 未实现 |
| 文件监听 | inotify/kqueue | ❌ 未实现 |
| 内存映射文件 | mmap | ❌ 未实现 |
| 临时文件 | `tempfile` | ❌ 未实现 |
| 异步 I/O | `async_read`/`async_write` | ✅ 已完成（2026-07-08 Phase 2 新增 3 个 async native：`async_sleep_ms(ms: i64) -> Future<Unit>`（异步睡眠，子线程 sleep 后通知主线程）、`async_tcp_read(handle: i64, n: i64) -> Future<Result<Vec<u8>, String>>`（异步读 TCP，基于已有 `tcp_streams` handle table）、`async_tcp_write(handle: i64, bytes: Vec<u8>) -> Future<Result<i64, String>>`（异步写 TCP）；用 `std::thread` + `std::sync::mpsc` + `thread_local` 实现（零新依赖，不用 mio）；标准库 `std/async.th` 索引；VM 调度器通过 `ASYNC_IO.with(\|s\| s.poll())` 把已完成 I/O 的 Future 标记为 Ready 并把任务移回 `ready_queue`；详见 MEMO.md 2026-07-08 feat 条目） |

### 4.5 网络与通信

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| TCP 客户端 | `tcp_connect` / `tcp_read` / `tcp_write` / `tcp_close` / `tcp_set_timeout` | ✅ 已完成（2026-07-07 Stage 3 新增 5 个 TCP 原语 native：`tcp_connect(host: String, port: i64) -> Result<i64>` 返回 handle、`tcp_read(handle, n) -> Result<Vec<i64>>` 读最多 n 字节、`tcp_write(handle, data) -> Result<i64>` 返回写入数、`tcp_close(handle) -> Unit`、`tcp_set_timeout(handle, ms) -> Unit`；handle table 模式 `Vm.tcp_streams: Vec<Option<TcpStream>>`，不引入新 `Value` variant；标准库 `std::net.th` 索引） |
| TCP 服务端 | `TcpListener::bind` | ✅ 已完成（2026-07-17：3 个 native `tcp_listen`/`tcp_accept`/`tcp_listener_close` + `std/net.th` 提供 `listen`/`accept`/`listener_close` wrapper；handle table 模式，与 `tcp_streams` 一致；net_test 9 项含 3 项服务端测试：listen 成功/port in use/close 后 accept） |
| UDP | `UdpSocket` | ❌ 未实现 |
| HTTP 客户端 | `http_get` / `http_post` | ✅ 已完成（2026-07-07 Stage 4 新增 2 个 HTTP/1.1 native：`http_get(url: String) -> Result<String>`、`http_post(url: String, body: String) -> Result<String>`；手写 HTTP/1.1 协议（请求行 + Host 头 + Content-Length/Content-Type + body），不依赖 reqwest/hyper；基于 `TcpStream` + `ToSocketAddrs` 实现；标准库 `std::http.th` 索引。**已知限制**：无重定向、无 chunked transfer-encoding、无 HTTPS） |
| HTTP 服务端 | Web 框架 | ❌ 未实现 |
| WebSocket | 双向通信 | ❌ 未实现 |
| TLS/SSL | 加密通信 | ❌ 未实现 |
| DNS 解析 | 域名查询 | ❌ 未实现 |
| URL 解析 | URL 结构化 | ❌ 未实现 |
| Socket 原始 | raw socket | ❌ 未实现 |
| Unix Domain Socket | IPC | ❌ 未实现 |
| gRPC | RPC 框架 | ❌ 未实现 |
| MQTT | 消息队列 | ❌ 未实现 |
| 序列化协议 | Protocol Buffers/Thrift | ❌ 未实现 |

### 4.6 时间与日期

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 当前时间戳 | `time_now`/`time_now_ms` | ✅ 已完成 |
| 当前日期 | `time_date` | ✅ 已完成 |
| 当前时间 | `time_time` | ✅ 已完成 |
| 日期时间 | `time_datetime` | ✅ 已完成 |
| 睡眠 | `time_sleep_ms` | ✅ 已完成 |
| 计时器 | `start_timer`/`elapsed` | ✅ 已完成（std） |
| Date 类型 | Date struct（年/月/日） | ✅ 已完成（2026-07-17 标准库扩展第三波第 8 项，路径 B：复用 `Type::Struct`/`Value::Struct`/`Op::NewStruct`，不新增 HIR 类型；`tenth/std/date.th` Date struct + 构造/转换/格式化/运算/比较函数；native 名 `date_i64_add_days` 带 `_i64_` 前缀是工程妥协，避免与 user function `date_add_days(Date, i64)` 同名冲突；算法 Howard Hinnant date_algorithms 工业级已验证，`date_to_days` 与 `days_to_date` 互逆；5 个 date native `date_to_unix_days`/`date_from_unix_days`/`date_i64_add_days`/`date_diff_days`/`date_day_of_week`；VM + 解释器双路径注册；`date_test.rs` 30 项 VM 路径测试；详见 MEMO.md 2026-07-17 第三波条目） |
| 日期解析 | 字符串 → 日期 | ✅ 已完成（2026-07-17 第三波第 8 项：`date_from_unix_days(year, month, day)` native 构造 Date struct，`std/date.th` wrapper；Date struct 字段为 i64 year/month/day，无字符串 parse 路径——构造函数即"解析"路径；运行时 use 加载 .th 模块暂不支持，parse 路径已通过 stdlib_test `th_parse_date` 索引测试覆盖） |
| 日期格式化 | 日期 → 字符串 | ✅ 已完成（2026-07-17 第三波第 8 项：`std/date.th::date_to_string(date)` 返回 `"YYYY-MM-DD"` 格式字符串） |
| 日期运算 | 加减天数/月份 | ✅ 已完成（2026-07-17 第三波第 8 项：`date_i64_add_days(date, days)` native 加减天数，`std/date.th::date_add_days`/`date_sub_days` wrapper；`date_diff_days(a, b)` native 日期差；底层用 Howard Hinnant `date_to_days` 实现） |
| 星期计算 | day_of_week | ✅ 已完成（2026-07-17 第三波第 8 项：`date_day_of_week(date)` native 返回 0-6（0=Sunday, 6=Saturday），底层用 Howard Hinnant 算法 weekday_from_days） |
| 时区 | 时区转换 | ❌ 未实现（本次仅 UTC，时区转换不在第三波范围） |
| 日历 | 农历/回历等 | ❌ 未实现 |
| Duration 类型 | 时间间隔 | ✅ 已完成（2026-07-17 快赢批次第 49 项，纯 .th 实现，路径 B 复用 `Type::Struct`/`Value::Struct`/`Op::NewStruct` 机制同 Date 类型；`tenth/std/duration.th` 187 行 1 struct + 21 函数——构造 4 `from_secs/from_millis/from_micros/from_nanos`、访问 4 `as_secs/as_millis/as_micros/as_nanos`、算术 4 `add/sub/mul_scalar/div_scalar`、比较 6 `eq/lt/le/gt/ge/ne`、格式化 1 `to_string`（自动选单位 ns/us/ms/s/X.Ys/XmYs/XhYm + 负数前缀）、零值 2 `zero/is_zero`；`Duration` struct `{ nanos: i64 }` i64 纳秒存储覆盖 ±292 年；`tenth/std/prelude.th` 添加 Duration 模块索引；`tenth/tests/duration_test.rs` 15 测试 + `tenth/tests/stdlib_test.rs` `th_parse_duration` 索引测试；纯标准库实现不修改任何 Rust 源码；**已知变通**：发现 pre-existing bug（`eval.rs:28` `Literal::Int(n, _)` 和 `execute.rs:230` `Op::PushInt(n)` 硬编码 I32 忽略字面量 dtype），用 `date_i64_add_days` native 返回 I64 标记值的变通方案绕过，详见 MEMO PROJ-007 候选条目） |
| Instant / SystemTime | 精确时间测量 | ❌ 未实现（不在第三波范围） |

### 4.7 系统与进程

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 命令行参数 | `cli_args_count`/`cli_arg` | ✅ 已完成（基础） |
| 环境变量 | `env_get` / `env_set` | ✅ 已完成（2026-07-07 Stage 2 新增 2 个 native：`env_get(name: String) -> Result<String>`（未找到返回 `Err`）、`env_set(name: String, value: String) -> Unit`；标准库 `std::env.th` 提供 `get`/`get_result`/`get_or_empty`/`set`/`exit` 包装） |
| 执行子进程 | `Command::new` | ✅ 已完成（2026-07-17：4 个 native `command_new`/`command_arg`/`command_run`/`command_output` + `std/process.th` 提供 `new`/`arg`/`run`/`output` wrapper；句柄消费语义——run/output 互斥消费，二次调用返回 Err；跨平台 Windows `cmd /c` 适配保证 echo 等内建命令可用；process_test 5 项：new/output/run/二次 output 消费句柄/无效句柄） |
| 进程退出 | `exit(code)` | ✅ 已完成（2026-07-07 Stage 2 新增 `exit(code: i64) -> 不返回` native，直接调用 `std::process::exit(code)`；标准库 `std::env.th` 索引） |
| 进程 ID | `getpid` | ❌ 未实现 |
| 工作目录 | `getcwd`/`chdir` | ❌ 未实现 |
| 信号处理 | signal handler | ❌ 未实现 |
| 用户/权限 | uid/gid | ❌ 未实现 |
| 系统信息 | OS/CPU/内存 | ❌ 未实现 |
| 管道 | pipe | ❌ 未实现 |
| 重定向 | stdin/stdout 重定向 | ❌ 未实现 |

### 4.8 序列化

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| JSON 编解码 | `json_encode`/`json_decode` | ✅ 已完成（基础） |
| TOML 解析 | | ⚠️ 有 `toml.th` 但功能有限 |
| 模型保存/加载 | `save_model`/`load_model` | ✅ 已完成（L2.2 修复 2026-08-02：`utils/serialization.th` 返回类型注解错误（`-> i32` vs `save_weights` 返回 Unit）致编译失败，已修复为不写注解默认 Unit；`std::utils::serialization::save_model/load_model/save_checkpoint` 现可用） |
| 检查点 | `save_checkpoint` | ✅ 已完成（同上，L2.2 修复后可用） |
| CSV | | ❌ 未实现 |
| XML | | ❌ 未实现 |
| YAML | | ❌ 未实现 |
| Protocol Buffers | | ❌ 未实现 |
| MessagePack | | ❌ 未实现 |
| BSON | | ❌ 未实现 |
| Pickle (Python 兼容) | | ❌ 未实现 |
| SafeTensors | AI 模型格式 | ❌ 未实现 |
| ONNX | 模型交换格式 | ❌ 未实现 |
| GGUF / llama.cpp | LLM 模型格式 | ❌ 未实现 |
| HDF5 | 科学数据格式 | ❌ 未实现 |
| Parquet | 列式存储 | ❌ 未实现 |

### 4.9 加密与安全

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 哈希函数 | SHA-256/MD5 | ✅ 已完成（SHA-256/SHA-512/MD5，6 个 native：sha256/sha512/md5（Vec 输入）+ sha256_str/sha512_str/md5_str（String 输入），返回小写 hex；基于 sha2 0.10 + md-5 0.10 crate；VM+解释器双侧注册；hash_test 25 项含 RFC 6234/1321 标准向量验证；新增 std/crypto/hash.th wrapper） |
| HMAC | 消息认证码 | ❌ 未实现 |
| 对称加密 | AES | ❌ 未实现 |
| 非对称加密 | RSA/ECC | ❌ 未实现 |
| 数字签名 | 签名/验证 | ❌ 未实现 |
| 密码哈希 | bcrypt/argon2 | ❌ 未实现 |
| 随机数安全 | CSPRNG | ✅ 已完成（2026-07-17 快赢批次第 106 项修复：`tenth/src/runtime/interpreter/natives.rs` `random_int`/`random_float` 2 处删除 `DefaultHasher + SystemTime` 熵源（SipHash-13 with fixed keys + 纳秒时刻，可预测），改用 `rand::thread_rng().r#gen::<u64>()` / `r#gen::<f64>()`，与 VM 路径 `runtime/vm/natives.rs:635-657` 已用的 `rand::thread_rng()` 对齐；解释器与 VM 行为不一致已消除；stdlib_test 132 passed + native_parity_test 35 passed 验证） |
| 证书 | X.509 | ❌ 未实现 |
| TLS | 传输层安全 | ❌ 未实现 |

### 4.10 测试框架

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 单元测试 | `#[test]` | ❌ 未实现（测试在 Rust 侧） |
| 断言 | `assert!`/`assert_eq!` | ⚠️ 部分（assert/assert_eq native 已实现 VM+解释器双侧，assert_test 8 项行为测试；但无 #[test] 属性、无测试框架） |
| 测试框架 | `describe`/`it` | ❌ 未实现 |
| 属性测试 | fuzz testing | ❌ 未实现 |
| 基准测试 | `bench` | ❌ 未实现 |
| 测试覆盖率 | coverage | ❌ 未实现 |
| Mock | 模拟对象 | ❌ 未实现 |
| 集成测试 | | ❌ 未实现（Tenth 侧） |
| 快照测试 | | ❌ 未实现 |
| TDD 工具 | watch + auto-run | ❌ 未实现 |

### 4.11 日志与可观测性

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 日志 | `debug`/`info`/`warn`/`error` | ✅ 已完成（std） |
| 日志级别控制 | `set_level` | ✅ 已完成 |
| 结构化日志 | JSON 日志 | ❌ 未实现 |
| 日志轮转 | log rotation | ❌ 未实现 |
| 分布式追踪 | OpenTelemetry | ❌ 未实现 |
| 指标收集 | metrics | ❌ 未实现 |
| 性能追踪 | tracing | ❌ 未实现 |

### 4.12 压缩与归档

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| Gzip | 压缩/解压 | ❌ 未实现 |
| Zip | 归档 | ❌ 未实现 |
| Tar | 归档 | ❌ 未实现 |
| Zstd | 压缩 | ❌ 未实现 |
| Brotli | 压缩 | ❌ 未实现 |
| LZ4 | 压缩 | ❌ 未实现 |

---

## 五、Tenth 特定需求（AI 原生语言）

### 5.1 张量系统

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 张量创建 | `tensor[[...]]` 字面量 | ✅ 已完成（2026-07-31 修复 `tensor<f64>([...])` 语法：HIR 编译为 `Call("tensor", [ArrayLiteral])`，原 native 仅克隆参数返回 `Value::Vec`/`Value::Array` 导致后续张量运算失败；新增 `runtime/value.rs::array_to_tensor` 递归转换 Vec/Array→Tensor（`flatten_values` 计算形状与数据 + `unpack_shared` 解包 Shared 元素），VM + 解释器双侧 `tensor` native 调用该转换；`tensor<f64>([1,2,3]) + tensor<f64>([10,20,30])` 现正确产出 `[11,22,33]`；**M3.1 补注（2026-08-04）**：`tensor[[...]]` 字面量编译期 shape 追踪——直通 TensorLiteral 的 `Known` 维度（此前 `Lowerer::new` 预注册 tensor 函数签名拦截致 shape 清零为 [Any]），字面量 matmul/广播编译期拦截；不规则字面量（行长度不一致）编译期 TypeError（结构静态可知）；嵌套 3D 字面量保守退化 [Any]（不误报 2D，运行时仍可用）） |
| 随机张量 | `rand`/`randn` | ✅ 已完成（2026-07-31 修复 `randn<T>`/`zeros<T>`/`ones<T>`/`rand<T>` 在泛型函数体内的实例化：`hir/lower/lower_expr.rs` `NATIVE_GENERIC_CTORS` 分支接受 `Type::TypeParam`（保留原始 func_name + TypeParam dtype），`hir/lower/mod.rs` `substitute_kind_in_place` 在 TypeParam 替换为 BaseType 后按 (name, dtype) 改写 func_name 如 `randn`+F32→`randn_f32`/F16→`zeros_f16`/BF16→`zeros_bf16`；`fn make_noise<T>(n) -> Tensor[T, 1] { randn<T>(n) }` + `make_noise<f32>(4)` 现正确实例化） |
| 元素级运算 | `+`/`-`/`*`/`/` + 广播 | ✅ 已完成 |
| 矩阵乘法 | `.matmul()` | ✅ 已完成 |
| Batched Matmul / 3D 张量矩阵乘 | 批量矩阵乘 `(B, M, K) @ (B, K, N)` | ✅ 已完成（2026-07-06 新增 `Tensor::bmm` 方法：3D@3D→3D，逐 batch 2D matmul；可微 `TapeOp::BatchedMatMul`，反向逐 batch 与 2D MatMul 一致；VM + 解释器双路径注册为 tensor method；batch 维 B 与内侧 K 必须匹配，不做广播；仅 3D@3D，4D+ 未实现；解除 True MultiheadAttention 阻塞——见 AUDIT-11.1.4。详见 docs/语言参考手册.md §11.7） |
| 转置 | `.transpose()` | ✅ 已完成 |
| 求和/均值 | `.sum()`/`.mean()` | ✅ 已完成 |
| 激活函数 | relu/sigmoid/tanh/gelu | ✅ 已完成 |
| Softmax | `.softmax()` | ✅ 已完成 |
| Reshape | `.reshape()` | ✅ 已完成（2026-07-06 接入 autodiff：`TapeOp::Reshape`，backward=`grad.reshape(input.shape())`，从 input 取原始 shape；T7 分类属 Expand，与 Transpose 同类重排产生新 shape） |
| Flatten | `.flatten()` | ✅ 已完成 |
| Concat | `.cat()` | ✅ 已完成 |
| Masked Fill | `.masked_fill()` | ✅ 已完成（2026-07-06 接入 autodiff：`TapeOp::MaskedFill`，backward=`grad*(1-mask)`，mask 位置梯度归零，mask 不可微阻断链式传播；T7 分类属 Preserve，与 Scatter 同类 shape 不变） |
| Permute | `.permute()` | ✅ 已完成 |
| Broadcast To | `.broadcast_to()` | ✅ 已完成 |
| 张量元数据 | `.numel()`/`.ndim()`/`.shape_tensor()`/`.nbytes()` | ✅ 已完成（VM + Interpreter 双路径；`.shape_tensor()` 返回 `Tensor[f64, ndim]` 1D f64 张量，各元素为维度大小——**不是** i64 数组，取第 i 维大小用 `x.shape_tensor()[i] as i64`；`clip_scalar(v)` 按标量值裁剪）。**已知不一致**：`.shape()` 方法 ❌ 未实现（运行时无对应 native），但 `hir/types.rs:392` 有一处方法分支为类型系统遗留误标——类型检查能过、运行时崩溃，待清理（详见 AUDIT.md §六 #15 / §11.4 AUDIT-11.4.12）。`shape(x)` 自由函数 ❌ 未实现 |
| Conv2D | 2D 卷积 | ✅ 已完成 |
| Dropout | 随机置零 | ✅ 已完成 |
| BatchNorm | 批归一化 | ✅ 已完成 |
| LayerNorm | 层归一化 | ✅ 已完成 |
| 多精度张量 | f32/f16/bf16 张量 | ✅ f32 已完成 Phase 1+2+3+4+5.1+5.2+5.4+5.5（TensorData enum + 8 构造器 + dtype 分支运算；前端 Lexer f32/f64 后缀字面量 + AST/HIR Literal dtype 贯通 + Lower 内置函数 dtype 推断 + TensorLiteral 元素 dtype 推断；Op::PushFloat32/MakeTensor dtype + Value::Float32 + VM 算术 f32 分支含 Float32×Tensor；interpreter.rs 硬编码 F64 清理 + native 函数 f32 支持 randn_f32/zeros_f32/ones_f32/rand_f32/grad/cross_entropy/math_*；自动微分方案 B 天然实现：前向 f32 + 反向 f64 + 梯度按参数 dtype 写回，误差 < 1e-5；WASM 后端策略 A f32→f64 提升：修复 wasm.rs:1417 F32Const 死代码 bug，与下游 F64 处理一致，f32 e2e 测试 14 用例；tenthc 自举同步：复用 Token.ival 字段存 dtype（0=F64, 1=F32）避免扩展两侧结构，HirType.sub=3 表 F32，4 step 改动 lower.th/lexer.th/parser.th/wasm.th，5 步渐进验证自举三路径未破坏；Phase 5.4 标准库 f32 化精简版：14 个 std 文件新增 26 个 _f32 副本函数（optim/init/nn），prelude.th 索引同步（已于 Phase 5.6 真泛型改造中删除）；Phase 5.5 zeros_f32/ones_f32/rand_f32 native 注册补全；Interpreter 修复 Add/Sub/Mul/Div Float32 标量×标量 5 分支 + Float32×Tensor 2 分支 + grad native f32 zeros + dropout f32 dtype 保持），**f16/bf16 Phase 1 已实现**（2026-07-08）：Cargo.toml 添加 `half = "2"` 依赖，TensorData 新增 F16(ArrayD<f16>)/BF16(ArrayD<bf16>) 变体 + 全部方法扩展（dtype/shape/len/ndim/as_f64_view/as_f16_slice/as_bf16_slice/mapv/view/as_standard_layout/iter/broadcast）+ 构造器（from_data_f16/from_data_bf16/from_vec_f16/from_vec_bf16/zeros_f16/zeros_bf16/ones_f16/ones_bf16/full_f16/full_bf16）+ dtype 提升表 promote_dtype（F64+任何→F64, F32+F32→F32, F16+F16→F16, BF16+BF16→BF16, F16+BF16→F32）+ add/sub/mul/div 重写为 elementwise_binary 通用方法 + reductions/scalar ops/unary ops/transpose/broadcast_to/reshape/flatten/gelu/softmax/im2col/permute/assign_/masked_fill/scatter/gather 全部加 F16/BF16 分支 + autodiff 策略 B 简化版（F16/BF16 param 的 acc_grad 返回 Err，前向可参与计算，反向 grad 不回写）+ natives.rs/main.rs 双侧注册 zeros_f16/ones_f16/zeros_bf16/ones_bf16 + HIR 白名单（lower_expr.rs/closures.rs/types.rs）+ methods.rs/vm.rs nbytes 加 F16/BF16→2 字节；**Phase 2 已完成**（2026-07-09，四缺口全部完成）：缺口 4（tenthc 字面量）——`tenthc/lexer/lexer.th` 扩展 f16(ival=2)/bf16(ival=3) 后缀检测（浮点+整数两路径，边界检查防 3.14factor 误匹配）；`tenthc/hir/lower.th:601-611` FloatLiteral 按 ival 写入 HIR sub=14(F16)/15(BF16)；`tenthc/compile/wasm.th:477-492` is_expr_float 识别 F16/BF16（sub=14/15）；parser.th ival 透传复用 Phase 5.2 step 2 既有逻辑。缺口 2（WASM）——`bytecode.rs:610-615` dtype_code 扩展为完整 0-3 编码（F64=0/F32=1/F16=2/BF16=3）；`wasm/mod.rs` F16/BF16→ValType::I64（8 字节对齐，WASM 原生不支持 f16）+ HOST_MAKE_TENSOR_F16=18/HOST_MAKE_TENSOR_BF16=19 常量；`wasm/host.rs`+`wasmtime_host.rs` 双侧注册 host_make_tensor_f16/bf16 stub（签名 (i32,i32,i32)->i64，与 tensor_from_vec 一致）；`wasm/sections.rs` import 声明同步；`wasm/compile.rs:761-833` MakeTensor 按 dtype 分发到对应 hostcall（F16→18, BF16→19, F64/F32→17）。缺口 1（JIT）——`translator.rs:470-478` MakeTensor 4 dtype 分发（0/1/2/3）；`hostcalls.rs:470-526` 实现 host_make_tensor_f16/bf16（从栈上 Value 转 f16/bf16 构造 TensorData）；`vm.rs:995-1063` VM 路径 MakeTensor 支持 4 dtype（含 Tensor::from_vec_f16/bf16）。缺口 3（autodiff F32 中间累加 AMP）——`tensor.rs:307-383` acc_grad 移除 early-return Err，F16/BF16 param 走 F32 中间累加策略（grad buffer 存为 F32，避免 F16 溢出 max≈65504 和 BF16 精度损失）；`autodiff.rs:72-85` dispatch_float! F16/BF16 走 f32 路径；种子梯度 F16/BF16 用 F32 ones；Reshape 反向 F16/BF16 grad 转 F32（不再强制提升 F64）。**Phase 3 F16/BF16 算子覆盖补齐已完成**（2026-07-17 标准库扩展第三波第 7 项，选项 3 AMP 暂缓）：`runtime/tensor/methods.rs` 修改 9 个算子——P0 修复 layer_norm F16/BF16 panic 隐患（原 `if self.is_f32() { ... } else { self.data.as_f64().unwrap() }` 改为 `match self.dtype` 四分支，F16/BF16 转 f32 计算后转回原 dtype）；P1 核心算子 matmul/bmm 新增 `matmul_low_prec`/`bmm_low_prec` 私有方法（F16/BF16 转 f32 计算后转回原 dtype），im2col 输出保留原 dtype，max_pool2d_with_argmax/avg_pool2d 输出构造新增 F16/BF16 分支；P2 次要算子 sum_axis F16/BF16 转 f32 计算后转回原 dtype，cat 新增 dtype 提升表（同 dtype 保留，混合提升到 F64），select then/else 同为 F16 或同为 BF16 时保留原 dtype。关键设计：F16/BF16 算子走 f32 中间计算（利用 f32 数值精度），结果转回原 dtype；不修改 autodiff backward（dispatch_float! 宏已覆盖 F16/BF16 走 f32 中间路径）；不修改 ops.rs（Phase 1 兼容层移除属后续 Phase 3/4 整体重构，不在本次范围）；不触及 HIR/tenthc/自举三路径。f16_coverage_test 33 passed（7 组：layer_norm 4 + matmul 4 + bmm 3 + im2col/Conv2D 4 + pool 4 + sum_axis/cat/select 7 + autodiff 端到端 3）；全量测试无回归；自举通过。
验证：autodiff 54/54、vm_autodiff 15/15、tensor_features 18/18、jit 10/10、wasm_backend_minimal 10/10、lib 16/16、module 6/6、integration 14/14 均 0 回归；自举 [OK] Full compiler compiled、cargo build --release exit 0。Phase 5.0 调研：Tensor[T,..] 泛型化在当前类型系统不可行（Type::Tensor.dtype: BaseType 硬约束），降级为路径 C 后缀复制策略（Phase 5.6 真泛型改造已完成：Type::Tensor.dtype 改为 Box<Type>，支持 TypeParam，11 个 _f32 副本已删除，7 个函数改泛型） |
| 混合精度训练 | AMP | ⚠️ 部分完成（2026-07-09 F32 中间累加策略：F16/BF16 param 的 grad 累加用 F32 中间表示，避免 F16 溢出和 BF16 精度损失，详见 f16/bf16 Phase 2 缺口 3；**2026-07-17 状态修正**：原标注"已完成"与实际不符，实际只完成 acc_grad 部分 F32 中间累加，autocast/GradScaler/loss_scaling 全 src 无任何实现，混合精度梯度 fp16 仍见 §5.2 同名条目 ❌；本次第三波只补齐 F16/BF16 算子覆盖，AMP 暂缓） |
| 稀疏张量 | SparseTensor | ❌ 未实现 |
| 量化张量 | int8/int4 量化 | ❌ 未实现 |
| 张量并行 | 跨设备分片 | ❌ 未实现 |
| 内存高效注意力 | FlashAttention | ❌ 未实现 |
| 梯度检查点 | gradient checkpointing | ❌ 未实现 |
| 张量序列化 | 高效存储格式 | ✅ 已完成（2026-07-08 升级到 v2 格式：`[magic:"THW1"][version:i32=2][num_tensors] × [ndim][shape][dtype:i32][data]`，dtype 0=F32/1=F64/2=F16/3=BF16；F32=4字节，F64=8字节，F16/BF16=2字节；save 端按 dtype 分发写，消除原 `as_standard_layout()` 硬编码 f64 精度损失；向后兼容旧 v1 格式（无 magic 走旧路径纯 f64 兜底）；三处实现同步：`natives.rs:1064-1140(save)/1145-1276(load)`、`main.rs:2060-2132(save)/2118-2270(load)`、`native_parity_test.rs:205-271(save)/272-390(load)`） |
| 张量索引高级 | `tensor[0:3, ::2]` | ❌ 未实现（仅基础索引） |
| 广播规则完善 | NumPy 级广播 | ✅ 已完成（2026-07-08 审计确认基础设施完整：前向 `add_tensor/sub_tensor/mul_tensor/div_tensor` 已重写为 `elementwise_binary` 通用方法支持 NumPy 广播规则；autodiff backward 的 `unbroadcast` 已完整覆盖 Add/Sub/Mul/Div（`autodiff.rs:569-619` 调用行 579/581/598/599/614/615），unbroadcast 函数（`autodiff.rs:1560-1607`）已泛型化 `<E: FloatElem>` 支持 f32/f64；f16/bf16 同样纳入 elementwise_binary 通用方法，dtype 提升表完整。2026-07-17 测试补齐：新建 `tenth/tests/broadcast_test.rs` 33 项测试覆盖一元/二元/标量/跨 dtype/反向广播 autodiff/错误情况 6 大场景，运行时数值正确性覆盖缺口补齐，三重验证全绿——broadcast_test 33/33 + 全量 cargo test exit 0 + 自举 [OK]） |
| 张量比较 | `tensor > 0` 逐元素 | ✅ 已实现（2026-07-17 路径 Z：6 个比较 native `tensor_gt/lt/ge/le/eq/ne` + `std/nn/ops.th` 6 个泛型 wrapper `gt/lt/ge/le/eq/ne`；不新增 HIR 类型/TensorData/TapeOp，bool 沿用 f64 0.0/1.0 编码（未新增类型），比较结果固定 F64 张量；`hir/lower/types.rs` `resolve_builtin` 用 `broadcast_shapes` 推断输出 shape；比较不可微（不进入 tape）作为常量掩码，跨 dtype 输入 cast 到 f64 视图；`compare_test` 13 项测试覆盖前向数值/广播/跨 dtype/where_ 别名/autodiff 端到端 ReLU 等价） |
| Where / Select | 条件选择 | ✅ 已实现（2026-07-02 select 原语引入，三输入 `select(cond, then, else_)`，cond>0.5 选 then 否则选 else，支持 NumPy 广播；autodiff 链式法则 `d_then = grad * cond_mask`、`d_else = grad * (1 - cond_mask)`，cond 不可微；TapeOp::Select 算子，autodiff 算子集 21→22；标准库应用 `leaky_relu_select`、`huber_loss`、`huber_loss_train`；HIR lower/解释器 Var fallback/闭包自由变量三层白名单同步；VM 与解释器双侧 native 注册；tenthc/hir/lower.th 白名单同步。2026-07-17 新增 `where_` 别名 wrapper（`std/nn/ops.th`），与 select 语义一致，命名避 Tenth 关键字；同期落地 6 个比较 wrapper（gt/lt/ge/le/eq/ne，详见上一行"张量比较"条目）。详见 MEMO.md 2026-07-02 feat 条目 + 2026-07-17 feat 第二波第 4 项条目） |
| Scatter / Gather | 分散/聚集操作 | ✅ 已完成（scatter 支持任意 dim + 多维 index/src（PyTorch 对齐），覆盖语义 out-of-place；可微 `TapeOp::Scatter`，d_base=grad 恒等、d_src 通过 flatten_index 多维收集，index 不可微；gather 已实现 `gather(base, dim, index)`，out.shape==index.shape，dtype 跟随 base；可微 `TapeOp::Gather`，backward 用 scatter-add 语义（重复 index 累加），index 不可微；VM 与解释器双侧 native 注册。详见 docs/语言参考手册.md §11.8 / §11.10） |
| Sort / TopK | 排序/Top-K | ❌ 未实现 |
| Einsum | 爱因斯坦求和 | ❌ 未实现 |
| 张量拼接高级 | stack / hstack / vstack | ❌ 未实现 |
| Padding | 零填充/反射填充 | ❌ 未实现 |
| Pooling | 最大池化/平均池化 | ✅ 已完成（2026-07-17：`Tensor::max_pool2d`/`avg_pool2d` 方法 + `TapeOp::MaxPool2D`/`AvgPool2D` autodiff backward；`std/nn/pool.th` 4 个泛型函数；详见 §5.3 MaxPool/AvgPool 条目） |
| Upsample | 上采样 | ❌ 未实现 |

### 5.2 自动微分

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 计算图构建 | `new_grad()` | ✅ 已完成 |
| 参数注册 | `param()` | ✅ 已完成（护城河 A：≥1GB 参数编译期发梯度 OOM warning）**M3.1 补注（2026-08-04）**：类型直通（shape 黑洞修复）——param(x) 运行时恒等（注册 tape input 后原样返回），类型直通参数类型，`param(zeros(3,4))` 保持 `Tensor[f64,3,4]`，matmul/广播编译期检查对 param 包裹的张量恢复生效（此前注册返回 `Tensor[..]=[Any]`，shape 被清零漏到运行时） |
| 反向传播 | `backward()` | ✅ 已完成（护城河 A：返回 `Result`，梯度 shape 不匹配时报错，消除 5 处 silent squeeze；护城河 F：失败时自动调用 `formal_explain` 包装根因分析） |
| 关系调试器 | `explain_error()` / FormalExplain | ✅ 已完成（护城河 F Phase 1+2：T2 §4.3 算法，Tape DAG 反向 BFS，O(\|V\|+\|E\|)；C1 可达 + C2 相关两级分类，输出 ExplainsError/PartialExplain/Unrelated 三级；T7 定理 27 算子四类互斥完备分类；Phase 1 深化：错误信息流贯通——backward 各分支抛错改为 ShapeMismatch + 结构化 TapeErrorContext（真实 v_err/op/expected/actual），natives 双路径包裹层提取真实上下文传给 formal_explain；RootCause 边级归因（BFS parent 映射）；op_name 去重；Phase 2 深化：5 类错误分类（ShapeMismatch/SilentSqueeze/BroadcastFail/GradDrift/DtypeConflict）+ classify_error_type 方法 + DtypeConflict 运行时检测；20 项测试 relation_debugger_test + 18 项内联测试） |
| 梯度读取 | `grad()` | ✅ 已完成 |
| 停止梯度 | `stop_grad()` | ✅ 已完成 |
| 清零梯度 | `zero_grad()` | ✅ 已完成 |
| 交叉熵 | `cross_entropy()` | ✅ 已完成（2026-07-30 归一化修正：前向 `loss = -sum_i(target[i] * log(softmax(logits)[i])) / B`，`B = batch size` 即除最后一维外所有前导维度乘积，mean over batch；反向 `d_loss/d_logits = (softmax - target) / B`。**自由函数形式** `cross_entropy(logits, target)`，**无** `.cross_entropy()` 方法） |
| 28 个算子 backward | Add/Sub/Mul/Div/Neg/ReLU/MatMul/BatchedMatMul/Select/Scatter/Abs/Reshape/MaskedFill/Gather 等 | ✅ 已完成（2026-07-02 新增 Select：前向 cond>0.5 选 then 否则选 else，反向 d_then=grad*cond_mask、d_else=grad*(1-cond_mask)，cond 不可微，支持广播 unbroadcast；2026-07-02 新增 Abs：前向 |x|，反向 d|x|/dx=sign(x)，x=0 处取 0 次梯度中点；2026-07-06 新增 BatchedMatMul：3D@3D 逐 batch 2D matmul，反向与 2D MatMul 一致逐 batch 转置乘；2026-07-06 新增 Scatter：覆盖语义 out-of-place，d_base=grad 恒等、d_src=grad[index] 收集，index 不可微；2026-07-06 新增 Reshape：反向 grad.reshape(input.shape()) 回原始 shape；2026-07-06 新增 MaskedFill：反向 grad*(1-mask)，mask 不可微阻断链式传播，解除 True MHA w_q/w_k/w_v 梯度阻断；2026-07-06 新增 Gather：前向沿 dim 维用 index 索引 base，out.shape==index.shape；反向 d_base 用 scatter-add 语义（重复 index 累加），index 不可微；scatter 同期扩展为支持任意 dim + 多维 index/src，新增 flatten_index 辅助函数） |
| 二阶导数 | Hessian | ❌ 未实现 |
| 高阶导数 | n 阶导 | ❌ 未实现 |
| 前向模式 AD | JVP | ❌ 未实现（仅反向模式） |
| 梯度累积 | gradient accumulation | ✅ 已完成（2026-07-08 新增 `accumulate_loop<T>(forward_fn, micro_batches: Vec, accum_steps: i64)` 高阶函数，`tenth/std/optim/accumulate.th:46`；闭包语法 `|x| expr`（Tenth 惯例，参考 `std::collections::iter::map`）；模拟大 batch 训练——多个 micro_batch 前向+反向累积梯度后除以累积步数等价于大 batch 平均；JIT 路径友好） |
| 梯度裁剪 | gradient clipping | ✅ 已完成（`clip_grad_by_value` 可用；`clip_grad_by_norm` 已修复 JIT 路径 tensor-float 比较问题——2026-07-08 改用 `to_f64(norm)` 和 `to_f64(max_norm)` 强制提升到 f64 再比较，`tenth/std/optim/clip.th:37`，Tenth 无 `as f64` 语法用 `to_f64()` native；标准库 `optim/clip.th`；详见 §5.4 优化器同名条目） |
| 自定义算子 | 用户定义可微算子 | ✅ 已完成（2026-07-17 PROJ-006：选项 2 Rust 端注册版——用户在 Rust 实现 `CustomBackward` trait 并通过 `vm.register_custom_op()` / `interpreter.register_custom_op()` 注册到 `CustomOpRegistry`，Tenth 端通过 `std::autograd::call_custom_op1/2/3<T>` wrapper 调用 `__call_custom_op` native（op_id 作为第一个 Int 参数 + 可变数量张量输入）；`tenth/src/runtime/autodiff/custom.rs` 新建——`CustomBackward` trait 5 必填方法 `forward/backward/op_class/name/` + 2 可选方法 `forward_shape/backward_shapes`，`op_class` 返回 `TapeOpClass`（Construct/Preserve/Reduce/Expand 四类，对应护城河 F T7 完备性定理）；`CustomOpRegistry` = `HashMap<usize, Box<dyn CustomBackward>>` + `name_to_id` 索引 + 自增 `next_id`；`TapeOp::Custom(usize)` 新变体保持 `#[derive(Debug, Clone, PartialEq)]`；`op_name` 返回类型 `&'static str` → `Cow<'static, str>`（支持动态生成 `"Custom#{op_id}"`）；`Tape.custom_ops: Option<Rc<RefCell<CustomOpRegistry>>>` 字段 + `set_custom_ops` 注入模式，`Option` 包装默认零开销；backward native 注入 `tape.set_custom_ops(custom_ops)` 共享 Vm/Interpreter 与 Tape；**护城河 A 兼容**——Custom 算子 backward 阶段校验梯度数量（与 `backward_shapes` 声明一致）+ 每条梯度 shape，不通过返回 `ShapeMismatch` 错误（与现有 27 算子一致），消除 Custom 分支 silent squeeze；**护城河 F T7 完备性维护**——`relation_debugger.classify_tape_op` 新签名 `(op, registry: Option<&CustomOpRegistry>)`，Custom 分支查 registry 调用 `custom_op.op_class()` 获取真实代数类，registry 不可用或 op_id 不存在时 fallback 到 Construct 类（最保守，与 Input/Param 同类），维护四类互斥完备性，T7 定理依然成立；`tenth/std/autograd.th`（新建）三个泛型 wrapper；`tenth/tests/custom_autograd_test.rs`（新建）18 项测试覆盖基础前向/反向 + op_class 声明 + shape 校验 + finite-difference 数值梯度对比 + 与 matmul+sum 组合 + 错误情况 + 深度覆盖；**已知限制**：仅支持 Rust 端注册（Tenth 无 trait impl 语法），未接入 JIT/WASM 路径（遇 Custom op 触发既有 JIT→VM fallback，WASM 路径下不可用），Tenth 单线程模型下 `Rc<RefCell<>>` 共享安全） |
| 梯度检查点 | recomputation | ❌ 未实现 |
| 混合精度梯度 | fp16 梯度 | ❌ 未实现 |
| 稀疏梯度 | sparse backward | ❌ 未实现 |
| JIT 编译训练步 | `@jit train_step` | ❌ 未实现 |
| vmap / 批量向量化 | 自动批处理 | ❌ 未实现 |
| 函数变换 | `grad`/`vmap`/`pmap` 函数式 API | ❌ 未实现 |

### 5.3 神经网络库

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| Linear 层 | 全连接 | ✅ 已完成 |
| 激活函数 | relu/sigmoid/tanh/gelu/softmax | ✅ 已完成 |
| Dropout | 随机失活 | ✅ 已完成 |
| BatchNorm | 批归一化 | ✅ 已完成 |
| LayerNorm | 层归一化 | ✅ 已完成（2026-07-30：`make_layer_norm` 改为 f64 版本去泛型——同 make_feedforward_params/make_transformer_block_params，因 native 泛型构造函数限制；前向函数 `layer_norm<T>` 保留泛型 ✅） |
| Conv2D | 2D 卷积 | ✅ 已完成（L2.2 修复 2026-08-02：`conv2d` 泛型化 `<T>` + kH/kW 改为显式参数（运行时无 `shape()` 自由函数），`std::nn::conv::conv2d<T>` 现可用） |
| Embedding | 嵌入层 | ✅ 已完成（2026-07-30 修正：`embedding_lookup` 张量方法 ❌ 未实现，nn::embedding 改用 `gather(weight, 0, indices)` native 实现；autodiff 通过 `TapeOp::Gather` 反向 `d_base=scatter_add(grad, dim, index)`，index 不可微。**L2.2 修复 2026-08-02**：indices 1D vs weight 2D 的 ndim 不匹配改为 `indices→reshape[S,1]→+zeros(S,D)` 广播构造 [S,D] index 张量后 gather，`embedding<T>` 现可用（seq_len/dim 显式传入）。详见 AUDIT.md §六 #14 / §11.4 AUDIT-11.4.11） |
| Attention | 缩放点积注意力 | ✅ 已完成（L2.2 修复 2026-08-02：函数体引用未定义的 `shape()` 自由函数，`d_k` 改为显式参数传入，`scaled_dot_product_attention<T>`/`_batched<T>` 现可用） |
| MultiheadAttention | 多头注意力 | ✅ 已完成（2026-07-06 基于 bmm 重写为真正分头计算版本：q/k/v 投影后 reshape 为 (n_heads, seq_len, d_k)，bmm 计算各 head attention，reshape 回 (seq_len, d_model) 做 w_o 投影；Reshape/MaskedFill 接入 autodiff 后 w_q/w_k/w_v/x 梯度完整传播；multihead_attention_test 11 项测试全绿，含 n_heads=1 与单头等价、完整反向链等；详见 AUDIT-11.1.4。**2026-07-30 签名变更**：新增 `seq_len: i64, d_model: i64` 显式参数，因运行时无 `shape()` 自由函数能从张量取出 i64 维度；新签名为 `multihead_attention(x, w_q, w_k, w_v, w_o, mask, seq_len, d_model, n_heads, dropout_p)`，与 `tenth/std/nn/multihead_attention.th` 一致） |
| PositionalEncoding | 位置编码 | ✅ 已完成（2026-07-30：`positional_encoding` 等同 `make_*` 函数已改为 f64 版本——去掉 `<T>` 泛型，因编译器对 native 泛型构造函数 `randn<T>`/`zeros<T>`/`ones<T>` 强制要求 `type_args` 是 `Type::Base`，泛型 make_* 触发编译错误；泛型化推后到 native 泛型构造函数支持 Type::Tensor 后） |
| FeedForward | 前馈网络 | ✅ 已完成（2026-07-30：`make_feedforward_params` 改为 f64 版本去泛型；前向函数 `feedforward<T>` **保留泛型** ✅，body 只用 Tensor 方法合法） |
| Transformer | 编码器块 | ✅ 已完成（2026-07-30：`make_transformer_block_params` 改为 f64 版本去泛型；前向函数 `transformer_encoder_block<T>` **保留泛型** ✅，body 只用 Tensor 方法合法。**L2.2 修复 2026-08-02**：文件顶部补 `use std::nn::{layer_norm,multihead_attention,feedforward}` 导入，`std::nn::transformer::transformer_encoder_block<T>` 现可用） |
| 损失函数 | MSE/L1/BCE/CrossEntropy/HuberLoss | ✅ 已完成（2026-07-02 新增 Huber Loss：`huber_loss(pred, target, delta=1.0)` 用 select 原语实现二次区/线性区分段，`huber_loss_train` 含前向+反向；`std/nn/loss.th`） |
| Conv1D / Conv3D | 1D/3D 卷积 | ❌ 未实现 |
| RNN / LSTM / GRU | 循环神经网络 | ❌ 未实现 |
| Transposed Conv | 转置卷积 | ❌ 未实现 |
| Depthwise Conv | 深度可分离卷积 | ❌ 未实现 |
| GroupNorm / InstanceNorm | 其他归一化 | ❌ 未实现 |
| MaxPool / AvgPool | 池化层 | ✅ 已完成（2026-07-17：`std/nn/pool.th` 4 个泛型函数 `max_pool2d<T>`/`max_pool2d_explicit<T>`/`avg_pool2d<T>`/`avg_pool2d_explicit<T>` 包装 `tensor.max_pool2d(...)`/`tensor.avg_pool2d(...)`；autodiff backward：`TapeOp::MaxPool2D` argmax 路由（重复 max 取首个位置，与 PyTorch `return_indices=True` 一致）、`TapeOp::AvgPool2D` 均分到 window 内有效位置 `count_include_pad=False`（padding 边界有效位置数 < kernel_size 时按有效数归一化）；VM + 解释器双路径方法注册；pool_test 8 项 VM 路径集成测试：forward basic/stride/padding + backward argmax 路由 + avg 均分 + padding 边界） |
| AdaptivePool | 自适应池化 | ❌ 未实现 |
| Transformer Decoder | 解码器块 | ❌ 未实现 |
| Beam Search | 推理解码 | ❌ 未实现 |
| 模型保存/加载 | 序列化 | ✅ 已完成（L2.2 修复 2026-08-02：`utils/serialization.th` 注解错误修复，`std::utils::serialization::save_model/load_model/save_checkpoint` 现可用） |
| 模型量化 | int8/int4 | ❌ 未实现 |
| 模型剪枝 | pruning | ❌ 未实现 |
| 模型蒸馏 | knowledge distillation | ❌ 未实现 |
| 预训练模型加载 | 加载 HuggingFace 等 | ❌ 未实现 |
| 模型推理引擎 | 推理优化 | ❌ 未实现 |
| ONNX 导出/导入 | 模型交换 | ❌ 未实现 |
| TensorBoard / 日志 | 训练可视化 | ❌ 未实现 |
| 学习率调度器 | lr_scheduler | ✅ 已完成（2026-07-17：`std/optim/lr_schedule.th` 6 个 f64 标量函数 `cosine_lr`/`step_lr`/`exp_lr`/`warmup_lr`/`warmup_cosine_lr`/`warmup_step_lr`；参考 PyTorch CosineAnnealingLR/StepLR/ExponentialLR/LambdaLR 语义；纯 stdlib 无 native；`LR_EPS=1e-12` 防 0 除（cosine 衰减分母 `1+cos(...)` 在 t=T_max 处理论为 0）；`warmup_step_lr` 用步进累乘规避 Tenth 标量无 `pow` 方法限制；stdlib_test `th_parse_optim_lr_schedule` 测试） |
| 早停 / 检查点 | early stopping | ❌ 未实现 |
| 数据增强 | augmentation | ❌ 未实现 |
| 分布式训练 | DDP / 模型并行 | ❌ 未实现 |
| 梯度累积 | 大 batch 模拟 | ✅ 已完成（2026-07-08 新增 `accumulate_loop<T>(forward_fn, micro_batches: Vec, accum_steps: i64)` 高阶函数，`tenth/std/optim/accumulate.th:46`；闭包语法 `|x| expr`；模拟大 batch 训练——多个 micro_batch 前向+反向累积梯度后除以累积步数等价于大 batch 平均；详见 §5.2 自动微分同名条目） |
| 混合精度训练 | AMP | ⚠️ 部分完成（2026-07-09 F32 中间累加策略：F16/BF16 param 的 grad 用 F32 中间表示，避免 F16 溢出 max≈65504 和 BF16 精度损失；`tensor.rs:307-383` acc_grad + `autodiff.rs:72-85` dispatch_float! F16/BF16 走 f32 路径；详见 f16/bf16 Phase 2 缺口 3；**2026-07-17 状态修正**：原标注"已完成"与实际不符——实际只完成 acc_grad 部分 F32 中间累加，autocast/GradScaler/loss_scaling 全 src 无任何实现，混合精度梯度 fp16 见 §5.2 同名条目仍 ❌；本次第三波只补齐 F16/BF16 算子覆盖，AMP 暂缓） |

### 5.4 优化器

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| SGD | 随机梯度下降 | ✅ 已完成 |
| SGD + Momentum | 动量 | ✅ 已完成 |
| SGD + Weight Decay | 权重衰减 | ✅ 已完成 |
| Adam | 自适应矩估计 | ✅ 已完成 |
| AdaGrad | 自适应梯度 | ✅ 已完成 |
| RMSProp | 均方根传播 | ✅ 已完成 |
| AdamW | 解耦权重衰减 | ✅ 已完成（`adamw_step` 函数返回 tuple (new_w, new_m, new_v)；2026-07-08 新增 `adamw_step_w/_m/_v` 三个单值返回版本，`tenth/std/optim/adamw.th:65/87/104`，与 tuple 版本参数完全一致，分别返回 new_w/new_m/new_v；JIT 路径友好，避免 tuple 解构触发 fallback；标准库 `optim/adamw.th`） |
| Lion | 新优化器 | ❌ 未实现 |
| Adafactor | 因子化优化器 | ❌ 未实现 |
| LAMB | 层自适应 | ❌ 未实现 |
| NAdam | Nesterov + Adam | ❌ 未实现 |
| RAdam | 矩估计修正 | ❌ 未实现 |
| Lookahead | 前瞻优化器 | ❌ 未实现 |
| 学习率调度 | cosine/step/exponential | ✅ 已完成（2026-07-17：`std/optim/lr_schedule.th` `cosine_lr`/`step_lr`/`exp_lr`；详见 §5.3 学习率调度器条目） |
| 预热策略 | warmup | ✅ 已完成（2026-07-17：`std/optim/lr_schedule.th` `warmup_lr`/`warmup_cosine_lr`/`warmup_step_lr`；warmup 阶段线性插值，warmup 后切换主调度器） |
| 梯度裁剪 | clip by norm/value | ✅ 已完成（`clip_grad_by_value` 可用；`clip_grad_by_norm` 已修复 JIT 路径 tensor-float 比较问题——2026-07-08 改用 `to_f64(norm)` 和 `to_f64(max_norm)` 强制提升到 f64 再比较，`tenth/std/optim/clip.th:37`；标准库 `optim/clip.th`；详见 §5.2 自动微分同名条目） |

### 5.5 数据处理

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| DataLoader | 批量加载 | ✅ 已完成 |
| MNIST 加载 | 手写数字 | ✅ 已完成 |
| One-hot 编码 | `one_hot()` | ✅ 已完成 |
| 像素归一化 | `normalize_pixel()` | ✅ 已完成 |
| CIFAR-10 加载 | 图像数据集 | ❌ 未实现 |
| ImageNet 加载 | 大规模图像 | ❌ 未实现 |
| CSV 数据加载 | 表格数据 | ❌ 未实现 |
| JSON 数据集 | 结构化数据 | ❌ 未实现 |
| 文本数据集 | NLP 语料 | ❌ 未实现 |
| 数据增强 | 图像翻转/裁剪/旋转 | ❌ 未实现 |
| Tokenizer | 分词器 | ❌ 未实现 |
| BPE / WordPiece | 子词分词 | ❌ 未实现 |
| 数据缓存 | 预处理缓存 | ❌ 未实现 |
| 流式加载 | 大数据集流式读取 | ❌ 未实现 |
| 数据采样器 | 随机/加权/分层采样 | ❌ 未实现 |
| 多进程加载 | 并行数据加载 | ❌ 未实现 |

### 5.6 GPU / 加速计算

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| CUDA 后端 | NVIDIA GPU | ⚠️ 脚手架（无实际 kernel） |
| cuDNN 集成 | 深度学习加速库 | ❌ 未实现 |
| Metal 后端 | Apple GPU | ❌ 未实现 |
| Vulkan 后端 | 跨平台 GPU | ❌ 未实现 |
| OpenCL 后端 | 跨平台计算 | ❌ 未实现 |
| ROCm / HIP | AMD GPU | ❌ 未实现 |
| TPU 后端 | Google TPU | ❌ 未实现 |
| Apple Silicon | MPS / ANE | ❌ 未实现 |
| 算子融合 | kernel 自动融合 | ⚠️ 脚手架（fusion.rs） |
| 并行分解 | 自动并行化 | ⚠️ 脚手架（parallel.rs） |
| 内存池 | GPU 内存管理 | ❌ 未实现 |
| 流/异步执行 | CUDA streams | ❌ 未实现 |
| 多 GPU | 数据/模型并行 | ❌ 未实现 |
| NCCL | 多 GPU 通信 | ❌ 未实现 |
| 内核自动调优 | autotuning | ❌ 未实现 |
| 图优化 | 计算图编译优化 | ❌ 未实现 |

### 5.7 分布式

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| SPMD | 单程序多数据 | ❌ 未实现（有 `shard`/`node` 关键字） |
| 数据并行 | DDP | ❌ 未实现 |
| 模型并行 | 张量/流水线并行 | ❌ 未实现 |
| 参数服务器 | PS 架构 | ❌ 未实现 |
| All-Reduce | 集合通信 | ❌ 未实现 |
| RPC | 远程过程调用 | ❌ 未实现 |
| 分布式检查点 | | ❌ 未实现 |
| 弹性训练 | 故障恢复 | ❌ 未实现 |

### 5.8 AI 推理与部署

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 模型导出 | 导出为独立可执行 | ❌ 未实现 |
| ONNX 导出 | 跨框架交换 | ❌ 未实现 |
| TorchScript 等价 | 脚本化模型 | ❌ 未实现 |
| 推理服务器 | HTTP/gRPC 推理 API | ❌ 未实现 |
| 模型量化推理 | int8 推理 | ❌ 未实现 |
| 批处理推理 | dynamic batching | ❌ 未实现 |
| 模型版本管理 | model registry | ❌ 未实现 |
| A/B 测试 | 模型对比 | ❌ 未实现 |
| 边缘部署 | 移动端/嵌入式 | ❌ 未实现 |
| WebAssembly 推理 | 浏览器端推理 | ⚠️ 有 WASM 但未用于推理 |

---

## 六、通用编程生态（Tenth 作为通用语言）

### 6.1 Web 与网络服务

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| HTTP 服务端框架 | 路由/中间件 | ❌ 未实现 |
| HTTP 客户端 | 请求库 | ❌ 未实现 |
| WebSocket | 全双工通信 | ❌ 未实现 |
| REST API | RESTful 设计 | ❌ 未实现 |
| GraphQL | 查询语言 | ❌ 未实现 |
| 模板引擎 | HTML 渲染 | ❌ 未实现 |
| 静态文件服务 | | ❌ 未实现 |
| Cookie/Session | 会话管理 | ❌ 未实现 |
| CORS | 跨域处理 | ❌ 未实现 |
| 文件上传 | multipart | ❌ 未实现 |

### 6.2 数据库

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| SQLite | 嵌入式数据库 | ❌ 未实现 |
| PostgreSQL 驱动 | | ❌ 未实现 |
| MySQL 驱动 | | ❌ 未实现 |
| Redis 客户端 | KV 存储 | ❌ 未实现 |
| MongoDB 驱动 | 文档数据库 | ❌ 未实现 |
| ORM | 对象关系映射 | ❌ 未实现 |
| 连接池 | | ❌ 未实现 |
| 迁移工具 | schema 版本管理 | ❌ 未实现 |
| 向量数据库 | AI 向量检索 | ❌ 未实现 |

### 6.3 GUI 与图形

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 终端 UI | TUI 框架 | ❌ 未实现 |
| 桌面 GUI | Qt/GTK/原生窗口 | ❌ 未实现 |
| 图形渲染 | OpenGL/Vulkan/Metal | ❌ 未实现 |
| 图表绘制 | matplotlib 等价 | ❌ 未实现 |
| 图像处理 | PIL/OpenCV 等价 | ❌ 未实现 |
| 音频处理 | | ❌ 未实现 |
| 视频处理 | | ❌ 未实现 |
| 游戏引擎 | | ❌ 未实现 |

### 6.4 系统编程

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| FFI | 调用 C 库 | ❌ 未实现 |
| 内联汇编 | `asm!` | ❌ 未实现 |
| 原始指针 | `*const T`/`*mut T` | ❌ 未实现 |
| 内存布局控制 | `#[repr(C)]` | ❌ 未实现 |
| 位操作 | bitflags/位域 | ❌ 未实现 |
| 系统调用 | syscall 封装 | ❌ 未实现 |
| 共享内存 | shm | ❌ 未实现 |
| 进程间通信 | pipe/shared memory | ❌ 未实现 |
| 守护进程 | daemon | ❌ 未实现 |
| systemd 服务 | | ❌ 未实现 |

### 6.5 脚本与自动化

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| Shell 调用 | `exec("ls -la")` | ❌ 未实现 |
| 管道操作 | `command \| command` | ❌ 未实现 |
| 文件 glob | `*.txt` 匹配 | ❌ 未实现 |
| 正则替换 | `sed` 等价 | ❌ 未实现 |
| 定时任务 | cron 等价 | ❌ 未实现 |
| 文件监听 | watch | ❌ 未实现 |
| 环境管理 | venv 等价 | ❌ 未实现 |

---

## 七、语言生态与社区

| 能力 | 说明 | Tenth 状态 |
|------|------|-----------|
| 官方文档 | 语言手册 | ✅ 已完成 |
| API 参考 | 标准库文档 | ⚠️ 有 prelude 但无生成文档 |
| 教程 | 入门指南 | ❌ 未实现 |
| 示例库 | 代码示例 | ✅ 已完成（50+ 示例） |
| 包注册中心 | 中央仓库 | ❌ 未实现 |
| 官方网站 | tenth-lang.org | ❌ 未实现 |
| 社区论坛 | Discord/Forum | ❌ 未实现 |
| 贡献指南 | CONTRIBUTING.md | ✅ 已完成 |
| 行为准则 | CODE_OF_CONDUCT.md | ✅ 已完成 |
| 安全政策 | SECURITY.md | ✅ 已完成 |
| 版本管理 | semver | ✅ 已完成（v0.3.3） |
| 变更日志 | CHANGELOG | ❌ 未实现 |
| VSCode 扩展 | 语法高亮/LSP 集成 | ❌ 未实现 |
| JetBrains 插件 | IntelliJ/CLion | ❌ 未实现 |
| 在线 Playground | Web 端试用 | ❌ 未实现 |
| 语言规范 | 形式化规范 | ❌ 未实现（仅有手册） |
| 一致性测试套件 | conformance tests | ❌ 未实现 |
| 性能基准 | benchmark suite | ✅ 已实现（**2026-08-03 M2-A5 / D4 拍板**）：`perf_bench3.th`（fib(28)/loop 1e7/matmul150）固化进 CI 门槛——`tenth/tests/bench_gate_test.rs` 2 项（默认 `#[ignore]` 不拖慢普通 cargo test；`cargo test --release --test bench_gate_test -- --ignored` 执行；非 release `cfg!(debug_assertions)` 跳过）。**基准方法**：release + JIT 默认路径（不设 TENTH_NO_VM），`time_now_ms` 在 main 内部计时（JIT 编译发生在计时前），跑 3 次取中位数；阈值 fib(28)<100ms / loop 1e7<200ms / matmul150<20ms（= 本机实测中位数 28/66/0-1ms 的 3× 裕量，防 CI 抖动误报；M2 目标 fib<10ms/loop<100ms 为更严的达成线，CI 门槛留裕量）。手动复现：`cargo test --release --test bench_gate_test bench_report -- --ignored --nocapture` |
| 兼容性矩阵 | 平台支持表 | ❌ 未实现 |

---

## 八、平台支持

| 平台 | 说明 | Tenth 状态 |
|------|------|-----------|
| Linux x86_64 | | ✅ 可编译 |
| Linux aarch64 | | ❌ 未验证 |
| Windows x86_64 | | ✅ 可编译（当前环境） |
| macOS x86_64 | Intel | ❌ 未验证 |
| macOS aarch64 | Apple Silicon | ❌ 未验证 |
| WebAssembly | 浏览器 | ⚠️ 可编译到 WASM 但无浏览器运行时 |
| Android | | ❌ 未实现 |
| iOS | | ❌ 未实现 |
| 嵌入式 / RTOS | | ❌ 未实现 |

---

## 九、总结矩阵

| 大类 | 总项数 | ✅ 已完成 | ⚠️ 部分/脚手架 | ❌ 未实现 | 完成率 |
|------|--------|----------|--------------|----------|--------|
| 语言核心设计 | 101 | 84 | 4 | 13 | 83% |
| 编译器与工具链 | 78 | 39 | 6 | 33 | 50% |
| 运行时系统 | 21 | 3 | 3 | 15 | 14% |
| 标准库（通用） | 160 | 60 | 6 | 94 | 38% |
| AI 特定需求 | 162 | 68 | 6 | 88 | 42% |
| 通用编程生态 | 44 | 0 | 0 | 44 | 0% |
| 生态与社区 | 19 | 6 | 1 | 12 | 32% |
| 平台支持 | 9 | 2 | 1 | 6 | 22% |
| **合计** | **594** | **263** | **26** | **305** | **~44%** |

> 注：合计 = 各行之和，已按 2026-08-01 修正后的各行重新对齐。本次「语言核心设计」行经总师核实（见 MEMO.md 2026-08-01 docs 条目）修正为正文真实统计：**101 条目 = ✅73 / ⚠️10 / ❌18**（原矩阵 99/51/4/44 严重滞后——27 项已实现未标注、5 项半实现，均已修正为真值；正文 101 条目按 §1 各小节表格逐行统计）。2026-08-01 M1.2 后：**101 条目 = ✅74 / ⚠️9 / ❌18**（Union ⚠️→✅）。2026-08-01 M2.1 后：**101 条目 = ✅75 / ⚠️8 / ❌18**（泛型枚举 ⚠️→✅）。2026-08-01 M2.3 后：**101 条目 = ✅76 / ⚠️8 / ❌17**（标签 break ❌→✅）。2026-08-01 M2.5/M2.6 后：**101 条目 = ✅78 / ⚠️8 / ❌15**（Drop/RAII、Copy trait ❌→✅，矩阵行此前未随行同步，本次一并修正）。2026-08-02 M3.1 后：**101 条目 = ✅79 / ⚠️8 / ❌14**（自定义运算符 ❌→✅）。2026-08-02 M3.5 后：**101 条目 = ✅80 / ⚠️7 / ❌14**（Phantom ⚠️→✅）。2026-08-02 M3.3 后：**101 条目 = ✅81 / ⚠️7 / ❌13**（宏/元编程 ❌→✅，最小可行版本，见 MEMO M3.3）。2026-08-02 M3.4 后（经 §1 逐行核对修正）：**101 条目 = ✅84 / ⚠️4 / ❌13**（**弱引用 ❌→✅**，§1.5，本次任务；M1.1 智能指针/Pin 两处 ⚠️→✅ 此前未登记入注，本次一并修正）。编译器与工具链（正文 79：✅40/⚠️6/❌33）、运行时系统（21：✅4/⚠️2/❌15）、标准库（160：✅68/⚠️5/❌87）、AI 特定需求（162：✅70/⚠️6/❌86）四行与矩阵登记数字仍存在历史累计差异（矩阵数字未随历次新增条目同步），不在本次授权范围内，保持原登记值；通用编程生态/生态与社区/平台支持三行与正文一致。

---

## 十、关键洞察

### Tenth 的核心优势（已完成且做得好的）

1. **语言核心管线完整**：Lexer → Parser → HIR → VM/解释器/WASM 全通
2. **自举成功**：Tenth 编译器由 Tenth 编写
3. **自动微分可用**：28 算子全链路 backward
4. **神经网络标准库**：从 Linear 到 Transformer 覆盖完整
5. **工具链脚手架**：LSP + 包管理器 + REPL

### 最关键的缺口（按影响排序）

**第一梯队（不做就无法称为"AI 语言"）：**

1. 编译期 Shape 检查 — 唯一护城河
2. GPU 实际算子 — 没有 GPU 就不能训练真实模型
3. ~~f32 张量 — f64 在 AI 中不可接受~~（已解决，见 §5.1 多精度张量；f32 Phase 1-5 已完成双侧对齐，残余不对称见 `docs/f32-f64-parity-roadmap.md`）
4. ~~异步/并发 — 长训练需要~~（已部分解决，Phase 1 + Phase 2 完成：`async fn`/`await`/`spawn` 语法 + `Type::Future` + VM `Op::Await`/`Op::Spawn`/`Op::Yield` + `run_scheduler` 协作式调度器 + `ready_queue`/`suspended` 双队列 + 每任务独立 Frame 栈 + 3 个 async I/O native（`async_sleep_ms`/`async_tcp_read`/`async_tcp_write`）用 `std::thread` + mpsc + thread_local 实现；**Phase 3 遗留**：状态机 lower pass、WASM Future 支持、~~yield 语法层关键字~~（已解决：2026-07-17 Rust 侧接通 + 2026-07-25 tenthc 侧接通）、spawn 真并行；详见 §1.3 yield/协程、§3 协程/绿色线程与 async runtime、§4.4 异步 I/O 条目）

**第二梯队（不做就无法称为"通用语言"）：**

5. ~~网络/HTTP — 最基础的通用能力~~（已解决，2026-07-07 Stage 1-4 完成：`std::io`/`std::env`/`std::net`/`std::http` 四模块 13 个 native，覆盖 stderr/stdin/env/exit/TCP 客户端/HTTP 客户端；详见 §4.4/§4.5/§4.7）
6. 数据库驱动 — 数据驱动应用必需
7. ~~正则表达式 — 文本处理刚需~~（已解决，2026-07-08 实现：基于 Rust `regex = "1"` crate，6 个 native（`regex_compile`/`regex_match`/`regex_find`/`regex_find_all`/`regex_replace`/`regex_split`）+ `std/regex.th` 6 个 wrapper；handle table 模式与 `tcp_streams` 一致，VM + 解释器双侧注册；详见 §4.2 正则表达式条目）
8. FFI — 复用现有生态的唯一途径
9. ~~元组类型 — 语言基础类型缺失~~（已解决，2026-07-08 全管线实现：`Value::Tuple` + `Type::Tuple` + `ExprKind::Tuple`/`Pattern::Tuple` + 4 个 VM Op（MakeTuple/IsTuple/TupleGet/Try, opcode 49-52）；支持创建/解构/match/函数返回多值；空元组 `()` 编译为 `Value::Unit` 向后兼容；详见 §1.2 元组类型条目，`tuple_test.rs` 12 项测试覆盖）
10. ~~异常处理 / `?` 操作符 — 错误处理太原始~~（已解决，2026-07-08 `?` 操作符全管线实现：方案 B 新增 `Op::Try` 指令，从 `Result<T>` 提取 T，VM 通过 frame 恢复实现 early return，解释器通过 `TryPropagate` 实现错误传播；`try` 关键字保留为未来异常处理机制预留；`try_operator_test.rs` 12 项测试覆盖；详见 §1.3 try 表达式/`?` 操作符条目）

**第三梯队（不做就无法形成"生态"）：**

11. 包注册中心 — tenthpm 没有中央仓库就是空壳
12. VSCode 扩展 — 没有 IDE 集成就没有用户
13. 在线 Playground — 降低试用门槛
14. 测试框架（Tenth 侧）— 用户无法写测试
15. 文档生成 — 标准库无 API 文档

### 总体评估

这个项目完成度约 30%，但**已完成的 30% 是最难的部分**（编译器前端+VM+自举+自动微分）。剩下的 70% 更多是工程量而非技术突破——但工程量本身就是巨大的。

---

> 本文档基于 Tenth v0.3.3 源码和文档梳理，随项目演进持续更新。
