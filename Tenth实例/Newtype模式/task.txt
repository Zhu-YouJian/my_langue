# Newtype 模式（元组结构体）

展示单字段元组结构体 `struct Meters(f64)` 的构造、字段访问与类型区分。

## 覆盖

- `struct Meters(f64)` 声明与 `Meters(3.5)` 构造
- 字段访问：`m._0`（不支持 `.0`）
- 类型区分：裸 `f64` 不能直接传给 `Meters` 形参（编译期报错）

## 运行

```
tenth run newtype.th
```
