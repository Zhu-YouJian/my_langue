使用 Adam 优化器训练线性回归模型。

Adam (Adaptive Moment Estimation) 结合了动量和自适应学习率：
- 一阶矩估计（动量）：m = β1*m + (1-β1)*g
- 二阶矩估计（RMSProp）：v = β2*v + (1-β2)*g²
- 偏差修正后更新参数

目标：学习 y = 2x + 1
超参数：lr=0.01, β1=0.9, β2=0.999

涉及特性：张量运算、自动微分、pow/sqrt、for-in 循环
