// 快速排序 — Tenth 实现
// 原地不可变风格：返回新 Vec

fn quicksort(arr: Vec<i64>) -> Vec<i64> {
    let len = arr.len();
    if len <= 1 {
        return arr;
    };

    // 取第一个元素为基准
    let pivot = arr[0];
    let mut less = Vec::new();
    let mut equal = Vec::new();
    let mut greater = Vec::new();

    for i in 0..len {
        let elem = arr[i];
        if elem < pivot {
            less.push(elem);
        } else if elem > pivot {
            greater.push(elem);
        } else {
            equal.push(elem);
        };
    };

    let sorted_less = quicksort(less);
    let sorted_greater = quicksort(greater);

    // 合并 sorted_less + equal + sorted_greater
    let mut result = Vec::new();
    for i in 0..sorted_less.len() {
        result.push(sorted_less[i]);
    };
    for i in 0..equal.len() {
        result.push(equal[i]);
    };
    for i in 0..sorted_greater.len() {
        result.push(sorted_greater[i]);
    };

    result
}

// 打印 Vec 元素
fn print_vec(v: Vec<i64>, label: str) {
    println(label);
    for i in 0..v.len() {
        print("  ");
        println(v[i]);
    };
}

fn print(s: str) { println(s); }

fn main() {
    let mut input = Vec::new();
    input.push(3);
    input.push(1);
    input.push(4);
    input.push(1);
    input.push(5);
    input.push(9);
    input.push(2);
    input.push(6);

    print_vec(input, "Input:");

    let sorted = quicksort(input);

    print_vec(sorted, "Sorted:");
}
