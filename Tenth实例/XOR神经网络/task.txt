# XOR 神经网络

用 Tenth 内置 autodiff 训练一个 2 层 MLP 学习 XOR 函数。

## 网络结构

```
输入(2) → 全连接(2→4) + sigmoid → 全连接(4→1) + sigmoid → 输出(1)
```

## 运行

```bash
tenth run Tenth实例/XOR神经网络/xor_net.th
```

## 预期输出

```
x=[0,0] → ~0
x=[0,1] → ~1
x=[1,0] → ~1
x=[1,1] → ~0
```
