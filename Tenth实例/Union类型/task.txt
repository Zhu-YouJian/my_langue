# Union 联合类型

展示 Tenth 的 tagged union（带"当前活跃字段"标记的联合类型）：声明、构造、字段访问与修改。

## 覆盖

- `union Name { 字段: 类型, ... }` 声明
- `U { 字段: 值 }` 构造（恰好激活一个字段）
- 字段访问 / 修改：仅允许当前活跃字段，访问非活跃字段报错

## 运行

```
tenth run union_demo.th
```
