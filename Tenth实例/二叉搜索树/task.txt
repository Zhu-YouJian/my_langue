实现二叉搜索树（BST），支持插入、查找和中序遍历。

BST 性质：左子树所有节点值 < 根节点值 < 右子树所有节点值

操作：
- insert(tree, val): 插入值，返回新树
- search(tree, val): 查找值是否存在
- inorder(tree): 中序遍历（输出有序序列）

插入序列：50, 30, 70, 20, 40, 60, 80
中序遍历应输出：20, 30, 40, 50, 60, 70, 80

涉及特性：枚举递归、match、函数式不可变更新
