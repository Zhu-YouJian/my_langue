# P1 差分检查器语料 · 回归守护夹具（本清单应全 PASS + 退出码 0）
#
# 用途：把已修复的 AUDIT 缺陷固化成**常驻探针**，防止「修复后被静默改回去」。
# 与 `diff_divergence.txt` 的区别：那条记录的是**修复前**的真分歧证据（历史）；
# 本条是修复后的**守护**清单（回归即判红）。
#
# 跑法（项目根，env 必须干净——检查器会硬校验）：
#   tenth\target\release\tenth.exe run tenth-lens\main.th --diff tenth-lens\corpus\diff_regressions.txt
#
# 格式：`标签|路径.th|期望`
#   期望 clean = 默认路径应退出码 0；error = 应非零退出；any = 不做期望约束

# AUDIT-11.4.85：JIT A1 调用点「分叉前统一物化」——调用作第二操作数 + 循环 + 不可内联被调函数。
# 修复前：默认路径 exit 1 + 「无法比较」（期望 clean 会判红 ⇒ 清单有效地钉住该缺陷）。
gap018_m1_minimal|tenth-lens/tests/fixtures/gap018_m1_minimal.th|clean
